var e=[{id:`e-co-cache-01`,subject:`co`,chapter:`存储系统`,difficulty:2,minutes:12,tags:[`Cache`,`地址划分`,`平均访问时间`],stem:`某计算机主存地址为 $32$ 位，按字节编址，Cache 的数据区容量为 $32\\text{ KB}$，主存块大小为 $32\\text{ B}$，Cache 采用四路组相联映射，访问方式为非同时访问（先访问 Cache，缺失时再访问主存）。

1. 求主存地址中标记（Tag）、组号、块内地址各占多少位。
2. 设 Cache 初始为空，CPU 依次访问十进制地址 $0$、$32$、$1024$、$0$（单位均为字节），判断每次访问是否命中，并说明理由。
3. 设 Cache 命中时的访问时间为 $2\\text{ ns}$，一次缺失的缺失代价为 $50\\text{ ns}$，求上述访问序列的命中率与平均访问时间。`,points:[{label:`算出块内地址位数`,score:2,answer:`块大小为 $32\\text{ B}=2^{5}\\text{ B}$，故块内地址占 $5$ 位。`},{label:`算出行数、组数与组号位数`,score:2,answer:`Cache 行数 $=32\\text{ KB}\\div 32\\text{ B}=1024$ 行；四路组相联时组数 $=1024\\div 4=256=2^{8}$，故组号占 $8$ 位。`},{label:`算出标记位数`,score:2,answer:`标记位数 $=32-8-5=19$ 位。主存地址格式为：$19$ 位标记 $+$ $8$ 位组号 $+$ $5$ 位块内地址。`},{label:`四次访问的命中判断与理由`,score:3,answer:`前三次均缺失、第四次命中，命中 $1$ 次、缺失 $3$ 次。地址 $0$（块号 $0$、组 $0$）、地址 $32$（块号 $1$、组 $1$）、地址 $1024$（块号 $32$、组 $32$）各自落入的组都是空的（有效位为 $0$），故都缺失；第 $4$ 次访问地址 $0$ 时，第 $0$ 组中已有主存第 $0$ 块且标记相符，故命中。`},{label:`命中率与平均访问时间`,score:3,answer:`命中率 $H=\\frac{1}{4}=25\\%$；$T_a=H\\times T_c+(1-H)\\times T_m=0.25\\times 2+0.75\\times 50=38\\text{ ns}$。`}],solution:`## 参考解答

**1. 地址位数划分**

- 块大小 $32\\text{ B}=2^{5}\\text{ B}$，故块内地址占 $5$ 位（地址最低的 $5$ 位）。
- Cache 总行数 $=32768\\text{ B}\\div 32\\text{ B}=1024$ 行。
- 四路组相联：组数 $=$ 总行数 $\\div$ 路数 $=1024\\div 4=256=2^{8}$，故组号占 $8$ 位。
- 标记位数 $=32-8-5=19$ 位，即标记是主存地址的最高 $19$ 位（做标记比较时与 Cache 行中保存的标记逐位比较）。

主存地址格式：$19$ 位标记 $+$ $8$ 位组号 $+$ $5$ 位块内地址。

**2. 四次访问的命中情况**

由组号 $=$ 块号 $\\bmod$ 组数，逐次判断：

- 访问 $0$：块号 $=0\\div 32=0$，组号 $=0\\bmod 256=0$，标记 $=0$（二进制全 $0$）。Cache 初始为空，第 $0$ 组所有行有效位为 $0$，故**缺失**，把主存第 $0$ 块调入第 $0$ 组。
- 访问 $32$：块号 $=32\\div 32=1$，组号 $=1\\bmod 256=1$，落在第 $1$ 组。该组为空，故**缺失**，把主存第 $1$ 块调入第 $1$ 组。
- 访问 $1024$：块号 $=1024\\div 32=32$，组号 $=32\\bmod 256=32$，落在第 $32$ 组（不是第 $0$ 组）。该组为空，故**缺失**，把主存第 $32$ 块调入第 $32$ 组。
- 访问 $0$：块号 $0$、组号 $0$、标记 $0$。第 $0$ 组中已有主存第 $0$ 块，有效位为 $1$ 且标记相符，故**命中**。四路组相联此时只占用 $1$ 行，不存在替换问题。

统计：命中 $1$ 次，缺失 $3$ 次。

**3. 命中率与平均访问时间**

$H=\\frac{1}{4}=25\\%$，缺失率 $1-H=75\\%$。

$T_a=H\\times T_c+(1-H)\\times T_m=0.25\\times 2\\text{ ns}+0.75\\times 50\\text{ ns}=0.5\\text{ ns}+37.5\\text{ ns}=38\\text{ ns}$`,pitfalls:`> 常见丢分：① 把「总行数」当成「组数」，忘记除以路数 $4$（组数是 $256$ 而不是 $1024$）；② 组号位移算错，地址 $1024$ 的块号是 $32$，应落入第 $32$ 组而不是第 $0$ 组；③ 平均访问时间别把命中时间和缺失代价用反（本题缺失代价已含把数据交付 CPU 的全部时间，不要再累加 $T_c$）。`},{id:`e-co-mem-expand-02`,subject:`co`,chapter:`存储系统`,difficulty:2,minutes:12,tags:[`存储器扩展`,`位扩展`,`字扩展`,`地址范围`],stem:`某计算机按字节编址，CPU 有 $24$ 根地址线 $A_{23}\\sim A_{0}$。现用若干片 $1\\text{M}\\times 4$ 位的 DRAM 芯片构成容量为 $4\\text{M}\\times 8$ 位的主存，主存起始地址为 $000000\\text{H}$，采用译码片选法，由地址高位经译码器产生各组的片选信号。

1. 共需要多少片 DRAM 芯片？其中片内地址多少位、用于选择芯片（组）的地址多少位？
2. 说明这些芯片如何组织（位扩展分成几组、每组几片、数据线如何连接）。
3. 写出 $4$ 组芯片各自对应的主存地址范围（十六进制）。
4. 若改用 $2\\text{M}\\times 8$ 位的 DRAM 芯片实现同样的主存容量，需要多少片？此时片内地址和片选地址各多少位？`,points:[{label:`算出芯片总数与地址位数分配`,score:3,answer:`需 $(4\\text{M}\\div 1\\text{M})\\times(8\\div 4)=4\\times 2=8$ 片。主存总容量 $4\\text{M B}=2^{22}\\text{ B}$，需 $22$ 位地址；每组（单片）$1\\text{M}$ 个单元，片内地址 $=\\log_2(2^{20})=20$ 位（$A_{19}\\sim A_0$），片选地址 $=22-20=2$ 位（$A_{21}A_{20}$）。`},{label:`说明位扩展与字扩展的组织方式`,score:2,answer:`每 $2$ 片 $1\\text{M}\\times 4$ 位芯片并联做位扩展，得到一组 $1\\text{M}\\times 8$ 位；共 $4$ 组再做字扩展，拼成 $4\\text{M}\\times 8$ 位。同组两片共用地址线与片选信号，一片接数据线 $D_7\\sim D_4$，另一片接 $D_3\\sim D_0$。`},{label:`写出四组芯片的地址范围`,score:3,answer:`每组容量 $1\\text{M B}=100000\\text{H}$。第 $0$ 组（$A_{21}A_{20}=00$）：$000000\\text{H}\\sim 0FFFFF\\text{H}$；第 $1$ 组（$01$）：$100000\\text{H}\\sim 1FFFFF\\text{H}$；第 $2$ 组（$10$）：$200000\\text{H}\\sim 2FFFFF\\text{H}$；第 $3$ 组（$11$）：$300000\\text{H}\\sim 3FFFFF\\text{H}$。`},{label:`改用大容量芯片后的结果`,score:2,answer:`需要 $4\\text{M}\\times 8\\div(2\\text{M}\\times 8)=2$ 片；每片 $2\\text{M}$ 个单元，片内地址 $21$ 位（$A_{20}\\sim A_0$），片选地址 $22-21=1$ 位（$A_{21}$）。`}],solution:`## 参考解答

**1. 芯片数量与地址位数**

- 位方向：$8\\div 4=2$；字方向：$4\\text{M}\\div 1\\text{M}=4$；故共需 $2\\times 4=8$ 片。
- 主存容量 $4\\text{M}\\times 8$ 位，按字节编址即 $4\\text{M B}=2^{22}\\text{ B}$，需要 $22$ 位地址。
- 每组（位扩展后的一个 $1\\text{M}\\times 8$ 位模块）有 $1\\text{M}=2^{20}$ 个单元，故片内地址为 $20$ 位，即 $A_{19}\\sim A_0$。
- 片选地址 $=22-20=2$ 位，即 $A_{21}A_{20}$，经 $2$ 线$-4$ 线译码器产生 $4$ 个片选信号。

**2. 组织方式**

位扩展：$2$ 片 $1\\text{M}\\times 4$ 位并联成 $1\\text{M}\\times 8$ 位。两片共用全部地址线 $A_{19}\\sim A_0$ 与同一个片选信号，但数据线分开：一片提供高 $4$ 位 $D_7\\sim D_4$，另一片提供低 $4$ 位 $D_3\\sim D_0$。
字扩展：$4$ 组并联到系统数据总线上，用 $A_{21}A_{20}$ 译码区分，同一时刻只有一组被选中。

**3. 各组地址范围**

每组 $1\\text{M B}=2^{20}\\text{ B}=100000\\text{H}$，$4$ 组共 $4\\text{M B}$：

- 第 $0$ 组 $A_{21}A_{20}=00$：$000000\\text{H}\\sim 0FFFFF\\text{H}$
- 第 $1$ 组 $A_{21}A_{20}=01$：$100000\\text{H}\\sim 1FFFFF\\text{H}$
- 第 $2$ 组 $A_{21}A_{20}=10$：$200000\\text{H}\\sim 2FFFFF\\text{H}$
- 第 $3$ 组 $A_{21}A_{20}=11$：$300000\\text{H}\\sim 3FFFFF\\text{H}$

CPU 虽然有 $24$ 根地址线（可寻址 $16\\text{M B}$），本主存只占 $4\\text{M B}$，实际只用到 $A_{21}\\sim A_0$，$A_{23}A_{22}$ 不参与片选。

**4. 改用 $2\\text{M}\\times 8$ 位芯片**

片数 $=4\\text{M}\\times 8\\div(2\\text{M}\\times 8)=2$ 片。每片 $2\\text{M}=2^{21}$ 个单元，片内地址 $21$ 位（$A_{20}\\sim A_0$）；片选地址 $=22-21=1$ 位（$A_{21}$），两片分别占 $000000\\text{H}\\sim 1FFFFF\\text{H}$ 与 $200000\\text{H}\\sim 3FFFFF\\text{H}$。`,pitfalls:`> 常见丢分：① 位扩展只加宽字长、不增加单元数，字扩展只增加单元数、不加宽字长，两者不能混；② 看到 CPU 有 $24$ 根地址线就直接认为主存容量是 $2^{24}\\text{ B}$，本题主存只有 $4\\text{M B}$，只用到 $A_{21}\\sim A_0$；③ 每组容量写成 $0FFFFF\\text{H}$ 而不是 $100000\\text{H}$，导致地址范围首位写错。`},{id:`e-co-float-03`,subject:`co`,chapter:`数据的表示与运算`,difficulty:3,minutes:15,tags:[`IEEE 754`,`浮点数`,`对阶`,`规格化`],stem:`设浮点数采用 IEEE 754 单精度格式（$32$ 位）：$1$ 位符号位 $S$、$8$ 位阶码 $E$（移码，偏置值为 $127$）、$23$ 位尾数 $M$（隐含最高位 $1$，即真值的有效部分为 $1.M$）。

1. 将十进制数 $x=-12.375$ 转换为 IEEE 754 单精度格式：写出符号位、阶码、尾数的二进制，并给出 $32$ 位机器数的十六进制表示。
2. 已知 $y=+0.5$，用浮点数加减运算的步骤求 $x+y$：写出对阶过程、尾数运算结果、规格化后的阶码与尾数，并给出结果的十六进制表示与十进制真值。`,points:[{label:`写出 $|x|$ 的二进制并规格化`,score:2,answer:`$12.375=1100.011_2$；规格化为 $1.100011\\times 2^{3}$，阶的真值 $e=3$。`},{label:`写出 $x$ 的 IEEE 754 机器数与十六进制`,score:3,answer:`符号位 $S=1$；阶码 $E=127+3=130=10000010_2$；尾数 $M$ 为 $100011$ 后补 $0$ 到 $23$ 位，即 $10001100000000000000000$。机器数为 $1\\,10000010\\,10001100000000000000000$，即 $\\text{C1460000H}$。`},{label:`对阶过程`,score:2,answer:`$y=+0.5=1.0\\times 2^{-1}$，阶码 $E_y=127+(-1)=126$。$E_x-E_y=130-126=4$，小阶向大阶看齐，把 $y$ 的尾数右移 $4$ 位：由 $1.00000000000000000000000$ 变为 $0.00010000000000000000000$（阶值取 $2^{3}$）。`},{label:`尾数运算与规格化`,score:3,answer:`两数符号相反，用绝对值相减：$1.100011-0.000100=1.011111$，结果符号取 $x$ 的符号（负）。$1.011111$ 已在 $[1,2)$ 内，无需左规或右规，阶码保持 $130$（即 $2^{3}$）。`},{label:`结果的机器数、十六进制与真值`,score:2,answer:`符号位 $1$、阶码 $10000010$、尾数 $01111100000000000000000$，即 $1\\,10000010\\,01111100000000000000000=\\text{C13E0000H}$；真值 $=-(1.011111)_2\\times 2^{3}=-11.875$。`}],solution:`## 参考解答

**1. 把 $x=-12.375$ 转成 IEEE 754 单精度**

整数部分：$12=1100_2$；小数部分：$0.375=0.25+0.125=0.011_2$。故

$|x|=1100.011_2=1.100011_2\\times 2^{3}$，阶的真值 $e=3$。

- 符号位：$x<0$，$S=1$。
- 阶码（移码）：$E=e+127=3+127=130=10000010_2$。
- 尾数：小数点后的 $100011$ 补齐到 $23$ 位，得 $10001100000000000000000$。

机器数排布为 $S$ $E$ $M$：$1\\,10000010\\,10001100000000000000000$，写成十六进制即

$1100\\ 0001\\ 0100\\ 0110\\ 0000\\ 0000\\ 0000\\ 0000_2=\\text{C1460000H}$。

**2. 求 $x+y$，其中 $y=+0.5$**

（1）**转成浮点形式**：$|y|=0.5=1.0_2\\times 2^{-1}$，$S_y=0$，$E_y=-1+127=126=01111110_2$，$M_y=00000000000000000000000$。

（2）**对阶**：$E_x-E_y=130-126=4>0$，小阶向大阶看齐，把 $y$ 的尾数右移 $4$ 位（含隐含的 $1$）：

$y$ 的有效部分由 $1.00000000000000000000000$ 变为 $0.00010000000000000000000$，阶统一为 $2^{3}$。

（3）**尾数运算**：两数异号，做减法（大数减小数）：

$1.100011-0.000100=1.011111$

符号取绝对值较大者 $x$ 的符号，即负。本例无舍入误差，结果可精确表示。

（4）**规格化**：$1.011111$ 满足 $1.M$ 且 $M\\neq 0$（真值落在 $[1,2)$），已是规格化形式，阶码仍为 $130$。

（5）**写回机器数与求真值**：$S=1$、$E=10000010$、$M=01111100000000000000000$，

$1\\,10000010\\,01111100000000000000000_2=\\text{C13E0000H}$

真值 $=-(1.011111)_2\\times 2^{3}=-(1+0.25+0.125+0.0625+0.03125+0.015625)\\times 8=-1.484375\\times 8=-11.875$，与直接计算 $-12.375+0.5=-11.875$ 相符。`,pitfalls:`> 常见丢分：① 对阶方向搞反，总是小阶向大阶看齐（尾数右移），本题要右移 $y$ 的尾数 $4$ 位；② 阶码字段存的是移码 $E=e+127$，不能把 $e=3$ 直接填进去；③ 算完 $1.011111\\times 2^{3}$ 就停手，忘记回填成 $32$ 位格式并给出十六进制。`},{id:`e-co-pipeline-04`,subject:`co`,chapter:`指令流水线`,difficulty:3,minutes:15,tags:[`指令流水线`,`数据冒险`,`加速比`,`吞吐率`],stem:`某 CPU 采用 $5$ 段指令流水线：取指 IF、译码 ID、执行 EX、访存 MEM、写回 WB，每段占 $1$ 个时钟周期，时钟频率为 $1\\text{ GHz}$。规定：寄存器堆在时钟周期的前半拍写入、后半拍读出，因此某指令的 ID 段与其前驱指令的 WB 段处于同一时钟周期时即可读到新值；ALU 指令的结果在 EX 段末产生，LOAD 指令的数据在 MEM 段末取得，两者都在 WB 段写回寄存器；流水线不设数据旁路（转发）。

按程序顺序执行下列 $5$ 条指令（均为 $32$ 位定长指令，R1、R3、R5、R7、R9 为通用寄存器）：

I1：LOAD R1, 0(R2)
I2：ADD R3, R1, R4
I3：SUB R5, R3, R6
I4：AND R7, R3, R8
I5：OR R9, R5, R7

1. 若没有任何冲突，执行完这 $5$ 条指令需要多少个时钟周期？
2. 逐条判断数据冒险：指出哪些指令对之间存在冒险、各需插入多少个停顿周期。
3. 实际执行完这 $5$ 条指令共需多少个时钟周期？相当于多少纳秒？
4. 若不用流水线、每条指令仍需 $5$ 个时钟周期串行执行，求该流水线的加速比与吞吐率（条/秒）。`,points:[{label:`无冲突时所需周期数`,score:2,answer:`$k$ 段流水线执行 $n$ 条指令需 $k+(n-1)$ 个时钟周期，故 $5+(5-1)=9$ 个时钟周期。`},{label:`I1 与 I2 的冒险及停顿数`,score:3,answer:`I2 在 ID 段读 R1，而 R1 由 I1（LOAD）在 WB 段写回。无转发时 I2 的 ID 必须不早于 I1 的 WB。I1 的 WB 在第 $5$ 个周期，I2 原定第 $3$ 个周期进入 ID，故需插入 $5-3=2$ 个停顿周期，I2 的 ID 推迟到第 $5$ 个周期。`},{label:`I3、I4、I5 的冒险及停顿数`,score:3,answer:`I3 读 R3（由 I2 在 WB 段写回）：I2 因前面停顿整体后移，其 WB 在第 $8$ 个周期，而 I3 原定 ID 在第 $6$ 个周期，需停 $2$ 个周期。I4 同样读 R3，但受前面累计 $4$ 个周期停顿影响，其 ID 已推迟到第 $9$ 个周期，晚于第 $8$ 个周期，无需停顿。I5 读 R5（I3 的 WB 在第 $11$ 个周期）和 R7（I4 的 WB 在第 $12$ 个周期），原定 ID 在第 $10$ 个周期，需停 $2$ 个周期，ID 推迟到第 $12$ 个周期。`},{label:`实际总周期数与执行时间`,score:2,answer:`理想 $9$ 个周期加停顿 $2+2+0+2=6$ 个周期，共 $15$ 个时钟周期。时钟周期 $=1\\text{ GHz}^{-1}=1\\text{ ns}$，执行时间 $=15\\times 1\\text{ ns}=15\\text{ ns}$。`},{label:`加速比与吞吐率`,score:2,answer:`串行执行需 $5\\times 5=25$ 个时钟周期，加速比 $S=\\frac{25}{15}\\approx 1.67$；吞吐率 $TP=\\frac{5}{15\\text{ ns}}\\approx 3.33\\times 10^{8}$ 条/秒，即平均每 $3$ 个时钟周期完成 $1$ 条指令。`}],solution:`## 参考解答

**1. 无冲突时的周期数**

$k=5$ 段、$n=5$ 条，理想周期数 $=k+(n-1)=5+4=9$ 个时钟周期（第 $1$ 条在第 $5$ 周期完成，之后每周期完成 $1$ 条，第 $5$ 条在第 $9$ 周期完成）。

**2. 逐条判断数据冒险**

判据：无转发时，后继指令的 ID 段必须不早于前驱指令的 WB 段（前半拍写、后半拍读，同一周期不算冒险）。

- I1 与 I2：I2 读 R1，R1 由 I1 在 WB 写回。I1 的 WB 在第 $5$ 周期，I2 原定 ID 在第 $3$ 周期，需停 $5-3=2$ 个周期。
- I2 与 I3：I3 读 R3，来自 I2 的 WB。I2 因上述停顿整体后移 $2$ 个周期，WB 落在第 $8$ 周期；I3 原定 ID 在第 $6$ 周期，需停 $8-6=2$ 个周期。
- I2 与 I4：I4 也读 R3。由于前面已累计停顿 $4$ 个周期，I4 的 ID 推迟到第 $9$ 周期，晚于 I2 的 WB（第 $8$ 周期），不需停顿。
- I3、I4 与 I5：I5 读 R5（I3 的 WB 在第 $11$ 周期）和 R7（I4 的 WB 在第 $12$ 周期）。累计停顿 $4$ 个周期后 I5 原定 ID 在第 $10$ 周期，须等到 R7 写回，需停 $12-10=2$ 个周期。

停顿总数 $=2+2+0+2=6$ 个周期。

**3. 实际周期数与时间**

各指令各段所占的时钟周期为：

- I1：IF 第 $1$、ID 第 $2$、EX 第 $3$、MEM 第 $4$、WB 第 $5$ 周期
- I2：IF 第 $2$、ID 第 $5$（停顿 $2$ 拍）、EX 第 $6$、MEM 第 $7$、WB 第 $8$ 周期
- I3：IF 第 $5$、ID 第 $8$（停顿 $2$ 拍）、EX 第 $9$、MEM 第 $10$、WB 第 $11$ 周期
- I4：IF 第 $8$、ID 第 $9$、EX 第 $10$、MEM 第 $11$、WB 第 $12$ 周期
- I5：IF 第 $9$、ID 第 $12$（停顿 $2$ 拍）、EX 第 $13$、MEM 第 $14$、WB 第 $15$ 周期

用「理想 $9$ 个周期 $+$ 停顿 $6$ 个周期」也可直接得到 $15$ 个时钟周期。

执行时间 $=15\\times\\frac{1}{1\\text{ GHz}}=15\\times 1\\text{ ns}=15\\text{ ns}$。

**4. 加速比与吞吐率**

串行方式下每条指令仍需 $5$ 个时钟周期，$5$ 条共 $25$ 个时钟周期：

$S=\\frac{25}{15}=\\frac{5}{3}\\approx 1.67$

$TP=\\frac{5}{15\\text{ ns}}=\\frac{5}{15\\times 10^{-9}\\text{ s}}\\approx 3.33\\times 10^{8}$ 条/秒。`,pitfalls:`> 常见丢分：① 忘记「停顿会顺延其后所有指令」，只给 I5 单独记 $2$ 个停顿，而忽略前面已累计的 $4$ 个停顿（正确总停顿是 $6$ 个周期）；② 无转发时 LOAD 的后继指令要等到 WB 才拿到数据，别以为 MEM 段末有数据就只停 $1$ 拍；③ 加速比的基准是串行执行的 $25$ 个周期，不是理想流水线的 $9$ 个周期。`},{id:`e-co-cpi-05`,subject:`co`,chapter:`CPU 与指令周期`,difficulty:2,minutes:10,tags:[`CPI`,`MIPS`,`主频`,`执行时间`],stem:`某计算机主频为 $2\\text{ GHz}$，某程序共需执行 $10^{7}$ 条指令。这些指令按 CPI 分为四类，各类的指令占比与 CPI 分别为：

运算类指令占 $40\\%$，$CPI=1$；
取数/存数类指令占 $30\\%$，$CPI=2$；
转移类指令占 $20\\%$，$CPI=3$；
其他指令占 $10\\%$，$CPI=4$。

1. 求该程序的平均 CPI。
2. 求该程序的执行时间（以 ms 为单位）以及该机器的 MIPS（百万条指令/秒）。
3. 若通过优化把转移类指令的 CPI 由 $3$ 降为 $2$，其余各类占比与 CPI 不变，执行时间变为多少？比原来降低了百分之几？
4. 另有一台主频为 $3\\text{ GHz}$ 的机器运行同一程序，但该程序在这台机器上需执行 $1.2\\times 10^{7}$ 条指令、平均 CPI 为 $2.5$。通过计算判断哪台机器更快。`,points:[{label:`计算平均 CPI`,score:2,answer:`加权平均：$CPI=0.4\\times 1+0.3\\times 2+0.2\\times 3+0.1\\times 4=0.4+0.6+0.6+0.4=2.0$。`},{label:`计算总时钟周期数与执行时间`,score:2,answer:`总时钟周期数 $=10^{7}\\times 2.0=2\\times 10^{7}$；$T=\\frac{2\\times 10^{7}}{2\\times 10^{9}\\text{ Hz}}=10^{-2}\\text{ s}=10\\text{ ms}$。`},{label:`计算 MIPS`,score:2,answer:`$\\text{MIPS}=\\frac{f}{CPI\\times 10^{6}}=\\frac{2\\times 10^{9}}{2.0\\times 10^{6}}=1000$（$f$ 为主频），即每秒 $10^{9}$ 条指令。`},{label:`优化后的执行时间与降幅`,score:2,answer:`新 $CPI=0.4\\times 1+0.3\\times 2+0.2\\times 2+0.1\\times 4=1.8$；$T=\\frac{10^{7}\\times 1.8}{2\\times 10^{9}}=9\\text{ ms}$，降低 $(10-9)\\div 10=10\\%$。`},{label:`两台机器的性能比较`,score:2,answer:`$T_2=\\frac{1.2\\times 10^{7}\\times 2.5}{3\\times 10^{9}}=\\frac{3\\times 10^{7}}{3\\times 10^{9}}=0.01\\text{ s}=10\\text{ ms}$，与第一台机器相同，两者性能相当——主频高不等于更快，还取决于指令条数与 CPI。`}],solution:`## 参考解答

**1. 平均 CPI**

平均 CPI 是各类指令的 CPI 按指令占比的加权和：

$CPI=0.4\\times 1+0.3\\times 2+0.2\\times 3+0.1\\times 4=0.4+0.6+0.6+0.4=2.0$。

**2. 执行时间与 MIPS**

总时钟周期数 $=10^{7}\\times 2.0=2\\times 10^{7}$ 个周期。

$T=\\frac{N\\times CPI}{f}=\\frac{2\\times 10^{7}}{2\\times 10^{9}\\text{ Hz}}=0.01\\text{ s}=10\\text{ ms}$（$N$ 为指令条数，$f$ 为主频）。

$\\text{MIPS}=\\frac{f}{CPI\\times 10^{6}}=\\frac{2\\times 10^{9}}{2.0\\times 10^{6}}=1000$，即该程序在这台机器上平均每秒执行 $10^{9}$ 条指令。

**3. 优化转移类指令后的变化**

转移类 $CPI$ 由 $3$ 降为 $2$，新的平均 CPI 为

$CPI'=0.4\\times 1+0.3\\times 2+0.2\\times 2+0.1\\times 4=0.4+0.6+0.4+0.4=1.8$。

$T'=\\frac{10^{7}\\times 1.8}{2\\times 10^{9}}=9\\times 10^{-3}\\text{ s}=9\\text{ ms}$。

降低比例 $=\\frac{10-9}{10}=10\\%$（与平均 CPI 的降幅 $\\frac{2.0-1.8}{2.0}=10\\%$ 一致，因为指令条数与主频都没变）。

**4. 与第二台机器比较**

$T_2=\\frac{1.2\\times 10^{7}\\times 2.5}{3\\times 10^{9}}=\\frac{3.0\\times 10^{7}}{3\\times 10^{9}}=0.01\\text{ s}=10\\text{ ms}$。

两台机器的执行时间都是 $10\\text{ ms}$，性能相同：第二台主频虽高（$3\\text{ GHz}$ 对 $2\\text{ GHz}$），但指令条数多 $20\\%$、平均 CPI 高 $25\\%$，两个因素刚好抵消。比较机器快慢必须算执行时间 $T=\\frac{N\\times CPI}{f}$，不能只看主频。`,pitfalls:`> 常见丢分：① 平均 CPI 用简单平均算成 $(1+2+3+4)\\div 4=2.5$，必须按指令占比加权；② MIPS 的分母是 $CPI\\times 10^{6}$（百万条），不是 $10^{9}$，且 CPI 要用平均 CPI；③ 只凭主频 $3\\text{ GHz}>2\\text{ GHz}$ 就断定第二台更快，没有代入 $T=\\frac{N\\times CPI}{f}$ 比较。`},{id:`e-co-dma-06`,subject:`co`,chapter:`IO 与中断`,difficulty:2,minutes:12,tags:[`DMA`,`中断`,`总线带宽`,`磁盘`],stem:`某计算机中磁盘与主存之间既可采用中断方式也可采用 DMA 方式传送数据，已知：

CPU 主频为 $500\\text{ MHz}$；
磁盘转速为 $6000$ 转/分，每个磁道有 $100$ 个扇区，每个扇区 $512\\text{ B}$；
系统总线数据线宽度为 $32$ 位，总线时钟频率为 $50\\text{ MHz}$，每个总线周期传送 $1$ 个 $32$ 位字；
DMA 方式下，每传送 $1$ 个 $32$ 位字占用 $1$ 个总线周期，此外每处理完 $1$ 个扇区还需额外占用 $20$ 个总线周期用于 DMA 控制器的内部处理；
中断方式下，每传送 $1$ 个字节就要中断一次，每次中断服务程序含 $20$ 条指令、平均 $CPI$ 为 $2$。

1. 求该磁盘的数据传输率（B/s）。
2. 求系统总线的带宽（B/s）。
3. 用 DMA 方式把一个磁道的数据全部读入主存，求 DMA 占用总线的时间（以 $\\mu$s 为单位）以及该时间占这个磁道传输时间的百分比。
4. 若改用中断方式传送同样多的数据，求 CPU 用于中断服务的时间（以 ms 为单位）及占磁道传输时间的百分比，并说明哪种方式对 CPU 的打扰更小。`,points:[{label:`求磁盘的数据传输率`,score:2,answer:`转速 $6000$ 转/分 $=100$ 转/秒，转一圈需 $10\\text{ ms}$，即传送一个磁道需 $10\\text{ ms}$。每磁道容量 $=100\\times 512\\text{ B}=51200\\text{ B}$，故数据传输率 $=\\frac{51200\\text{ B}}{10\\text{ ms}}=5.12\\times 10^{6}\\text{ B/s}$。`},{label:`求总线带宽与传送一个磁道所需的数据总线周期数`,score:2,answer:`总线宽度 $32$ 位 $=4\\text{ B}$，带宽 $=4\\text{ B}\\times 50\\text{ MHz}=2\\times 10^{8}\\text{ B/s}=200\\text{ MB/s}$。传送 $51200\\text{ B}$ 需 $51200\\div 4=12800$ 个数据总线周期。`},{label:`求 DMA 占用总线的时间及占比`,score:3,answer:`每扇区额外 $20$ 个周期，共 $100\\times 20=2000$ 个；总周期数 $=12800+2000=14800$。总线周期 $=\\frac{1}{50\\text{ MHz}}=20\\text{ ns}$，占用总线时间 $=14800\\times 20\\text{ ns}=296000\\text{ ns}=296\\,\\mu\\text{s}$，占磁道传输时间的 $\\frac{296\\,\\mu\\text{s}}{10\\text{ ms}}=2.96\\%$。`},{label:`求中断方式的 CPU 服务时间`,score:2,answer:`CPU 时钟周期 $=\\frac{1}{500\\text{ MHz}}=2\\text{ ns}$；一次中断耗时 $20\\times 2=40$ 个时钟周期 $=80\\text{ ns}$。$51200\\text{ B}$ 需中断 $51200$ 次，CPU 时间 $=51200\\times 80\\text{ ns}=4096000\\text{ ns}=4.096\\text{ ms}$。`},{label:`两种方式的比较结论`,score:1,answer:`中断方式占磁道传输时间的 $\\frac{4.096\\text{ ms}}{10\\text{ ms}}=40.96\\%$，几乎把 CPU 拖满；DMA 方式只占用总线 $296\\,\\mu\\text{s}$（约 $2.96\\%$），CPU 只在传送开始与结束时介入。因此 DMA 方式对 CPU 的打扰小得多。`}],solution:`## 参考解答

**1. 磁盘的数据传输率**

转速 $6000$ 转/分 $=\\frac{6000}{60}=100$ 转/秒，转一圈的时间为

$\\frac{1}{100}\\text{ s}=10\\text{ ms}$，即读出一个磁道的数据需 $10\\text{ ms}$。

每个磁道的容量 $=100\\times 512\\text{ B}=51200\\text{ B}$，所以

数据传输率 $=\\frac{51200\\text{ B}}{10\\times 10^{-3}\\text{ s}}=5.12\\times 10^{6}\\text{ B/s}$。

**2. 总线带宽**

总线数据线 $32$ 位 $=4\\text{ B}$，一个总线周期传送 $4\\text{ B}$：

总线每秒可传送 $50\\times 10^{6}$ 次，故带宽 $=4\\text{ B}\\times 50\\times 10^{6}=2\\times 10^{8}\\text{ B/s}=200\\text{ MB/s}$。

单个总线周期 $=\\frac{1}{50\\text{ MHz}}=20\\text{ ns}$。

**3. DMA 占用总线的时间**

- 数据传送周期数：$51200\\text{ B}\\div 4\\text{ B}=12800$ 个。
- 每扇区额外开销：$100$ 个扇区 $\\times 20=2000$ 个总线周期。
- 合计 $12800+2000=14800$ 个总线周期。

占用总线时间 $=14800\\times 20\\text{ ns}=296000\\text{ ns}=296\\,\\mu\\text{s}$。

占磁道传输时间的比例 $=\\frac{296\\,\\mu\\text{s}}{10000\\,\\mu\\text{s}}=2.96\\%$。

**4. 中断方式的 CPU 开销与比较**

CPU 时钟周期 $=\\frac{1}{500\\text{ MHz}}=2\\text{ ns}$，一次中断服务需

$20\\times 2=40$ 个时钟周期 $=40\\times 2\\text{ ns}=80\\text{ ns}$。

一个磁道共 $51200\\text{ B}$，每次中断传送 $1\\text{ B}$，需中断 $51200$ 次：

CPU 用于中断的时间 $=51200\\times 80\\text{ ns}=4096000\\text{ ns}=4.096\\text{ ms}$。

占磁道传输时间的比例 $=\\frac{4.096\\text{ ms}}{10\\text{ ms}}=40.96\\%$。

**结论**：中断方式下 CPU 要在 $4.096\\text{ ms}$ 内反复响应中断、执行服务程序，占用了整个传输过程近 $41\\%$ 的时间；DMA 方式下数据传送由 DMA 控制器完成，只占用总线 $296\\,\\mu\\text{s}$（约 $2.96\\%$），CPU 仅需在传送前后做少量初始化与善后处理。所以 DMA 方式对 CPU 的打扰远小于中断方式。`,pitfalls:`> 常见丢分：① 算磁盘传输率时只算出「每磁道 $51200\\text{ B}$」就停手，忘记除以转一圈的时间 $10\\text{ ms}$；② DMA 的额外开销是每个扇区各加 $20$ 个总线周期（共 $2000$ 个），漏算会少 $40\\,\\mu\\text{s}$；③ 中断次数等于字节数 $51200$，若按扇区数 $100$ 计算会相差 $512$ 倍。`}],t=[{id:`e-co2-twos-comp-01`,subject:`co`,chapter:`数据的表示与运算`,difficulty:3,minutes:12,tags:[`补码`,`定点数`,`溢出判断`,`双符号位`],stem:`设机器字长 $8$ 位，最高位为符号位。

定点整数 $x=-93$，$y=+57$；另有两个定点小数 $a=-0.625$，$b=-0.5$，它们采用 $8$ 位定点小数补码表示（$1$ 位符号位、$7$ 位小数位，小数点隐含在符号位之后）。

1. 写出 $[-93]_{\\text{补}}$、$[+57]_{\\text{补}}$ 与 $[-57]_{\\text{补}}$ 的二进制和十六进制形式。
2. 求 $[x+y]_{\\text{补}}$，写出二进制加法过程，给出结果的十进制真值，并判断是否溢出。
3. 求 $[x]_{\\text{补}}+[-57]_{\\text{补}}$，写出加法过程与机器数结果，并判断是否溢出。
4. 求 $[a]_{\\text{补}}+[b]_{\\text{补}}$，写出加法过程、结果的机器数与十进制真值，并判断是否溢出。
5. 分别用单符号位法、双符号位法和进位判断法说明第 3 问发生溢出的依据。`,points:[{label:`写出三个补码`,score:2,answer:`$93=01011101_2$，取反加一得 $[-93]_{\\text{补}}=10100011_2=\\text{A3H}$；$[+57]_{\\text{补}}=00111001_2=\\text{39H}$；$57$ 取反加一得 $[-57]_{\\text{补}}=11000111_2=\\text{C7H}$。`},{label:`$x+y$ 的运算结果与溢出判断`,score:2,answer:`$[x+y]_{\\text{补}}=10100011+00111001=11011100_2=\\text{DCH}$；$11011100$ 的真值为 $-(256-220)=-36$，与 $-93+57=-36$ 一致，两操作数异号，结果必然不溢出。`},{label:`$x-57$ 的运算结果与溢出判断`,score:2,answer:`$10100011+11000111=1\\,01101010$，舍去最高位进位后为 $01101010_2=+106$，而真值应为 $-150$，超出 $8$ 位补码整数范围 $-128\\sim+127$，故发生溢出。`},{label:`定点小数相加与溢出判断`,score:2,answer:`$0.625=0.1010000_2$，故 $[a]_{\\text{补}}=1.0110000$、$[b]_{\\text{补}}=1.1000000$。相加：$1.0110000+1.1000000=10.1110000$，取低 $8$ 位得 $0.1110000$，即 $+0.875$；真值应为 $-1.125$，超出 $-1\\sim+1-2^{-7}$ 的范围，发生溢出。`},{label:`三种溢出判断方法`,score:2,answer:`① 单符号位法：两操作数符号位都是 $1$（均为负），而结果符号位为 $0$，同号相加得异号，故溢出；② 双符号位法：$11.0110000+11.1000000$ 得 $10.1110000$，两个符号位为 $10$，表示负溢；③ 进位判断法：最高位向外的进位 $C_f=1$，次高位向符号位的进位 $C=0$，$C_f\\oplus C=1$，故溢出。`}],solution:`## 参考解答

**1. 三个补码**

- $93=64+16+8+4+1$，故 $93=01011101_2$；取反得 $10100010$，末位加 $1$ 得 $[-93]_{\\text{补}}=10100011_2=\\text{A3H}$。
- $57=32+16+8+1$，故 $[+57]_{\\text{补}}=00111001_2=\\text{39H}$。
- $57$ 取反得 $11000110$，末位加 $1$ 得 $[-57]_{\\text{补}}=11000111_2=\\text{C7H}$。

验算：$10100011_2$ 按权展开为无符号数 $163$，$163-256=-93$；$11000111_2$ 为无符号数 $199$，$199-256=-57$，均正确。

**2. 求 $[x+y]_{\\text{补}}$**

$[x+y]_{\\text{补}}=[x]_{\\text{补}}+[y]_{\\text{补}}$，直接按二进制相加：

$10100011+00111001=11011100_2=\\text{DCH}$。

结果符号位为 $1$，是负数，真值为 $-(256-220)=-36$，与 $-93+57=-36$ 相符。两操作数一正一负，异号相加不会溢出（结果的绝对值一定小于两个操作数中绝对值较大者）。

**3. 求 $[x]_{\\text{补}}+[-57]_{\\text{补}}$**

$10100011+11000111=1\\,01101010$。

$8$ 位机器数只能保存低 $8$ 位，即结果为 $01101010_2=+106$；但参与运算的真值之和应为 $-93+(-57)=-150$，小于 $-128$，已超出 $8$ 位补码整数的表示范围，故**溢出**，$01101010$ 是个错误结果。同号相加得到异号的结果，正是溢出的信号。

**4. 求定点小数的和**

$0.625=0.5+0.125=0.1010000_2$。

$[a]_{\\text{补}}$：取 $0.1010000$ 的每位取反加 $1$，得 $1.0110000$；验算其无符号值为 $176$，$176-256=-80$，$-80\\div 128=-0.625$，正确。

$[b]_{\\text{补}}$：$0.5=0.1000000_2$，同理得 $1.1000000$；无符号值 $192$，$192-256=-64$，$-64\\div 128=-0.5$，正确。

相加：$1.0110000+1.1000000=10.1110000$，舍去最高位进位，取 $8$ 位得 $0.1110000$，即 $+(0.5+0.25+0.125)=+0.875$。

两个小数均为负，$-0.625-0.5=-1.125$，超出定点小数范围 $[-1,\\,1-2^{-7}]$，故**溢出**（负溢），$0.1110000$ 是错误结果。

**5. 三种溢出判断方法**

- **单符号位法**：两操作数符号位同为 $1$，结果符号位为 $0$，同号相加而结果变号，判为溢出。
- **双符号位法**：把符号位扩展为两位（正数用 $00$、负数用 $11$）再相加，$11.0110000+11.1000000=10.1110000$，结果双符号位为 $10$，属负溢（低位进位破坏了高符号位）；若为 $01$ 则为正溢；$00$、$11$ 表示正常。
- **进位判断法**：记 $C_f$ 为最高位（符号位）向外的进位、$C$ 为次高位向符号位的进位。本例 $1.0110000+1.1000000$：低位 $7$ 位 $0110000+1000000=1110000$ 未产生进位，故 $C=0$；符号位 $1+1+0$ 产生进位，故 $C_f=1$。$C_f\\oplus C=1\\oplus 0=1$，判为溢出。

三种方法的结论一致：该运算发生了溢出。`,pitfalls:`> 常见丢分：① 求 $[-57]_{\\text{补}}$ 时对符号位也一起取反后再加一，写成 $01000111$，正确做法是「绝对值取反加一」得到 $11000111$；② 溢出只看最后的符号位是否与操作数相同，而忘记先确认两个操作数是否同号——异号相加永远不会溢出，第 2 问结果符号位为 $1$ 属正常；③ 定点小数的权值是 $2^{-1}\\sim 2^{-7}$，把 $0.1110000$ 误读成 $+0.875$ 之外的值，或忘记真值 $-1.125$ 已超出 $[-1,\\,1-2^{-7}]$ 而漏判溢出。`},{id:`e-co2-hamming-02`,subject:`co`,chapter:`数据的表示与运算`,difficulty:2,minutes:12,tags:[`海明校验`,`校验码`,`码距`,`纠错`],stem:`对 $8$ 位数据进行海明校验（数据位记为 $D_7\\sim D_0$，其值为 $10110100$，其中 $D_7$ 是最高位）。编码规则：校验位记为 $P_1,P_2,P_4,P_8$ 等，放在位序号为 $2$ 的幂的位置上；位序号从 $1$ 开始编号，数据位由低位到高位依次填入其余位置；采用**偶校验**（每个校验组中 $1$ 的个数为偶数）。

1. 求所需校验位的位数 $r$ 与海明码的总位数，并说明 $r$ 的确定依据。
2. 写出 $12$ 个位序号分别对应的位（哪些是校验位、哪些是数据位），并列出每个校验位所负责的位序号集合。
3. 逐位计算各校验位的值，写出完整的海明码。
4. 若接收方收到的海明码第 $6$ 位由 $1$ 错变为 $0$，求各校验组的校验结果（校验子），指出出错位置，并写出纠正后的数据。
5. 说明该编码方案的纠错与检错能力。`,points:[{label:`确定校验位位数与总位数`,score:2,answer:`校验位位数 $r$ 需满足 $2^{r}\\ge k+r+1$，其中 $k=8$。$r=3$ 时 $2^{3}=8<8+3+1=12$，不够；$r=4$ 时 $2^{4}=16\\ge 8+4+1=13$，满足，故 $r=4$，海明码共 $12$ 位。`},{label:`位置分配与校验分组`,score:2,answer:`位序号 $1,2,4,8$ 为校验位 $P_1,P_2,P_4,P_8$；其余位置依次放数据位：$3\\to D_0$，$5\\to D_1$，$6\\to D_2$，$7\\to D_3$，$9\\to D_4$，$10\\to D_5$，$11\\to D_6$，$12\\to D_7$。分组：$P_1$ 负责 $1,3,5,7,9,11$；$P_2$ 负责 $2,3,6,7,10,11$；$P_4$ 负责 $4,5,6,7,12$；$P_8$ 负责 $8,9,10,11,12$。`},{label:`计算各校验位的值与完整海明码`,score:2,answer:`$P_1$：$3,5,7,9,11$ 位为 $0,0,0,1,0$，$1$ 的个数为 $1$（奇），取 $P_1=1$；$P_2$：$3,6,7,10,11$ 位为 $0,1,0,1,0$，$1$ 的个数为 $2$（偶），取 $P_2=0$；$P_4$：$5,6,7,12$ 位为 $0,1,0,1$，$1$ 的个数为 $2$（偶），取 $P_4=0$；$P_8$：$9,10,11,12$ 位为 $1,1,0,1$，$1$ 的个数为 $3$（奇），取 $P_8=1$。海明码由 $H_1$ 到 $H_{12}$ 为 $100001011101$。`},{label:`第 6 位出错的定位与纠正`,score:3,answer:`第 $6$ 位错变为 $0$ 后重新校验：$P_1$ 组 $1$ 的个数仍为 $2$（偶），$S_1=0$；$P_2$ 组变为 $1$ 个 $1$（奇），$S_2=1$；$P_4$ 组变为 $1$ 个 $1$（奇），$S_4=1$；$P_8$ 组仍为 $4$ 个 $1$（偶），$S_8=0$。校验子 $S_8S_4S_2S_1=0110_2=6$，指出第 $6$ 位出错，把它取反恢复为 $1$，纠正后海明码仍为 $100001011101$，还原出的数据为 $D_7\\sim D_0=10110100$。`},{label:`纠错与检错能力`,score:1,answer:`海明码的最小码距 $d=3$，能纠正 $1$ 位错；若同一方案只用于检错，可检出 $2$ 位错，但纠 $1$ 位错与检 $2$ 位错不能同时实现。若要同时纠 $1$ 位错并检 $2$ 位错，需再增加 $1$ 位全体校验位构成扩展海明码。`}],solution:`## 参考解答

**1. 校验位位数**

设校验位 $r$ 位、数据位 $k=8$ 位。带 $r$ 位校验位要能指出「哪一位出错」或「无错」，共 $k+r+1$ 种状态，故须

$2^{r}\\ge k+r+1=8+r+1=r+9$。

- $r=3$：左边 $2^{3}=8$，右边 $3+9=12$，$8<12$，不够。
- $r=4$：左边 $2^{4}=16$，右边 $4+9=13$，$16\\ge 13$，满足。

故取 $r=4$，海明码共 $k+r=12$ 位。

**2. 位置分配与分组**

数据位按「低位到高位」依次填入非 $2$ 的幂的位序号：

- 位 $1,2,4,8$：校验位 $P_1,P_2,P_4,P_8$。
- 位 $3\\to D_0=0$，位 $5\\to D_1=0$，位 $6\\to D_2=1$，位 $7\\to D_3=0$，
- 位 $9\\to D_4=1$，位 $10\\to D_5=1$，位 $11\\to D_6=0$，位 $12\\to D_7=1$。

每个校验位负责「位序号二进制表示中含对应权值」的位：

- $P_1$ 负责 $1,3,5,7,9,11$（位序号最低位为 $1$）
- $P_2$ 负责 $2,3,6,7,10,11$（位序号次低位为 $1$）
- $P_4$ 负责 $4,5,6,7,12$
- $P_8$ 负责 $8,9,10,11,12$

**3. 计算各校验位**

采用偶校验，即每个校验组中 $1$ 的总数为偶数：

- $P_1$ 组（除 $P_1$ 外）：$D_0=0$、$D_1=0$、$D_3=0$、$D_4=1$、$D_6=0$，$1$ 的个数为 $1$，需补成偶数，故 $P_1=1$。
- $P_2$ 组：$D_0=0$、$D_2=1$、$D_3=0$、$D_5=1$、$D_6=0$，$1$ 的个数为 $2$，已是偶数，故 $P_2=0$。
- $P_4$ 组：$D_1=0$、$D_2=1$、$D_3=0$、$D_7=1$，$1$ 的个数为 $2$，已是偶数，故 $P_4=0$。
- $P_8$ 组：$D_4=1$、$D_5=1$、$D_6=0$、$D_7=1$，$1$ 的个数为 $3$，需补成偶数，故 $P_8=1$。

于是各位为

- $H_1=P_1=1$，$H_2=P_2=0$，$H_3=D_0=0$，$H_4=P_4=0$，
- $H_5=D_1=0$，$H_6=D_2=1$，$H_7=D_3=0$，$H_8=P_8=1$，
- $H_9=D_4=1$，$H_{10}=D_5=1$，$H_{11}=D_6=0$，$H_{12}=D_7=1$。

按位序号 $H_1$ 到 $H_{12}$ 写出海明码为 $100001011101$。

验算四个校验组中 $1$ 的个数分别为 $2,2,2,4$，全为偶数，编码正确。

**4. 第 6 位出错时的定位与纠正**

出错后的码字为 $100000011101$（第 $6$ 位由 $1$ 变为 $0$）。逐组重算校验子：

- $P_1$ 组（$1,3,5,7,9,11$）：$1,0,0,0,1,0$，$1$ 的个数为 $2$，偶，$S_1=0$。
- $P_2$ 组（$2,3,6,7,10,11$）：$0,0,0,0,1,0$，$1$ 的个数为 $1$，奇，$S_2=1$。
- $P_4$ 组（$4,5,6,7,12$）：$0,0,0,0,1$，$1$ 的个数为 $1$，奇，$S_4=1$。
- $P_8$ 组（$8,9,10,11,12$）：$1,1,1,0,1$，$1$ 的个数为 $4$，偶，$S_8=0$。

校验子 $S_8S_4S_2S_1=0110_2=6$，即第 $6$ 位出错（校验子直接给出出错位序号），把第 $6$ 位取反恢复为 $1$，纠正后的海明码仍为 $100001011101$。

按第 2 问的位置对应关系读出 $D_7\\sim D_0$：$H_{12},H_{11},H_{10},H_9,H_7,H_6,H_5,H_3$ 即 $1,0,1,1,0,1,0,0$，数据为 $10110100$，与原数据一致。

**5. 纠错与检错能力**

$r=4$、$k=8$ 的海明码最小码距 $d=3$：

- $d=3$ 时可纠正 $1$ 位错（$2^{0}$ 个错也能定位），即能定位并纠正任意 $1$ 位错误；
- 若只用于检错，可检出 $2$ 位错；
- 但纠 $1$ 位错与检 $2$ 位错不能同时满足，若两者都要，需再加 $1$ 位对所有位做奇偶校验的附加位，构成「纠一检二」的扩展海明码。`,pitfalls:`> 常见丢分：① 校验位位数直接用 $2^{r}\\ge k$ 算成 $r=3$，漏掉「要能指出错误位置还需 $+r+1$」这一项，正解是 $2^{r}\\ge k+r+1$，$r=4$；② 数据位填入次序颠倒，把 $D_7$ 放到位 $3$、$D_0$ 放到位 $12$，导致整个码字与答案不符，题目已说明「由低位到高位依次填入」；③ 校验子的权值顺序写反，$S_8S_4S_2S_1=0110$ 对应第 $6$ 位而不是第 $9$ 位（$1001$）；④ 忘记题目要求偶校验，误用奇校验算出 $P_1=0$、$P_8=0$。`},{id:`e-co2-cache-write-03`,subject:`co`,chapter:`存储系统`,difficulty:3,minutes:15,tags:[`Cache`,`写回法`,`写直达法`,`LRU 替换`,`命中率`],stem:`某计算机的 Cache 数据区共 $4$ 个块，采用**全相联**映射与 **LRU** 替换算法，主存块与 Cache 块大小相同（每次访问 $1$ 个块）。

Cache 采用**写回法**（write-back）并配合**写分配**（write-allocate）：写命中时只修改 Cache 中的副本并置脏位；写缺失时先把主存块调入 Cache 再修改。Cache 的命中时间为 $5\\text{ ns}$，一次缺失的缺失代价为 $45\\text{ ns}$。

以主存块号给出的访问序列（$R$ 表示读、$W$ 表示写）为：

R0、R1、R2、W3、R0、W1、W2、R0、R4、R1、R5、R0

1. 逐步写出这 $12$ 次访问中每一次的命中或缺失、是否发生替换、被替换的块号以及是否写回主存。
2. 求该访问序列的命中率。
3. 按公式 $T_a=H\\times T_c+(1-H)\\times T_m$ 求平均访问时间。
4. 写出序列结束时 Cache 中各块的状态，并统计写回法下主存被写的次数。
5. 若改用写直达法（write-through，写命中时同时写 Cache 和主存，写缺失时不分配），主存被写的次数是多少？简述两种写策略各自的优缺点。`,points:[{label:`前八次访问的命缺失与替换过程`,score:3,answer:`第 $1$ 次 R0 缺失，调入块 $0$，无替换；第 $2$ 次 R1 缺失，调入块 $1$；第 $3$ 次 R2 缺失，调入块 $2$；第 $4$ 次 W3 写缺失，按写分配调入块 $3$ 并置脏，此时 $4$ 个块框占满；第 $5$ 次 R0 命中；第 $6$ 次 W1 命中并置脏；第 $7$ 次 W2 命中并置脏；第 $8$ 次 R0 命中。共命中 $4$ 次、缺失 $4$ 次，未发生替换。`},{label:`后四次访问的替换与写回`,score:2,answer:`第 $9$ 次 R4 缺失，最久未用的是脏块 $3$，替换它并**写回主存 $1$ 次**，调入块 $4$；第 $10$ 次 R1 命中；第 $11$ 次 R5 缺失，最久未用的是脏块 $2$，替换它并**再写回主存 $1$ 次**，调入块 $5$；第 $12$ 次 R0 命中。`},{label:`命中率`,score:2,answer:`命中 $6$ 次（第 $5,6,7,8,10,12$ 次），缺失 $6$ 次，$H=\\frac{6}{12}=50\\%$。`},{label:`平均访问时间`,score:2,answer:`$T_a=H\\times T_c+(1-H)\\times T_m=0.5\\times 5\\text{ ns}+0.5\\times 45\\text{ ns}=2.5+22.5=25\\text{ ns}$。`},{label:`终止状态、写回次数与两种写策略比较`,score:3,answer:`结束时 Cache 中为块 $0$、块 $1$、块 $4$、块 $5$，其中块 $1$ 是脏块（第 $6$ 次写命中修改过），块 $0,4,5$ 干净；写回法下主存被写 $2$ 次。写直达法下每次写操作都要写主存：W3（写缺失，不分配，直接写主存）、W1、W2 各 $1$ 次，共 $3$ 次，且 Cache 行无需脏位。写回法访存次数少、写操作在 Cache 中完成，适合写密集型程序，但需要脏位、替换时可能要写回，一致性维护复杂；写直达法实现简单、主存与 Cache 内容始终一致，便于多主设备共享，但每次写都要访问主存，可用写缓冲缓解。`}],solution:`## 参考解答

**1. 逐步分析十二次访问**

初始 Cache 为空，$4$ 个块框可存放任意主存块（全相联），LRU 次序按「最近使用」维护。

- 第 $1$ 次 R0：Cache 空，**缺失**，调入主存块 $0$（干净），无替换。Cache：$0$。
- 第 $2$ 次 R1：**缺失**，调入块 $1$，无替换。Cache：$0,1$。
- 第 $3$ 次 R2：**缺失**，调入块 $2$，无替换。Cache：$0,1,2$。
- 第 $4$ 次 W3：写**缺失**，按写分配先调入块 $3$ 再修改，块 $3$ 置脏，$4$ 个块框刚好占满。Cache：$0,1,2,3_{\\text{脏}}$。
- 第 $5$ 次 R0：块 $0$ 在 Cache 中，**命中**。LRU 次序变为 $1,2,3,0$。
- 第 $6$ 次 W1：**命中**，只写 Cache，块 $1$ 置脏。次序 $2,3,0,1_{\\text{脏}}$。
- 第 $7$ 次 W2：**命中**，块 $2$ 置脏。次序 $3_{\\text{脏}},0,1_{\\text{脏}},2_{\\text{脏}}$。
- 第 $8$ 次 R0：**命中**。次序 $3_{\\text{脏}},1_{\\text{脏}},2_{\\text{脏}},0$。
- 第 $9$ 次 R4：**缺失**，需替换最久未用的块 $3$；块 $3$ 是脏块，故**写回主存 $1$ 次**，然后调入块 $4$。次序 $1_{\\text{脏}},2_{\\text{脏}},0,4$。
- 第 $10$ 次 R1：**命中**。次序 $2_{\\text{脏}},0,4,1_{\\text{脏}}$。
- 第 $11$ 次 R5：**缺失**，替换最久未用的块 $2$；块 $2$ 也是脏块，**再写回主存 $1$ 次**，调入块 $5$。次序 $0,4,1_{\\text{脏}},5$。
- 第 $12$ 次 R0：**命中**。次序 $4,1_{\\text{脏}},5,0$。

**2. 命中率**

命中出现在第 $5,6,7,8,10,12$ 次，共 $6$ 次；缺失 $6$ 次（第 $1,2,3,4,9,11$ 次）。总访问 $12$ 次，故

$H=\\frac{6}{12}=50\\%$，缺失率 $1-H=50\\%$。

**3. 平均访问时间**

$T_a=H\\times T_c+(1-H)\\times T_m=0.5\\times 5\\text{ ns}+0.5\\times 45\\text{ ns}=2.5\\text{ ns}+22.5\\text{ ns}=25\\text{ ns}$。

**4. 结束状态与主存写次数**

序列结束时 Cache 的 $4$ 个块框存放块 $4$、块 $1$、块 $5$、块 $0$，其中：

- 块 $1$：第 $6$ 次写命中被修改过，是**脏块**，尚未写回主存；
- 块 $0$、块 $4$、块 $5$：只被读过，是干净块。

被替换的脏块有两块：块 $3$（第 $9$ 次替换）和块 $2$（第 $11$ 次替换），每次替换都要把它们写回主存。

故写回法下**主存被写 $2$ 次**（此外读缺失引起的 $6$ 次主存读不计入「写」）。

**5. 写直达法的写次数与比较**

写直达法下，任何一次写操作都要写主存：

- W3：写缺失且不分配，直接写主存，$1$ 次；
- W1：写命中，同时写 Cache 和主存，$1$ 次；
- W2：写命中，同样写主存，$1$ 次。

共 **$3$ 次**，且 Cache 行不需要脏位，永远不会发生写回（本例中即减少 $2$ 次写回，但多出 $1$ 次写主存）。

比较：

- **写回法**：主存写次数少（本题 $2<3$），写操作大多在 Cache 内完成，速度快，适合写密集型程序；代价是每行需要 $1$ 位脏位，替换时可能要写回，Cache 与主存内容可能不一致。
- **写直达法**：控制简单，Cache 与主存内容始终保持一致，便于多主设备（如 DMA、多核）共享数据与系统恢复；代价是每次写都要访问主存，写开销大，通常要配写缓冲（write buffer）来缓解。`,pitfalls:`> 常见丢分：① 忘记写分配——写缺失时先把块调入 Cache 再写，因此 W3 使 Cache 变满，直接影响后续的替换与写回次数；② LRU 次序不更新，第 $9$ 次误把块 $0$ 或块 $4$ 当作最久未用块，导致写回次数算错（正确是替换脏块 $3$，写回第 $1$ 次）；③ 把「主存写次数」算成「变更过的块数」$3$（块 $1,2,3$），其实块 $1$ 在序列结束时仍留在 Cache 中是脏块但未写回，写回只有 $2$ 次；④ 混淆写回与写直达的适用场景，误认为写直达的主存写次数一定更少。`},{id:`e-co2-expand-opcode-04`,subject:`co`,chapter:`指令系统`,difficulty:2,minutes:10,tags:[`指令格式`,`扩展操作码`,`编码空间`],stem:`某计算机指令字长 $16$ 位，采用扩展操作码方式，每条指令的地址码字段均为 $4$ 位。现要求该指令系统中有三地址指令 $15$ 条、二地址指令 $12$ 条、一地址指令 $24$ 条，剩下的编码全部用来表示零地址指令。

1. 说明扩展操作码的基本设计思想，并写出三地址、二地址、一地址、零地址指令的操作码字段各占多少位。
2. 逐级分析三地址、二地址、一地址指令所用掉的编码数，以及各自还剩多少个编码可以作为下一级扩展的「扩展标志」。
3. 求零地址指令最多可以有多少条。
4. 若把三地址指令改为 $16$ 条，其余要求不变，是否还能设计出二地址及以下地址的指令？说明理由。`,points:[{label:`扩展操作码的设计思想与各字段位数`,score:2,answer:`扩展操作码让地址码个数不同的指令共用同一指令字长：地址码少的指令其操作码长，地址码多的指令其操作码短，用短操作码中未被占用的编码作为前缀去扩展出更长的操作码。三地址指令操作码 $16-3\\times 4=4$ 位；二地址 $16-2\\times 4=8$ 位；一地址 $16-4=12$ 位；零地址 $16$ 位。`},{label:`逐级编码空间与剩余编码数`,score:3,answer:`三地址：$4$ 位操作码共 $2^{4}=16$ 个编码，用 $15$ 个，剩 $1$ 个作扩展标志。二地址：$8$ 位操作码中高 $4$ 位必须是那个扩展标志，低 $4$ 位任取，共 $1\\times 2^{4}=16$ 个编码，用 $12$ 个，剩 $4$ 个。一地址：$12$ 位操作码中高 $8$ 位为上述 $4$ 个剩余编码，低 $4$ 位任取，共 $4\\times 2^{4}=64$ 个编码，用 $24$ 个，剩 $40$ 个。`},{label:`零地址指令的最多条数`,score:3,answer:`零地址指令操作码 $16$ 位，高 $12$ 位只能是那 $40$ 个剩余编码，低 $4$ 位任取，故最多 $40\\times 2^{4}=640$ 条。`},{label:`三地址指令改为 16 条的可行性判断`,score:2,answer:`不可行。三地址指令的操作码只有 $4$ 位，$16$ 条三地址指令把 $16$ 个编码全部用尽，没有任何编码能作为二地址、一地址、零地址指令的扩展前缀，因此这三类指令一条也无法表示。要留出扩展空间，至少要让三地址指令的条数少于 $16$。`}],solution:`## 参考解答

**1. 设计思想与各字段位数**

扩展操作码的核心是「用不同的操作码长度表示不同地址码个数的指令」：先给地址码最多的指令分配最短的操作码，把其中未用到的编码留作扩展标志；地址码减少一个，操作码就增加一个地址码字段的位数，在上一级留下的编码后面继续细分。这样在固定字长下既保证指令字长统一，又能明显扩大可表示的指令条数。

按指令字长 $16$ 位、每个地址码字段 $4$ 位：

- 三地址指令：操作码 $16-3\\times 4=4$ 位，地址码 $12$ 位；
- 二地址指令：操作码 $16-2\\times 4=8$ 位，地址码 $8$ 位；
- 一地址指令：操作码 $16-1\\times 4=12$ 位，地址码 $4$ 位；
- 零地址指令：操作码 $16$ 位，无地址码。

**2. 逐级编码空间**

- **三地址**：$4$ 位操作码共 $2^{4}=16$ 个编码，用去 $15$ 个，剩 $16-15=1$ 个编码（例如 $1111$）作为二地址指令的扩展标志。
- **二地址**：操作码 $8$ 位，其中高 $4$ 位必须取那个唯一的扩展标志，低 $4$ 位可以任意取值，故可用编码数为 $1\\times 2^{4}=16$。用去 $12$ 个，剩 $16-12=4$ 个作为一地址指令的扩展标志。
- **一地址**：操作码 $12$ 位，其中高 $8$ 位必须是上述 $4$ 个剩余编码之一，低 $4$ 位任意取值，故可用编码数为 $4\\times 2^{4}=64$。用去 $24$ 个，剩 $64-24=40$ 个作为零地址指令的扩展标志。

**3. 零地址指令的最多条数**

零地址指令操作码 $16$ 位，高 $12$ 位只能是上述 $40$ 个剩余编码，低 $4$ 位可自由取值，故最多

$40\\times 2^{4}=40\\times 16=640$ 条。

此时该指令系统共可表示 $15+12+24+640=691$ 条不同的指令。

**4. 三地址指令改为 16 条**

$4$ 位操作码一共只有 $2^{4}=16$ 个编码。若三地址指令占满 $16$ 条，则 $16$ 个编码全部用尽，没有任何编码可以充当「扩展标志」，二地址指令的可用编码数变成 $0\\times 2^{4}=0$，一地址、零地址同样为 $0$。因此二地址及以下地址的指令一条也设计不出来，这种要求不可行。

要同时表示 $16$ 条三地址指令是不可能的；实际设计中必须牺牲一部分三地址指令的编码空间，例如只保留 $15$ 条三地址指令，才有一级扩展的余地。`,pitfalls:`> 常见丢分：① 忽略「每级扩展要乘以前一级剩余的编码数」，把零地址指令数直接算成 $2^{16-12}=16$ 或 $2^{4}=16$，正确是 $40\\times 2^{4}=640$；② 计算二地址指令的编码空间时忘记乘扩展标志数，误认为 $8$ 位操作码有 $2^{8}=256$ 个编码；③ 余数写错，把三地址剩余编码当成 $16-15=0$ 或把一地址剩余算成 $64-12=52$——一地址用的是 $24$ 条，剩 $40$ 条；④ 第 4 问只答「不行」而没指出原因是 $4$ 位操作码的 $16$ 个编码被用尽、扩展标志数为 $0$。`},{id:`e-co2-bus-burst-05`,subject:`co`,chapter:`总线`,difficulty:2,minutes:12,tags:[`总线带宽`,`突发传送`,`总线占用率`,`DMA`],stem:`某同步总线有 $32$ 位地址线、$64$ 位数据线，总线时钟频率为 $50\\text{ MHz}$，一个总线周期为 $1$ 个时钟周期。

该总线支持突发传送：一次突发事务由 $1$ 个地址周期和紧随其后的 $4$ 个数据周期组成，每个数据周期传送 $1$ 个 $64$ 位字。现用 DMA 控制器通过该总线把 $512\\text{ KB}$ 的数据块从外设传送到主存，并且每完成一次事务后总线至少要空闲 $1$ 个时钟周期，以便 CPU 使用总线。

1. 求该总线的理论最大数据传输率（假设每个总线周期都传送数据）。
2. 求一次突发事务传送的数据量与占用的总线时间。
3. 求 DMA 传送完 $512\\text{ KB}$ 数据所需的总时间，以及这段时间内平均有效数据传输率。
4. 求 DMA 占用总线的时间占第 3 问总时间的百分比，CPU 最多能获得多大比例的总线周期。
5. 若要求 CPU 至少获得 $30\\%$ 的总线周期，每次事务后至少要插入几个空闲周期？`,points:[{label:`理论最大数据传输率`,score:2,answer:`数据线 $64$ 位 $=8\\text{ B}$，每个总线周期可传 $8\\text{ B}$，故最大传输率 $=8\\text{ B}\\times 50\\times 10^{6}\\text{ /s}=4\\times 10^{8}\\text{ B/s}=400\\text{ MB/s}$。`},{label:`一次突发事务的数据量与时间`,score:2,answer:`一次事务含 $4$ 个数据周期，传送 $4\\times 8\\text{ B}=32\\text{ B}$；占用 $1+4=5$ 个总线周期。总线周期 $=\\frac{1}{50\\text{ MHz}}=20\\text{ ns}$，故一次事务耗时 $5\\times 20\\text{ ns}=100\\text{ ns}$。`},{label:`传送 512 KB 的总时间与有效传输率`,score:3,answer:`$512\\text{ KB}=524288\\text{ B}$，需事务数 $=524288\\div 32=16384$ 次；每次事务后还要空闲 $1$ 个周期，故每次实际占 $6$ 个总线周期。总周期数 $=16384\\times 6=98304$，总时间 $=98304\\times 20\\text{ ns}=1966080\\text{ ns}\\approx 1.966\\text{ ms}$。平均有效传输率 $=\\frac{524288\\text{ B}}{1.96608\\text{ ms}}\\approx 2.667\\times 10^{8}\\text{ B/s}\\approx 266.7\\text{ MB/s}$。`},{label:`总线占用率与 CPU 可用比例`,score:2,answer:`DMA 实际占用周期数 $=16384\\times 5=81920$，占总周期数的 $\\frac{81920}{98304}\\approx 83.3\\%$，即占用总线时间约 $81920\\times 20\\text{ ns}=1.6384\\text{ ms}$；空闲周期 $=16384\\times 1=16384$，占 $\\frac{16384}{98304}\\approx 16.7\\%$，这就是 CPU 最多能获得的总线周期比例。`},{label:`满足 CPU 占 30% 所需的空闲周期数`,score:1,answer:`设每个事务后插入 $x$ 个空闲周期，需 $\\frac{x}{5+x}\\ge 30\\%$，解得 $x\\ge\\frac{1.5}{0.7}\\approx 2.14$，取整数 $x\\ge 3$。即每个事务后至少插入 $3$ 个空闲周期（此时 CPU 可得 $\\frac{3}{8}=37.5\\%$，若只插 $2$ 个则仅 $\\frac{2}{7}\\approx 28.6\\%$，不满足）。`}],solution:`## 参考解答

**1. 理论最大数据传输率**

数据线宽 $64$ 位，即每个总线周期传送

$64\\div 8=8\\text{ B}$。

若每个总线周期都在传送数据，则

$8\\text{ B}\\times 50\\times 10^{6}\\text{ 周期/s}=4\\times 10^{8}\\text{ B/s}=400\\text{ MB/s}$。

这是不考虑地址周期和空闲周期的上限值。

**2. 一次突发事务的数据量与时间**

一次事务包含 $1$ 个地址周期和 $4$ 个数据周期：

- 传送数据量 $=4\\times 8\\text{ B}=32\\text{ B}$；
- 占用总线周期数 $=1+4=5$ 个。

总线周期 $=\\frac{1}{50\\text{ MHz}}=20\\text{ ns}$，故一次事务耗时

$5\\times 20\\text{ ns}=100\\text{ ns}$。

**3. 传送 512 KB 的总时间与有效传输率**

$512\\text{ KB}=512\\times 1024\\text{ B}=524288\\text{ B}$，所需事务数

$524288\\div 32=16384$ 次。

题目要求每完成一次事务后总线至少空闲 $1$ 个时钟周期，故每次事务实际占用 $5+1=6$ 个总线周期：

总周期数 $=16384\\times 6=98304$ 个，

总时间 $=98304\\times 20\\text{ ns}=1966080\\text{ ns}=1.96608\\text{ ms}\\approx 1.97\\text{ ms}$。

平均有效传输率

$\\frac{524288\\text{ B}}{1.96608\\times 10^{-3}\\text{ s}}=2.6667\\times 10^{8}\\text{ B/s}\\approx 266.7\\text{ MB/s}$。

也可用比例验证：$6$ 个周期中只有 $4$ 个是数据周期，故有效传输率 $=400\\times\\frac{4}{6}\\approx 266.7\\text{ MB/s}$，与上式一致。

**4. 总线占用率与 CPU 可用比例**

DMA 实际占用总线的周期数 $=16384\\times 5=81920$ 个（含 $16384$ 个地址周期与 $65536$ 个数据周期），对应时间

$81920\\times 20\\text{ ns}=1638400\\text{ ns}=1.6384\\text{ ms}$。

占用率为

$\\frac{81920}{98304}=\\frac{5}{6}\\approx 83.3\\%$。

剩余的空闲周期 $=16384\\times 1=16384$ 个，占

$\\frac{16384}{98304}=\\frac{1}{6}\\approx 16.7\\%$，

这就是 CPU 在这段时间内最多能获得的总线周期比例。

**5. 满足 CPU 占 30% 所需的空闲周期数**

设每次事务后插入 $x$ 个空闲周期，则每个事务共占 $5+x$ 个周期，其中 CPU 可用 $x$ 个，要求

$\\frac{x}{5+x}\\ge 30\\%$。

即 $x\\ge 0.3(5+x)$，整理得 $0.7x\\ge 1.5$，$x\\ge\\frac{1.5}{0.7}\\approx 2.14$。

$x$ 必须取整数，故至少插入 $3$ 个空闲周期。检验：$x=2$ 时比例为 $\\frac{2}{7}\\approx 28.6\\%<30\\%$，不够；$x=3$ 时为 $\\frac{3}{8}=37.5\\%\\ge 30\\%$，满足要求。`,pitfalls:`> 常见丢分：① 用 $64$ 位当成 $64\\text{ B}$ 计算，带宽算成 $3200\\text{ MB/s}$，正确是 $64\\div 8=8\\text{ B}$；② 计算总时间时漏掉每次事务后的 $1$ 个空闲周期，算成 $16384\\times 5=81920$ 个周期即 $1.6384\\text{ ms}$，从而把有效传输率误算成 $320\\text{ MB/s}$；③ 总线占用率的分母用「DMA 占用周期」而不是「包含空闲周期的总周期」，$83.3\\%$ 与 $100\\%$ 混淆；④ 第 5 问用 $x\\ge 5\\times 30\\%$ 直接得 $2$ 个周期，忽略了分母也随 $x$ 增大，正确解是 $x\\ge 3$。`},{id:`e-co2-disk-time-06`,subject:`co`,chapter:`存储系统`,difficulty:2,minutes:12,tags:[`磁盘`,`平均存取时间`,`旋转延迟`,`数据传输率`],stem:`某磁盘有 $4$ 个盘片、$8$ 个记录面，每个记录面有 $2000$ 个磁道（即 $2000$ 个柱面），每个磁道有 $500$ 个扇区，每个扇区存放 $512\\text{ B}$。

磁盘转速为 $6000$ 转/分，平均寻道时间为 $8\\text{ ms}$。

1. 求该磁盘的数据传输率与平均旋转延迟。
2. 求该磁盘的总容量（以字节和 GB 为单位）。
3. 求随机读取 $1$ 个扇区的平均存取时间。
4. 现有一个 $2.56\\text{ MB}$（即 $2560000\\text{ B}$）的文件在磁盘上连续存放，求读出整个文件所需的平均时间（忽略磁道之间切换的寻道时间，只计第一个磁道的平均寻道时间），并求这段时间内的平均有效传输率。`,points:[{label:`数据传输率与平均旋转延迟`,score:2,answer:`转速 $6000$ 转/分 $=100$ 转/秒，转一圈需 $\\frac{1}{100}\\text{ s}=10\\text{ ms}$。每个磁道容量 $=500\\times 512\\text{ B}=256000\\text{ B}=250\\text{ KB}$，故传输率 $=\\frac{256000\\text{ B}}{10\\text{ ms}}=25.6\\times 10^{6}\\text{ B/s}=25.6\\text{ MB/s}$。平均旋转延迟为半圈，即 $10\\div 2=5\\text{ ms}$。`},{label:`磁盘总容量`,score:2,answer:`容量 $=$ 记录面数 $\\times$ 每面磁道数 $\\times$ 每道扇区数 $\\times$ 扇区大小 $=8\\times 2000\\times 500\\times 512\\text{ B}=4.096\\times 10^{9}\\text{ B}$，即 $4096\\text{ MB}\\approx 4.096\\text{ GB}$。`},{label:`读一个扇区的平均存取时间`,score:3,answer:`读 $1$ 个扇区：传输时间 $=\\frac{512\\text{ B}}{25.6\\times 10^{6}\\text{ B/s}}=20\\,\\mu\\text{s}=0.02\\text{ ms}$。平均存取时间 $=$ 平均寻道时间 $+$ 平均旋转延迟 $+$ 传输时间 $=8+5+0.02=13.02\\text{ ms}$。`},{label:`读出整个文件的时间与有效传输率`,score:3,answer:`文件 $2560000\\text{ B}\\div 256000\\text{ B/道}=10$ 个磁道。传输时间 $=10\\times 10\\text{ ms}=100\\text{ ms}$，再加上第一个磁道的平均寻道时间 $8\\text{ ms}$ 与平均旋转延迟 $5\\text{ ms}$，总时间 $=8+5+100=113\\text{ ms}$。平均有效传输率 $=\\frac{2560000\\text{ B}}{113\\text{ ms}}\\approx 22.7\\text{ MB/s}$，低于 $25.6\\text{ MB/s}$，因为寻道和等待旋转的时间被分摊到了整个文件的读取过程中。`}],solution:`## 参考解答

**1. 数据传输率与平均旋转延迟**

转速 $6000$ 转/分，即

$6000\\div 60=100$ 转/秒，转一圈的时间 $=\\frac{1}{100}\\text{ s}=10\\text{ ms}=10000\\,\\mu\\text{s}$。

每个磁道的容量

$500\\text{ 扇区}\\times 512\\text{ B}=256000\\text{ B}=250\\text{ KB}$。

磁盘转一圈恰好读完一个磁道，故数据传输率

$\\frac{256000\\text{ B}}{10\\times 10^{-3}\\text{ s}}=25.6\\times 10^{6}\\text{ B/s}=25.6\\text{ MB/s}$。

平均旋转延迟是等待目标扇区转到磁头下的平均时间，取半圈：

$\\frac{10\\text{ ms}}{2}=5\\text{ ms}$。

**2. 磁盘总容量**

磁盘容量由记录面数、每面磁道数、每道扇区数和扇区大小共同决定：

总容量 $=8\\times 2000\\times 500\\times 512\\text{ B}$。

分步计算：每面容量 $=2000\\times 256000\\text{ B}=5.12\\times 10^{8}\\text{ B}$；$8$ 个面

$8\\times 5.12\\times 10^{8}=4.096\\times 10^{9}\\text{ B}$。

即 $4096\\times 10^{6}\\text{ B}=4096\\text{ MB}\\approx 4.096\\text{ GB}$。

**3. 读一个扇区的平均存取时间**

- 平均寻道时间：$8\\text{ ms}$（题给）。
- 平均旋转延迟：$5\\text{ ms}$（第 1 问）。
- 传输时间：$\\frac{512\\text{ B}}{25.6\\times 10^{6}\\text{ B/s}}=2\\times 10^{-5}\\text{ s}=20\\,\\mu\\text{s}=0.02\\text{ ms}$。

故平均存取时间

$T=8\\text{ ms}+5\\text{ ms}+0.02\\text{ ms}=13.02\\text{ ms}$。

可见寻道与旋转延迟占绝对主导（$13\\text{ ms}$ 对 $0.02\\text{ ms}$），这也是磁盘随机访问速度远低于内存的根本原因。

**4. 读出 2.56 MB 文件的时间**

文件大小 $2560000\\text{ B}$，而每个磁道存放 $256000\\text{ B}$，需要的磁道数

$2560000\\div 256000=10$ 个磁道。

连续存放时，读完 $10$ 个磁道只需让磁盘转 $10$ 圈：

传输（旋转）时间 $=10\\times 10\\text{ ms}=100\\text{ ms}$。

再加上开始读取之前的一次平均寻道与平均旋转等待：

总时间 $=8\\text{ ms}+5\\text{ ms}+100\\text{ ms}=113\\text{ ms}$。

平均有效传输率

$\\frac{2560000\\text{ B}}{113\\times 10^{-3}\\text{ s}}\\approx 2.265\\times 10^{7}\\text{ B/s}\\approx 22.65\\text{ MB/s}$。

它比第 1 问的 $25.6\\text{ MB/s}$ 略低，因为 $13\\text{ ms}$ 的定位时间被摊到了 $113\\text{ ms}$ 的读取过程中；文件越大，这部分开销被摊得越薄，有效传输率就越接近 $25.6\\text{ MB/s}$。`,pitfalls:`> 常见丢分：① 转速不换算单位，直接拿 $6000$ 去算转一圈时间，正确是 $6000\\div 60=100$ 转/秒、转一圈 $10\\text{ ms}$；② 平均旋转延迟取整圈 $10\\text{ ms}$ 而不是半圈 $5\\text{ ms}$；③ 总容量漏乘记录面数（容量必须写成「记录面数 $\\times$ 柱面数 $\\times$ 每道扇区数 $\\times$ 扇区大小」，注意柱面数等于每面磁道数）；④ 读一个扇区时把传输时间漏掉或错用成整圈时间 $10\\text{ ms}$，正确传输时间是 $20\\,\\mu\\text{s}$；⑤ 第 4 问只算 $10$ 圈的 $100\\text{ ms}$ 而忘记加上一次平均寻道与平均旋转延迟。`}],n=[{id:`co-fixed-point-add-overflow`,subject:`co`,chapter:`数据的表示与运算`,tags:[`补码`,`溢出`,`计算`],title:`补码加减运算怎么算？溢出如何判定？`,back:`## 结论
- 补码把减法统一成加法：$[A-B]_{\\text{补}}=[A]_{\\text{补}}+[-B]_{\\text{补}}$，符号位一起参与运算，最高位向外的进位**直接舍去**。
- 只有**同号相加**或**异号相减**才可能溢出，异号相加结果绝不会溢出。
- 一位符号位判据：$V=C_{s}\\oplus C_{1}$，即符号位进位与最高数值位进位**异或**；双符号位时 $01$ 为正溢、$10$ 为负溢。
> 易错：有进位≠溢出，丢掉最高进位恰恰是补码运算的正常现象，要看两个进位是否一致。`,importance:5,hint:`同号相加才溢出，进位异或判溢出`},{id:`co-code-conversion`,subject:`co`,chapter:`数据的表示与运算`,tags:[`原码`,`补码`,`移码`],title:`由真值如何快速写出原码、反码、补码与移码？`,back:`## 结论
- **原码**：符号位 $0$ 表示正、$1$ 表示负，数值位放绝对值。
- **反码**：正数与原码相同；负数符号位不变、数值位**逐位取反**。
- **补码**：正数与原码相同；负数可在反码末位加 $1$，也可用速算法"从右往左第一个 $1$ 及其右侧不变，其余取反"。
- **移码**：把补码的符号位取反即可，$[x]_{\\text{移}}=[x]_{\\text{补}}\\oplus 2^{n-1}$。
> 易错：$-128$ 的 $8$ 位补码是 $10000000$，它**没有**对应的原码和反码，故补码比原码多表示一个数。`,importance:4,hint:`负数取反加一得补码，符号位取反得移码`},{id:`co-ieee754-single-double`,subject:`co`,chapter:`数据的表示与运算`,tags:[`浮点数`,`IEEE754`],title:`IEEE 754 单精度与双精度的字段划分和真值公式有何异同？`,back:`## 结论
- 单精度 $32$ 位 = $1$ 位符号 $+8$ 位阶码 $+23$ 位尾数，阶码偏移量 $127$。
- 双精度 $64$ 位 = $1$ 位符号 $+11$ 位阶码 $+52$ 位尾数，偏移量 $1023$。
- 规格化数的真值公式完全相同：$x=(-1)^{S}\\times 1.M\\times 2^{E-bias}$，尾数都**隐含最高位 $1$**，故精度比存储位数多一位。
> 易错：偏移量取 $2^{k-1}-1$ 而不是 $2^{k-1}$；**阶码位数决定表示范围，尾数位数决定精度**。`,importance:5,hint:`阶码管范围，尾数管精度`},{id:`co-float-normalize`,subject:`co`,chapter:`数据的表示与运算`,tags:[`浮点数`,`规格化`],title:`浮点数为什么要规格化？左规与右规分别在什么时候用？`,back:`## 结论
- **规格化**要求尾数最高数值位是有效数字，使同一数的表示**唯一**，并让尾数有效位数被充分利用。
- IEEE 754 用**隐含最高位 $1$** 实现规格化，所以尾数实际精度比存储的位数多一位。
- 尾数运算后若形如 $00.0\\cdots$ 或 $11.1\\cdots$，需**左规**：尾数左移、阶码相应减小。
- 若尾数绝对值超过 $1$（形式溢出），需**右规**：尾数右移一位、阶码加 $1$。
> 易错：右规最多只做一次；阶码溢出才是真溢出，尾数溢出只是形式问题。`,importance:4,hint:`左规阶码减，右规阶码加`},{id:`co-float-add-steps`,subject:`co`,chapter:`数据的表示与运算`,tags:[`浮点数`,`加减运算`],title:`浮点数加减运算分哪几步？每一步的要点是什么？`,back:`## 结论
- 五步：**对阶 → 尾数加减 → 规格化 → 舍入 → 判溢出**。
- **对阶**遵循"小阶向大阶看齐"，把阶码小的尾数右移、阶码补到与大阶相同，移出的低位被舍弃。
- 舍入常用**就近舍入（0 舍 1 入）**或恒置 $1$ 法；规格化后再舍入可能又需规格化，要循环判断。
- 最后检查阶码是否上溢（真溢出）或下溢（按机器零处理）。
> 易错：对阶绝不能把大阶的尾数左移；先对阶后加减的次序不可颠倒，否则尾数无法直接对齐。`,importance:5,hint:`对阶小向大，末尾判溢出`},{id:`co-bcd-check-code`,subject:`co`,chapter:`数据的表示与运算`,tags:[`BCD`,`校验码`,`海明码`],title:`BCD 码与奇偶校验、海明校验分别有什么能力？`,back:`## 结论
- **BCD 码**用 $4$ 位二进制表示一位十进制数：$8421$ 码只取前 $10$ 个编码，$1010\\sim1111$ 为非法码；余三码等于 $8421$ 码加 $3$。
- **奇偶校验**只增加 $1$ 位校验位，码距为 $2$，能**检出奇数个错**但无法定位，检不出偶数个错。
- **海明码**把校验位放在 $2^{i}$ 位上，校验位个数满足 $2^{k}\\ge n+k+1$，码距为 $3$，可**检错并纠正一位错**。
> 易错：码距越大检错纠错能力越强；只检错不纠错需码距 $2$，纠一位错需码距 $3$。`,importance:4,hint:`奇偶只检错，海明能纠一位`},{id:`co-endian-alignment`,subject:`co`,chapter:`数据的表示与运算`,tags:[`大小端`,`边界对齐`],title:`大端与小端存放有什么区别？为什么要边界对齐？`,back:`## 结论
- **大端**：数据的高位字节存放在低地址，符合人的书写顺序；**小端**：低位字节存放在低地址。
- 同一数据按两种端序存放时**字节顺序相反，但每个字节内部的位序不变**。
- **边界对齐**要求多字节数据的起始地址是其长度的整数倍，这样一次访存就能取完；否则可能跨两个存储字，需要多次访存。
> 易错：结构体为满足对齐会插入填充字节，故其总长度大于各成员长度之和，是典型的"以空间换时间"。`,importance:4,hint:`大端高位在低地址，对齐为省访存`},{id:`co-memory-hierarchy-locality`,subject:`co`,chapter:`存储系统`,tags:[`存储层次`,`局部性`],title:`存储层次为什么能兼顾速度与容量？局部性原理指什么？`,back:`## 结论
- 层次自顶向下：**寄存器 → Cache → 主存 → 辅存**，越往上越快、越小、越贵。
- 依据是**局部性原理**：**时间局部性**指刚被访问的数据很可能马上又被访问（如循环变量）；**空间局部性**指相邻地址的数据很可能接着被访问（如数组、顺序执行的指令）。
- 上层是下层内容的**副本**，命中时由上层直接服务，缺失才向下层取，于是整体速度接近上层、容量接近下层。
> 易错：Cache 的调度对程序员**透明**，虚存的页调度由操作系统参与，两者不能混为一谈。`,importance:4,hint:`时间空间局部性，上层下层做副本`},{id:`co-sram-dram-rom`,subject:`co`,chapter:`存储系统`,tags:[`SRAM`,`DRAM`,`刷新`],title:`SRAM、DRAM 与 ROM 各有什么特性？DRAM 为什么要刷新？`,back:`## 结论
- **SRAM**用触发器存储，速度快、功耗大、集成度低、**无需刷新**，常用作 Cache。
- **DRAM**用电容存储，电荷会泄漏，必须**定期刷新**（通常每 $2$ 毫秒一遍），刷新以**行**为单位；速度较慢但集成度高、价格低，用作主存。
- **ROM**掉电不丢失，包括 PROM、EPROM、EEPROM 与 Flash，常用来固化 BIOS 等程序。
> 易错：刷新操作由存储器控制器完成、按行进行，会占用访存时间但不改变数据；DRAM 是**破坏性读出**，读后要重写。`,importance:5,hint:`SRAM 不刷新，DRAM 按行刷新`},{id:`co-memory-chip-address-range`,subject:`co`,chapter:`存储系统`,tags:[`主存扩展`,`地址分配`,`计算`],title:`由芯片规格如何计算所需片数与各片的地址范围？`,back:`## 结论
- 片数 $=\\dfrac{\\text{主存总容量}}{\\text{单片容量}}$；位扩展看数据线位数差，字扩展看地址单元数差。
- 字扩展时**低位地址**接各芯片的地址引脚，**高位地址**经译码器产生片选信号。
- 例：用 $16\\mathrm{K}\\times 8$ 位芯片组成 $64\\mathrm{K}\\times 8$ 位主存，需 $4$ 片，用 $A_{15}A_{14}$ 译码。
- 片选编码为 $00$ 的那片，地址范围是 $0000\\mathrm{H}\\sim 3\\mathrm{FFF}\\mathrm{H}$。
> 易错：$16\\mathrm{K}$ 的芯片只需 $14$ 根地址线，多出的高位地址绝不能接进芯片地址引脚，它属于片选地址。`,importance:5,hint:`低地址进芯片，高地址做片选`},{id:`co-cache-hit-ratio`,subject:`co`,chapter:`存储系统`,tags:[`Cache`,`计算`],title:`Cache 命中率与平均访问时间怎么算？`,back:`## 结论
- 设访问 Cache 次数为 $N_c$、访问主存次数为 $N_m$，则命中率 $H=\\dfrac{N_c}{N_c+N_m}$，缺失率 $M=1-H$。
- 若 Cache 与主存**同时启动**，平均访问时间 $T_a=H\\cdot T_c+(1-H)\\cdot T_m$。
- 若必须**先查 Cache、未命中再访存**，则 $T_a=T_c+M\\cdot T_m$，此时还要算上查 Cache 的那一拍。
- 访问效率 $e=\\dfrac{T_c}{T_a}$，$H$ 越高越接近 $1$。
> 易错：$H$、$M$ 都要用**小数**代入，别把百分数直接乘；两个公式对应不同的访存过程，先看题目描述再套。`,importance:5,hint:`H 用小数，缺失率别忘 1-H`},{id:`co-cache-address-fields`,subject:`co`,chapter:`存储系统`,tags:[`Cache`,`地址划分`,`计算`],title:`Cache 的地址字段如何划分？标记、组号、块内地址各几位？`,back:`## 结论
- 主存地址从低位到高位依次是**块内地址、行号（组号）、标记**。
- 块内地址位数 $b=\\log_{2}(\\text{块大小})$，是三种映射方式都必须有的字段。
- **直接映射**：行号位数 $=\\log_{2}(\\text{行数})$，标记位数 = 地址总位数 $-$ 行号位数 $-$ $b$。
- **组相联**：组号位数 $=\\log_{2}(\\text{组数})$，组内相联度只增加比较器数量，**不占地址位**；全相联没有行号与组号字段。
> 易错：相联度翻倍会使组数减半、组号位少一位，于是**标记位反而变长**，别把相联度当成地址字段。`,importance:5,hint:`标记加组号加块内，相联度不占位`},{id:`co-cache-write-replace`,subject:`co`,chapter:`存储系统`,tags:[`Cache`,`写策略`,`替换算法`],title:`Cache 的替换算法与写策略各有哪些？写直达和写回怎么选？`,back:`## 结论
- **替换算法**：随机、FIFO、LRU、LFU，其中 LRU 最符合时间局部性、命中率较高，但需要额外硬件记录访问历史。
- **写命中**：**写直达**同时修改 Cache 与主存，一致性好但访存频繁，常配写缓冲；**写回**只改 Cache 并置**脏位**，被替换时才写回主存，省访存但一致性差。
- **写不命中**：**写分配**先把主存块调入再写，常与写回搭配；**非写分配**直接写主存，常与写直达搭配。
> 易错：替换算法只在**组相联和全相联**中才需要；写直达法的 Cache 与主存内容始终一致，写回法则可能不一致。`,importance:5,hint:`写直达配写不分配，写回配写分配`},{id:`co-tlb-page-table-cache`,subject:`co`,chapter:`存储系统`,tags:[`虚拟存储`,`TLB`,`页表`],title:`TLB、页表与 Cache 在地址变换中如何配合？`,back:`## 结论
- 虚地址分为**虚页号 + 页内偏移**，页内偏移在整个变换过程中**保持不变**。
- 变换顺序：用虚页号查 **TLB**，命中直接得到物理页号；未命中则查主存中的**页表**（可能是多级页表），并把该页表项**回填 TLB**。
- 页表项有效位为 $0$ 表示该页不在主存，触发**缺页**，需从辅存调入后再访问。
- 得到物理地址后，再用它去访问 **Cache 与主存**取数据。
> 易错：TLB 中存放的是**页表项的副本**；缺页时 TLB 必然缺失，但 TLB 缺失不一定是缺页。`,importance:5,hint:`偏移不变，先快表后页表`},{id:`co-interleaving-bandwidth`,subject:`co`,chapter:`存储系统`,tags:[`多模块`,`交叉编址`,`带宽`],title:`多模块交叉编址存储器如何提高访存带宽？`,back:`## 结论
- **顺序编址（高位交叉）**：同一模块内地址连续，连续访问都落在同一模块，只能扩大容量，**不能提高带宽**。
- **交叉编址（低位交叉）**：地址低位选择模块、高位选择模块内地址，连续地址轮流落到不同模块，可实现**流水线式的连续访存**。
- 设模块数为 $m$、存取周期为 $T$、总线传送周期为 $r$，要不间断供数需满足 $m\\ge T/r$。
- $m$ 体低位交叉的带宽极限可达单模块的 $m$ 倍。
> 易错：只有**低位交叉**才能提速；交叉存储器要求各模块分时错开启动，不是同时传送。`,importance:4,hint:`低位交叉才提速，模块数大于 T 比 r`},{id:`co-instruction-format-opcode`,subject:`co`,chapter:`指令系统`,tags:[`指令格式`,`操作码`],title:`指令格式包含哪些字段？操作码扩展的基本思路是什么？`,back:`## 结论
- 一条指令由**操作码 OP + 地址码 A + 寻址方式字段**组成，地址码可有 $0\\sim 4$ 个。
- 操作码位数 $n$ 决定最多 $2^{n}$ 条指令：**定长操作码**译码简单但浪费编码，**扩展操作码**让地址码少的指令使用更长的操作码。
- 扩展的铁律：**短码不能是长码的前缀**，否则译码歧义；各类指令条数之和不能超过可用编码总数。
> 易错：地址码个数不同会导致指令字长不同；扩展操作码是在**有限字长**内兼顾指令条数与寻址范围的折中。`,importance:4,hint:`短码不做长码前缀`},{id:`co-addressing-effective-address`,subject:`co`,chapter:`指令系统`,tags:[`寻址方式`,`有效地址`,`计算`],title:`各种寻址方式的有效地址如何计算？各需访存几次？`,back:`## 结论
- **立即寻址**：形式地址就是操作数，取指后即可用，**不再访存**。
- **直接寻址**：$EA=A$，取操作数访存 $1$ 次；**间接寻址**：$EA=(A)$，每级间址访存 $1$ 次，一次间址共访存 $2$ 次。
- **寄存器寻址**：操作数在寄存器中，不访存；**寄存器间接寻址**：$EA=(R)$，访存 $1$ 次。
- **相对寻址** $EA=(PC)+A$，**基址寻址** $EA=(BR)+A$（基址由系统给定、面向操作系统），**变址寻址** $EA=(IX)+A$（变址值用户可改、面向数组）。
> 易错：相对寻址中的 $PC$ 已是**下一条指令**的地址；比较访存次数时要把取指与取操作数分开计算。`,importance:5,hint:`立即不访存，间接多一次`},{id:`co-risc-why-pipeline-friendly`,subject:`co`,chapter:`指令系统`,tags:[`RISC`,`CISC`,`流水线`],title:`为什么 RISC 比 CISC 更适合做指令流水线？`,back:`## 结论
- RISC 指令**长度固定、格式规整**，取指的同时就能完成译码和读寄存器，各段负载均衡。
- RISC 采用 \`Load/Store\` 结构，**只有访存指令访问主存**，运算都在寄存器间进行，访存段不易成为瓶颈。
- RISC 大多数指令**一个时钟周期**完成，控制器多用**硬布线**，便于按段切分流水寄存器。
- CISC 指令长度可变、功能复杂，各条指令执行时间差异大，段间严重不平衡，常需多拍甚至微程序完成。
> 易错：CISC 并非不能流水，只是要付出更复杂的取指对齐与译码代价。`,importance:4,hint:`定长单周期，访存只 load store`},{id:`co-instruction-word-length`,subject:`co`,chapter:`指令系统`,tags:[`指令字长`,`机器字长`],title:`指令字长、机器字长与存储字长是什么关系？`,back:`## 结论
- **机器字长**是 CPU 一次能处理的二进制位数，等于通用寄存器与 ALU 的宽度，直接决定运算精度。
- **指令字长**是一条指令的位数；**存储字长**是一个存储单元能存放的位数。
- 三者可以互不相等，但常令指令字长等于存储字长的整数倍，便于按存储字取指：半字长指令取一次，双字长指令要取两次。
> 易错：机器字长决定数据总线宽度与寄存器位数；指令字长**可变**时取指必须先找到指令边界，这正是 CISC 流水的难点。`,importance:4,hint:`机器字长看寄存器，指令字长看指令`},{id:`co-variable-length-fetch-align`,subject:`co`,chapter:`指令系统`,tags:[`变长指令`,`对齐`,`取指`],title:`变长指令如何确定指令边界？为什么要求指令对齐？`,back:`## 结论
- 变长指令必须先取出足够字节，再由**操作码**判定整条指令的长度，因此取指可能跨存储字、分多次访存。
- **指令对齐**指指令起始地址是字长或 $2$ 的幂次的整数倍，不足处用填充字节补齐，例如定长 $4$ 字节的指令按 $4$ 字节边界存放。
- 对齐后一条指令不会跨越两个存储字，一次访存即可取完，流水线的取指段才稳定。
> 易错：定长指令天然对齐、无需填充；变长指令未对齐时，取指要多等一次访存，效果等同于插入气泡。`,importance:3,hint:`先取字节再判长度，对齐才不跨字`},{id:`co-opcode-extension-calc`,subject:`co`,chapter:`指令系统`,tags:[`扩展操作码`,`计算`],title:`扩展操作码的编码题如何计算各类指令的条数上限？`,back:`## 结论
- 设指令字长 $16$ 位、每个地址码 $4$ 位，则三地址指令的操作码只有 $4$ 位，最多 $2^{4}=16$ 条。
- 若三地址指令只用了 $m$ 个编码，余下的 $2^{4}-m$ 个编码就作为**扩展前缀**，每条前缀再接 $4$ 位地址码，可构成 $(2^{4}-m)\\times 2^{4}$ 条二地址指令。
- 逐层类推：下一层条数上限 = 上一层**剩余编码数** $\\times 2^{\\text{本层新增编码位数}}$。
> 易错：扩展要牺牲编码做标志位，实际可用条数比理论最大值少；算完必须验证各类指令条数总和不超过编码总数。`,importance:4,hint:`剩余编码乘 2 的位数次方`},{id:`co-timing-control-modes`,subject:`co`,chapter:`CPU 与指令周期`,tags:[`时序`,`控制方式`],title:`CPU 的时序控制方式有哪几种？同步与异步控制怎么区分？`,back:`## 结论
- **同步控制**：所有机器周期都按统一的时钟节拍推进，每个微操作在固定节拍完成；电路简单，但节拍长度要按最慢部件确定，快部件被拖慢。
- **异步控制**：没有统一节拍，靠**应答（握手）信号**决定下一步何时开始，各操作时间可长可短，效率高但控制电路复杂。
- **联合控制**：多数操作同步，少数耗时长的操作（乘除、访存）采用异步，是实际机器常用的折中方案。
> 易错：同步方式的代价是"以慢就快"、时间浪费；异步方式时序设计复杂，对信号传输延迟要求更严。`,importance:3,hint:`同步统一节拍，异步靠应答`},{id:`co-fetch-indirect-cycle`,subject:`co`,chapter:`CPU 与指令周期`,tags:[`取指周期`,`间址周期`,`微操作`],title:`取指周期与间址周期各完成哪些微操作？`,back:`## 结论
- **取指周期**：$PC$ 送 $MAR$；发读命令，$M(MAR)$ 送 $MDR$；$MDR$ 送 $IR$；$PC$ 加 $1$ 指向下一条指令。
- **间址周期**：把 $IR$ 中的形式地址字段送 $MAR$；发读命令，$M(MAR)$ 送 $MDR$；再把 $MDR$ 的内容送回 $IR$ 的地址字段，得到**有效地址**。
- 取指周期**每条指令都必须有**；间址周期只在指令采用间接寻址时才出现，多次间址就重复多次。
> 易错：取指是"取出指令本身"，间址是"用形式地址去取操作数的地址"；有间址时访存次数比直接寻址多一次。`,importance:4,hint:`取指每条都有，间址按需重复`},{id:`co-datapath-microoperation`,subject:`co`,chapter:`CPU 与指令周期`,tags:[`数据通路`,`微操作`],title:`数据通路与微操作是什么关系？`,back:`## 结论
- **数据通路**是数据在各部件之间传送所经过的路径，由 ALU、寄存器、内部总线与多路选择器构成。
- **微操作**是在数据通路上一步完成的**基本传送或运算**，如 $PC\\to MAR$、$MDR\\to IR$、$R_1+R_2\\to R_3$。
- 一条机器指令可分解为若干微操作序列，控制信号负责选通通路与打开寄存器写入端，所以微操作是控制器设计的**最小单位**。
> 易错：目的端由控制信号决定，同一时刻**不允许两条通路写同一个寄存器**，否则产生数据冲突。`,importance:4,hint:`微操作是通路上的最小一步`},{id:`co-microprogram-vs-hardwired`,subject:`co`,chapter:`CPU 与指令周期`,tags:[`微程序`,`硬布线`,`控制器`],title:`微程序控制与硬布线控制各有什么优缺点？`,back:`## 结论
- **硬布线控制**用组合逻辑门电路直接产生控制信号，**速度最快**，但设计复杂、修改困难，适合指令规整的 RISC。
- **微程序控制**把每条机器指令编成一段**微程序**存放在控制存储器 CM 中，设计规整、便于扩充与修改，但要逐条读取微指令，**速度较慢**，适合 CISC。
- 微指令由操作控制字段与顺序控制字段组成：一条机器指令对应一段微程序，一条微指令对应若干微操作。
> 易错：微程序存放在**控制存储器**中而不是主存；微指令操作的对象是数据通路，与用户程序的指令不是一回事。`,importance:4,hint:`硬布线快而难改，微程序慢而易扩`},{id:`co-controller-registers`,subject:`co`,chapter:`CPU 与指令周期`,tags:[`控制器`,`寄存器`],title:`控制器的组成与 PC、IR、MAR、MDR 各自的作用是什么？`,back:`## 结论
- 控制器由**程序计数器 PC、指令寄存器 IR、时序电路与微操作信号发生器**组成，负责按节拍发出控制信号。
- **PC**存放下一条指令地址，位数等于主存地址位数，取指后自动加上指令长度。
- **IR**存放当前正在执行的指令，位数等于指令字长。
- **MAR**存放要访问的主存单元地址，**MDR**存放与主存交换的数据，其位数由**存储字长**决定。
> 易错：取指的数据流是 $PC\\to MAR\\to \\text{主存}\\to MDR\\to IR$；MDR 宽度不等于机器字长，别按 CPU 位数去填。`,importance:4,hint:`PC 存地址，IR 存指令，MDR 看存储字长`},{id:`co-exception-interrupt-response`,subject:`co`,chapter:`CPU 与指令周期`,tags:[`异常`,`中断响应`,`中断隐指令`],title:`异常与中断在什么时刻被响应？响应时硬件做了哪几件事？`,back:`## 结论
- 外部**中断**通常在一条指令**执行结束之后**才被检测并响应；**异常**由指令执行**过程中**的内部条件触发，如缺页、除零、非法操作码。
- 响应时硬件自动执行**中断隐指令**（它不是程序中的指令，用户无法编程调用），完成三件事：**关中断、保存断点与程序状态字、按中断向量送 PC** 转入服务程序。
- 通用寄存器等现场的保护由**中断服务程序**用指令完成。
> 易错：保存的断点是**下一条**待执行指令的地址；关中断是为了让"保存现场"过程不被打断。`,importance:5,hint:`隐指令关中断、存断点、取向量`},{id:`co-pipeline-throughput-efficiency`,subject:`co`,chapter:`指令流水线`,tags:[`流水线`,`计算`],title:`流水线的吞吐率、加速比与效率怎么算？`,back:`## 结论
- 设 $k$ 段流水线、每段一个时钟周期 $\\Delta t$、共执行 $n$ 条指令，总时间 $T=(k+n-1)\\Delta t$。
- **吞吐率** $TP=\\dfrac{n}{(k+n-1)\\Delta t}$，理想极限是 $\\dfrac{1}{\\Delta t}$。
- **加速比** $S=\\dfrac{nk}{k+n-1}$，极限为段数 $k$；**效率** $E=\\dfrac{S}{k}=\\dfrac{n}{k+n-1}$，极限为 $1$。
- 时钟周期 $\\Delta t$ 取**最慢一段的逻辑延迟**加流水寄存器延迟。
> 易错：装满流水线需要 $k-1$ 个周期；段数越多极限加速比越高，但寄存器开销与段间不平衡会吃掉收益。`,importance:5,hint:`总时间 k 加 n 减一，加速比极限是 k`},{id:`co-pipeline-forwarding-load-use`,subject:`co`,chapter:`指令流水线`,tags:[`数据冒险`,`转发`,`阻塞`],title:`转发能解决哪些数据冒险？load-use 为什么必须阻塞？`,back:`## 结论
- **转发（旁路）**把运算结果从流水寄存器直接送到后面指令所需的入口，使相邻的 RAW 冒险不必等写回寄存器，省下等待时间。
- **load-use 冒险**中前一条是取数指令，数据要到 **MEM 段末**才拿到，而后续指令在 **EX 段初**就要使用，转发也来不及，只能**阻塞一拍**再转发。
- 相邻两条 ALU 运算指令之间的冲突可用转发解决，也可利用寄存器堆"前半拍写、后半拍读"化解。
> 易错：转发只解决数据冒险，对结构冒险与控制冒险无效；阻塞会插入气泡，使实际 CPI 大于 $1$。`,importance:5,hint:`转发解决 RAW，load-use 必阻塞一拍`},{id:`co-superscalar-superpipeline`,subject:`co`,chapter:`指令流水线`,tags:[`超标量`,`动态调度`],title:`超标量、超流水与动态流水线有什么区别？`,back:`## 结论
- **超标量**：每个时钟周期**并发发射多条**指令，依靠多套功能部件与多发射端口提高并行度。
- **超流水**：把流水段继续细分以提高主频，每拍仍只发射一条指令，用"更细的流水"换取速度。
- **动态流水线**：由**硬件在运行时**调度指令（乱序执行、记分牌、Tomasulo），允许越过被阻塞的指令继续发射无关指令。
- 超标量可静态调度（编译器排）也可动态调度（硬件排）。
> 易错：超标量靠**硬件**发现并行性，VLIW 靠**编译器**在指令中显式编码并行性，两者不是一回事。`,importance:3,hint:`超标量多发射，超流水细分段`},{id:`co-pipeline-register-role`,subject:`co`,chapter:`指令流水线`,tags:[`流水寄存器`,`时序`],title:`流水线寄存器起什么作用？为什么不能省略？`,back:`## 结论
- 流水寄存器把相邻各段**隔离**开：同一时钟沿统一锁存上一段的输出，本拍供下一段使用。
- 它使各段能够**并行工作**，是流水线成立的前提；若没有它，后段的组合逻辑会直接影响前段输出，产生竞争与错误结果。
- 寄存器本身有建立与传输延迟，故时钟周期 = 最慢段逻辑延迟 + 寄存器延迟。
> 易错：段数越多寄存器总开销越大，加速比不会无限提升；寄存器的位宽必须能容纳该段产生的全部信息，包括数据、控制信号与目的寄存器号。`,importance:3,hint:`隔离各段，同沿锁存`},{id:`co-pipeline-cpi-stall`,subject:`co`,chapter:`指令流水线`,tags:[`CPI`,`停顿`,`计算`],title:`流水线冲突如何折算成对 CPI 的影响？`,back:`## 结论
- 理想流水线 $CPI=1$，每发生一次停顿就多花若干时钟周期，故 $CPI=1+\\text{平均每条指令的停顿周期数}$。
- 停顿周期数 = 各类冒险的**发生频率** $\\times$ 每次造成的**停顿拍数**，再按类求和。
- 例：$20\\%$ 的指令引发 $2$ 拍阻塞，则 $CPI=1+0.2\\times 2=1.4$。
- 转发、分支预测、增加功能部件等手段，本质都是把这些附加停顿压回 $0$。
> 易错：算加速比与执行时间必须代入**实际 CPI**，不能默认 $CPI=1$；结构冒险的停顿取决于部件冲突频率。`,importance:4,hint:`实际 CPI 等于 1 加平均停顿`},{id:`co-bus-bandwidth-calc`,subject:`co`,chapter:`总线`,tags:[`总线带宽`,`计算`],title:`总线带宽如何计算？与总线宽度、频率是什么关系？`,back:`## 结论
- **总线带宽**（数据传输率）$=$ 总线宽度（字节）$\\times$ 总线工作频率。
- 例：$32$ 位总线、工作频率 $66\\mathrm{MHz}$，带宽 $=4\\mathrm{B}\\times 66\\times 10^{6}=264\\mathrm{MB/s}$。
- 若一个总线周期需要用 $k$ 个时钟才传送一次数据，实际带宽还要再除以 $k$。
- 地址线与数据线复用、插入等待周期都会降低有效带宽。
> 易错：$32$ 位是 **$4$ 字节**而不是 $32$ 字节；总线宽度特指**数据线**根数，与地址线根数无关。`,importance:4,hint:`字节数乘频率，32 位是 4 字节`},{id:`co-bus-arbitration-timing`,subject:`co`,chapter:`总线`,tags:[`总线仲裁`,`总线定时`],title:`总线仲裁与总线定时各有哪几种方式？`,back:`## 结论
- **仲裁**解决多个主设备争用总线的问题：**集中式**有链式查询、计数器定时查询、独立请求三种，另有分布式仲裁。链式查询线少但对**靠近控制器的设备有利**，独立请求响应最快但连线最多。
- **定时**规定总线操作的时间配合：**同步定时**由统一时钟约定，速度快、适合速度相近的设备；**异步定时**靠**握手（应答）**信号，适应性强但传输较慢。
- 异步定时按互锁程度又分不互锁、半互锁、全互锁，全互锁最可靠。
> 易错：仲裁决定"谁用总线"，定时决定"何时用、用多久"，两者是不同层面的问题。`,importance:4,hint:`仲裁选主设备，定时定时间配合`},{id:`co-bus-classification-perf`,subject:`co`,chapter:`总线`,tags:[`总线分类`,`性能指标`],title:`总线如何分类？主要性能指标有哪些？`,back:`## 结论
- 按**功能**分三类：**数据总线**（双向，宽度决定一次能传送多少位）、**地址总线**（单向，由 CPU 输出，根数决定可寻址空间大小）、**控制总线**（各信号方向不一）。
- 按**层次位置**分：片内总线、系统总线、通信总线；按传输方式还可分串行与并行。
- 主要性能指标：**总线宽度、总线频率、带宽（数据传输率）、信号线数、是否总线复用**。
> 易错：地址总线是**单向**的、数据总线是**双向**的；总线复用能减少引脚数，但会牺牲有效带宽。`,importance:3,hint:`数据双向，地址单向`},{id:`co-io-three-modes`,subject:`co`,chapter:`IO 与中断`,tags:[`IO 方式`,`对比`],title:`程序查询、中断与 DMA 三种 I/O 方式如何对比？`,back:`## 结论
- **程序查询**：CPU 反复读取状态寄存器等待设备就绪，CPU 与外设完全**串行**工作，效率最低。
- **中断方式**：外设就绪后主动发出中断请求，CPU 暂停现行程序，通过服务程序传送**一个字节或一个字**，传送仍由 CPU 参与。
- **DMA 方式**：由 DMA 控制器直接在主存与外设之间成批传送，**不经过 CPU**，只在传送前后打扰 CPU 一次，适合磁盘等高速块设备。
> 易错：中断方式仍要 CPU 执行服务程序搬数据，只有 DMA 才把传送工作从 CPU 手里拿走；DMA 请求的优先级通常**高于**中断请求。`,importance:5,hint:`查询串行，中断传一个，DMA 成批`},{id:`co-interrupt-handling-flow`,subject:`co`,chapter:`IO 与中断`,tags:[`中断处理`,`现场保护`],title:`中断服务程序的完整处理流程是怎样的？`,back:`## 结论
- 典型流程：**关中断 → 保护断点与现场 → 判优并转入服务程序 → 执行中断服务 → 恢复现场 → 开中断 → 中断返回**。
- 保护现场是把相关寄存器内容**压栈**，恢复现场则是**出栈**还原，两者的次序相反、内容一一对应。
- 中断返回由**中断返回指令**完成，它把保存的断点地址送回 PC，使原程序接着执行。
> 易错：先关中断是为了保证"保存现场"过程本身不被打断；单重中断在整个服务期间不开中断，多重中断则在进入服务程序后尽早开中断。`,importance:4,hint:`关中断保现场，恢复后开中断返回`},{id:`co-dma-transfer-cycle-stealing`,subject:`co`,chapter:`IO 与中断`,tags:[`DMA`,`周期挪用`],title:`DMA 的三种传送方式是什么？周期挪用指什么？`,back:`## 结论
- DMA 传送前，CPU 需向控制器设置**主存起始地址、传送长度、传送方向**并启动。
- **停止 CPU 访存**：DMA 独占总线成批传送，控制简单，但 CPU 长时间无法访存。
- **周期挪用（周期窃取）**：DMA 趁 CPU 不使用总线时**借用一个存储周期**传送一个字，CPU 只被延迟一个周期，主存利用率最高。
- **交替访存**：把存储周期分给 CPU 与 DMA 轮流使用，无需申请总线，但要求主存速度足够高。
> 易错：DMA 传送结束后由控制器发**中断**通知 CPU 做善后，整个传送过程既不执行程序，也不需要 CPU 干预。`,importance:5,hint:`周期挪用借一个存储周期`},{id:`co-io-interface-port`,subject:`co`,chapter:`IO 与中断`,tags:[`IO 接口`,`端口编址`],title:`I/O 接口有哪些功能？端口有哪两种编址方式？`,back:`## 结论
- I/O 接口的主要功能：**地址译码与设备选择、数据缓冲、速度匹配、格式转换（串并转换）、传送控制与状态报告**。
- **端口**是接口中可被 CPU 直接访问的寄存器，分为**数据端口、状态端口、控制端口**。
- **统一编址（存储器映射）**：I/O 与主存共用地址空间，用普通访存指令访问，不需专用指令，但会占用一部分主存地址空间。
- **独立编址**：I/O 有独立地址空间和专门的 I/O 指令，不占主存空间，但需要额外的控制信号线。
> 易错：状态端口与数据端口可能共用同一地址，靠读写信号区分；统一编址时不能使用 I/O 指令。`,importance:4,hint:`统一占主存空间，独立需专用指令`},{id:`co-interrupt-mask-multiple`,subject:`co`,chapter:`IO 与中断`,tags:[`多重中断`,`屏蔽字`],title:`中断屏蔽字如何控制多重中断的处理次序？`,back:`## 结论
- **多重中断**指在中断服务过程中**提前开中断**，允许更高优先级的中断打断当前服务程序，形成中断嵌套。
- **中断屏蔽字**的每一位对应一个中断源：$1$ 表示**屏蔽（禁止）**，$0$ 表示允许响应。
- 编写规则：某中断源的服务程序中，把**优先级不高于自己**的各位都置 $1$。
- 于是处理次序由屏蔽字决定，可与硬件优先级不同，从而灵活调整。
> 易错：屏蔽字中**自己也必须屏蔽**，否则同级中断会反复打断自己；屏蔽字只能改变**处理**次序，改变不了硬件判优决定的**响应**次序。`,importance:4,hint:`同级及低级都屏蔽，自己也要屏蔽`}],r=[{id:`co2-von-neumann-five-parts`,subject:`co`,chapter:`系统概述`,tags:[`冯·诺依曼`,`存储程序`],title:`冯·诺依曼结构包含哪五大部件？「存储程序」指什么？`,back:`## 结论
- 五大部件：**运算器、控制器、存储器、输入设备、输出设备**，运算器与控制器合称 CPU。
- **存储程序**：程序与数据同以二进制形式事先存入主存，机器按地址顺序自动取指执行，无须人工干预。
- 由此引申出**程序控制**：指令的执行顺序由程序本身决定，一条指令由操作码与地址码组成。
> 易错：存储程序与程序控制是冯·诺依曼机的两条基本思想，缺一不可；现代机器已由"以运算器为中心"改为"以存储器为中心"。`,importance:5,hint:`五部件加存储程序，指令与数据同存主存`},{id:`co2-cpu-memory-io-roles`,subject:`co`,chapter:`系统概述`,tags:[`CPU`,`主存`,`分工`],title:`CPU、主存与 I/O 设备在程序运行中如何分工？`,back:`## 结论
- **CPU**：控制器取指、译码并发出控制信号，运算器完成算术逻辑运算，寄存器暂存操作数与结果。
- **主存**：按地址存放指令和数据，是 CPU 能**直接访问**的部件，速度介于 CPU 与辅存之间。
- **I/O 设备**：经接口与主机相连，完成数据输入输出与格式转换，速度远低于 CPU，靠接口缓冲匹配速度。
- 执行一条指令的信息流：取指 → 译码 → 取数 → 运算 → 写回。
> 易错：CPU 不能直接访问辅存，辅存数据必须先调入主存；速度差由 I/O 接口解决。`,importance:4,hint:`CPU 算与控，主存直接访，外设靠接口`},{id:`co2-translate-compile-link`,subject:`co`,chapter:`系统概述`,tags:[`编译`,`汇编`,`链接`],title:`编译、汇编、链接与解释有什么区别？`,back:`## 结论
- **翻译**把源程序整体转换成目标程序后再执行；**解释**不生成完整目标程序，逐句翻译并立即执行，便于调试但效率低。
- 高级语言程序变为可执行程序一般经四步：**预处理 → 编译 → 汇编 → 链接**。
- **编译**把源程序变成汇编语言程序；**汇编**把汇编程序变成机器语言目标模块；**链接**把多个目标模块与库函数合并成可执行文件。
> 易错：汇编语言与机器语言一一对应，汇编只是助记符替换；链接解决的是模块之间的符号引用问题。`,importance:3,hint:`编译汇编链接三步走，解释不生成目标程序`},{id:`co2-system-hierarchy-levels`,subject:`co`,chapter:`系统概述`,tags:[`层次结构`,`虚拟机`],title:`计算机系统的层次结构由低到高是怎样的？`,back:`## 结论
- 由低到高依次是：**硬件 → 微程序级 → 机器语言级（传统机器级）→ 操作系统级 → 汇编语言级 → 高级语言级**。
- 低层是高层的基础，高层通过**翻译或解释**在低层上实现；层次越高，抽象程度越高、越面向用户。
- **虚拟机**指用软件扩充出来的层次，如操作系统把裸机改造成功能更强、使用更方便的机器。
> 易错：微程序级由硬件直接解释执行，处于软硬件交界处；操作系统级通过系统调用向上提供各种服务。`,importance:3,hint:`硬件打底，微程序到高级语言逐层虚拟`},{id:`co2-word-length-three-kinds`,subject:`co`,chapter:`系统概述`,tags:[`机器字长`,`存储字长`],title:`机器字长、存储字长与指令字长分别由什么决定？`,back:`## 结论
- **机器字长**：CPU 一次能并行处理的二进制位数，由通用寄存器与 ALU 的宽度决定，也决定数据总线宽度与运算精度。
- **存储字长**：一个存储单元能存放的二进制位数，与主存的编址方式有关。
- **指令字长**：一条指令的二进制位数，由操作码与地址码的位数决定。
- 三者可以互不相等，实际机器常取指令字长为存储字长的整数倍，以便按存储字整取指令。
> 易错：MDR 位数等于存储字长，MAR 位数等于地址线根数，二者都不等于机器字长。`,importance:4,hint:`机器字长看寄存器，存储字长看单元`},{id:`co2-amdahl-speedup`,subject:`co`,chapter:`性能指标`,tags:[`Amdahl`,`加速比`,`计算`],title:`Amdahl 定律怎么用？系统加速比如何计算？`,back:`## 结论
- 设可改进部分占原执行时间的比例为 $f$、该部分被加速 $n$ 倍，则整体加速比 $S=\\dfrac{1}{(1-f)+\\dfrac{f}{n}}$。
- 加速比上限为 $\\dfrac{1}{1-f}$，被不可改进的部分锁死。
- 例：$f=0.4$、$n\\to\\infty$ 时，$S\\le\\dfrac{1}{0.6}\\approx 1.67$。
- 推论：优化占比大的部分收益最高，只优化一小部分即使无限快也提升有限。
> 易错：$f$ 必须按**原系统**的执行时间比例代入，用优化之后的比例代入就错了。`,importance:5,hint:`加速比上限是 1 除以 1 减 f`},{id:`co2-throughput-vs-response`,subject:`co`,chapter:`性能指标`,tags:[`吞吐量`,`响应时间`],title:`吞吐量与响应时间有什么区别？各由什么决定？`,back:`## 结论
- **响应时间**（执行时间）：完成一个任务从开始到结束所用的时间，衡量"单个任务有多快"。
- **吞吐量**：单位时间内完成的任务数，衡量"整机单位时间能干多少活"。
- 缩短响应时间通常能提高吞吐量，但两者不等价：流水线提高了吞吐量，单条指令的延迟却基本不变。
- 吞吐量还受部件并行度、总线带宽、I/O 速度等因素影响。
> 易错：比较 CPU 快慢要看运行同一程序的**执行时间**，评价系统处理能力才看吞吐量，两个指标不可混用。`,importance:4,hint:`吞吐量看单位时间任务数，响应时间看单个任务`},{id:`co2-benchmark-programs`,subject:`co`,chapter:`性能指标`,tags:[`基准测试`,`性能评价`],title:`基准测试程序如何评价计算机性能？使用时要注意什么？`,back:`## 结论
- **基准测试程序**是一组有代表性的程序，用它们的运行时间来比较不同机器，比只看主频或指令条数可靠。
- 真实性由高到低：**真实程序（如 SPEC）> 核心程序（抽取热点片段）> 玩具程序（求素数、快排等）**。
- 常用表示：SPEC 比值（相对参考机的执行时间之比）、几何平均值（比算术平均更合理）。
> 易错：基准程序可能被厂商针对性优化，结果还受编译器与 Cache 容量影响；不同指令集的 MIPS 不能直接比较。`,importance:2,hint:`真实优于核心优于玩具，比值取几何平均`},{id:`co2-why-clock-not-performance`,subject:`co`,chapter:`性能指标`,tags:[`主频`,`执行时间`],title:`为什么主频高不等于性能好？`,back:`## 结论
- 程序执行时间 $=\\dfrac{\\text{指令条数}\\times CPI}{\\text{主频}}$，主频只是三个因素之一。
- 主频高但 **CPI 大**（访存多、流水线停顿多）时，执行时间反而更长；提频还受功耗与散热限制。
- **指令条数**由指令系统与编译器决定，不同指令集完成同一功能所需的指令数不同，不能直接比。
- 因此评价性能应比较运行同一程序的**执行时间**或基准测试结果。
> 易错：主频是时钟周期的倒数，只说明节拍快慢，不代表每一拍完成的有用工作更多。`,importance:4,hint:`指令数乘 CPI 除主频，主频只是一项`},{id:`co2-ips-mips-mflops`,subject:`co`,chapter:`性能指标`,tags:[`MIPS`,`MFLOPS`,`计算`],title:`IPS、MIPS 与 MFLOPS 之间如何换算？它们有什么局限？`,back:`## 结论
- $\\text{MIPS}=\\dfrac{\\text{主频}}{CPI\\times 10^{6}}$，表示每秒执行多少**百万条**指令；$1\\ \\text{MIPS}=10^{6}\\ \\text{IPS}$。
- **MFLOPS** 统计每秒百万次**浮点运算**，$1\\ \\text{MFLOPS}=10^{6}\\ \\text{FLOPS}$。
- 这些指标都与指令系统绑定：同一功能在不同机器上指令条数不同，故 **MIPS 不能跨指令集比较**。
> 易错：MIPS 高不代表跑得快，可能每条指令做的事更少，只有同机同程序才可比。`,importance:4,hint:`MIPS 是主频除 CPI 再除 10 的 6 次方`},{id:`co2-radix-conversion`,subject:`co`,chapter:`数据的表示与运算`,tags:[`进制转换`,`计算`],title:`二、八、十、十六进制之间如何快速互转？`,back:`## 结论
- 二进制与八、十六进制：以小数点为界，**三位一组**或**四位一组**直接对应，不足位补 $0$。
- 其他进制转十进制：按权展开求和，如 $1011.01_{2}=11.25$。
- 十进制整数转其他进制：**除基取余、逆序排列**；十进制小数用**乘基取整、顺序排列**。
- 十进制小数常无法用有限位二进制精确表示，如 $0.1$ 就是无限循环小数。
> 易错：小数部分也要向小数点方向分组补零，方向弄反必错；八进制一位对应 $3$ 位二进制，十六进制一位对应 $4$ 位。`,importance:4,hint:`三位一组转八，四位一组转十六`},{id:`co2-ascii-chinese-encoding`,subject:`co`,chapter:`数据的表示与运算`,tags:[`ASCII`,`汉字编码`],title:`ASCII 码与汉字编码各是怎样组织的？`,back:`## 结论
- **ASCII** 用 $7$ 位表示 $128$ 个字符，含 $32$ 个控制字符与 $95$ 个可显示字符；数字、大小写字母的编码各自连续。
- 大写字母与小写字母的编码相差 $32$，数字 $0\\sim9$ 的编码为 $48\\sim57$。
- 汉字编码：**区位码**各加 $32$ 得**国标码**，国标码两字节最高位置 $1$ 得**机内码**，以区别于 ASCII；显示时用**字形码**。
> 易错：一个汉字机内码占 $2\\mathrm{B}$，ASCII 字符占 $1\\mathrm{B}$；汉字不能只用 $7$ 位编码。`,importance:4,hint:`国标码加最高位得机内码`},{id:`co2-code-distance-capability`,subject:`co`,chapter:`数据的表示与运算`,tags:[`码距`,`检错纠错`],title:`码距与检错、纠错能力是什么关系？`,back:`## 结论
- **码距** $d$ 是任意两个合法码字之间不同的二进制位数的最小值，码距越大抗干扰能力越强。
- 码距为 $d$ 时最多**检出 $d-1$ 位错**，或**纠正 $\\lfloor (d-1)/2\\rfloor$ 位错**；若要检 $e$ 位错同时纠 $t$ 位错，需 $d\\ge e+t+1$ 且 $e\\ge t$。
- **奇偶校验**码距为 $2$，只能查出**奇数个**错且无法定位；**海明码**码距为 $3$，可纠正一位错。
> 易错：增加一位校验位只能做到"检错"或"纠错"之一；要同时纠 $1$ 位错、检 $2$ 位错需码距 $4$。`,importance:5,hint:`检 d 减 1 位，纠一半向下取整`},{id:`co2-hamming-check-bits`,subject:`co`,chapter:`数据的表示与运算`,tags:[`海明码`,`计算`],title:`海明码需要几位校验位？如何定位出错位？`,back:`## 结论
- 设信息位 $n$ 位、校验位 $k$ 位，须满足 $2^{k}\\ge n+k+1$，其中 $1$ 对应"无错"这一状态。
- 校验位插在第 $1,2,4,8,\\cdots$ 即 $2^{i}$ 位上，其余位置依次放信息位。
- 各组校验结果拼成**指误字**，其值就是出错位号，为 $0$ 表示无错，取反即完成纠正。
- 例：$n=8$ 时 $2^{4}=16\\ge 8+4+1$，故 $k=4$，编码总长 $12$ 位。
> 易错：海明码只能纠正一位错，且校验位必须放在 $2^{i}$ 位上，不能随意排列。`,importance:5,hint:`2 的 k 次方不小于 n 加 k 加 1`},{id:`co2-crc-mod2-division`,subject:`co`,chapter:`数据的表示与运算`,tags:[`CRC`,`模 2 除法`,`计算`],title:`CRC 校验如何用生成多项式和模 2 除法求校验码？`,back:`## 结论
- 生成多项式的**最高次幂 $k$** 就是校验位数：若 $G(x)$ 对应 $k+1$ 位二进制串，则有 $k$ 位校验位。
- 编码：在信息位后**添 $k$ 个 $0$**，用**模 $2$ 除法**（按位异或、不借位）除以 $G(x)$，所得**余数**即校验位，用它替换那 $k$ 个 $0$ 便得 CRC 码。
- 校验：接收方用同一多项式去除收到的码字，余数为 $0$ 判为无错。
> 易错：模 $2$ 除法只做异或、不产生借位；余数不足 $k$ 位时高位要补 $0$；CRC 主要用于检错而非纠错。`,importance:4,hint:`信息后添 k 个 0，模 2 除得余数`},{id:`co2-twos-complement-mod`,subject:`co`,chapter:`数据的表示与运算`,tags:[`补码`,`模运算`],title:`为什么定点数用补码表示？模运算怎么解释？`,back:`## 结论
- 补码把减法并入加法：$[x-y]_{\\text{补}}=[x]_{\\text{补}}+[-y]_{\\text{补}}$，只需一个加法器，硬件简单、运算快。
- 原理是**模运算**：$n$ 位补码以 $2^{n}$ 为模，$-y$ 与 $2^{n}-y$ 同余，相加后**最高位的进位自然丢失**，结果仍然正确。
- 补码**零的表示唯一**，并且比原码、反码多表示一个最小负数 $-2^{n-1}$。
- 符号位与数值位一起参加运算，不必单独处理符号。
> 易错：补码的符号位是"权为负"的数值位，并不是独立的符号标记。`,importance:5,hint:`模 2 的 n 次方，进位丢弃结果对`},{id:`co2-one-bit-multiplication`,subject:`co`,chapter:`数据的表示与运算`,tags:[`一位乘法`,`Booth`,`计算`],title:`定点一位乘法：原码与补码 Booth 法的规则有何不同？`,back:`## 结论
- **原码一位乘法**：符号位单独异或决定积的符号，数值部分取绝对值相乘；乘数末位为 $1$ 加被乘数、为 $0$ 加 $0$，每步**部分积右移一位**，共 $n$ 次。
- **补码 Booth 一位乘法**：符号位参加运算，由乘数最低位与其右邻附加位组成判断对：$01$ 加 $[x]_{\\text{补}}$，$10$ 加 $[-x]_{\\text{补}}$，$00$、$11$ 不加，每步做**算术右移**。
- Booth 法遇到连 $0$、连 $1$ 的乘数时可减少加法次数。
> 易错：补码乘法必须算术右移，部分积取双符号位以防止溢出。`,importance:4,hint:`原码看末位，Booth 看末两位`},{id:`co2-alternating-division`,subject:`co`,chapter:`数据的表示与运算`,tags:[`除法`,`不恢复余数`,`计算`],title:`原码加减交替除法（不恢复余数法）的规则是什么？`,back:`## 结论
- 原码除法**符号单独处理**：商的符号由两数符号异或得到，数值部分用绝对值相除。
- **恢复余数法**：先减除数，若余数为负就加回除数恢复原值，再上商 $0$，步骤较多。
- **加减交替法**：余数为正时上商 $1$，下一步**减**除数；余数为负时上商 $0$，下一步**加**除数，然后左移一位，省去了恢复余数这一步，实际机器多采用它。
> 易错：减法用补码加法实现；每一步上商后都要左移一位再做下一步，商的位数等于数值位位数。`,importance:3,hint:`正上商 1 下步减，负上商 0 下步加`},{id:`co2-arithmetic-vs-logical-shift`,subject:`co`,chapter:`数据的表示与运算`,tags:[`移位`,`算术移位`],title:`算术移位与逻辑移位有什么区别？移位对真值有什么影响？`,back:`## 结论
- **算术移位**面向带符号数，**符号位保持不变**，只移动数值位；**逻辑移位**把整串代码一起移，空位一律补 $0$。
- 补位规则：原码左右移都补 $0$；补码**左移补 $0$、右移补符号位**；反码左移补 $0$、右移补 $1$。
- 对真值的影响：左移一位相当于乘 $2$，右移一位相当于除以 $2$（舍去低位），前提是不溢出、不丢失有效高位。
- 逻辑左移会丢掉最高有效位，逻辑右移丢掉最低位。
> 易错：左移时若丢掉的位是有效高位就是溢出，结果错误；循环移位不改变数值大小。`,importance:4,hint:`算术保符号，逻辑一律补零`},{id:`co2-ieee754-special-values`,subject:`co`,chapter:`数据的表示与运算`,tags:[`IEEE754`,`特殊值`,`计算`],title:`IEEE 754 中非规格化数、无穷大与 NaN 如何表示？`,back:`## 结论
- 阶码字段全 $0$、尾数非 $0$：**非规格化数**，隐含位为 $0$，真值 $(-1)^{S}\\times 0.M\\times 2^{1-bias}$。
- 阶码全 $0$、尾数全 $0$：**$\\pm0$**。
- 阶码全 $1$、尾数全 $0$：**$\\pm\\infty$**；阶码全 $1$、尾数非 $0$：**NaN**，如 $0/0$ 的结果。
> 易错：非规格化数的隐含位是 $0$、阶码按 $1-bias$ 算，不能再用 $1.M$ 公式；NaN 参与运算结果仍是 NaN。`,importance:5,hint:`阶码全 0 非规格化，全 1 无穷或 NaN`},{id:`co2-single-float-range-precision`,subject:`co`,chapter:`数据的表示与运算`,tags:[`IEEE754`,`表示范围`,`计算`],title:`IEEE 754 单精度能表示的最大数、最小正数与精度分别是多少？`,back:`## 结论
- 单精度共 $32$ 位：$1$ 位符号、$8$ 位阶码、$23$ 位尾数，偏移量 $127$。
- 最大规格化数 $(2-2^{-23})\\times 2^{127}\\approx 3.4\\times 10^{38}$；最小规格化正数 $2^{-126}$。
- 精度看尾数：有效位 $24$ 位，$\\varepsilon=2^{-23}$，不超过 $2^{24}$ 的整数都能精确表示。
> 易错：范围看阶码、精度看尾数，二者不可互换；双精度用 $11$ 位阶码、$52$ 位尾数。`,importance:5,hint:`最大 2 减 2 的负 23 次方乘 2 的 127 次方`},{id:`co2-float-alignment-rounding`,subject:`co`,chapter:`数据的表示与运算`,tags:[`对阶`,`舍入`],title:`对阶为什么必须小阶向大阶看齐？浮点数有哪些舍入方法？`,back:`## 结论
- 对阶就是把两个数的阶码变成相同。**小阶向大阶看齐**时，阶码小的数尾数**右移**，移出的是低位，误差小且阶码只增不减。
- 反过来让大阶向小阶看齐，大阶数的尾数要**左移**，高位被移出会丢失有效数字，还可能溢出，误差大得多。
- 舍入方法：**就近舍入**（IEEE 默认"就近舍入到偶数"）、**朝 $0$ 舍入**（截断）、**朝 $+\\infty$ 舍入**、**朝 $-\\infty$ 舍入**；手工计算常用**恒置 $1$ 法**或 $0$ 舍 $1$ 入。
> 易错：右移移出的位不能直接丢弃，要留作舍入判断；舍入后可能又需要规格化。`,importance:5,hint:`小阶右移损失小，舍入后可能再规格化`},{id:`co2-byte-word-addressing`,subject:`co`,chapter:`数据的表示与运算`,tags:[`编址`,`字地址`,`计算`],title:`按字节编址与按字编址有什么区别？字地址与字节地址如何换算？`,back:`## 结论
- **编址单位**决定一个地址对应多少位：按字节编址时每地址对应 $8$ 位，按字编址时每地址对应一个机器字。
- 按字节编址时地址连续递增 $1$；按字编址时字地址 $=$ 字节地址 $\\div$ 字长字节数，即右移 $\\log_{2}$ 字长位。
- 例：字长 $32$ 位、按字节编址时，第 $100$ 个字的地址是 $400$；按字编址则地址就是 $100$。
> 易错：编址单位与实际访存单位不一定相同；字长 $32$ 位时一个字占 $4$ 字节，不是 $1$ 字节。`,importance:4,hint:`字地址等于字节地址除字长字节数`},{id:`co2-struct-alignment-size`,subject:`co`,chapter:`数据的表示与运算`,tags:[`边界对齐`,`存储空间`,`计算`],title:`边界对齐时结构体的存储空间如何计算？`,back:"## 结论\n- 每个成员的**起始偏移**必须是其自身长度（或编译规定的对齐值）的整数倍，否则要在它前面**插入填充字节**。\n- 结构体的**总大小**必须是最大成员长度的整数倍，不足时末尾再填充，这样数组中每个元素都能对齐。\n- 例：`char` + `int` + `char` 在 $32$ 位机上为 $1+3+4+1+3=12$ 字节：`int` 需 $4$ 字节对齐，总大小也对齐到 $4$。\n> 易错：调整成员顺序（把长的放前面）常能减少填充；填充字节不存有效数据，故结构体大小大于各成员长度之和。",importance:4,hint:`偏移按自身长度对齐，总大小按最大成员对齐`}],i=[{id:`co3-memory-classify`,subject:`co`,chapter:`存储系统`,tags:[`存储器分类`,`存取方式`],title:`存储器按存取方式、介质、易失性与作用分别可以分成哪几类？`,back:`## 结论
- 按**存取方式**分：**随机存取**（RAM，访问时间与地址无关）、**顺序存取**（磁带）、**直接存取**（磁盘）、**相联存取**（按内容查找）。
- 按**介质**分半导体、磁表面与光存储器；按**易失性**分易失（DRAM）与非易失（ROM、Flash、磁盘）。
- 按**作用**分寄存器、Cache、主存、辅存，越靠上越快越贵越小。
> 易错：随机存取强调时间与地址无关，不是"能任意改写"；相联存取靠内容比较，硬件代价最高。`,importance:3,hint:`随机顺序直接相联，四种存取方式`},{id:`co3-memory-hierarchy-transfer-unit`,subject:`co`,chapter:`存储系统`,tags:[`存储层次`,`传送单位`],title:`存储层次中各层之间传送数据的单位是什么？各层指标如何变化？`,back:`## 结论
- 层次自上而下为**寄存器、Cache、主存、辅存**：速度递减、容量递增、单位成本递减。
- **CPU 与 Cache** 之间按**字**传送；**Cache 与主存**之间以**块（行）**为单位整块调入调出。
- **主存与辅存**之间以**页**或段为单位传送，由操作系统与硬件共同管理。
- 整块传送把一次缺失的代价摊到整块数据上，靠的正是空间局部性。
> 易错：Cache 的调入与替换由**硬件**完成、对程序员透明；虚存的页调度需要操作系统参与。`,importance:3,hint:`字对上 Cache，块对主存，页对辅存`},{id:`co3-memory-perf-metrics`,subject:`co`,chapter:`存储系统`,tags:[`性能指标`,`存取周期`],title:`存取时间、存取周期与存储器带宽有什么区别？`,back:`## 结论
- **存取时间** $T_a$ 是从发出读写命令到数据可用的时间；**存取周期** $T_m$ 是连续两次独立访存所需的最小间隔。
- 因为 DRAM 读出后要**重写**、总线与存储阵列需要恢复，所以总有 $T_m>T_a$。
- **带宽** $=$ 一次传送的数据位数 $\\div$ 存取周期，表示单位时间能传送多少数据。
- 其余常用指标：容量、单位成本、功耗与可靠性。
> 易错：算带宽必须用存取周期而不是存取时间；两者相差一个恢复时间，混用会把带宽算大。`,importance:3,hint:`周期等于时间加恢复，带宽除周期`},{id:`co3-locality-analysis`,subject:`co`,chapter:`存储系统`,tags:[`局部性`,`数组访问`],title:`怎样判断一段程序的时间局部性与空间局部性？`,back:`## 结论
- **时间局部性**看同一数据或指令是否被**反复访问**，典型是循环体内的变量与循环指令。
- **空间局部性**看访问地址是否**集中连续**，典型是数组顺序遍历、指令顺序执行。
- 二维数组按**行**访问时相邻元素地址连续，空间局部性好；按**列**访问时每步跨越一整行，局部性差。
- 顺序执行的指令本身空间局部性很强，所以取指也能被指令 Cache 很好地服务。
> 易错：判据是**地址访问序列**而不是语句多少；递归调用栈与紧凑结构体同样有良好局部性。`,importance:4,hint:`看地址序列，行访问优于列访问`},{id:`co3-rom-family`,subject:`co`,chapter:`存储系统`,tags:[`ROM`,`Flash`],title:`PROM、EPROM、EEPROM 与 Flash 各有什么特点？`,back:`## 结论
- **掩膜 ROM** 在制造时写入，用户不能修改；**PROM** 可由用户**一次性**写入，写完即固化。
- **EPROM** 用紫外线擦除后可重写，但擦除是**整片**进行，还需专用设备。
- **EEPROM** 可**电擦除**、能按字节改写、支持在线编程，代价是集成度低、价格高。
- **Flash** 由 EEPROM 发展而来，擦写以**块**为单位、速度快容量大，广泛用作 U 盘与固态硬盘。
> 易错：它们都**掉电不丢失**，属非易失性存储器；Flash 改写一个字节常要先搬移整个块。`,importance:3,hint:`一次写入、紫外擦、电擦、按块擦`},{id:`co3-dram-destructive-read`,subject:`co`,chapter:`存储系统`,tags:[`DRAM`,`破坏性读出`],title:`什么是破坏性读出？为什么 DRAM 读出后必须重写？`,back:`## 结论
- DRAM 用**电容**上的电荷表示信息，读出时电荷被送到位线上并被消耗，原信息随即被破坏，故称**破坏性读出**。
- 因此每次读操作后都要**重写（再生）**，把数据重新写回电容，这使读写周期长于存取时间。
- SRAM 用**双稳态触发器**存储，读出的是电平而不改变状态，属**非破坏性读出**，无需重写。
- 重写由存储器内部电路自动完成，与周期性刷新是两件事。
> 易错：刷新是**周期性**的、按行补齐电荷；重写是**每次读出后**的恢复动作，不能混为一谈。`,importance:4,hint:`电容读出即破坏，读后必重写`},{id:`co3-dram-address-multiplex-pins`,subject:`co`,chapter:`存储系统`,tags:[`DRAM`,`地址复用`,`计算`],title:`DRAM 为什么把地址分两次传送？芯片引脚数如何计算？`,back:`## 结论
- DRAM 容量大、地址位数多，若一次送入会使**地址引脚**过多，故把地址拆成**行地址与列地址**分两次传送，由 $\\overline{RAS}$ 与 $\\overline{CAS}$ 分别选通。
- 于是地址引脚约为 $\\dfrac{1}{2}\\log_{2}(\\text{单元数})$ 根，是原来的一半。
- 例：$16\\mathrm{M}\\times 8$ 位 DRAM 需 $24$ 位地址，地址引脚只需 $12$ 根，数据引脚 $8$ 根。
> 易错：地址复用是用**时间换引脚数**，会使访存变慢；计算时先求单元数对应的地址位数再折半。`,importance:4,hint:`行列分两次送，地址引脚折半`},{id:`co3-dram-refresh-modes`,subject:`co`,chapter:`存储系统`,tags:[`DRAM`,`刷新方式`],title:`集中、分散与异步刷新各有什么特点？刷新开销怎么算？`,back:`## 结论
- 设芯片有 $R$ 行、刷新间隔为 $t$。**集中刷新**连续刷完所有行，期间无法访存，形成**死时间**，比例为 $R\\cdot t_{\\text{刷}}/t$。
- **分散刷新**在每个存取周期后顺带刷新一行，没有死区，但**存取周期变长**、系统变慢。
- **异步刷新**把 $t$ 均分给 $R$ 行，每过 $t/R$ 刷新一行，兼取两者之长，最常用。
- 刷新以**行**为单位，只需行地址，由存储器控制器自动完成，不占用 CPU。
> 易错：三种方式刷新总次数相同，差别只在时间如何分配；刷新不是指令，也不需要列地址。`,importance:4,hint:`集中有死区，分散周期长，异步折中`},{id:`co3-decoding-line-partial`,subject:`co`,chapter:`存储系统`,tags:[`地址译码`,`片选`],title:`线选法与译码片选法有什么区别？部分译码为什么会地址重叠？`,back:`## 结论
- **线选法**用一根高位地址线直接接一片的片选端，不用译码器，但地址**不连续**、空间浪费，只适合芯片很少的场合。
- **全译码**把全部高位地址送入译码器，每个存储单元的地址**唯一**，不存在重叠。
- **部分译码**只用部分高位地址译码，未参与译码的地址位取任意值都选中同一片，于是**一个单元对应多个地址**，即地址重叠。
- 片选信号通常**低电平有效**；片选无效时芯片数据线呈**高阻**，不会干扰总线。
> 易错：地址重叠会被误认为容量变大，还可能导致误改数据；线选法可能出现两片同时被选中。`,importance:4,hint:`全译码地址唯一，部分译码会重叠`},{id:`co3-bit-word-expansion-count`,subject:`co`,chapter:`存储系统`,tags:[`存储器扩展`,`芯片数`,`计算`],title:`字位同时扩展时，所需芯片数与各信号线如何连接？`,back:`## 结论
- 位扩展片数 $=$ 目标字长 $\\div$ 单片字长；字扩展组数 $=$ 目标单元数 $\\div$ 单片单元数；总片数 $=$ 两者之积。
- 位扩展时各片**地址线与片选线全并联**，数据线分别接数据总线的不同位。
- 字扩展时各片**数据线并联**，由高位地址经译码器分别提供片选，保证同一时刻只选中一组。
- 例：$1\\mathrm{K}\\times 4$ 位芯片构成 $2\\mathrm{K}\\times 8$ 位主存需 $4$ 片。
> 易错：芯片地址引脚只接**低位**地址线，多出的高位地址一律用于片选；先位扩展凑字长，再字扩展凑容量。`,importance:5,hint:`位置扩并数据，字扩展译片选`},{id:`co3-dual-port-ram`,subject:`co`,chapter:`存储系统`,tags:[`双端口 RAM`,`访存冲突`],title:`双端口 RAM 如何提高并行访存能力？冲突时怎么处理？`,back:`## 结论
- **双端口 RAM** 具有两套独立的地址、数据与控制端口，允许两个部件（CPU 与 I/O、两个 CPU）同时访问同一存储器。
- 两端口访问**不同单元**时可以完全并行，各自独立读写。
- 访问**同一单元**时发生冲突，由仲裁逻辑决定谁先访问，另一方通过忙信号**插入等待**。
- 它提高的是**并行访问能力**，而多模块交叉存储器靠多个模块错开启动来提高**带宽**。
> 易错：双端口 RAM 也不能同时读写同一单元；冲突是让一方等待，而不是两次访问结果都出错。`,importance:2,hint:`两套端口可并行，同单元须仲裁`},{id:`co3-interleaving-n-word-time`,subject:`co`,chapter:`存储系统`,tags:[`低位交叉`,`带宽`,`计算`],title:`低位交叉存储器连续读取 $n$ 个字需要多少时间？带宽是多少？`,back:`## 结论
- 设模块数为 $m$、存取周期为 $T$、总线传送周期为 $r$，各模块错开 $r$ 启动。
- 连续读取 $n$ 个字的时间 $t=T+(n-1)r$：第一个字要等一个完整存取周期，之后每 $r$ 就能送出一个字。
- 要不间断供数必须满足 $m\\ge T/r$；此时带宽可达单模块的 $m$ 倍，$m$ 越大越接近 $1/r$ 的极限。
- 高位交叉（顺序编址）中连续地址落在同一模块，只能扩展容量，**不能提高带宽**。
> 易错：$T$ 是存取周期、$r$ 是总线传送周期，两者不能混；$n$ 个字用的是 $(n-1)r$ 而不是 $nr$。`,importance:5,hint:`首字等 T，其余每 r 一个字`},{id:`co3-virt-addr-page-table-size`,subject:`co`,chapter:`存储系统`,tags:[`虚拟存储`,`页表大小`,`计算`],title:`虚拟地址如何划分字段？页表项位数与页表大小怎么算？`,back:`## 结论
- 虚拟地址 $=$ **虚页号 $+$ 页内偏移**，偏移位数 $=\\log_{2}(\\text{页面大小})$；物理地址 $=$ **物理页号 $+$ 同一偏移**。
- 页表项位数 $=$ 物理页号位数 $+$ 有效位、修改位等标志位，常凑成整字节。
- 页表大小 $=$ 虚页数 $\\times$ 页表项长度，虚页数 $=2^{\\text{虚页号位数}}$。
- 页表本身按页存放时占 $\\dfrac{\\text{页表大小}}{\\text{页面大小}}$ 页，页数太多就要用**多级页表**。
> 易错：页内偏移在变换前后**不变**；多级页表省的是连续空间，代价是访存次数增加。`,importance:5,hint:`虚页号加偏移，虚页数乘表项长`},{id:`co3-tlb-access-count-page-fault`,subject:`co`,chapter:`存储系统`,tags:[`TLB`,`缺页`,`访存次数`],title:`TLB 命中与缺失时各需访存几次？缺页时系统做了什么？`,back:`## 结论
- 虚页号先查 **TLB**：命中即得物理页号，再用它访问 Cache 或主存取数，比缺失少一次查页表的访存。
- TLB 缺失时要到主存中查**页表**取页表项，再**回填 TLB**，故访存次数多一次。
- 页表项有效位为 $0$ 说明该页不在主存，触发**缺页**：操作系统从辅存调入页面、更新页表与 TLB，再重新执行这条指令。
- 最坏情况依次包含查 TLB、查页表、访存取数多次主存访问，缺页还要加一次磁盘 I/O。
> 易错：TLB 缺失**不等于**缺页；缺页时必然 TLB 缺失，但 TLB 缺失时页很可能就在主存里。`,importance:5,hint:`命中少一次查表，缺页要调页`},{id:`co3-paging-segmentation-segpage`,subject:`co`,chapter:`存储系统`,tags:[`页式`,`段式`,`段页式`],title:`页式、段式与段页式虚拟存储各有什么优缺点？`,back:`## 结论
- **页式**：地址空间等分成固定大小的页，划分由硬件完成、对用户透明；页内无碎片，最后一页有**内部碎片**。
- **段式**：按逻辑单位（代码段、数据段）划分，长度可变、便于共享与保护，但会产生**外部碎片**。
- **段页式**：先按逻辑分段，段内再分页，兼具逻辑性与内存利用率，地址变换需先查**段表**再查**页表**，开销最大。
- 页式只需一次查表即可定位，段页式至少要查两次表。
> 易错：页的划分对用户不可见，段是用户可见的逻辑单位；共享与保护看**段**，内存分配看**页**。`,importance:4,hint:`页有内部碎片，段有外部碎片`},{id:`co3-cache-why-fast`,subject:`co`,chapter:`Cache`,tags:[`Cache`,`局部性`],title:`Cache 为什么能提高访存速度？它对程序员透明吗？`,back:`## 结论
- 依据**局部性原理**，把主存中近期会被访问的**块**整块复制到速度更快、容量更小的 Cache 中，让多数访问在上层完成。
- 块传送使一次缺失换来后续多次命中，用空间局部性摊薄调入开销。
- 命中率高时平均访问时间接近 Cache 而远小于主存，实现**速度接近 Cache、容量仍是主存**。
- 地址映射、替换与写回全部由**硬件**完成，对程序员与应用**透明**，程序无需改一行代码即可加速。
> 易错：Cache 的内容只是主存的**副本**，不是独立地址空间；命中率受容量、块大小与程序局部性共同影响。`,importance:4,hint:`局部性加整块调入，硬件透明`},{id:`co3-cache-miss-penalty-amat`,subject:`co`,chapter:`Cache`,tags:[`Cache`,`缺失代价`,`计算`],title:`缺失代价是什么？平均访存时间 AMAT 怎么算？`,back:`## 结论
- $AMAT=$ 命中时间 $+$ 缺失率 $\\times$ 缺失代价，缺失代价是从下层取回整块并装入 Cache 的时间。
- 同时访问时 $T_a=H\\cdot T_c+(1-H)\\cdot T_m$；先查 Cache 未命中再访存时 $T_a=T_c+(1-H)\\cdot T_m$。
- 例：$T_c=1$、$T_m=20$ 周期、$H=0.95$ 时，$T_a=0.95+0.05\\times 20=1.95$ 周期。
- 提高命中率、降低缺失代价（多级 Cache、写缓冲）都能缩短 $AMAT$。
> 易错：两种访存顺序的公式不能混用；先查 Cache 时 $T_c$ 已含在过程中，不要再重复相加。`,importance:5,hint:`命中时间加缺失率乘代价`},{id:`co3-cache-capacity-field-calc`,subject:`co`,chapter:`Cache`,tags:[`Cache`,`地址划分`,`计算`],title:`由容量、块大小与路数如何求行数、组数和各地址字段的位数？`,back:`## 结论
- 行数 $=\\dfrac{\\text{Cache 容量}}{\\text{块大小}}$；组数 $=\\dfrac{\\text{行数}}{\\text{路数}}$。
- 块内地址位数 $b=\\log_{2}(\\text{块大小})$，组号位数 $=\\log_{2}(\\text{组数})$。
- 标记位数 $=$ 主存地址位数 $-$ 组号位数 $-$ $b$；全相联没有组号字段，标记最长。
- 直接映射行号 $=$ 块号 $\\bmod$ 行数，组相联组号 $=$ 块号 $\\bmod$ 组数，组内可放任意一路。
> 易错：相联度加倍则组数减半、组号少一位，**标记反而多一位**。`,importance:5,hint:`行数除块，组数除路，组号取对数`},{id:`co3-cache-set-assoc-hardware`,subject:`co`,chapter:`Cache`,tags:[`组相联`,`标记比较`],title:`组相联 Cache 如何判断命中？为什么比较器数量随路数增加？`,back:`## 结论
- 组相联把主存块按模映射到某一**组**，组内各路的标记必须**同时比较**，某路标记相等且有效位为 $1$ 即为命中。
- 因此每组需要**路数**个标记比较器，再用一个多路选择器按命中结果选出数据。
- **全相联**相当于只有一组、路数等于行数，要与所有行的标记比较，硬件成本最高，只用于行数很少的场合。
- **直接映射**每组只有一路，只需一个比较器，硬件最省，但冲突缺失最多。
> 易错：提高相联度降低冲突缺失，代价是比较器与选择器成倍增长，本质是**用硬件换命中率**。`,importance:3,hint:`组内标记同时比，路数即比较器数`},{id:`co3-cache-replace-lru-hw`,subject:`co`,chapter:`Cache`,tags:[`LRU`,`替换算法`],title:`LRU 替换算法如何用硬件实现？它比 FIFO、随机好在哪？`,back:`## 结论
- **LRU** 淘汰最久未被访问的块，符合时间局部性，命中率通常高于 FIFO 与随机算法。
- 实现思路一：每行设一个**计数器**，命中行清零、其余加一，替换时选计数值最大的一路。
- 实现思路二：用**栈**记录各路的访问顺序，命中或装入时把该路移到栈顶，栈底就是最久未使用者。
- 比较只需在**组内**（全相联则在全部行之间）进行，故路数越多硬件越复杂。
> 易错：LRU 要记录访问历史，开销大于 FIFO；LFU 按访问**次数**淘汰，依据与 LRU 不同。`,importance:3,hint:`计数器清零或栈顶移动，栈底淘汰`},{id:`co3-cache-write-buffer`,subject:`co`,chapter:`Cache`,tags:[`写策略`,`写缓冲`],title:`写缓冲起什么作用？写直达与写回为什么都需要它？`,back:`## 结论
- **写缓冲**是位于 Cache 与主存之间的小容量先进先出队列：CPU 把数据放入缓冲即可继续执行，由缓冲在后台写入主存。
- **写直达**每次写命中都写主存，访存频繁，配写缓冲后能把等待主存的时间隐藏起来。
- **写回**只在被替换且**脏位**为 $1$ 时才写回主存，平时不占总线，但替换瞬间可能需要等待。
- 写不命中时，**写分配**先调入整块再写，**非写分配**直接写主存；常见搭配是写回配写分配、写直达配非写分配。
> 易错：缓冲填满时 CPU 仍会被迫等待；写回法的 Cache 与主存可能**不一致**，需一致性协议维护。`,importance:4,hint:`写缓冲隐藏写主存时间`},{id:`co3-cache-split-unified`,subject:`co`,chapter:`Cache`,tags:[`分离 Cache`,`统一 Cache`],title:`分离 Cache 与统一 Cache 各有什么利弊？为什么 L1 要分指令与数据 Cache？`,back:`## 结论
- **统一 Cache** 把指令与数据放在一起，容量利用率高、总缺失率较低，但取指与取数会争用同一个 Cache。
- **分离 Cache** 把 L1 分成**指令 Cache 与数据 Cache**，两者可同时访问，取指与访存不再争用端口，便于流水线并行。
- 代价是同样容量被拆成两块，各自命中率略低，且需要两套标记与替换逻辑。
- 典型方案是 **L1 分离、L2 统一**：L1 保证带宽，L2 保证容量与命中率。
> 易错：分离 Cache 的收益是**减少结构冒险**，不是提高命中率；统一 L2 能同时容纳指令与数据，利用率更高。`,importance:3,hint:`L1 分离减冲突，L2 统一省空间`},{id:`co3-multilevel-cache-avg-time`,subject:`co`,chapter:`Cache`,tags:[`多级 Cache`,`平均访问时间`,`计算`],title:`多级 Cache 中 L1、L2、L3 各承担什么角色？平均访问时间如何逐层计算？`,back:`## 结论
- 典型分工：**L1** 紧邻 CPU、容量小速度最快；**L2** 居中，常由各核独享；**L3** 容量最大，由所有核共享。
- L1 缺失时到 L2 去取，避免每次都访主存，从而显著**降低平均缺失代价**。
- 设 L1 命中时间 $t_1$、缺失率 $M_1$，L2 为 $t_2$ 与 $M_2$，主存为 $t_m$，则 $T_a=t_1+M_1\\big(t_2+M_2\\cdot t_m\\big)$。
> 易错：L2 的缺失率是**局部缺失率**，指 L1 缺失的访问中又在 L2 缺失的比例，别与全局缺失率混用。`,importance:5,hint:`L1 快 L3 大，逐层嵌套算时间`},{id:`co3-cache-block-size-tradeoff`,subject:`co`,chapter:`Cache`,tags:[`块大小`,`命中率`],title:`块大小如何影响命中率与缺失代价？块、行数与容量是什么关系？`,back:`## 结论
- 容量一定时块越大则**行数越少**：行数 $=\\dfrac{\\text{容量}}{\\text{块大小}}$。
- 块增大能更好地利用**空间局部性**，减少首次访问造成的强制缺失；但行数随之减少，**冲突缺失**增加。
- 块越大不仅命中时间略升，**缺失代价**也明显上升，因为每次都要从主存搬来更长的一块。
- 所以块大小存在**最优值**，通常取几个字到一个存储字组，并与主存的传送单位相匹配。
> 易错：不能只按局部性一味加大块；行数太少还会放大替换算法的影响，冲突缺失可能反超收益。`,importance:3,hint:`块大省强制缺失，行少增冲突`}],a=[{id:`co4-relative-addressing-pc`,subject:`co`,chapter:`指令系统`,tags:[`寻址方式`,`计算`],title:`相对寻址的转移目标地址怎么算？偏移量为什么按补码看？`,back:`## 公式
- 转移目标地址 $EA=(PC)+A$，这里的 $PC$ 是**取完本条指令之后**的值，即已经自增了指令字长。
- 形式地址 $A$ 按**补码**解释，可正可负，所以既能向前转移也能向后转移。
## 意义
- 位移量相对当前 PC，写出的程序与装入位置无关，便于**程序浮动**、支持位置无关代码。
- 相对寻址多用于转移指令和循环控制。
> 易错：先让 PC 自增再与 A 相加；A 为负时用补码相加，别丢掉符号位。`,importance:5,hint:`先自增 PC，再加补码偏移`},{id:`co4-opcode-extension-design`,subject:`co`,chapter:`指令系统`,tags:[`操作码`,`计算`],title:`扩展操作码怎么设计？空闲编码如何换来更多指令？`,back:`## 基本思路
- 定长 $n$ 位操作码最多表示 $2^{n}$ 条指令。若某类指令用不满，就把**空闲编码**留作扩展标志，供地址码更长的指令使用。
## 计算套路
- 高位是低位操作码的**前缀**：某编码一旦被当作扩展标志，就不能再表示一条完整指令。
- 例：4 位操作码中三地址指令占 12 条，剩 4 个编码给二地址，二地址最多 $4\\times 2^{4}=64$ 条。
## 结论
- 每下降一级地址码个数，指令条数上限就乘以新增地址位对应的 $2$ 的幂，但要先扣掉上级已占用的编码。
> 易错：不是简单连乘，必须先减去被上级拿走的编码。`,importance:5,hint:`短码是长码前缀，先扣占用再乘`},{id:`co4-instruction-types`,subject:`co`,chapter:`指令系统`,tags:[`指令类型`,`概念`],title:`指令按功能分成哪几类？特权指令与访管指令有何不同？`,back:`## 功能分类
- **数据传送**：只搬运数据，不改内容，如取数、存数、寄存器间传送。
- **算术逻辑**：加减、与或非、比较，会置标志位；**移位**含算术、逻辑、循环移位。
- **转移**：无条件与条件转移；**串操作**：成批搬运字符串；还有 IO 指令与特权指令。
## 特权与访管
- 特权指令（置 PSW、开中断、清内存、停机）只能在核心态执行，用户态执行会触发异常。
- 访管指令是用户程序**主动**进入核心态的入口，用于请求操作系统服务，本身不是特权指令。
> 易错：访管不是越权，而是自愿陷入。`,importance:3,hint:`访管自愿陷入，特权禁止用户用`},{id:`co4-indirect-and-stack-addressing`,subject:`co`,chapter:`指令系统`,tags:[`寻址方式`,`访存次数`],title:`间接寻址能间接几次？堆栈寻址的有效地址怎么得到？`,back:`## 间接寻址
- 一次间接：$EA=(A)$，取操作数要访存 2 次，先读地址再读数据。
- 多次间接靠**间址标志位**判断：标志为 1 说明还要再间址一级，每多一级多访存一次，直到标志为 0 才取到操作数。
## 堆栈寻址
- 栈顶由 **SP** 指出，$EA=(SP)$。硬堆栈用寄存器组实现，速度快、容量小。
- 软堆栈在主存中实现，入栈时先改 SP 再写入，出栈时先读出再改 SP，每次都要访存。
> 易错：间址次数由标志位决定，与指令中地址字段的位数无关。`,importance:4,hint:`间址看标志位，堆栈看 SP`},{id:`co4-address-count-vs-memory`,subject:`co`,chapter:`指令系统`,tags:[`指令格式`,`访存次数`],title:`地址码个数如何影响访存次数与指令字长？`,back:`## 关系
- 地址码越多，一条指令能直接给出的操作数地址越多，**取操作数所需访存次数越少**，但指令字变长、取指也变慢。
- 三地址指令可一次完成「两读一写」；二地址指令常把目的地址兼作源，需额外一次送数或写回。
## 统计口径
- 取指令本身至少访存 1 次（Cache 命中时另算），操作数若在主存还要按寻址方式追加。
- 立即数与寄存器寻址取操作数**不访存**，寄存器间接要访存 1 次，直接寻址 1 次，一次间接 2 次。
> 易错：把「取指」和「取数」分开累加，别漏掉取指那一次。`,importance:4,hint:`地址码多则取数访存少但指令长`},{id:`co4-cpu-four-work-cycles`,subject:`co`,chapter:`CPU 与指令周期`,tags:[`指令周期`,`微操作`],title:`四个工作周期如何划分？CPU 靠什么判断当前处于哪个周期？`,back:`## 四个工作周期
- **取指周期**：任何指令都有，按 PC 取指令送 IR，并让 PC 自增。
- **间址周期**：只有间接寻址的指令才有，用于取出操作数的有效地址。
- **执行周期**：完成本条指令的真正功能，不同指令差别最大。
- **中断周期**：若允许响应中断，则保存断点并转入中断服务程序。
## 判断依据
- CPU 内设 FE、IND、EX、INT 四个触发器，用它们的组合状态标志当前处于哪个周期。
> 易错：间址、执行、中断这三个周期并非每条指令都经历，中断周期只有响应中断时才进入。`,importance:4,hint:`FE/IND/EX/INT 四个触发器`},{id:`co4-cpu-alu-and-registers`,subject:`co`,chapter:`CPU 与指令周期`,tags:[`CPU 结构`,`寄存器`],title:`运算器由哪些部件组成？哪些寄存器用户可见、哪些不可见？`,back:`## 运算器组成
- 核心是 **ALU**，另有通用寄存器组、累加器 ACC、暂存器、移位电路和标志寄存器 PSW。
- ALU 是组合逻辑，两个输入端必须同时有效；单总线一次只送一个数据，故一个操作数先入 ACC，另一个经暂存器锁存。
## 可见性
- 用户可见：通用寄存器、PC、PSW、SP，程序可读可改。
- 用户不可见：IR、MAR、MDR、暂存器，只供硬件使用。
## 位数
- PC 与 MAR 的位数等于**地址位数**，决定寻址空间；MDR 等于**存储字长**；IR 等于**指令字长**；通用寄存器等于机器字长。
> 易错：暂存器不可见，ACC 与 PC 一般可见。`,importance:4,hint:`ACC 加暂存器喂 ALU，IR/MAR/MDR 不可见`},{id:`co4-single-bus-datapath`,subject:`co`,chapter:`CPU 与指令周期`,tags:[`数据通路`,`微操作`],title:`单总线数据通路为什么一次只能送一个数据？`,back:`## 结构特点
- 所有寄存器都挂在同一条内部总线上，任一时刻**只允许一个部件把数据送上总线**，否则多个输出源会互相冲突。
## 微操作拆分
- 一次 ALU 运算要拆成多步：一个操作数送 ACC，另一个送暂存器，ALU 运算，结果写回目的寄存器。
- 每个微操作对应一组控制信号，用**时钟边沿**保证先输出、后打入的时序。
## 代价
- 单总线结构简单、成本低，但并行度差，一条指令往往要占用多个时钟周期；改用双总线、多总线可让两个操作数同时送达 ALU。
> 易错：总线上同时只能有一个输出源，控制信号必须互斥。`,importance:4,hint:`一条总线一拍只送一个数据`},{id:`co4-microinstruction-encoding`,subject:`co`,chapter:`CPU 与指令周期`,tags:[`微程序`,`控制器`],title:`微指令的编码方式有哪几种？水平型与垂直型怎么区分？`,back:`## 三种编码
- **直接编码**：一位对应一个微命令，不必译码、速度最快，但字长最长。
- **字段直接编码**：把互斥的微命令编在一组，组内译码产生命令，每组要留出**全 0 编码**表示不发命令。
- **字段间接编码**：字段的含义再由另一字段译码决定，字长更短，但译码级数多、更慢。
## 水平型与垂直型
- 水平型微指令一次能并行发出多个微命令，微程序短、执行快，但字长大、编写难。
- 垂直型类似机器指令，一次只完成一个微操作，字长小、易编写，但微程序长、速度慢。
> 易错：字段直接编码若不预留空编码，同组微命令会冲突。`,importance:4,hint:`直接最快最长，字段编码要留空码`},{id:`co4-microaddress-and-cm`,subject:`co`,chapter:`CPU 与指令周期`,tags:[`微程序`,`微地址`],title:`下一条微指令的地址从哪里来？CM 与微程序是什么关系？`,back:`## 概念
- 一条机器指令对应一段**微程序**，全部存放在 CPU 内部的只读**控制存储器 CM** 中，CM 每个单元存一条微指令，其字长即微指令字长。
## 微地址的形成
- 微程序入口地址由**指令操作码**经微地址形成部件译码得到。
- 顺序执行时由微指令中的**下地址字段**直接给出后继地址。
- 遇到分支时，按微指令的**转移方式**（如按条件标志 ZF 判断）修改下地址字段，得到转移目标。
## 特点
- 取微指令在 CPU 内部完成，**不算访存**；微程序一经固化就不能被用户程序修改。
> 易错：CM 与主存是两套存储，不要把取微指令计入访存次数。`,importance:3,hint:`操作码给入口，下址字段给后继`},{id:`co4-pipeline-space-time-diagram`,subject:`co`,chapter:`指令流水线`,tags:[`流水线`,`时空图`],title:`流水线时空图怎么画、怎么读？横纵坐标各表示什么？`,back:`## 画法
- 横轴表示**时间**（以时钟周期为单位），纵轴表示**流水段**（各功能部件）。
- 每个任务在它经过的每一段上占一个方块，方块随时间向右下方推进，形成阶梯状。
## 读图
- 同一任务的方块在时间上不重叠；同一时刻不同任务落在不同段，体现重叠执行。
- 设有 $k$ 段、共 $n$ 个任务，从第一个任务进入到最后流出共需 $k+(n-1)$ 个时钟周期。
- 图中出现的空白称为**气泡**，说明该段被闲置，多由数据冒险、控制冒险或资源冲突引起。
> 易错：先数段数 k、再数任务数 n，最后检查气泡，三者决定性能公式的取用。`,importance:4,hint:`横轴时间纵轴段，空白即气泡`},{id:`co4-pipeline-limit-speedup`,subject:`co`,chapter:`指令流水线`,tags:[`流水线`,`计算`],title:`流水线段数越多越快吗？极限加速比由什么决定？`,back:`## 公式
- 顺序执行用 $T_0=nk$，流水执行用 $T=k+n-1$，故加速比 $S=\\frac{nk}{k+n-1}$。
- 吞吐率 $TP=\\frac{n}{k+n-1}$，$n$ 很大时趋近每周期完成一个任务。
## 极限
- 当 $n\\to\\infty$ 时 $S\\to k$，即**极限加速比等于流水段数**，段数定了上限。
- 所以只增加任务数不可能超过段数倍加速，想更快只能加深流水线或让每段并行（超标量）。
> 易错：分母里的 $(k-1)$ 是填满流水线的时间，算加速比时不能漏。`,importance:5,hint:`n 趋于无穷时加速比趋于 k`},{id:`co4-structural-control-hazard`,subject:`co`,chapter:`指令流水线`,tags:[`冒险`,`分支预测`],title:`结构冒险与控制冒险怎么解决？分支预测与延迟槽是什么？`,back:`## 结构冒险
- 多条指令争用同一部件，例如同一拍既要取指又要取数访存。解决：把**指令 Cache 与数据 Cache 分开**、给部件分设资源或增设部件，实在冲突就让后到的指令停顿一拍。
## 控制冒险
- 转移指令改写 PC，使已取入的指令作废。解决办法有：
- **分支预测**：静态预测（如总猜不发生转移）或动态预测（用历史表记录转移走向）。
- **延迟槽**：在转移指令后插入一条无论转移与否都要执行的指令，用它填补停顿。
- 也可等分支结果确定后再清空流水线，或提前算出转移目标地址以减少损失。
> 易错：控制冒险的根源是 PC 被改写，数据冒险的根源是数据未就绪。`,importance:5,hint:`结构分 Cache，控制靠预测与延迟槽`},{id:`co4-pipeline-clock-stage`,subject:`co`,chapter:`指令流水线`,tags:[`流水线`,`时钟周期`],title:`流水线的时钟周期由什么决定？为什么段数不能无限增加？`,back:`## 时钟周期
- 时钟周期由**最慢的一段**加锁存器开销决定，可用 $T=\\max(t_1,t_2,\\dots ,t_k)+t_{reg}$ 表示，其中 $t_{reg}$ 是流水寄存器的传输与建立时间。
## 不能无限分段的原因
- 段与段之间必须插入流水寄存器，它的开销固定；段数越多，这段固定开销占总周期的比例越大，收益迅速衰减。
- 分段越细，冒险出现的频率越高，需要的转发、阻塞和控制逻辑越复杂，实际加速比远低于理论值。
> 易错：理想公式常忽略 t_reg，做设计题时不能忽略。`,importance:3,hint:`最长段加锁存器开销定周期`},{id:`co4-bus-structure`,subject:`co`,chapter:`总线`,tags:[`总线`,`结构`],title:`单总线、双总线、三总线结构各有什么特点？总线为什么要复用？`,back:`## 三种结构
- **单总线**：所有部件挂一条总线，结构简单、成本低，但同一时刻只能有一对部件通信，容易成为速度瓶颈。
- **双总线**：在 CPU 与主存间增设高速存储总线，缓解访存压力，让 IO 与访存可以分开进行。
- **三总线**：再加 DMA 总线，高速外设可与主存直接成块传送，CPU 只做启动和善后。
## 总线复用
- 地址线与数据线分时复用能减少引脚数、降低成本，但同一时刻只能传一种信息，需要额外的控制信号来区分，并牺牲一点速度。
> 易错：单总线的瓶颈是「分时共享」，不是线不够宽。`,importance:3,hint:`单总线瓶颈在分时共享`},{id:`co4-bus-burst-transaction`,subject:`co`,chapter:`总线`,tags:[`总线`,`突发传送`],title:`总线事务包含哪些阶段？突发传送为什么能提高带宽？`,back:`## 总线事务
- 一次总线事务通常包括**请求总线、总线仲裁、寻址、数据传送、释放总线**几个阶段，其中寻址与仲裁的开销往往比传数据本身还大。
## 突发传送
- 一次寻址后连续传送同一行（地址连续）的多个数据：只在第一个数据周期给出地址，后续周期只传数据。
- 省去每个数据的寻址开销，**有效带宽**明显提高；连续传送的数据个数称为突发长度。
- 由于要求地址连续，突发传送最适合 Cache 行填充这类成批取数的场合。
> 易错：突发传送提高的是有效带宽，总线宽度与频率并没有改变。`,importance:4,hint:`一次寻址连传多个，省下寻址开销`},{id:`co4-bus-async-handshake`,subject:`co`,chapter:`总线`,tags:[`总线`,`总线定时`],title:`异步定时的三种互锁方式有什么区别？`,back:`## 三种互锁
- **不互锁**：主设备发出请求后不等待应答，过一段时间自行撤销；从设备也只发一次应答，双方都不确认对方是否收到，最快但最不可靠。
- **半互锁**：主设备收到应答后才撤销请求，从设备发出应答后不等请求撤销就撤回应答。
- **全互锁**：请求与应答互相等待对方撤销后才结束，可靠性最高，但一次握手耗时最长。
## 适用
- 异步定时靠握手信号协调，能兼容速度差异很大的设备，代价是控制复杂、传送较慢；同步定时统一时钟，简单快速但不能迁就慢设备。
> 易错：越「全」越可靠越慢，按可靠性要求选择互锁级别。`,importance:3,hint:`不互锁快、半互锁中、全互锁稳`},{id:`co4-io-polling-serial`,subject:`co`,chapter:`IO 与中断`,tags:[`IO 方式`,`程序查询`],title:`程序查询方式中 CPU 与 IO 为什么是串行的？开销怎么估算？`,back:`## 工作过程
- CPU 启动外设后，反复**读状态端口**判断设备是否就绪；未就绪就继续查询，就绪才通过数据端口传一个数据。
- 传送期间 CPU 被完全占用，不能做别的事，因此 CPU 与外设是**串行**关系。
## 开销估算
- 传送一个数据的开销约为「查询次数乘以每次查询时间再加传送时间」；外设越慢，CPU 空转越多，利用率越低。
## 改进方向
- 让 CPU 在等待期间去执行别的程序，就得到中断方式；让数据成块直接进出主存，就得到 DMA 方式。
> 易错：查询方式不需要中断机制，但无法与主机并行工作。`,importance:4,hint:`查询期间 CPU 全被占用，串行`},{id:`co4-interrupt-implicit-instruction`,subject:`co`,chapter:`IO 与中断`,tags:[`中断`,`隐指令`],title:`中断隐指令完成哪三件事？为什么它不占指令的编码空间？`,back:`## 三件事
- **关中断**：把 PSW 中的中断允许位清零，保证保存断点的过程不会被打断。
- **保存断点**：把当前 PC 的值（即下一条指令的地址）压入堆栈或存入指定单元。
- **取服务程序入口**：用中断类型号查中断向量表，把得到的入口地址送 PC，转入服务程序。
## 为什么叫隐指令
- 它是 CPU 在**中断响应阶段由硬件自动完成**的操作序列，对程序员不提供指令形式，也不占用操作码编码空间，所以称为隐指令。
> 易错：保存通用寄存器等现场是中断服务程序做的，不属于隐指令。`,importance:5,hint:`关中断、存断点、取入口`},{id:`co4-interrupt-classification-vector`,subject:`co`,chapter:`IO 与中断`,tags:[`中断`,`分类`],title:`中断如何分类？中断向量与中断向量表各是什么？`,back:`## 分类
- 按来源：**内中断**（又称异常，如溢出、除零、缺页、非法操作码）来自 CPU 内部；**外中断**（如 IO 完成、时钟、键盘）来自外部设备。
- 按可否屏蔽：**可屏蔽中断**受 PSW 的中断允许位控制，**不可屏蔽中断**（如电源故障）必须立即响应。
## 向量与向量表
- 每个中断源对应一个**中断向量**，即服务程序的入口地址。
- 所有向量按中断类型号集中存放在内存的**中断向量表**中，响应时用类型号索引取出向量送 PC。
> 易错：向量表在内存而不是 CPU 寄存器，类型号到表项的换算常考。`,importance:4,hint:`内异常外中断，类型号查向量表`},{id:`co4-dma-controller-process`,subject:`co`,chapter:`IO 与中断`,tags:[`DMA`,`IO 接口`],title:`DMA 控制器由哪些部件组成？一次 DMA 传送分哪几步？`,back:`## DMA 控制器组成
- **主存地址寄存器**存放要访问的主存地址，**字计数器**记录还要传多少个数据，另有数据缓冲寄存器、DMA 请求触发器、控制与状态逻辑以及中断机构。
## 传送过程
- **预处理**：CPU 设置主存起始地址、传送长度和传送方向，启动外设。
- **数据传送**：设备每准备好一个数据就发 DMA 请求，控制器向 CPU 申请总线，得到总线后直接在主存与外设之间传送，地址与计数自动修改。
- **后处理**：全部传完后发中断，CPU 做数据校验与善后。
> 易错：数据不经过 CPU 的寄存器，CPU 只在开头和结尾参与。`,importance:4,hint:`地址加计数，预处理—传送—后处理`},{id:`co4-io-channel-processor`,subject:`co`,chapter:`IO 与中断`,tags:[`IO 通道`,`IO 处理机`],title:`IO 通道与 IO 处理机是什么？比 DMA 强在哪里？`,back:`## 层次关系
- 按 CPU 介入程度由多到少：程序查询、中断、DMA、**IO 通道**、**IO 处理机**。
## IO 通道
- 通道是一台专门管理 IO 的处理器，拥有自己的**通道指令**，能执行通道程序。
- CPU 只需执行一条 IO 指令给出通道程序首地址，通道就能独立控制多台设备并行工作，完成后再中断通知 CPU。
## IO 处理机
- 通道进一步独立，拥有自己的指令系统和局部存储器，几乎是一台完整的处理机，与主机并行程度最高。
> 易错：通道执行通道指令而非 CPU 的机器指令，两者指令系统不同。`,importance:3,hint:`通道有自己的通道指令，比 DMA 更独立`},{id:`co4-disk-address-and-access-time`,subject:`co`,chapter:`IO 与中断`,tags:[`磁盘`,`计算`],title:`磁盘地址如何编排？存取时间由哪几部分组成？`,back:`## 地址格式
- 磁盘地址按**驱动器号、柱面（磁道）号、盘面（磁头）号、扇区号**的顺序编排，柱面号在前是为了减少磁头移动。
- 各字段位数由数量决定，如柱面号位数 $m$ 满足 $2^{m}\\ge$ 磁道数。
## 存取时间
- $T_a=T_s+\\frac{1}{2r}+\\frac{b}{rN}$：$T_s$ 是寻道时间，$\\frac{1}{2r}$ 是平均旋转延迟（$r$ 为转速，单位转每秒），$\\frac{b}{rN}$ 是传输时间，$N$ 为一条磁道的字节数。
- 平均旋转延迟取半圈，实际还要加控制器与排队时间。
> 易错：转速常给 rpm，先换算成转每秒，如 7200 rpm 等于 120 r/s。`,importance:5,hint:`柱面盘面扇区，寻道加半圈加传输`},{id:`co4-ssd-vs-disk`,subject:`co`,chapter:`IO 与中断`,tags:[`SSD`,`外设`],title:`SSD 与磁盘相比有哪些优劣？为什么需要磨损均衡？`,back:`## 性能对比
- SSD 没有机械部件，**随机读写快、延迟低、抗震动、无噪声**，随机访问几乎与顺序访问一样快。
- 磁盘靠寻道和旋转定位，随机访问慢，但容量大、每 GB 价格低，顺序读写性能好。
## 磨损均衡
- 闪存以块为单位擦除、以页为单位读写，每个块的**擦写次数有限**。
- 把写操作尽量均匀分散到各块，避免个别块提前损坏，这就是磨损均衡。
## 其他差异
- SSD 写入前必须先擦除整块，会产生写放大；磁盘则存在盘片旋转的机械延迟与噪声。
> 易错：SSD 快在随机访问，磁盘仍在大容量冷数据上占优势。`,importance:2,hint:`SSD 随机快，闪存块要均匀擦写`}],o=[{id:`q-co-data-01`,subject:`co`,chapter:`数据的表示与运算`,type:`single`,stem:`某 IEEE 754 单精度浮点数的数符为 $0$，阶码字段的值为 $130$，尾数部分（不含隐含的最高位）为 $0.25$，则该浮点数的真值是？`,options:[`2.0`,`1.25`,`5.0`,`10.0`],answer:[3],analysis:`## 解析
- 阶码真值 $e = 130 - 127 = 3$，偏移量取 $127$ 而不是 $128$。
- 规格化数真值 $x = 1.M \\times 2^{e} = 1.25 \\times 2^{3} = 10.0$。
> $2.0$ 漏掉了尾数隐含的最高位 $1$；$5.0$ 把偏移量错记成 $128$，阶码算成了 $2$。`,difficulty:2,tags:[`IEEE754`,`浮点数`,`真值计算`]},{id:`q-co-data-02`,subject:`co`,chapter:`数据的表示与运算`,type:`single`,stem:`用 16 位补码表示定点小数（最高位为符号位，小数点在符号位之后），则能表示的最小负数是？`,options:[`$-1$`,`$-(1-2^{-15})$`,`$-32768$`,`$-2^{-15}$`],answer:[0],analysis:`## 解析
- $n$ 位（含 1 位符号位）补码定点小数的范围是 $-1 \\le x \\le 1 - 2^{-(n-1)}$。
- $16$ 位时下限为 $-1$（编码 $1.00\\cdots0$），上限才是 $1 - 2^{-15}$。
> $-(1-2^{-15})$ 是紧挨下限的次小负数，被当成了最小值；$-32768$ 把定点整数的范围搬到了小数上。`,difficulty:2,tags:[`补码`,`定点小数`,`表示范围`]},{id:`q-co-data-03`,subject:`co`,chapter:`数据的表示与运算`,type:`single`,stem:`8 位补码整数加法：$[x]_{\\text{补}} = 01111111$，$[y]_{\\text{补}} = 00000001$，相加时符号位进位 $C_s = 0$、最高数值位进位 $C_1 = 1$。下列判断正确的是？`,options:[`两数均为正数，结果无误，真值为 $+128$`,`发生正溢出，因为两个正数相加却得到符号位为 $1$ 的结果`,`发生负溢出，因为最高数值位向符号位产生了进位`,`仅凭这两个进位无法判断，必须改用双符号位重新运算`],answer:[1],analysis:`## 解析
- 判溢出的判据是两个进位是否一致：$V = C_s \\oplus C_1 = 0 \\oplus 1 = 1$，故发生溢出。
- 两个正数相加结果为负，属于正溢出，其正确结果 $+128$ 已超出 8 位补码范围 $[-128, 127]$。
> 把「最高数值位有进位」直接当成负溢出是常见误判；认为结果无误，则忽略了符号位已被数值位的进位冲掉。`,difficulty:2,tags:[`补码`,`溢出判断`,`进位`]},{id:`q-co-data-04`,subject:`co`,chapter:`数据的表示与运算`,type:`single`,stem:`8 位机器数采用移码表示定点整数，偏移量取 $2^{7} = 128$，则真值 $-1$ 的移码是？`,options:[`$11111111$`,`$10000001$`,`$01111111$`,`$10000000$`],answer:[2],analysis:`## 解析
- 移码等于真值加偏移量：$-1 + 128 = 127$，写成 8 位二进制即 $01111111$。
- 也可由补码求：$-1$ 的补码是 $11111111$，再把符号位取反得 $01111111$。
> $11111111$ 是补码而不是移码；$10000000$ 对应真值 $0$，那是移码的零，不要与补码唯一的零混淆。`,difficulty:1,tags:[`移码`,`补码`,`机器数转换`]},{id:`q-co-data-05`,subject:`co`,chapter:`数据的表示与运算`,type:`judge`,stem:`判断：在 $n$ 位机器数中，补码的零只有一种表示形式，因此 $n$ 位补码比 $n$ 位原码多表示一个负数。`,options:[`正确`,`错误`],answer:[0],analysis:`## 解析
- 原码中 $+0$（$00\\cdots0$）与 $-0$（$10\\cdots0$）占用两个编码，实际只能表示 $2^{n}-1$ 个不同的数。
- 补码的零唯一，$2^{n}$ 个编码全部有效，多出来的那个编码就是最小负数 $-2^{n-1}$。
> 多表示出的是绝对值最大的负数，而不是最大正数：正数上界仍为 $2^{n-1}-1$，正负两侧并不对称。`,difficulty:1,tags:[`补码`,`原码`,`表示范围`]},{id:`q-co-mem-01`,subject:`co`,chapter:`存储系统`,type:`single`,stem:`某机主存地址为 32 位、按字节编址，Cache 数据区容量为 32KB，主存块大小为 32B，采用 4 路组相联映射，则主存地址中组号（组索引）字段占多少位？`,options:[`5 位`,`8 位`,`10 位`,`19 位`],answer:[1],analysis:`## 解析
- 块内地址占 $\\log_2 32 = 5$ 位；组数 $= 32\\text{KB} \\div (32\\text{B} \\times 4) = 256$ 组。
- 组号占 $\\log_2 256 = 8$ 位，剩余 $32 - 5 - 8 = 19$ 位作标记（Tag）。
> $5$ 位是块内地址的位数；$10$ 位是直接映射时的索引位数（共 $1024$ 块），四路组相联把它减少了 $2$ 位。`,difficulty:2,tags:[`Cache`,`组相联`,`地址划分`]},{id:`q-co-mem-02`,subject:`co`,chapter:`存储系统`,type:`single`,stem:`某 Cache 的存取时间为 1ns，主存存取时间为 19ns，采用「先访问 Cache，不命中再访问主存」的方式，命中率为 90%，则平均访问时间约为？`,options:[`2.8ns`,`2.9ns`,`3.0ns`,`20ns`],answer:[2],analysis:`## 解析
- 平均访问时间 $T_a = H \\cdot T_c + (1-H)(T_c + T_m)$，不命中时那一次 Cache 访问也不能省。
- 代入得 $0.9 \\times 1 + 0.1 \\times 20 = 3.0$ ns。
> $2.8$ ns 认为不命中时只花主存时间，漏掉了先查 Cache 的 1ns；$20$ ns 把不命中的访问时间当成了平均值。`,difficulty:1,tags:[`Cache`,`命中率`,`平均访问时间`]},{id:`q-co-mem-03`,subject:`co`,chapter:`存储系统`,type:`single`,stem:`某分页虚拟存储器中，访问 TLB 需 10ns，访问主存需 100ns。若 TLB 命中率为 98%，且 TLB 不命中时需先访问主存查页表再访问主存取数据（不考虑缺页与 Cache），则平均访问时间约为？`,options:[`112ns`,`110ns`,`102ns`,`210ns`],answer:[0],analysis:`## 解析
- 命中时问一次 TLB 再访存：$10 + 100 = 110$ ns；不命中时还要多访存一次查页表：$10 + 100 + 100 = 210$ ns。
- $T_a = 0.98 \\times 110 + 0.02 \\times 210 = 107.8 + 4.2 = 112$ ns。
> $110$ ns 漏算了 TLB 缺失时多访存的那一次；$102$ ns 把 TLB 与主存的访问时间当成了并行，两种情况都少算了时间。`,difficulty:3,tags:[`TLB`,`虚拟存储`,`平均访问时间`]},{id:`q-co-mem-04`,subject:`co`,chapter:`存储系统`,type:`single`,stem:`某主存采用 4 体低位交叉编址，每个模块的存取周期 $T = 200$ ns，总线传送周期 $r = 50$ ns。连续读取 8 个字（字长与总线宽度相同）所需的时间约为？`,options:[`350ns`,`800ns`,`1600ns`,`550ns`],answer:[3],analysis:`## 解析
- 低位交叉编址按流水线方式启动各模块，连续读 $n$ 个字的时间为 $T + (n-1)r$。
- 代入 $200 + 7 \\times 50 = 550$ ns，即只有第 1 个字要等满一个存取周期。
> $350$ ns 是按连续读 4 个字算的结果；$1600$ ns 相当于每个字都独占一个完整存取周期，完全没有流水重叠。`,difficulty:3,tags:[`交叉存储器`,`带宽`,`流水存取`]},{id:`q-co-mem-05`,subject:`co`,chapter:`存储系统`,type:`single`,stem:`关于 Cache 的写回法（Write Back），下列说法正确的是？`,options:[`写命中时只修改 Cache 中的副本并置修改位，仅当该行被替换出时才写回主存`,`写命中时必须同时修改 Cache 与主存，以保持两者内容一致`,`写不命中时必须先把主存块调入 Cache 再写，这是写回法的规定`,`写回法不需要替换算法，所以实现起来比写直达法更简单`],answer:[0],analysis:`## 解析
- 写回法的要点是「先改 Cache，被换出时才写主存」，靠每行的修改位（脏位）判断是否需要写回。
- 它能明显减少访存次数，代价是主存数据可能暂时过期，控制逻辑比写直达法复杂。
> 「写命中时同时改 Cache 与主存」是写直达法的做法；写不命中时是否调块由写分配策略决定，与写回法本身无关。`,difficulty:1,tags:[`Cache`,`写策略`,`写回法`]},{id:`q-co-mem-06`,subject:`co`,chapter:`存储系统`,type:`judge`,stem:`判断：在 Cache 中，主存块（Cache 行）的容量越大，Cache 的命中率就一定越高。`,options:[`正确`,`错误`],answer:[1],analysis:`## 解析
- 块增大能更好地利用空间局部性，但 Cache 能容纳的块数随之减少，块间冲突与替换开销上升，命中率可能反而下降。
- 命中率随块增大是先升后降的，存在最佳块大小，还要受一次调块时间与总容量的制约。
> 干扰说法把空间局部性绝对化了：局部性只在有限范围内起作用，块一旦跨过这个范围，带来的就是纯粹的额外开销。`,difficulty:2,tags:[`Cache`,`块大小`,`命中率`]},{id:`q-co-inst-01`,subject:`co`,chapter:`指令系统`,type:`single`,stem:`某指令系统指令字长 16 位，每个地址码占 4 位，采用扩展操作码方式。已知三地址指令 8 条、二地址指令 32 条，则一地址指令最多可有多少条？`,options:[`1024 条`,`1536 条`,`2048 条`,`4096 条`],answer:[1],analysis:`## 解析
- 三地址时操作码 4 位，共 16 个编码，用掉 8 个，余 8 个留作扩展。
- 二地址操作码 8 位，可得 $8 \\times 16 = 128$ 个编码，用掉 32 个还剩 96 个；一地址操作码 12 位，故最多 $96 \\times 16 = 1536$ 条。
> $2048$ 条是漏减了二地址已用的 32 个编码；$1024$ 条则少算了一级地址码所提供的扩展倍数。`,difficulty:3,tags:[`扩展操作码`,`指令格式`,`编码`]},{id:`q-co-inst-02`,subject:`co`,chapter:`指令系统`,type:`single`,stem:`某机采用基址寻址方式，基址寄存器的内容为 2000H，指令中形式地址字段为 8 位补码 F0H，则该操作数的有效地址是？`,options:[`20F0H`,`20EFH`,`1F10H`,`1FF0H`],answer:[3],analysis:`## 解析
- 基址寻址的有效地址 $EA = (BR) + A$，形式地址按补码解释：$\\text{F0H} = -16$。
- 故 $EA = 2000\\text{H} - 10\\text{H} = 1\\text{FF0H}$。
> $20\\text{F0H}$ 把 $\\text{F0H}$ 当成无符号数 $+240$ 去加；$1\\text{F10H}$ 则把偏移方向做反了。`,difficulty:2,tags:[`基址寻址`,`有效地址`,`补码`]},{id:`q-co-inst-03`,subject:`co`,chapter:`指令系统`,type:`single`,stem:`某机指令字长 16 位，采用定长操作码，共有 64 条指令，且只有一地址指令这一种格式，则该指令中形式地址字段最多可占多少位？`,options:[`8 位`,`9 位`,`10 位`,`12 位`],answer:[2],analysis:`## 解析
- 64 条指令需要 $\\log_2 64 = 6$ 位操作码，定长操作码要占满这 6 位。
- 指令字长 16 位，故地址字段最多 $16 - 6 = 10$ 位，可寻址 $2^{10} = 1024$ 个编址单位。
> $8$ 位是误以为 64 条指令需要 8 位操作码；$12$ 位则是把操作码当成 4 位（只够 16 条指令）来算的。`,difficulty:1,tags:[`指令格式`,`操作码`,`定长编码`]},{id:`q-co-inst-04`,subject:`co`,chapter:`指令系统`,type:`single`,stem:`下列关于 RISC 的叙述中，正确的是？`,options:[`指令数量少、寻址方式简单，采用定长指令字和 Load/Store 结构，便于流水线实现`,`指令数量多、寻址方式丰富，指令字长可变，一条指令可完成复杂操作`,`必须采用微程序控制器实现，因为指令系统简单所以控制逻辑特别规整`,`各条指令的执行时间差异很大，通常远大于一个时钟周期`],answer:[0],analysis:`## 解析
- RISC 的设计取向是简单规整：定长指令字、少量寻址方式、只有 Load/Store 能访存，运算都在寄存器之间进行。
- 正因如此它适合硬布线控制器与流水线，绝大多数指令可在一个时钟周期内完成。
> 「指令多、寻址方式丰富、字长可变」是 CISC 的特征；RISC 因为规整而更适合硬布线控制，并非必须用微程序。`,difficulty:2,tags:[`RISC`,`CISC`,`指令系统`]},{id:`q-co-cpu-01`,subject:`co`,chapter:`CPU 与指令周期`,type:`single`,stem:`某程序共含 $10^{6}$ 条指令，平均 CPI 为 2，CPU 主频为 1GHz，则该程序的执行时间与机器的 MIPS 分别是？`,options:[`$500\\mu s$、500`,`2ms、1000`,`2ms、2000`,`2ms、500`],answer:[3],analysis:`## 解析
- 执行时间 $T = \\dfrac{IN \\times CPI}{f} = \\dfrac{10^{6} \\times 2}{10^{9}} = 2$ ms。
- MIPS $= \\dfrac{f}{CPI \\times 10^{6}} = \\dfrac{10^{9}}{2 \\times 10^{6}} = 500$。
> 算成 $1000$ 是把 CPI 当成了 $1$；算成 $2000$ 则把公式倒过来乘了 CPI。`,difficulty:2,tags:[`CPI`,`MIPS`,`性能计算`]},{id:`q-co-cpu-02`,subject:`co`,chapter:`CPU 与指令周期`,type:`single`,stem:`下列关于微程序控制器的叙述中，错误的是？`,options:[`一条机器指令的功能由一段微程序实现，微程序由若干条微指令组成`,`微指令的操作控制字段产生微命令，直接控制数据通路中的各部件`,`微程序控制器比硬布线控制器执行速度快，因此更适合用于 RISC 处理器`,`控制存储器（CM）用来存放微程序，通常由只读存储器 ROM 构成`],answer:[2],analysis:`## 解析
- 微程序控制器要逐条取出微指令才能产生控制信号，每条微指令都要访问控制存储器，速度比硬布线慢。
- RISC 指令规整又强调速度，一般用硬布线控制器；CISC 才较多采用微程序控制器。
> 「速度更快、更适合 RISC」把两者的优缺点正好对调了；其余三条都是对微程序控制器的正确描述。`,difficulty:2,tags:[`微程序控制器`,`硬布线`,`控制存储器`]},{id:`q-co-cpu-03`,subject:`co`,chapter:`CPU 与指令周期`,type:`single`,stem:`某机指令与数据同存于主存，一条一地址指令的操作数采用一次间接寻址（形式地址字段直接给出间址单元的地址）。执行这条指令的过程中，CPU 至少需要访问主存多少次？`,options:[`4 次`,`3 次`,`2 次`,`1 次`],answer:[1],analysis:`## 解析
- 取指令访存 1 次：按 PC 读出指令送入 IR。
- 取有效地址再访存 1 次：读出间址单元的内容作为操作数地址；按该地址取操作数又访存 1 次，合计 3 次。
> 算成 2 次是漏掉了访问间址单元那一次；算成 1 次则把间接寻址当成了立即寻址，操作数并没有随指令一起取来。`,difficulty:2,tags:[`寻址方式`,`访存次数`,`指令周期`]},{id:`q-co-cpu-04`,subject:`co`,chapter:`CPU 与指令周期`,type:`single`,stem:`关于时钟周期、机器周期与指令周期三者的关系，下列说法正确的是？`,options:[`时钟周期是最小的时序单位，不同指令的指令周期可以由数目不同的机器周期组成`,`一个机器周期一定等于一个时钟周期，二者只是名称不同`,`指令周期由若干时钟周期组成，且每条指令的指令周期长度都相同`,`机器周期的长度只由 CPU 主频决定，与主存的工作速度无关`],answer:[0],analysis:`## 解析
- 时序层次是逐级包含的：指令周期 $\\supset$ 机器周期 $\\supset$ 时钟周期，时钟周期是最小单位，即主频的倒数。
- 不同指令需要的机器周期数可以不同（取数指令与加法指令就不同），所以指令周期长度并不固定。
> 「机器周期等于时钟周期」混淆了两个层次；「与主存速度无关」忽略了机器周期通常按一次访存的时间来确定。`,difficulty:1,tags:[`指令周期`,`机器周期`,`时钟周期`]},{id:`q-co-pipe-01`,subject:`co`,chapter:`指令流水线`,type:`single`,stem:`某指令流水线共 5 段，每段执行时间均为 1 个时钟周期，连续执行 100 条指令。若不用流水线，每条指令需 5 个时钟周期，则加速比约为？`,options:[`2.5`,`4.0`,`5.0`,`4.8`],answer:[3],analysis:`## 解析
- $k$ 段流水线执行 $n$ 条指令共需 $k + n - 1 = 5 + 100 - 1 = 104$ 个时钟周期。
- 非流水线需 $5 \\times 100 = 500$ 个周期，故加速比 $S = 500 / 104 \\approx 4.8$。
> $5.0$ 是 $n$ 趋于无穷时的极限加速比，本题 $n$ 只有 100，充满与排空的开销不可忽略。`,difficulty:2,tags:[`流水线`,`加速比`,`吞吐率`]},{id:`q-co-pipe-02`,subject:`co`,chapter:`指令流水线`,type:`single`,stem:`某流水线由 4 段组成，各段执行时间依次为 2ns、3ns、2ns、1ns（段间锁存器延迟忽略不计），则流水线的时钟周期和执行 10 条指令所需时间分别是？`,options:[`3ns、39ns`,`2ns、26ns`,`3ns、30ns`,`8ns、80ns`],answer:[0],analysis:`## 解析
- 流水线时钟周期取最慢段：$\\max(2, 3, 2, 1) = 3$ ns，快段要插入等待时间以对齐节拍。
- 执行 $n$ 条指令需 $k + n - 1 = 4 + 10 - 1 = 13$ 个时钟周期，$13 \\times 3 = 39$ ns。
> $2$ ns 取了最短段或平均值；$30$ ns 按「每周期完成一条」估算，漏掉了流水线充满所需的 3 个周期。`,difficulty:2,tags:[`流水线`,`时钟周期`,`执行时间`]},{id:`q-co-pipe-03`,subject:`co`,chapter:`指令流水线`,type:`single`,stem:`在 IF、ID、EX、MEM、WB 五段流水线中，采用数据旁路（转发）技术后，下列哪种情况仍然必须插入一个停顿周期？`,options:[`前一条指令在 EX 段算出结果，后一条指令紧跟其后使用该结果`,`前一条是取数（Load）指令，后一条指令紧跟其后使用该指令装入的数据`,`前一条是无条件转移指令，后一条是顺序存放的指令`,`两条相邻指令使用的通用寄存器完全不同`],answer:[1],analysis:`## 解析
- 前一条在 EX 段末期就得到运算结果，可用旁路直接送到后一条的 EX 输入端，不必停顿。
- Load 的数据要到 MEM 段末才拿到，后一条紧跟使用它时等不到，只能停顿 1 个周期（Load-Use 相关）。
> 「EX 段算出结果」属于可用转发解决的情形；「寄存器完全不同」连数据相关都不存在，更不需要停顿。`,difficulty:3,tags:[`流水线`,`数据冒险`,`转发`]},{id:`q-co-pipe-04`,subject:`co`,chapter:`指令流水线`,type:`judge`,stem:`判断：指令流水线中的结构冒险可以通过重复设置硬件资源来消除，数据冒险则可用转发技术和插入气泡（停顿）来解决。`,options:[`正确`,`错误`],answer:[0],analysis:`## 解析
- 结构冒险源于部件争用，多配一套资源（如指令 Cache 与数据 Cache 分开）就能让双方并行访问，从而消除冲突。
- 数据冒险由数据相关引起：能转发的用转发，转发解决不了的（如 Load-Use）插入气泡，也可由编译器调度缓解。
> 注意「消除」与「缓解」的差别：转发并不能覆盖所有数据冒险，所以数据冒险一般是「解决」，说彻底消除不严谨。`,difficulty:2,tags:[`流水线`,`结构冒险`,`数据冒险`]},{id:`q-co-bus-01`,subject:`co`,chapter:`总线`,type:`single`,stem:`某同步总线的时钟频率为 100MHz，数据线宽度为 32 位，每个总线周期传送一个 32 位数据且无等待周期，则该总线的带宽为？`,options:[`100MB/s`,`400MB/s`,`200MB/s`,`800MB/s`],answer:[1],analysis:`## 解析
- 每个总线周期传送 32 位即 4 字节，故带宽 $= 100 \\times 10^{6} \\times 4 = 4 \\times 10^{8}$ B/s。
- 即 400MB/s，折合 3200Mb/s，注意字节与比特相差 8 倍。
> $800$ MB/s 是把 32 位错当成 64 位（8 字节）；$100$ MB/s 则漏乘了每个总线周期实际传送的字节数。`,difficulty:1,tags:[`总线`,`带宽`,`同步总线`]},{id:`q-co-bus-02`,subject:`co`,chapter:`总线`,type:`single`,stem:`某总线数据线宽度为 32 位，总线时钟为 66MHz，一个总线周期包含 2 个时钟周期，每个总线周期传送 4 字节，则传输 4KB 数据大约需要？`,options:[`$31\\mu s$`,`$15.5\\mu s$`,`$62\\mu s$`,`$124\\mu s$`],answer:[0],analysis:`## 解析
- $4\\text{KB} = 4096$ 字节，每个总线周期送 4 字节，共需 $4096 \\div 4 = 1024$ 个总线周期。
- 每个总线周期占 2 个时钟，共 2048 个时钟，$2048 \\div (66 \\times 10^{6}) \\approx 31\\mu s$。
> $15.5\\mu s$ 漏乘了每个总线周期的 2 个时钟；$62\\mu s$ 则把时钟数又翻了一倍，重复计算了等待节拍。`,difficulty:2,tags:[`总线`,`传输时间`,`总线周期`]},{id:`q-co-bus-03`,subject:`co`,chapter:`总线`,type:`single`,stem:`下列关于总线的叙述中，正确的是？`,options:[`总线带宽只取决于总线宽度，与总线的工作频率无关`,`同步总线不使用时钟信号，靠请求与应答（握手）信号协调双方传送`,`链式查询仲裁方式对电路故障最不敏感，且各设备的优先级可以灵活改变`,`总线复用可以用同一组信号线分时传送地址与数据，但会降低传输效率`],answer:[3],analysis:`## 解析
- 总线带宽由总线宽度与工作频率共同决定，$带宽 = 总线宽度 \\times 工作频率$，缺一不可。
- 总线复用省下了引脚，代价是地址与数据要分时传送，一次传送被拆成两步，效率随之下降。
> 「同步总线不用时钟」把同步与异步说反了；链式查询对电路故障最敏感，且优先级由连线顺序固定，无法灵活改变。`,difficulty:2,tags:[`总线`,`总线复用`,`仲裁`]},{id:`q-co-io-01`,subject:`co`,chapter:`IO 与中断`,type:`single`,stem:`在中断响应阶段，由中断隐指令（硬件自动完成的操作序列）完成的工作是？`,options:[`保存通用寄存器的内容，并在返回前负责恢复现场`,`保护现场并修改 PC，使其指向中断服务程序的入口地址`,`关中断、保存断点（如 PC 与 PSW），并形成中断服务程序的入口地址`,`保存断点、保存通用寄存器的内容并关闭中断`],answer:[2],analysis:`## 解析
- 中断隐指令不是一条真正的指令，而是响应中断时硬件自动完成的动作：关中断、保存断点、取中断向量形成入口地址。
- 保存通用寄存器（保护现场）要由中断服务程序用指令完成，恢复现场同样是软件职责。
> 把「保存通用寄存器」划进隐指令是常见混淆；只说「保护现场并修改 PC」则漏掉了关中断这一关键动作。`,difficulty:2,tags:[`中断`,`中断隐指令`,`断点`]},{id:`q-co-io-02`,subject:`co`,chapter:`IO 与中断`,type:`single`,stem:`某机有 4 级中断，1 级优先级最高、4 级最低。若 CPU 正在执行 3 级中断服务程序时，同时收到 2 级和 4 级中断请求，则下列说法正确的是？`,options:[`先响应 4 级中断，因为它与 2 级同时到达时排在前面`,`2 级与 4 级中断都必须等 3 级服务结束后才能被响应`,`2 级中断可打断 3 级并被优先服务，返回后继续执行 3 级，3 级结束后再响应 4 级`,`CPU 已进入中断服务程序并关闭了中断，两个请求都会被丢弃`],answer:[2],analysis:`## 解析
- 中断嵌套的前提是开中断：高优先级请求能打断低优先级的中断服务程序，低优先级请求只能等待。
- 2 级高于 3 级，故打断 3 级先被服务；4 级低于 3 级，要等 3 级服务结束（且 2 级已返回）后才能响应。
> 「按到达先后响应」忽略了优先级判优；「都必须等 3 级结束」则等于否认了中断嵌套的存在。`,difficulty:3,tags:[`中断`,`中断嵌套`,`优先级`]},{id:`q-co-io-03`,subject:`co`,chapter:`IO 与中断`,type:`single`,stem:`某 DMA 控制器采用周期挪用（周期窃取）方式传送数据，每传送 1 字节需占用 1 个总线周期，每个总线周期为 $0.5\\mu s$。若要传送 20KB 数据，DMA 占用总线的时间约为？`,options:[`1.024ms`,`5.12ms`,`20.48ms`,`10.24ms`],answer:[3],analysis:`## 解析
- $20\\text{KB} = 20 \\times 1024 = 20480$ 字节，故共需 20480 个总线周期。
- 占用时间为 $20480 \\times 0.5\\mu s = 10240\\mu s = 10.24$ ms。
> $20.48$ ms 相当于把每个总线周期当成 $1\\mu s$；$1.024$ ms 则少算了一个数量级，把 KB 当成 1000 字节也补不回来。`,difficulty:2,tags:[`DMA`,`周期挪用`,`总线占用`]},{id:`q-co-io-04`,subject:`co`,chapter:`IO 与中断`,type:`judge`,stem:`判断：在 DMA 方式下，每传送一个数据都需要 CPU 执行一次中断服务程序。`,options:[`正确`,`错误`],answer:[1],analysis:`## 解析
- DMA 传送由 DMA 控制器直接在主存与外设之间进行，不经过 CPU，也不需要执行程序。
- 只在整块数据传送开始前由 CPU 初始化，传完后 DMA 控制器发一次中断请 CPU 做善后，中断次数与数据个数无关。
> 把「DMA 也会用到中断」误读成「每传一个数据中断一次」，是区分 DMA 与中断方式时最常见的混淆点。`,difficulty:1,tags:[`DMA`,`中断`,`数据传送`]}],s=[{id:`q-co2-data-01`,subject:`co`,chapter:`数据的表示与运算`,type:`single`,stem:`8 位补码整数 $[x]_{\\text{补}} = 10000000$、$[y]_{\\text{补}} = 11111111$，相加时符号位进位 $C_s = 1$、最高数值位进位 $C_1 = 0$。下列判断正确的是？`,options:[`结果为 $01111111$ 即 $+127$，最高位进位被自然丢弃，故运算没有溢出`,`两数之和为 $-129$，仍在 8 位补码可表示的范围内，结果正确`,`只有 $C_s = 0$ 且 $C_1 = 1$ 时才可能溢出，故本例不会溢出`,`两个负数相加却得到正数，发生负溢出，正确结果 $-129$ 已低于下限 $-128$`],answer:[3],analysis:`## 解析
- $[x]_{\\text{补}} = 10000000$ 的真值是 $-128$，$[y]_{\\text{补}}$ 的真值是 $-1$。
- 判据 $V = C_s \\oplus C_1 = 1$，两负数相加得正数属负溢出，结果 $-129$ 已越界。
> 只记一种进位组合会漏判负溢出；$+127$ 也不是 8 位补码的正确结果。`,difficulty:2,tags:[`补码`,`溢出判断`,`表示范围`]},{id:`q-co2-data-02`,subject:`co`,chapter:`数据的表示与运算`,type:`single`,stem:`真值 $-12.5$ 用 IEEE 754 单精度浮点数格式表示，则其机器数（十六进制）是？`,options:[`$41480000\\text{H}$`,`$\\text{C1480000H}$`,`$\\text{C1480001H}$`,`$\\text{C1C80000H}$`],answer:[1],analysis:`## 解析
- 规格化得 $-12.5 = -1.1001 \\times 2^{3}$，故数符 $S = 1$、阶码真值为 $3$。
- 阶码 $E = 3 + 127 = 130$，尾数取 $1001$ 后补零，得 $\\text{C1480000H}$。
> 偏移量记成 $128$ 则为 $\\text{C1C80000H}$；丢掉数符是 $41480000\\text{H}$；末位多加 $1$ 是 $\\text{C1480001H}$。`,difficulty:3,tags:[`IEEE754`,`浮点数`,`机器数转换`]},{id:`q-co2-data-03`,subject:`co`,chapter:`数据的表示与运算`,type:`single`,stem:`浮点加减运算中，设 $x = 1.1001 \\times 2^{3}$、$y = -1.0101 \\times 2^{1}$（尾数用双符号位补码表示），对阶与规格化的正确做法是？`,options:[`把小阶的 $x$ 阶码降为 $1$、尾数左移 $2$ 位，再与 $y$ 相加`,`两数阶码相差 $2$，应把 $y$ 的尾数左移 $2$ 位以实现对齐`,`对阶后两数尾数直接相加即为规格化结果，无需判断是否溢出`,`把 $y$ 的阶码升为 $3$、尾数算术右移 $2$ 位再相加；若和的尾数溢出则右移一位并把阶码加 $1$`],answer:[3],analysis:`## 解析
- 对阶是「小阶向大阶看齐」：$y$ 阶码由 $1$ 升到 $3$，尾数算术右移 $2$ 位，移出位参加舍入。
- 尾数用双符号位相加后出现 $01$ 或 $10$ 即为溢出，须右规：尾数右移 $1$ 位、阶码加 $1$。
> 大阶降为小阶会让 $x$ 尾数左移丢高位；左移 $y$ 的尾数等于把它的值放大 $4$ 倍，属原则性错误。`,difficulty:2,tags:[`浮点数`,`对阶`,`规格化`]},{id:`q-co2-data-04`,subject:`co`,chapter:`数据的表示与运算`,type:`single`,stem:`采用海明码校验，若信息位为 $32$ 位，则至少需要多少位校验位，编码后的总长为多少位？`,options:[`$4$ 位，总长 $36$ 位`,`$5$ 位，总长 $37$ 位`,`$6$ 位，总长 $38$ 位`,`$7$ 位，总长 $39$ 位`],answer:[2],analysis:`## 解析
- 海明码需满足 $2^{k} \\ge n + k + 1$，右边的 $1$ 对应「无错」这一状态。
- $k = 5$ 时 $32 < 38$，不够；$k = 6$ 时 $64 \\ge 39$，故校验位 $6$ 位，总长 $38$ 位。
> 选 $5$ 位是把不等式当成 $2^{k} \\ge n + 1$；选 $7$ 位多算一位，$k = 6$ 已经够用。`,difficulty:2,tags:[`海明码`,`校验位`,`编码`]},{id:`q-co2-data-05`,subject:`co`,chapter:`数据的表示与运算`,type:`single`,stem:`设 CRC 的生成多项式为 $G(x) = x^{3} + x + 1$，待发送的信息码为 $1101$，则计算出的校验位（余数）是？`,options:[`$001$`,`$101$`,`$110$`,`$111$`],answer:[0],analysis:`## 解析
- $G(x)$ 最高次为 $3$，校验位取 $3$ 位，对应的二进制串是 $1011$。
- 把信息码左移 $3$ 位得 $1101000$，对 $1011$ 做模 $2$ 除法（逐位异或、不借位），余数为 $001$。
> 忘记添 $0$ 直接除 $1101$ 得 $110$；只添 $1$ 个、$2$ 个 $0$ 分别得 $111$ 与 $101$，都不是答案。`,difficulty:3,tags:[`CRC`,`模 2 除法`,`检错编码`]},{id:`q-co2-data-06`,subject:`co`,chapter:`数据的表示与运算`,type:`single`,stem:`某机按字节编址、采用小端方式存放数据，并规定 4 字节的整型数据必须按 4 字节边界对齐。某结构体首成员为 1 字节的字符型，第二成员为 4 字节的整型，则该整型成员相对结构体首地址的偏移量，以及 $0\\text{x}12345678$ 存入该成员后其低地址单元中的内容分别是？`,options:[`偏移量 $1$，内容 $12\\text{H}$`,`偏移量 $4$，内容 $12\\text{H}$`,`偏移量 $4$，内容 $78\\text{H}$`,`偏移量 $1$，内容 $78\\text{H}$`],answer:[2],analysis:`## 解析
- 字符成员占 $1$ 字节，整型成员要求起始地址是 $4$ 的倍数，故补 $3$ 字节填充，偏移量为 $4$。
- 小端方式把低位字节放在低地址，$0\\text{x}12345678$ 的最低字节在低地址单元，即 $78\\text{H}$。
> 偏移量 $1$ 表示未做对齐，一次访存可能跨两个存储字；内容 $12\\text{H}$ 是按大端方式存放。`,difficulty:2,tags:[`小端`,`边界对齐`,`数据存放`]},{id:`q-co2-data-07`,subject:`co`,chapter:`数据的表示与运算`,type:`judge`,stem:`判断：算术右移时符号位保持不变、数值位右移、低位丢弃，对负数做除以 $2$ 的运算应使用算术右移；而逻辑右移时最高位一律补 $0$。`,options:[`正确`,`错误`],answer:[0],analysis:`## 解析
- 算术右移把符号位复制到空出的高位：$[-5]_{\\text{补}} = 11111011$ 算术右移 $1$ 位得 $11111101$，即 $-3$。
- 逻辑右移不管符号、高位一律补 $0$，只适用于无符号数；左移时两种移位的规则相同。
> 判断正确。若把负数当无符号数逻辑右移，$11111011$ 会变成 $+125$，符号与数值都被破坏。`,difficulty:1,tags:[`算术移位`,`逻辑移位`,`补码`]},{id:`q-co2-mem-01`,subject:`co`,chapter:`存储系统`,type:`single`,stem:`用 $512\\text{K} \\times 8$ 位的 SRAM 芯片组成容量为 $2\\text{M} \\times 32$ 位的主存，共需要多少片芯片？`,options:[`$4$ 片`,`$8$ 片`,`$16$ 片`,`$32$ 片`],answer:[2],analysis:`## 解析
- 位扩展加宽数据线：$32 \\div 8 = 4$ 片并联才凑成一个 $512\\text{K} \\times 32$ 位的存储组。
- 字扩展增加单元数：$2\\text{M} \\div 512\\text{K} = 4$ 组，共 $4 \\times 4 = 16$ 片，组内共用片选。
> 答 $4$ 片只算一个方向；答 $32$ 片把容量比错算成 $8$；答 $8$ 片漏掉一半位扩展。`,difficulty:2,tags:[`存储器扩展`,`位扩展`,`字扩展`]},{id:`q-co2-mem-02`,subject:`co`,chapter:`存储系统`,type:`single`,stem:`某 SRAM 芯片容量为 $8\\text{K} \\times 8$ 位，按字节编址，若把它安排在以 $2000\\text{H}$ 为首地址的地址空间中，则该芯片的末地址是？`,options:[`$2\\text{FFFH}$`,`$3\\text{FFEH}$`,`$3\\text{FFFH}$`,`$4000\\text{H}$`],answer:[2],analysis:`## 解析
- $8\\text{K}$ 个地址单元的跨度是 $2000\\text{H}$，故末地址为首地址加容量再减 $1$。
- 末地址 $= 2000\\text{H} + 1\\text{FFFH} = 3\\text{FFFH}$，片选由高位地址译码产生。
> $4000\\text{H}$ 没减 $1$；$3\\text{FFEH}$ 少数一个单元；$2\\text{FFFH}$ 把容量当成 $4\\text{K}$。`,difficulty:1,tags:[`地址范围`,`片选`,`末地址`]},{id:`q-co2-mem-03`,subject:`co`,chapter:`存储系统`,type:`single`,stem:`某分页存储管理系统按字节编址，逻辑地址为 $32$ 位，页面大小为 $4\\text{KB}$，每个页表项占 $4\\text{B}$，则一个进程的页表最大为多少？`,options:[`$512\\text{KB}$`,`$1\\text{MB}$`,`$2\\text{MB}$`,`$4\\text{MB}$`],answer:[3],analysis:`## 解析
- 页内偏移占 $12$ 位，页号占 $32 - 12 = 20$ 位，一个进程最多 $2^{20}$ 个页面。
- 每页占一个页表项，故页表大小 $= 2^{20} \\times 4\\text{B} = 4\\text{MB}$。
> $2\\text{MB}$ 把页大小记成 $8\\text{KB}$；$1\\text{MB}$ 把页表项当成 $1\\text{B}$，页表本身也要分页。`,difficulty:2,tags:[`分页`,`页表大小`,`逻辑地址`]},{id:`q-co2-mem-04`,subject:`co`,chapter:`存储系统`,type:`single`,stem:`某 Cache 的存取时间为 $2\\text{ns}$，主存存取时间为 $20\\text{ns}$，采用「先访问 Cache，不命中再访问主存」的方式。若要求平均访问时间不超过 $4\\text{ns}$，则命中率至少要达到多少？`,options:[`80%`,`85%`,`90%`,`95%`],answer:[2],analysis:`## 解析
- 不命中用时 $2 + 20 = 22\\text{ns}$，故平均访问时间 $T_a = 2 + (1 - H) \\times 20$。
- 令 $T_a \\le 4$ 得 $H \\ge 90\\%$，此时 $T_a$ 恰好为 $4\\text{ns}$。
> $85\\%$ 时 $T_a = 5\\text{ns}$ 仍超标；$95\\%$ 虽满足但不是「至少」所需的命中率。`,difficulty:3,tags:[`Cache`,`命中率`,`平均访问时间`]},{id:`q-co2-mem-05`,subject:`co`,chapter:`存储系统`,type:`judge`,stem:`判断：DRAM 采用异步刷新时，把 $2\\text{ms}$ 的刷新周期平均分配给全部刷新行，因此可以完全消除刷新造成的「死时间」。`,options:[`正确`,`错误`],answer:[1],analysis:`## 解析
- 异步刷新把 $2\\text{ms}$ 分成若干段，每段只刷新一行，如 $128$ 行时约每 $15.6\\mu s$ 刷新一行。
- 刷新期间存储器仍不能响应 CPU 访存，「死时间」只是被分散得很短，并没有消失。
> 判断错误：「缩短并分散」不等于「消除」；集中刷新的死时间最长，分散刷新则明显拉长了存取周期。`,difficulty:2,tags:[`DRAM`,`刷新`,`死时间`]},{id:`q-co2-inst-01`,subject:`co`,chapter:`指令系统`,type:`single`,stem:`某指令系统指令字长 $16$ 位，每个地址码 $4$ 位，采用扩展操作码。已知三地址指令 $12$ 条、二地址指令 $63$ 条，则一地址指令最多可有多少条？`,options:[`$4$ 条`,`$16$ 条`,`$64$ 条`,`$256$ 条`],answer:[1],analysis:`## 解析
- 三地址指令操作码 $4$ 位共 $16$ 个编码，用掉 $12$ 个后还剩 $4$ 个可用于扩展。
- 每个剩余编码再借 $4$ 位可得 $16$ 个二地址编码，共 $64$ 个；用掉 $63$ 个后剩 $1$ 个，一地址最多 $16$ 条。
> 答 $4$ 条漏乘 $16$；答 $64$ 条忘记减去二地址已用的 $63$ 个编码；答 $256$ 条少减了一级。`,difficulty:3,tags:[`扩展操作码`,`指令格式`,`编码`]},{id:`q-co2-inst-02`,subject:`co`,chapter:`指令系统`,type:`single`,stem:`某机采用相对寻址方式，一条转移指令长 $2$ 字节，存放在首地址为 $2000\\text{H}$ 的内存单元中，指令第二字节给出的偏移量为 $08\\text{H}$（补码）。则该转移指令的目标地址是？`,options:[`$1\\text{FF8H}$`,`$2002\\text{H}$`,`$2008\\text{H}$`,`$200\\text{AH}$`],answer:[3],analysis:`## 解析
- 相对寻址 $EA = (PC) + A$，$(PC)$ 是取出本条指令后的值，即 $2000\\text{H} + 2 = 2002\\text{H}$。
- 偏移量 $08\\text{H} = +8$，故 $EA = 200\\text{AH}$，向地址增大的方向转移。
> $2008\\text{H}$ 漏加指令长度；$2002\\text{H}$ 把当前 PC 当目标；$1\\text{FF8H}$ 方向做反了。`,difficulty:2,tags:[`相对寻址`,`有效地址`,`PC`]},{id:`q-co2-inst-03`,subject:`co`,chapter:`指令系统`,type:`single`,stem:`某机指令字长 $32$ 位，采用 $8$ 位定长操作码。若把指令格式由二地址改为三地址（各地址码等长），则每个地址码最多可有多少位？`,options:[`$6$ 位`,`$8$ 位`,`$10$ 位`,`$12$ 位`],answer:[1],analysis:`## 解析
- 操作码占 $8$ 位，三地址格式余下 $32 - 8 = 24$ 位供三个地址码分配。
- 三个字段等长时每个最多 $24 \\div 3 = 8$ 位，可寻址 $2^{8} = 256$ 个编址单位。
> $12$ 位是二地址格式的结果；$6$ 位相当于按四个地址码平均分配；$10$ 位把操作码当成了 $2$ 位。`,difficulty:1,tags:[`指令格式`,`地址码`,`定长操作码`]},{id:`q-co2-inst-04`,subject:`co`,chapter:`指令系统`,type:`single`,stem:`在下列寻址方式中，指令执行阶段不需要访问主存就能取得操作数的是？`,options:[`立即寻址`,`直接寻址`,`一次间接寻址`,`变址寻址`],answer:[0],analysis:`## 解析
- 立即寻址的形式地址字段就是操作数，取指阶段已随指令送入 CPU，执行阶段访存 $0$ 次。
- 直接寻址、变址寻址各访存 $1$ 次；一次间接寻址要访存 $2$ 次，先取操作数地址再取操作数。
> 立即寻址与直接寻址只差一次访存，性质却完全不同：前者操作数在指令中，后者仍在主存。`,difficulty:2,tags:[`寻址方式`,`访存次数`,`立即寻址`]},{id:`q-co2-cpu-01`,subject:`co`,chapter:`CPU 与指令周期`,type:`single`,stem:`某机采用定长单字长指令，取指周期要完成取指令并修改 PC。下列微操作序列中正确的是？`,options:[`$(PC) \\to MAR$，$M(MAR) \\to MDR$，$(MDR) \\to IR$，$(PC) + 1 \\to PC$`,`$(PC) \\to MAR$，$M(MAR) \\to IR$，$(PC) + 1 \\to PC$`,`$(MAR) \\to PC$，$M(MAR) \\to MDR$，$(MDR) \\to IR$`,`$(PC) \\to MDR$，$M(MDR) \\to IR$，$(PC) + 1 \\to PC$`],answer:[0],analysis:`## 解析
- 取指通路是 $(PC) \\to MAR$、$M(MAR) \\to MDR$、$(MDR) \\to IR$，读出的数据必须经 MDR 中转。
- 取指最后 PC 加 $1$（定长单字长指令即加一个指令字长），指向下一条指令。
> 漏写 $M(MAR) \\to MDR$ 忽略了 MDR 的作用；$(MAR) \\to PC$ 方向做反；$(PC) \\to MDR$ 跳过了 MAR。`,difficulty:2,tags:[`取指周期`,`微操作`,`数据通路`]},{id:`q-co2-cpu-02`,subject:`co`,chapter:`CPU 与指令周期`,type:`single`,stem:`某机主频为 $500\\text{MHz}$，每个机器周期包含 $2$ 个时钟周期。某指令的指令周期由 $3$ 个机器周期组成，则该指令的执行时间是多少？`,options:[`$4\\text{ns}$`,`$6\\text{ns}$`,`$8\\text{ns}$`,`$12\\text{ns}$`],answer:[3],analysis:`## 解析
- 时钟周期是主频的倒数：$1 \\div (500 \\times 10^{6}) = 2\\text{ns}$。
- 机器周期 $= 2$ 个时钟周期 $= 4\\text{ns}$，三个机器周期共 $12\\text{ns}$。
> $6\\text{ns}$ 把机器周期当一个时钟周期；$8\\text{ns}$ 只算两个机器周期；$4\\text{ns}$ 是一个机器周期。`,difficulty:2,tags:[`指令周期`,`机器周期`,`时钟周期`]},{id:`q-co2-cpu-03`,subject:`co`,chapter:`CPU 与指令周期`,type:`single`,stem:`某机有 $4$ 级中断，$1$ 级优先级最高。为使高优先级中断能打断低优先级的中断服务程序，各级都要设置屏蔽字（某位为 $1$ 表示屏蔽该级请求，屏蔽字从左到右依次对应 $1 \\sim 4$ 级）。则 $2$ 级中断服务程序应设置的屏蔽字是？`,options:[`$1111$`,`$0011$`,`$1000$`,`$0111$`],answer:[3],analysis:`## 解析
- 屏蔽字原则：屏蔽本级与所有更低级，把优先级更高的级对应位置 $0$，允许它们打断自己。
- $2$ 级服务程序中只有 $1$ 级位置 $0$，$2$、$3$、$4$ 级位置 $1$，即 $0111$。
> $1111$ 连 $1$ 级也屏蔽，等于取消嵌套；$0011$ 允许同级重入；$1000$ 反而允许低级中断打断 $2$ 级。`,difficulty:3,tags:[`中断`,`屏蔽字`,`中断嵌套`]},{id:`q-co2-cpu-04`,subject:`co`,chapter:`CPU 与指令周期`,type:`judge`,stem:`判断：取指周期中 CPU 把 PC 的内容送入 MAR，从主存读出指令送入 IR，随后修改 PC；若指令字长不等，则 PC 每次增加的字节数也可以不同。`,options:[`正确`,`错误`],answer:[0],analysis:`## 解析
- 取指通路固定为 $PC \\to MAR \\to 主存 \\to MDR \\to IR$，与此同时 PC 加上本条指令的长度。
- 变长指令字下各条指令长度不同，PC 的增量也就不同，取指时还要先译码确定本条指令的长度。
> 判断正确。「PC 每次一定加 $1$」只在按字节编址且指令长度恰为 $1$ 字节时才成立。`,difficulty:1,tags:[`取指周期`,`PC`,`变长指令`]},{id:`q-co2-pipe-01`,subject:`co`,chapter:`指令流水线`,type:`single`,stem:`某 $4$ 段指令流水线各段执行时间均为 $1$ 个时钟周期，时钟周期为 $2\\text{ns}$。连续执行 $100$ 条指令时，该流水线的实际吞吐率约为？`,options:[`$125$ MIPS`,`$485$ MIPS`,`$500$ MIPS`,`$1000$ MIPS`],answer:[1],analysis:`## 解析
- 执行 $100$ 条需 $4 + 100 - 1 = 103$ 个时钟周期，即 $206\\text{ns}$。
- $TP = 100 \\div 206\\text{ns} \\approx 485$ MIPS，极限吞吐率才是 $500$ MIPS。
> 选 $500$ MIPS 忽略充满与排空；$125$ MIPS 把四个时钟一条当常态；$1000$ MIPS 把周期算成 $1\\text{ns}$。`,difficulty:3,tags:[`流水线`,`吞吐率`,`性能计算`]},{id:`q-co2-pipe-02`,subject:`co`,chapter:`指令流水线`,type:`single`,stem:`某流水线按 IF、ID、EX、MEM、WB 五段执行，各段均为 $1$ 个时钟周期，并已采用转发技术。连续执行三条指令：I1 为取数指令，把主存数据装入 R1；I2 为加法指令，使用 R1；I3 为减法指令，与 I2 的结果相关。执行完这三条指令共需多少个时钟周期？`,options:[`$7$ 个`,`$8$ 个`,`$9$ 个`,`$15$ 个`],answer:[1],analysis:`## 解析
- 无相关时 $3$ 条指令只需 $5 + 3 - 1 = 7$ 个周期，但 I2 紧跟使用 I1 装入的 R1，属 Load-Use 相关。
- 装入的数据到 MEM 段末才得到，转发来不及，须在 ID 段停顿 $1$ 拍，共 $8$ 个周期。
> $7$ 个周期忽略了 Load-Use 相关；$9$ 个周期以为要停 $2$ 拍；$15$ 个周期是三条指令完全串行的结果。`,difficulty:3,tags:[`流水线`,`Load-Use`,`停顿`]},{id:`q-co2-pipe-03`,subject:`co`,chapter:`指令流水线`,type:`single`,stem:`下列关于流水线中控制冒险（转移指令引起）及其处理方法的叙述中，正确的是？`,options:[`用转发技术把转移目标地址提前送入 IF 段，即可完全消除控制冒险`,`把流水线深度增加到 $10$ 段以上，就能彻底消除控制冒险`,`只有条件转移指令才会引起控制冒险，无条件转移指令不会带来停顿`,`在 ID 段提前算出转移目标并判断转移方向，再配合延迟槽或分支预测，可减少流水线停顿`],answer:[3],analysis:`## 解析
- 控制冒险源于转移目标与方向要到较后阶段才能确定，而 IF 段已按顺序取入了后续指令。
- 处理办法：把目标地址计算与方向判断提前到 ID 段、设置延迟槽、采用静态或动态分支预测。
> 转发解决的是数据冒险；无条件转移同样要等目标地址；加深流水线只会加大预测失败的代价。`,difficulty:2,tags:[`流水线`,`控制冒险`,`分支预测`]},{id:`q-co2-pipe-04`,subject:`co`,chapter:`指令流水线`,type:`single`,stem:`某流水线由 $4$ 段组成，各段执行时间依次为 $3\\text{ns}$、$2\\text{ns}$、$4\\text{ns}$、$2\\text{ns}$，段间锁存器延迟为 $1\\text{ns}$。执行 $20$ 条指令所需时间约为？`,options:[`$92\\text{ns}$`,`$100\\text{ns}$`,`$115\\text{ns}$`,`$120\\text{ns}$`],answer:[2],analysis:`## 解析
- 时钟周期取最长段加锁存器延迟：$\\max(3, 2, 4, 2) + 1 = 5\\text{ns}$。
- 共需 $4 + 20 - 1 = 23$ 个时钟周期，总时间 $23 \\times 5 = 115\\text{ns}$。
> $92\\text{ns}$ 漏掉锁存器的 $1\\text{ns}$；$100\\text{ns}$ 按每周期一条算；$120\\text{ns}$ 多算一个周期。`,difficulty:3,tags:[`流水线`,`时钟周期`,`执行时间`]},{id:`q-co2-bus-01`,subject:`co`,chapter:`总线`,type:`single`,stem:`某同步总线的时钟频率为 $100\\text{MHz}$，数据线宽度为 $64$ 位。每个总线周期传送 $8$ 字节数据，且每次传送固定插入 $1$ 个等待周期（即一个总线周期占 $3$ 个时钟周期），则该总线的有效带宽约为？`,options:[`$267\\text{MB/s}$`,`$400\\text{MB/s}$`,`$800\\text{MB/s}$`,`$1600\\text{MB/s}$`],answer:[0],analysis:`## 解析
- 一个总线周期占 $3$ 个时钟即 $30\\text{ns}$，其中传送 $8$ 字节。
- 有效带宽 $= 8\\text{B} \\div 30\\text{ns} \\approx 267\\text{MB/s}$。
> $800\\text{MB/s}$ 忽略等待周期；$400\\text{MB/s}$ 把 $64$ 位当 $32$ 位；$1600\\text{MB/s}$ 按每周期 $16$ 字节算。`,difficulty:2,tags:[`总线`,`带宽`,`等待周期`]},{id:`q-co2-bus-02`,subject:`co`,chapter:`总线`,type:`single`,stem:`在集中式总线仲裁的三种方式中，关于独立请求方式的叙述正确的是？`,options:[`每个设备都有独立的请求线与允许线，响应速度最快但控制线最多，优先级可由程序灵活改变`,`所有设备共用一根请求线，优先级由设备在链上的位置决定，对电路故障最敏感`,`只用一根请求线和一根允许线，靠计数器的计数值决定本次由哪个设备使用总线`,`设备数量增加时所需控制线条数不变，因此在大规模系统中比链式查询更经济`],answer:[0],analysis:`## 解析
- 独立请求方式为每个设备配一对请求线与允许线，由仲裁器内部的排队电路判优，响应速度最快。
- 代价是控制线数量按 $2n$ 增长，线多成本高，因此适合设备数不太多的场合。
> 选项 B、C 分别描述链式查询与计数器定时查询；选项 D 把控制线随设备数增加的缺点说成了优点。`,difficulty:2,tags:[`总线`,`仲裁`,`独立请求`]},{id:`q-co2-bus-03`,subject:`co`,chapter:`总线`,type:`single`,stem:`某同步总线数据线宽度为 $32$ 位，每个总线周期传送一次数据，测得总线带宽为 $500\\text{MB/s}$，则该总线的时钟频率至少为？`,options:[`$62.5\\text{MHz}$`,`$125\\text{MHz}$`,`$250\\text{MHz}$`,`$500\\text{MHz}$`],answer:[1],analysis:`## 解析
- $32$ 位数据线一次传 $4$ 字节，每秒需传 $500 \\times 10^{6} \\div 4$ 次。
- 每个总线周期传一次，故时钟频率至少为 $125\\text{MHz}$，且 $1\\text{B} = 8\\text{b}$。
> $250\\text{MHz}$ 把数据线当 $16$ 位；$500\\text{MHz}$ 漏除每周期字节数；$62.5\\text{MHz}$ 当成 $64$ 位。`,difficulty:1,tags:[`总线`,`带宽`,`时钟频率`]},{id:`q-co2-io-01`,subject:`co`,chapter:`IO 与中断`,type:`single`,stem:`关于程序查询、程序中断和 DMA 三种输入输出方式，下列说法正确的是？`,options:[`程序查询方式下 CPU 与外设串行工作，CPU 利用率最低；DMA 方式由 DMA 控制器直接控制主存与外设间的传送，CPU 只在传送前初始化、传送后做结束处理`,`程序中断方式下每传送一个字节都要执行一次中断服务程序，因此 CPU 开销比程序查询方式更大`,`DMA 方式传送数据时仍要经过 CPU 的通用寄存器中转，所以传送速度比中断方式慢`,`三种方式中程序查询方式的 CPU 干预最少，因为 CPU 发出命令后就可以不管外设`],answer:[0],analysis:`## 解析
- CPU 干预程度依次降低：程序查询要反复查询状态、与外设串行，中断方式每批数据中断一次，DMA 只在传送前后各干预一次。
- DMA 由控制器直接在主存与外设之间传送数据，不经过 CPU 的通用寄存器，速度最高。
> 选项 B 混淆了中断次数与查询次数；选项 C 否认 DMA 不经过 CPU 的特点；选项 D 把三种方式的干预程度说反了。`,difficulty:1,tags:[`IO 方式`,`DMA`,`CPU 开销`]},{id:`q-co2-io-02`,subject:`co`,chapter:`IO 与中断`,type:`single`,stem:`某外设的数据传输率为 $1\\text{MB/s}$，CPU 用程序查询方式为其服务，每次查询并传送 $4$ 字节需要 $100$ 个时钟周期，CPU 主频为 $500\\text{MHz}$。则 CPU 用于该外设的时间约占总时间的多少？`,options:[`0.5%`,`2.6%`,`5.2%`,`26%`],answer:[2],analysis:`## 解析
- 每秒传送 $1\\text{MB} \\div 4\\text{B} = 262144$ 次，每次 $100$ 个时钟周期，共约 $2.62 \\times 10^{7}$ 个周期。
- 主频 $500\\text{MHz}$ 即每秒 $5 \\times 10^{8}$ 个周期，占用率约 $5.2\\%$。
> $0.5\\%$ 相当于每次只花 $10$ 个周期；$2.6\\%$ 少算一倍；$26\\%$ 是数量级错位。`,difficulty:3,tags:[`程序查询`,`CPU 占用率`,`IO 方式`]},{id:`q-co2-io-03`,subject:`co`,chapter:`IO 与中断`,type:`judge`,stem:`判断：在多重中断系统中，若 CPU 响应某级中断请求后没有开中断，则除优先级更高的中断请求外，其余中断请求都不能被响应。`,options:[`正确`,`错误`],answer:[1],analysis:`## 解析
- 关中断后 CPU 不再响应任何可屏蔽的中断请求，与其优先级高低无关。
- 多重中断要先关中断、保存断点、形成入口地址，进入服务程序后再开中断，更高优先级的请求才能嵌套。
> 判断错误：关中断屏蔽的是整个可屏蔽中断系统，而不是只屏蔽同级与低级请求。`,difficulty:2,tags:[`中断`,`多重中断`,`开中断`]}],c=[{id:`q-co3-data-01`,subject:`co`,chapter:`数据的表示与运算`,type:`single`,stem:`关于定点数与浮点数的比较，下列说法正确的是？`,options:[`浮点数运算比定点数简单，因为不必单独处理小数点的位置`,`定点数能表示的范围比浮点数大，而且精度可以随数据变化`,`定点数表示范围小、精度固定，运算规则简单；浮点数表示范围大，但运算步骤多、控制复杂`,`浮点数进行规格化是为了扩大可表示的数值范围`],answer:[2],analysis:`## 解析
- 定点数的小数点位置固定，表示范围小、精度固定，加减乘除都按统一规则进行，硬件实现简单。
- 浮点数用阶码与尾数分别承担范围与精度，可表示的范围大得多，但运算要经过对阶、尾数运算、规格化与舍入，控制复杂。
> 浮点运算步骤更多而不是更简单；范围大的是浮点数；规格化的目的是提高精度并保证表示唯一，不是扩大范围。`,difficulty:2,tags:[`定点数`,`浮点数`,`表示范围`,`对比`]},{id:`q-co3-data-02`,subject:`co`,chapter:`数据的表示与运算`,type:`single`,stem:`关于奇偶校验的叙述，正确的是？`,options:[`奇偶校验既能发现一位错，也能指出错在哪一位`,`奇偶校验只能发现奇数个位出错，既不能指出错位的具体位置，也发现不了偶数个位同时出错`,`奇偶校验的校验位位数随数据位数的增加而增加，检错能力强于海明码`,`只要增加一位校验位，奇偶校验就能发现并纠正所有的一位错`],answer:[1],analysis:`## 解析
- 奇偶校验只增加 $1$ 位校验位，使整个码字中 $1$ 的个数为奇数或偶数：任一位（或奇数个位）出错都会破坏这一性质，故能发现奇数个错。
- 但它无法指出错位在哪，偶数个位同时出错时奇偶性不变，因而会漏检，也不具备纠错能力。
> 需要多位校验位并具有纠错能力的是海明码；奇偶校验的校验位恒为 $1$ 位，不随数据位数增加。`,difficulty:2,tags:[`奇偶校验`,`校验码`,`检错`]},{id:`q-co3-data-03`,subject:`co`,chapter:`数据的表示与运算`,type:`single`,stem:`对 $8$ 位数据进行海明校验，要求既能发现一位错又能指出错在哪一位，至少需要几位校验位？`,options:[`$3$ 位`,`$4$ 位`,`$5$ 位`,`$8$ 位`],answer:[1],analysis:`## 解析
- 设校验位为 $k$ 位、数据位 $n=8$，海明码能纠正一位错的条件是 $2^{k} \\ge n + k + 1$。
- $k=3$ 时 $2^{3}=8 < 8+3+1=12$，不够；$k=4$ 时 $2^{4}=16 \\ge 13$，故至少 $4$ 位。
> $3$ 位只满足 $2^{k} \\ge n+1$，能检错却定位不了错位；$5$ 位、$8$ 位够用但不是最少。`,difficulty:2,tags:[`海明校验`,`校验位`,`编码`]},{id:`q-co3-data-04`,subject:`co`,chapter:`数据的表示与运算`,type:`judge`,stem:`判断：在浮点数的表示中，尾数的位数决定了数的表示范围，阶码的位数决定了数的精度。`,options:[`正确`,`错误`],answer:[1],analysis:`## 解析
- 阶码决定小数点的位置，阶码位数越多、可表示的指数范围越大，整个浮点数的范围随之扩大。
- 尾数位数决定有效数字的位数，尾数越长精度越高，但它并不改变指数部分的范围。
> 题目把两者的作用对调了：决定范围的是阶码、决定精度的是尾数，故该说法错误。`,difficulty:1,tags:[`浮点数`,`阶码`,`尾数`,`表示范围`]},{id:`q-co3-mem-01`,subject:`co`,chapter:`存储系统`,type:`single`,stem:`关于 SRAM 与 DRAM 的比较，下列说法正确的是？`,options:[`SRAM 用触发器存储信息，但同样需要定时刷新才能保持数据`,`DRAM 的读出是非破坏性的，因此不需要重写电路`,`SRAM 的集成度高、单位成本低，所以常用来做主存`,`DRAM 用电容存储信息，必须定时刷新，读出是破坏性的，集成度高于 SRAM`],answer:[3],analysis:`## 解析
- DRAM 靠电容存信息，电荷会泄漏因而要定时刷新；读出后原信息被破坏，还要重写，所以存取周期大于存取时间。
- SRAM 用触发器保存信息，不需刷新、速度快，但集成度低、价格高，适合作 Cache；DRAM 集成度高、成本低，适合作主存。
> 「SRAM 也要刷新」把两者特点弄反；DRAM 读出是破坏性的，漏掉重写电路就把周期当成了时间；集成度高、成本低也不是 SRAM 的特点。`,difficulty:1,tags:[`SRAM`,`DRAM`,`刷新`,`存储器对比`]},{id:`q-co3-mem-02`,subject:`co`,chapter:`存储系统`,type:`single`,stem:`与写回法相比，Cache 采用写直达法（Write Through）的特点是？`,options:[`写命中时只修改 Cache 中的副本并置修改位，被替换时才写回主存`,`写命中时同时修改 Cache 与主存，控制简单，但每次写都要访存，开销较大`,`写不命中时一定要先把主存块调入 Cache 再写，这是写直达法的规定`,`不需要设置修改位，因此替换算法的实现比写回法更复杂`],answer:[1],analysis:`## 解析
- 写直达法在写命中时同时改写 Cache 与主存，主存副本始终有效，控制简单，常配写缓冲来掩盖写主存的延迟，代价是访存次数多。
- 写回法只改 Cache 并置修改位，该行被替换出时才写回主存，访存少但主存内容可能暂时过期。
> 头一个选项描述的正是写回法；写不命中是否调块由写分配策略决定，与写直达本身无关；写回法才依赖修改位，逻辑更复杂。`,difficulty:2,tags:[`Cache`,`写直达`,`写回`,`写策略`]},{id:`q-co3-mem-03`,subject:`co`,chapter:`存储系统`,type:`single`,stem:`关于直接映射与组相联映射的比较，下列说法正确的是？`,options:[`组相联映射中每组只有一块，因此不需要替换算法`,`相同容量下，直接映射的标记字段比组相联映射更长`,`直接映射不需要替换算法、判定命中只比较一次，但冲突缺失多于组相联`,`组相联的相联度越高，组数越多，标记字段也越短`],answer:[2],analysis:`## 解析
- 直接映射中每个主存块只能放入唯一的一行，硬件只需比较一次标记，也不需要替换算法，但冲突缺失多、命中率偏低。
- 组相联的一组含多行，冲突缺失明显减少，必须配替换算法；总容量不变而相联度翻倍时，组数与组号位数各减一，标记字段反而加一位。
> 「每组只有一块」描述的是直接映射；相联度提高时组数减少、标记加长，故另外两个选项都把关系说反了。`,difficulty:2,tags:[`Cache`,`直接映射`,`组相联`,`替换算法`]},{id:`q-co3-mem-04`,subject:`co`,chapter:`存储系统`,type:`single`,stem:`某机采用两级 Cache：L1 存取时间 $1$ns、命中率 $90\\%$；L2 存取时间 $5$ns、命中率 $80\\%$；主存存取时间 $50$ns。访存时先查 L1，缺失再查 L2，L2 再缺失才访问主存，则平均访问时间约为？`,options:[`$2.5$ns`,`$1.5$ns`,`$3.0$ns`,`$6.5$ns`],answer:[0],analysis:`## 解析
- 平均访问时间 $T_a = t_1 + (1-H_1)t_2 + (1-H_1)(1-H_2)t_m$，$H_2$ 为 L2 局部命中率。
- 代入得 $1 + 0.1\\times5 + 0.1\\times0.2\\times50 = 2.5$ ns。
> $1.5$ns 认为 L2 必命中；$3.0$ns 把 L2 访问时间多算一倍；$6.5$ns 漏掉 L2 这一级。`,difficulty:3,tags:[`多级Cache`,`平均访问时间`,`命中率`]},{id:`q-co3-mem-05`,subject:`co`,chapter:`存储系统`,type:`single`,stem:`某磁盘转速为 $7200$ r/min，平均寻道时间为 $8$ms，数据传输率为 $20$MB/s。读出某磁道上连续存放的 $64$KB 数据，所需时间约为？`,options:[`$11.5$ms`,`$19.7$ms`,`$15.4$ms`,`$12.3$ms`],answer:[2],analysis:`## 解析
- 平均旋转延迟为半圈：$60/7200/2 \\approx 4.17$ ms。
- 传输时间 $= 64\\text{KB}/20\\text{MB/s} \\approx 3.28$ ms，加寻道 $8$ ms 共约 $15.4$ ms。
> 存取时间 = 寻道 + 旋转 + 传输：$11.5$ms 漏旋转延迟，$19.7$ms 用整圈算，$12.3$ms 漏传输时间。`,difficulty:3,tags:[`磁盘`,`寻道时间`,`旋转延迟`,`传输时间`]},{id:`q-co3-inst-01`,subject:`co`,chapter:`指令系统`,type:`single`,stem:`某机按字节编址、指令字长 $16$ 位，转移指令中的形式地址为 $8$ 位补码，采用相对寻址。若某条转移指令存放在 $2000\\text{H}$ 处、形式地址为 $\\text{F6H}$，则转移目标地址是？`,options:[`$1\\text{FF6H}$`,`$1\\text{FF8H}$`,`$20\\text{F8H}$`,`$200\\text{CH}$`],answer:[1],analysis:`## 解析
- 按字节编址、指令字长 16 位，取指后 PC 已自动加 2，即 PC = 2002H。
- 形式地址 F6H 是 8 位补码，真值为 -10，故目标地址 = 2002H - 0AH = 1FF8H。
> 1FF6H 是忘掉 PC 已加 2；20F8H 把 F6H 当无符号数；200CH 把负偏移当成正偏移。`,difficulty:3,tags:[`相对寻址`,`有效地址`,`程序计数器`]},{id:`q-co3-inst-02`,subject:`co`,chapter:`指令系统`,type:`single`,stem:`同一程序在 RISC 机上需 $2\\times10^{9}$ 条指令、平均 CPI 为 $1.2$，在 CISC 机上需 $1\\times10^{9}$ 条指令、平均 CPI 为 $2.4$。两机主频均为 $1$GHz，则两机的执行时间关系是？`,options:[`两机执行时间相同`,`RISC 机约为 CISC 机的一半`,`CISC 机约为 RISC 机的一半`,`RISC 机约为 CISC 机的 $2.4$ 倍`],answer:[0],analysis:`## 解析
- RISC 机需 $2\\times10^{9}\\times1.2$ 个周期，CISC 机需 $1\\times10^{9}\\times2.4$ 个，乘积都是 $2.4\\times10^{9}$。
- 两机主频相同，故执行时间相等，均为 $2.4$ s。
> 只比条数会误判 CISC 更快，只比 CPI 会误判 RISC 更快；本题两者恰好抵消。`,difficulty:3,tags:[`RISC`,`CISC`,`CPI`,`执行时间`]},{id:`q-co3-inst-03`,subject:`co`,chapter:`指令系统`,type:`single`,stem:`关于变址寻址与相对寻址的比较，下列说法正确的是？`,options:[`相对寻址以变址寄存器的内容为基准，变址寻址以程序计数器为基准`,`两种方式的形式地址都必须是无符号数，只能作正向偏移`,`变址寄存器的内容由指令中的形式地址直接给出，程序执行中不能改变`,`变址寻址以变址寄存器的内容为基准、形式地址作偏移，便于处理数组；相对寻址以 PC 为基准，多用于转移与程序浮动`],answer:[3],analysis:`## 解析
- 变址寻址的有效地址 $EA = (IX) + A$，变址寄存器的内容可在循环中修改，因此只要改 $IX$ 就能顺序访问数组元素。
- 相对寻址以程序计数器为基准 $EA = (PC) + A$，偏移量是带符号补码，可以实现程序浮动与就近转移。
> 把两种方式的基准寄存器互换即得错误选项；形式地址可正可负，变址寄存器的内容由程序修改而非指令给出，故另外两项也不对。`,difficulty:2,tags:[`变址寻址`,`相对寻址`,`寻址方式对比`]},{id:`q-co3-inst-04`,subject:`co`,chapter:`指令系统`,type:`judge`,stem:`判断：与 CISC 相比，RISC 的指令数目少、寻址方式少、指令字长固定，且只有取数/存数指令能访问主存，因而更适合采用硬布线控制器和流水线。`,options:[`正确`,`错误`],answer:[0],analysis:`## 解析
- RISC 的规整性使控制逻辑可以用组合逻辑直接实现，不必逐条读取微指令，指令字长固定也便于取指与译码对齐。
- 只有 Load/Store 能访存，访存被集中到少数指令，其余指令都在寄存器之间运算，流水线更容易调度。
> CISC 指令多、字长可变、寻址方式丰富，多采用微程序控制器；这正是两类指令系统的典型差别，故说法正确。`,difficulty:2,tags:[`RISC`,`CISC`,`硬布线`,`流水线`]},{id:`q-co3-cpu-01`,subject:`co`,chapter:`CPU 与指令周期`,type:`single`,stem:`某 CPU 采用单总线结构的数据通路，取指阶段依次完成 $(PC)\\to MAR$、$M(MAR)\\to MDR$、$(MDR)\\to IR$，随后修改 PC。若主存按字节编址、指令字长为 $32$ 位且按边界对齐存放，则 PC 的增量应为？`,options:[`$4$`,`$1$`,`$2$`,`$8$`],answer:[0],analysis:`## 解析
- 主存按字节编址时一个地址对应一个字节，一条 $32$ 位指令要占 $32 \\div 8 = 4$ 个编址单位。
- 因此取出指令后 PC 应加 $4$，指向下一条指令的首字节地址，即 $(PC)+4\\to PC$。
> 加 $1$ 是把编址单位当成了字；加 $2$ 对应 $16$ 位指令；加 $8$ 则把 $32$ 位误当成 $8$ 字节。`,difficulty:1,tags:[`数据通路`,`取指周期`,`程序计数器`]},{id:`q-co3-cpu-02`,subject:`co`,chapter:`CPU 与指令周期`,type:`single`,stem:`某微指令的操作控制字段采用字段直接编码，分为 $5$ 个互斥字段，各字段包含的微命令数依次为 $3$、$4$、$5$、$6$、$7$，每个字段还要留一个编码表示「不发出任何微命令」。该操作控制字段至少需要多少位？`,options:[`$13$ 位`,`$25$ 位`,`$14$ 位`,`$20$ 位`],answer:[2],analysis:`## 解析
- 字段直接编码把互斥的微命令编在同一字段内，每个字段留一个编码表示不操作，故位数取 $\\lceil \\log_2(n_i+1) \\rceil$。
- 五个字段依次需要 $2$、$3$、$3$、$3$、$3$ 位，合计 $14$ 位。
> $13$ 位是没有为「不操作」留编码；$25$ 位是直接编码（一位对应一个微命令）的位数；$20$ 位按每字段平均 $4$ 位估计，没有逐字段计算。`,difficulty:3,tags:[`微指令`,`字段直接编码`,`操作控制字段`]},{id:`q-co3-cpu-03`,subject:`co`,chapter:`CPU 与指令周期`,type:`single`,stem:`某微程序控制器中，机器指令共 $80$ 条，每条指令对应一段微程序、平均含 $6$ 条微指令，微指令字长为 $32$ 位。若只考虑这些微指令，则控制存储器的容量至少应为？`,options:[`$15360$B`,`$1920$B`,`$480$B`,`$240$B`],answer:[1],analysis:`## 解析
- 微指令总数为 $80 \\times 6 = 480$ 条，控制存储器的字数取决于需要存放的微指令条数。
- 每条微指令 $32$ 位，故容量至少为 $480 \\times 32$ 位 $= 15360$ 位 $= 1920$ 字节。
> $15360$B 把位数直接当成了字节数；$480$B 与 $240$B 都漏乘微指令字长，只算了微指令条数。`,difficulty:2,tags:[`控制存储器`,`微程序`,`容量计算`]},{id:`q-co3-cpu-04`,subject:`co`,chapter:`CPU 与指令周期`,type:`single`,stem:`关于硬布线控制器与微程序控制器的比较，下列说法正确的是？`,options:[`微程序控制器不需要控制存储器，控制信号由门电路阵列直接产生`,`硬布线控制器的控制信号要逐条读出微指令后才能产生，因此比微程序控制器慢`,`要增加一条新指令，硬布线控制器只需增加一段微程序，比微程序控制器更容易`,`硬布线控制器由组合逻辑电路直接产生控制信号，速度快但修改扩充困难；微程序控制器把微命令存入 CM，规整灵活但速度较慢`],answer:[3],analysis:`## 解析
- 硬布线控制器的控制信号由组合逻辑电路即时产生，延迟小、速度快，但指令一改就要重新设计电路，扩充与修改困难。
- 微程序控制器把微命令编成微指令存入控制存储器，改微程序即可改变指令功能，规整灵活，但必须逐条取出微指令，速度较慢。
> 「不需要 CM」「读取微指令更慢」都把两者的实现方式说反；增加微程序是微程序控制器的优点，不是硬布线的。`,difficulty:2,tags:[`硬布线`,`微程序`,`控制器对比`]},{id:`q-co3-cpu-05`,subject:`co`,chapter:`CPU 与指令周期`,type:`single`,stem:`某程序中可优化的部分占执行时间的 $60\\%$，若只把这一部分的速度提高到原来的 $3$ 倍（其余部分不变），则整个程序的加速比约为？`,options:[`约 $1.67$`,`$3$`,`$2.5$`,`约 $1.43$`],answer:[0],analysis:`## 解析
- Amdahl 定律：$S = 1 / ((1-f) + f/k)$，$f$ 为可优化部分比例，$k$ 为加速倍数。
- 代入得 $S = 1/(0.4+0.2) \\approx 1.67$，未优化的 $40\\%$ 部分限制了收益。
> $3$ 是局部加速比；$2.5$ 是 $k\\to\\infty$ 的极限；$1.43$ 把提高 $3$ 倍当成省 $3$ 倍。`,difficulty:3,tags:[`Amdahl定律`,`加速比`,`性能计算`]},{id:`q-co3-pipe-01`,subject:`co`,chapter:`指令流水线`,type:`single`,stem:`在指令流水线中，各段之间设置的流水线寄存器（锁存器）的主要作用是？`,options:[`消除流水线中的数据冒险，使相关指令不必插入停顿`,`增加数据通路的位数，从而提高总线带宽`,`暂存本段产生的运算结果与控制信息，隔离各段，使不同指令能同时在各段中工作`,`保存程序的断点与现场，供中断服务程序返回时使用`],answer:[2],analysis:`## 解析
- 流水线寄存器在时钟边沿锁存本段的运算结果与控制信息，供下一段在下一个时钟周期使用，同时把各段的组合逻辑隔开。
- 有了这种隔离，不同指令才能同时处在不同的段中，实现时间上的重叠，它本身并不改变数据通路的功能。
> 缓解数据冒险要靠转发与停顿；保存断点和现场属于中断响应的任务；流水线寄存器与总线位数无关。`,difficulty:1,tags:[`流水线`,`流水线寄存器`,`锁存器`]},{id:`q-co3-pipe-02`,subject:`co`,chapter:`指令流水线`,type:`single`,stem:`关于超标量与超流水线的比较，下列说法正确的是？`,options:[`超标量依靠提高时钟频率来加快执行，超流水线依靠增加功能部件实现并行`,`超标量通过配置多条流水线、多套功能部件，在每个时钟周期并发执行多条指令；超流水线把流水段进一步细分以提高主频`,`超标量在每个时钟周期只能流出一条指令，超流水线每个时钟周期可流出多条`,`两者都只是加深流水线的级数，并不改变每个时钟周期流出的指令条数`],answer:[1],analysis:`## 解析
- 超标量以空间换时间：同一时钟周期发射并执行多条指令，需要多套功能部件与更复杂的指令调度。
- 超流水线以时间换空间：把每段再细分为若干小段，缩短时钟周期、提高主频，但每个时钟周期仍只流出一条指令。
> 把提高主频说成超标量的手段，或认为超流水每周期能流出多条，都颠倒了二者的实现途径；二者可以结合使用。`,difficulty:2,tags:[`超标量`,`超流水线`,`指令级并行`]},{id:`q-co3-pipe-03`,subject:`co`,chapter:`指令流水线`,type:`single`,stem:`某 $5$ 段流水线理想情况下每个时钟周期完成一条指令，实际运行时平均每 $10$ 条指令要插入 $4$ 个停顿周期。连续执行 $1000$ 条指令的实际 CPI 约为？`,options:[`约 $1.04$`,`$1.0$`,`$4.0$`,`约 $1.4$`],answer:[3],analysis:`## 解析
- 停顿周期数为 $(1000/10) \\times 4 = 400$ 个。
- 总周期数 $= k + n - 1 + 400 = 1404$，故 CPI $= 1404/1000 \\approx 1.4$。
> $1.0$ 忽略停顿；$1.04$ 把每 $10$ 条 $4$ 个停顿算成每 $100$ 条；$4.0$ 把停顿个数当成 CPI。`,difficulty:3,tags:[`流水线`,`CPI`,`停顿周期`]},{id:`q-co3-pipe-04`,subject:`co`,chapter:`指令流水线`,type:`judge`,stem:`判断：流水线的级数（段数）越多，其加速比就一定越大，因此流水线设计得越深越好。`,options:[`正确`,`错误`],answer:[1],analysis:`## 解析
- $k$ 段流水线的理想加速比上限为 $k$，但实际还要计入每段后流水线寄存器的建立与传输延迟，段数越多这项固定开销所占比例越大。
- 段数增多还会带来更多数据、控制与结构冒险，转发与停顿逻辑更复杂，实际加速比往往明显低于理想值。
> 只有每段逻辑延迟远大于寄存器开销且冒险可控时，加深流水线才有收益，故「越深越好」不正确。`,difficulty:2,tags:[`流水线`,`加速比`,`流水线深度`]},{id:`q-co3-bus-01`,subject:`co`,chapter:`总线`,type:`single`,stem:`关于同步总线与异步总线的比较，下列说法正确的是？`,options:[`同步总线靠请求与应答信号协调，异步总线必须使用统一的时钟信号`,`异步总线的传输速度一定高于同步总线，因为它不受时钟周期的限制`,`同步总线中慢速设备可通过拉长时钟周期与快速设备并存，因而不存在设备速度不匹配的问题`,`同步总线由统一时钟协调收发双方，速度快、控制简单；异步总线用握手方式，能适应不同速度的设备，但时间开销更大`],answer:[3],analysis:`## 解析
- 同步总线以统一时钟为基准，一次传送占用的时钟数固定，控制简单、效率较高，但只能按最慢的设备来设计。
- 异步总线用请求与应答信号握手，收发双方按各自的速度配合，适应性强，代价是每次传送都要额外的握手时间。
> 把「时钟」与「握手」两种定时方式互换就得到错误选项；异步不受时钟限制并不等于一定更快，同步总线也绕不开最慢设备的限制。`,difficulty:2,tags:[`总线`,`同步总线`,`异步总线`,`握手`]},{id:`q-co3-bus-02`,subject:`co`,chapter:`总线`,type:`single`,stem:`某同步总线的数据线宽度为 $32$ 位，总线时钟频率为 $50$MHz，每个总线周期占 $4$ 个时钟周期并传送一次数据，则该总线的数据传输率约为？`,options:[`$200$MB/s`,`$12.5$MB/s`,`$25$MB/s`,`$50$MB/s`],answer:[3],analysis:`## 解析
- 每个总线周期传送 $32$ 位即 $4$ 字节，而它占 $4$ 个时钟周期。
- 每秒周期数 $= 50\\times10^{6}/4 = 12.5\\times10^{6}$，故传输率 $= 50$ MB/s。
> $200$MB/s 漏除 $4$ 个时钟；$12.5$MB/s 把周期数当成字节数；$25$MB/s 把总线宽度当成 $16$ 位。`,difficulty:2,tags:[`总线带宽`,`总线周期`,`传输率`]},{id:`q-co3-bus-03`,subject:`co`,chapter:`总线`,type:`single`,stem:`关于总线集中式仲裁的三种方式，下列说法正确的是？`,options:[`链式查询方式所需控制线最多，且各设备的优先级可以随意调整`,`计数器定时查询方式只需一根请求线，且计数值不能改变，优先级完全固定`,`独立请求方式中每个设备都有独立的请求线与允许线，响应最快、优先级最灵活，但控制线数量最多`,`三种仲裁方式的响应速度相同，区别只在于所需控制线的数量`],answer:[2],analysis:`## 解析
- 链式查询的请求线与允许线依次串接，控制线最少，但优先级由连线顺序固定，某个设备故障会阻断它后面的设备。
- 计数器定时查询用一组设备地址线，可通过改变计数初值调整优先级；独立请求方式为每个设备各配请求线与允许线，响应最快、优先级最灵活，但线数最多。
> 「链式控制线最多」「计数值不能改变」「三种方式速度相同」都与实际不符或过于绝对。`,difficulty:2,tags:[`总线仲裁`,`链式查询`,`独立请求`]},{id:`q-co3-bus-04`,subject:`co`,chapter:`总线`,type:`single`,stem:`某机地址总线为 $24$ 位、数据总线为 $32$ 位，主存按字节编址，则它能支持的最大主存容量是？`,options:[`$32$MB`,`$16$MB`,`$4$MB`,`$64$MB`],answer:[1],analysis:`## 解析
- 地址总线有 $24$ 根，可寻址 $2^{24} = 16$M 个地址单元，按字节编址时最大容量就是 $16$MB。
- 数据总线的宽度只决定一次能并行传送多少位，并不影响可寻址范围。
> $32$MB 与 $4$MB 把地址线位数分别当成了 $25$ 位与 $22$ 位；$64$MB 对应 $2^{26}$，来源是把 $32$ 位数据总线宽度当成了地址位数。`,difficulty:1,tags:[`地址总线`,`寻址范围`,`主存容量`]},{id:`q-co3-io-01`,subject:`co`,chapter:`IO 与中断`,type:`single`,stem:`关于程序查询、中断和 DMA 三种输入输出方式，下列说法正确的是？`,options:[`程序查询方式下 CPU 反复读取状态端口，与外设不能并行工作；中断方式下 CPU 可与外设并行；DMA 方式下数据传送不需要 CPU 干预`,`中断方式下 CPU 与外设不能并行工作，程序查询方式下可以`,`DMA 方式下每传送一个数据都要 CPU 执行一次中断服务程序`,`三种方式中程序查询方式的 CPU 利用率最高，因为它不需要中断开销`],answer:[0],analysis:`## 解析
- 程序查询方式下 CPU 不断查询外设状态，属于忙等，与外设串行工作，CPU 利用率最低。
- 中断方式下外设准备数据期间 CPU 继续执行原程序，靠一次中断服务程序完成一次传送；DMA 由控制器在设备与主存间直接传送，只在整批数据传完后中断一次。
> 「中断下不能并行」把查询与中断的特点说反；DMA 并非每传一个数据就中断一次；程序查询的 CPU 利用率最低。`,difficulty:1,tags:[`程序查询`,`中断`,`DMA`,`IO方式对比`]},{id:`q-co3-io-02`,subject:`co`,chapter:`IO 与中断`,type:`single`,stem:`某外设需传送 $1$KB 数据。中断方式下每传送 $1$ 字节申请一次中断，每次中断服务程序含 $50$ 条指令、平均 CPI 为 $4$；DMA 方式下整块传完只中断一次，服务程序含 $200$ 条指令、CPI 也为 $4$。忽略其他开销，两种方式下 CPU 用于中断服务程序的时钟周期数之比（中断 : DMA）约为？`,options:[`$1:256$`,`$128:1$`,`$256:1$`,`$1024:1$`],answer:[2],analysis:`## 解析
- 中断方式共中断 $1024$ 次，每次耗 $50 \\times 4 = 200$ 个周期，合计 $204800$ 个。
- DMA 方式只中断 $1$ 次，耗 $200 \\times 4 = 800$ 个周期，两者之比为 $204800:800 = 256:1$。
> $1024:1$ 只比中断次数，漏掉指令数与 CPI；$128:1$ 少算一半次数；$1:256$ 方向写反。`,difficulty:3,tags:[`中断`,`DMA`,`开销比较`]},{id:`q-co3-io-03`,subject:`co`,chapter:`IO 与中断`,type:`single`,stem:`关于 IO 端口的编址方式，下列说法正确的是？`,options:[`独立编址方式下，IO 端口与主存单元共用地址空间，用普通访存指令即可访问端口`,`统一编址方式下必须使用专门的 IN、OUT 指令，程序中要区分访存与 IO 操作`,`独立编址方式下 IO 端口不占主存地址空间，因此不需要专门的 IO 指令`,`统一编址方式下 IO 端口与主存单元共用地址空间，可用普通访存指令访问端口，但会占用一部分主存地址`],answer:[3],analysis:`## 解析
- 统一编址（存储器映射）把 IO 端口与主存单元放入同一地址空间，用普通访存指令就能读写端口，不必设专门的 IO 指令，代价是占用一部分主存地址。
- 独立编址另设一套 IO 地址空间，主存地址不受影响，但访问端口必须用 IN、OUT 等专门的 IO 指令，控制信号也要区分访存与 IO。
> 把两种编址方式的地址空间与指令特点交叉搭配，就得到另外三个错误选项。`,difficulty:2,tags:[`端口编址`,`统一编址`,`独立编址`,`IO接口`]},{id:`q-co3-io-04`,subject:`co`,chapter:`IO 与中断`,type:`judge`,stem:`判断：内部异常由 CPU 内部的特定事件引起，一般不能被屏蔽；外部中断中除不可屏蔽中断外，可以被 CPU 通过关中断加以屏蔽。`,options:[`正确`,`错误`],answer:[0],analysis:`## 解析
- 异常由 CPU 执行指令时在内部产生，如除数为 $0$、溢出、缺页、非法操作码，与当前指令直接相关，检测到就必须处理，无法用关中断屏蔽。
- 外中断由外部设备或时钟提出，开中断后才会被响应，除不可屏蔽中断外都可以被关中断屏蔽。
> 若认为关中断能屏蔽一切请求，就把可屏蔽外中断与内部异常混为一谈，二者的来源与可否屏蔽的性质并不相同。`,difficulty:2,tags:[`中断`,`异常`,`可屏蔽中断`]}];export{i as a,t as c,a as i,e as l,s as n,r as o,o as r,n as s,c as t};