import type { SeedQuestion } from '../question-types';

export const DS_QUESTIONS: SeedQuestion[] = [
  {
    id: 'q-ds-list-01',
    subject: 'ds',
    chapter: '线性表',
    type: 'single',
    stem: '在一个长度为 $n$ 的顺序表第 $i$ 个位置（$1 \\le i \\le n+1$）插入一个元素，需要向后移动的元素个数是？',
    options: ['$n-i$', '$n-i-1$', '$n-i+1$', '$i$'],
    answer: [2],
    analysis: `## 解析
- 在 $i$ 处插入必须挪走原第 $i$ 到第 $n$ 个元素，个数为 $n-i+1$。
- 当 $i=n+1$（表尾追加）时移动 $0$ 个；当 $i=1$（表头插入）时移动 $n$ 个，与公式吻合。
- 表头插入平均移动 $n/2$ 个，故插入操作平均时间复杂度为 $O(n)$。
> $n-i$ 是典型的差一错误，它把表尾的 $i=n+1$ 也算成移动 $1$ 个；$i$ 则把插入当成了删除。`,
    difficulty: 1,
    tags: ['顺序表', '插入移动', '时间复杂度'],
  },
  {
    id: 'q-ds-list-02',
    subject: 'ds',
    chapter: '线性表',
    type: 'single',
    stem: '在长度为 $n$ 的单链表中删除指针 $p$ 所指结点，若已知 $p$ 的前驱为 $q$（$q$ 不空），则正确操作是？',
    options: [
      '$q \\to next = p \\to next$ 再释放 $p$',
      '$q \\to next = p$ 再释放 $p$',
      '$p \\to next = p \\to next \\to next$',
      '释放 $p$ 后再令 $q \\to next = p \\to next$',
    ],
    answer: [0],
    analysis: `## 解析
- 删除结点要把前驱的指针绕过 $p$ 指向 $p$ 的后继，即先 $q \\to next = p \\to next$，再释放 $p$。
- 顺序不能颠倒：先释放 $p$ 就再也取不到 $p \\to next$，指针链会断掉。
- 若只给 $p$ 而没给前驱，才需要「复制后继数据再删后继」这类技巧。
> 选项 B 让 $q$ 直接指向 $p$，等于把 $p$ 自己接到前驱后面，断链后还会造成自环；选项 C 只是把 $p$ 的后继接到 $p$ 上，删掉的并不是 $p$。`,
    difficulty: 2,
    tags: ['单链表', '删除结点', '指针操作'],
  },
  {
    id: 'q-ds-list-03',
    subject: 'ds',
    chapter: '线性表',
    type: 'single',
    stem: '设长度分别为 $m$、$n$ 的两个有序单链表均带头结点，将它们归并为一条有序链表，最少需要比较关键字的次数约为？',
    options: ['$\\max(m, n)$', '$m+n$', '$m \\times n$', '$\\min(m, n)$'],
    answer: [3],
    analysis: `## 解析
- 二路归并每次比较取走一个结点，当某个链表先被取空时，另一个链表剩余部分直接整体接上，不再比较。
- 最好情况下一个链表的全部结点都比另一个小，只需 $\\min(m, n)$ 次比较；最坏情况交错，需要 $m+n-1$ 次。
- 归并是顺序访问，时间与两表长度之和成正比，即 $O(m+n)$。
> $m+n$ 混淆了「最坏比较次数」与「最少比较次数」；$m \\times n$ 误把归并当成了二重循环，实际归并只需顺序扫一遍两个表。`,
    difficulty: 2,
    tags: ['归并', '有序表', '比较次数'],
  },
  {
    id: 'q-ds-list-04',
    subject: 'ds',
    chapter: '线性表',
    type: 'judge',
    stem: '顺序表可以随机访问，即用 $O(1)$ 时间按下标存取任一元素；链表不支持随机访问，但已知某结点指针时插入删除只需 $O(1)$。',
    options: ['正确', '错误'],
    answer: [0],
    analysis: `## 解析
- 顺序表用连续空间加「首地址 + 下标 $\\times$ 元素大小」定位，因此按下标访问是 $O(1)$，是典型的随机存取结构。
- 链表结点地址不连续，找第 $k$ 个结点必须从头依次走，是 $O(n)$ 的顺序存取。
- 但链表插入删除只需改动常数个指针：已给前驱指针时插入为 $O(1)$，已给前驱指针时删除也为 $O(1)$。
> 容易掉进的坑是把「链表访问慢」直接推成「链表插入删除也慢」：若没给前驱指针，定位前驱确实要 $O(n)$，这一点要单独辨析。`,
    difficulty: 1,
    tags: ['顺序表', '链表', '随机访问'],
  },
  {
    id: 'q-ds-stack-01',
    subject: 'ds',
    chapter: '栈与队列',
    type: 'single',
    stem: '若元素入栈序列为 $1, 2, 3, 4$，则下列哪个序列**不可能**是出栈序列？',
    options: ['$4, 3, 2, 1$', '$1, 2, 3, 4$', '$3, 2, 4, 1$', '$4, 1, 2, 3$'],
    answer: [3],
    analysis: `## 解析
- 判断合法性可用「出栈序列中任一元素之后、比它小的元素必须递减」这一性质。
- $4, 1, 2, 3$ 中 $4$ 先出栈意味着 $1,2,3$ 已全部入栈，它们只能按 $3,2,1$ 的顺序出栈，不可能出现 $1,2,3$。
- 其余三项均可构造：$1,2,3,4$ 逐个进出；$3,2,4,1$ 是进 $1,2,3$ 出 $3,2$、进 $4$ 出 $4,1$；$4,3,2,1$ 全进再全出。
> 干扰项常把「合法序列」写成看着顺眼的 $4,1,2,3$，只要抓住「栈的后进先出使小元素相对次序必然逆序」即可排除。`,
    difficulty: 2,
    tags: ['栈', '出栈序列', '合法性判断'],
  },
  {
    id: 'q-ds-stack-02',
    subject: 'ds',
    chapter: '栈与队列',
    type: 'single',
    stem: '循环队列用数组 $A[0 \\ldots m-1]$ 存放，队头指针 $front$、队尾指针 $rear$ 均指向实际元素位置，采用「牺牲一个单元」区分队空队满，则队满条件是？',
    options: [
      '$(front+1) \\bmod m = rear$',
      '$front = rear$',
      '$rear = m-1$',
      '$(rear+1) \\bmod m = front$',
    ],
    answer: [3],
    analysis: `## 解析
- 牺牲一个单元后，队满时 $rear$ 沿环再走一格就追上 $front$，即 $(rear+1) \\bmod m = front$。
- $front = rear$ 用来表示队空，这正是必须牺牲一个单元的原因：否则队空队满条件无法区分。
- 队列中元素个数为 $(rear - front + m) \\bmod m$，队满时取值为 $m-1$。
> 选项 A 把 $front$ 与 $rear$ 的角色写反了，是典型的指针方向混淆；$rear = m-1$ 只是队尾到了数组末尾，与是否队满无关。`,
    difficulty: 2,
    tags: ['循环队列', '队满判断', '模运算'],
  },
  {
    id: 'q-ds-stack-03',
    subject: 'ds',
    chapter: '栈与队列',
    type: 'single',
    stem: '中缀表达式 $(A+B) \\times C - D / E$ 对应的后缀表达式是？',
    options: [
      '$A\\,B\\,C + \\times D\\,E / -$',
      '$A\\,B + C \\times D\\,E - /$',
      '$A\\,B + C \\times D\\,E / -$',
      '$A\\,B + C\\,D\\,E / \\times -$',
    ],
    answer: [2],
    analysis: `## 解析
- 按运算优先级与括号：先算 $(A+B)$ 得 $A\\,B+$，再与 $C$ 相乘得 $A\\,B+C\\times$，同时 $D/E$ 得 $D\\,E/$。
- 最后做减法，于是后缀式为 $A\\,B+C\\times D\\,E/-$。
- 后缀表达式不需要括号，操作符出现的位置就决定了结合顺序，正好适合用栈求值。
> 选项 B 的 $A\\,B+C\\times D\\,E-$ 把减号提前，等于先算完乘除再做加法，运算次序被改写；选项 A 的 $A\\,B\\,C+\\times$ 表示 $A+(B\\times C)$，括号位置被改掉。`,
    difficulty: 1,
    tags: ['栈', '后缀表达式', '中缀转后缀'],
  },
  {
    id: 'q-ds-stack-04',
    subject: 'ds',
    chapter: '栈与队列',
    type: 'single',
    stem: '设循环队列容量为 $m$（数组下标 $0$ 到 $m-1$），采用牺牲一个单元的方式判满，则队列最多能存放多少个元素？',
    options: ['$m$', '$m-1$', '$m+1$', '$m/2$'],
    answer: [1],
    analysis: `## 解析
- 队空与队满都表现为 $front = rear$，若允许存满 $m$ 个元素，这两种状态就无法区分。
- 因此必须空出一个单元，实际可用容量为 $m-1$，此时元素个数为 $(rear-front+m) \\bmod m$，其最大值正是 $m-1$。
- 若不愿浪费这个单元，就必须另设计数器或标志位，那属于另一种实现方案。
> 写成 $m$ 是最常见的错误记忆，它对应的是「加计数器」或「用队头队尾加长度」的实现；$m+1$ 则连数组本身都装不下。`,
    difficulty: 2,
    tags: ['循环队列', '容量', '判空判满'],
  },
  {
    id: 'q-ds-string-01',
    subject: 'ds',
    chapter: '串',
    type: 'single',
    stem: '串 $S$ 的长度为 $n$，则 $S$ 的非空子串个数为？',
    options: [
      '$n$',
      '$n(n-1)/2$',
      '$2^{n}-1$',
      '$n(n+1)/2$',
    ],
    answer: [3],
    analysis: `## 解析
- 子串由起始位置和长度共同确定：长度 $k$ 的子串有 $n-k+1$ 个，$k$ 从 $1$ 到 $n$。
- 求和得 $\\sum_{k=1}^{n}(n-k+1) = n(n+1)/2$。
- 注意子串必须连续，位置不同的相同内容也算不同子串，这里统计的是「位置子串」。
> $n(n-1)/2$ 是常见差一错误，它漏掉了 $n$ 个长度为 $1$ 的字符；$2^{n}-1$ 统计的是非空**子序列**，把不连续的取法也算了进来。`,
    difficulty: 1,
    tags: ['串', '子串', '计数'],
  },
  {
    id: 'q-ds-string-02',
    subject: 'ds',
    chapter: '串',
    type: 'single',
    stem: '模式串 $P = $ $\`abaabc\`$，采用下标从 $1$ 开始、$next[1]=0$ 的定义，则 $next[5]$ 的值为？',
    options: ['$0$', '$1$', '$2$', '$3$'],
    answer: [2],
    analysis: `## 解析
- $next[j]$ 是 $P[1 \\ldots j-1]$ 的最长相等真前后缀长度再加 $1$。
- 取 $j=5$，考察子串 $\`abaa\`$：最长相等的真前缀与真后缀都是 $\`a\`$，长度为 $1$。
- 故 $next[5] = 1+1 = 2$。整串依次为 $next[1]=0$，$next[2]=1$，$next[3]=1$，$next[4]=2$，$next[5]=2$，$next[6]=3$。
> $3$ 是把 $j=6$ 的结果挪到了 $j=5$，属于典型的差一错误；$0$ 只在 $j=1$ 时取到，若改用 $next[1]=-1$ 的定义则所有值整体左移一格，做题必须先看清定义与下标起点。`,
    difficulty: 3,
    tags: ['KMP', 'next数组', '串匹配'],
  },
  {
    id: 'q-ds-string-03',
    subject: 'ds',
    chapter: '串',
    type: 'judge',
    stem: 'KMP 算法中模式串的 $next$ 数组只取决于模式串自身的结构，与主串内容无关。',
    options: ['正确', '错误'],
    answer: [0],
    analysis: `## 解析
- $next$ 数组刻画的是模式串每个位置之前的「最长相等前后缀」，只用到模式串 $P$ 的字符。
- 因此无论主串是什么，同一个模式串的 $next$ 都唯一确定，可以事先算好复用。
- KMP 的优势在于主串指针不回溯，匹配失败时模式串按 $next$ 右移，从而把时间降到 $O(n+m)$。
> 干扰说法是「$next$ 依赖主串」：混入了朴素匹配的思想——朴素匹配才需要每次失败后回退主串指针重新比较。`,
    difficulty: 2,
    tags: ['KMP', 'next数组', '时间复杂度'],
  },
  {
    id: 'q-ds-tree-01',
    subject: 'ds',
    chapter: '树与二叉树',
    type: 'single',
    stem: '一棵完全二叉树共有 $1000$ 个结点，则它的高度（根结点在第 $1$ 层）为？',
    options: ['$9$', '$10$', '$500$', '$11$'],
    answer: [1],
    analysis: `## 解析
- 高度为 $h$ 的完全二叉树结点数满足 $2^{h-1} \\le n \\le 2^{h}-1$，据此可以框定高度范围。
- 由 $2^{9}=512 \\le 1000$ 且 $2^{10}-1 = 1023 \\ge 1000$，两个不等式把 $h$ 夹在 $10$ 上，故高度为 $10$。
- 也可用公式 $h = \\lfloor \\log_2 n \\rfloor + 1$，代入得 $\\lfloor 9.97 \\rfloor + 1 = 10$，与上面的夹逼一致。
- 换个数体会规律：$n=1023$ 时高度仍为 $10$，第一层到第十层正好排满；再加一个结点才升到 $11$。
> 选项 A 的 $9$ 是把「含 $1000$ 个结点」误当成 $n \\le 2^{9}-1 = 511$ 的情形；选项 D 的 $11$ 用了上取整 $\\lceil \\log_2 n \\rceil + 1$，忽略了上一层未填满并不算新高度。`,
    difficulty: 2,
    tags: ['完全二叉树', '高度', '结点数关系'],
  },
  {
    id: 'q-ds-tree-02',
    subject: 'ds',
    chapter: '树与二叉树',
    type: 'single',
    stem: '已知一棵二叉树的先序遍历为 $ABDECF$、中序遍历为 $DBEAFC$，则其后序遍历为？',
    options: ['$DEBCFA$', '$DEBFCA$', '$EDBFCA$', '$DBEFCA$'],
    answer: [1],
    analysis: `## 解析
- 先序首元素 $A$ 是根，在中序中 $A$ 把序列分为左子树 $DBE$ 与右子树 $FC$。
- 左子树先序为 $BDE$、中序为 $DBE$，得 $B$ 为根且 $D$ 在左、$E$ 在右，后序为 $DEB$。
- 右子树先序 $CF$、中序 $FC$，得 $C$ 为根且 $F$ 为左孩子，后序为 $FC$，合起来是 $DEBFCA$。
> $DEBCFA$ 把右子树的中序左右写反了，属于「子树结构没拆干净」；$EDBFCA$ 则把左子树内部顺序颠倒。`,
    difficulty: 2,
    tags: ['二叉树遍历', '先序中序还原', '后序序列'],
  },
  {
    id: 'q-ds-tree-03',
    subject: 'ds',
    chapter: '树与二叉树',
    type: 'single',
    stem: '以权值集合 $\\{2, 3, 5, 7, 11, 13\\}$ 构造哈夫曼树，其带权路径长度 $WPL$ 为？',
    options: ['$41$', '$97$', '$94$', '$113$'],
    answer: [1],
    analysis: `## 解析
- 每次取两个最小权值合并：$2$ 与 $3$ 合并为 $5$，再与 $5$ 合并为 $10$。
- 接着 $7$ 与 $10$ 合并为 $17$，$11$ 与 $13$ 合并为 $24$，最后 $17$ 与 $24$ 合并为 $41$。
- $WPL$ 等于每次合并产生的新结点权值之和：$5+10+17+24+41 = 97$，恰好等于各叶权乘层数之和。
> $41$ 是最后一次合并的根权值，即全部叶权之和，最容易误当成 $WPL$；$113$ 通常是把合并顺序打乱（先取非最小的两个）后累加的错误结果。`,
    difficulty: 3,
    tags: ['哈夫曼树', 'WPL', '贪心构造'],
  },
  {
    id: 'q-ds-tree-04',
    subject: 'ds',
    chapter: '树与二叉树',
    type: 'single',
    stem: '一棵有 $n$ 个结点的二叉树采用二叉链表存储（每个结点含左右两个指针域），则空指针（空链域）的个数为？',
    options: ['$n-1$', '$n$', '$n+1$', '$2n$'],
    answer: [2],
    analysis: `## 解析
- 二叉链表每个结点有 $2$ 个指针域，$n$ 个结点共 $2n$ 个指针域。
- 除根结点外，每个结点都被恰好一个指针指向，所以非空指针共有 $n-1$ 个。
- 于是空指针个数为 $2n-(n-1) = n+1$，这一结论与树的具体形状无关，也常用来解释线索二叉树「利用空链域存放前驱后继」的做法。
> 选项 B 把非空指针当成了 $n$ 个，等于凭空多算一条从外部指入根结点的指针；$n-1$ 是边的条数（非空指针数），与空指针正好互补。`,
    difficulty: 2,
    tags: ['二叉链表', '空指针', '性质'],
  },
  {
    id: 'q-ds-tree-05',
    subject: 'ds',
    chapter: '树与二叉树',
    type: 'single',
    stem: '关于平衡二叉树（$AVL$ 树），下列说法正确的是？',
    options: [
      '$AVL$ 树中任一结点的平衡因子绝对值不超过 $1$',
      '$AVL$ 树的查找、插入、删除平均时间均为 $O(1)$',
      '$AVL$ 树的中序遍历结果不一定有序',
      '插入一个结点后平衡因子绝对值最多达到 $3$',
    ],
    answer: [0],
    analysis: `## 解析
- $AVL$ 树的定义就是任一结点的左、右子树高度差（平衡因子）绝对值不超过 $1$，由此保证树高为 $O(\\log n)$。
- 查找、插入、删除都沿根到叶的路径进行，代价为 $O(\\log n)$，插入后通过单旋或双旋恢复平衡。
- 作为二叉排序树，$AVL$ 树的中序遍历必然得到递增有序序列。
> 选项 B 把 $O(\\log n)$ 记成了 $O(1)$；选项 C 混淆了「平衡性」与「有序性」，任何二叉排序树的中序序列都有序；选项 D 说的平衡因子达到 $3$ 只是插入后旋转前的中间状态，不是 $AVL$ 树允许的形态。`,
    difficulty: 2,
    tags: ['平衡二叉树', '平衡因子', '旋转'],
  },
  {
    id: 'q-ds-graph-01',
    subject: 'ds',
    chapter: '图',
    type: 'single',
    stem: '具有 $n$ 个顶点的无向连通图，其边数最少为多少？',
    options: ['$n-1$', '$n$', '$n+1$', '$\\lceil n/2 \\rceil$'],
    answer: [0],
    analysis: `## 解析
- 无向连通图的极小连通情形就是树：$n$ 个顶点恰好 $n-1$ 条边即可把全部顶点连成一片。
- 边数少于 $n-1$ 时图必然不连通，这是连通性与边数的基本下界。
- 反之，$n$ 个顶点的无向图若边数不少于 $n-1$ 也未必连通，还可能有环与孤立块。
> $n$ 是把「无向连通」记成了「每个顶点至少一条边」；$\\lceil n/2 \\rceil$ 更像是把图误当成路径或匹配结构。`,
    difficulty: 1,
    tags: ['连通性', '边数下界', '树'],
  },
  {
    id: 'q-ds-graph-02',
    subject: 'ds',
    chapter: '图',
    type: 'single',
    stem: '对有向无环图进行拓扑排序，下列叙述正确的是？',
    options: [
      '拓扑序列唯一当且仅当图中每个顶点的入度都小于等于 $1$',
      '同一个图的拓扑序列一定不唯一',
      '拓扑排序一定需要借助队列才能实现',
      '拓扑序列唯一当且仅当图中存在一条从首顶点到末顶点的有向路径',
    ],
    answer: [3],
    analysis: `## 解析
- 拓扑排序每一步可以选择任意一个入度为 $0$ 的顶点，因此只有在始终只有一个入度为 $0$ 的顶点时才唯一。
- 「每步唯一」等价于图中存在一条贯穿所有顶点的有向路径（哈密顿路径），此时排序结果被完全确定。
- 实现上既可用队列也可用栈，用栈得到的是另一种合法拓扑序，两者都正确。
> 选项 A 的「入度都小于等于 $1$」既不充分也不必要：链 $1 \\to 2 \\to 3$ 的拓扑序唯一，而加上 $1 \\to 3$ 后某些顶点入度变为 $2$ 却仍唯一；选项 B 错在「一定」二字，入度为 $0$ 的顶点多于一个时序列就不唯一。`,
    difficulty: 3,
    tags: ['拓扑排序', '唯一性', 'AOV网'],
  },
  {
    id: 'q-ds-graph-03',
    subject: 'ds',
    chapter: '图',
    type: 'single',
    stem: '一个具有 $n$ 个顶点、$e$ 条边的有向图，采用邻接表存储时需要的存储空间为？',
    options: [
      '$O(n+e)$',
      '$O(e)$',
      '$O(n)$',
      '$O(n^{2})$',
    ],
    answer: [0],
    analysis: `## 解析
- 邻接表需要 $n$ 个顶点表结点（每个存一个表头指针）加 $e$ 个边表结点，总共 $O(n+e)$。
- 稀疏图（$e$ 远小于 $n^{2}$）时邻接表明显优于邻接矩阵的 $O(n^{2})$。
- 邻接表还能方便地求出某顶点的所有邻接点，但求有向图某顶点的入度需要遍历整表。
> 只写 $O(e)$ 漏掉了头结点开销；$O(n^{2})$ 是邻接矩阵的代价，别把两种存储结构记混。`,
    difficulty: 1,
    tags: ['邻接表', '空间复杂度', '图的存储'],
  },
  {
    id: 'q-ds-graph-04',
    subject: 'ds',
    chapter: '图',
    type: 'judge',
    stem: '若某无向连通图中所有边的权值互不相同，则该图的最小生成树唯一。',
    options: ['正确', '错误'],
    answer: [0],
    analysis: `## 解析
- 权值互不相同时，每一步选最小可行边不会出现并列选择，$Prim$ 与 $Kruskal$ 得到的结果一致。
- 可用反证：若存在两棵不同的最小生成树，交换它们不同的边时会找到一个环，环上必有权值相同的极大或极小边，与权值互异矛盾。
- 注意权值有相同边时最小生成树仍可能唯一，所以「互不相同」只是充分条件而非必要条件。
> 容易混淆的是「$MST$ 的权值之和总是唯一」与「$MST$ 的边集唯一」：前者对任何连通图都成立，后者才需要额外条件。`,
    difficulty: 2,
    tags: ['最小生成树', '唯一性', '权值'],
  },
  {
    id: 'q-ds-graph-05',
    subject: 'ds',
    chapter: '图',
    type: 'single',
    stem: '使用 $Dijkstra$ 算法求某源点到其余各顶点的最短路径，下列叙述正确的是？',
    options: [
      '可以正确处理含负权边的图，只要没有负权回路',
      '要求所有边权非负，且按路径长度递增的顺序逐个确定顶点',
      '算法每轮从未确定集合中选一个到源点距离最大的顶点加入已确定集合',
      '时间复杂度与图的存储结构无关，恒为 $O(n^{2})$',
    ],
    answer: [1],
    analysis: `## 解析
- $Dijkstra$ 的贪心前提是边权非负：一旦某顶点被确定，其距离不会再被更短的路径改写。
- 算法每轮从尚未确定的顶点中选当前距离**最小**者加入已确定集合，并用它松弛邻接点。
- 用邻接矩阵实现是 $O(n^{2})$，用邻接表加堆优化可降为 $O(e \\log n)$，所以复杂度与存储和实现有关。
> 选项 A 是经典误区：含负权边应改用 $Floyd$ 或 $Bellman$-$Ford$；选项 C 把「最小」写成「最大」，正好与贪心策略相反；选项 D 忽略了堆优化。`,
    difficulty: 2,
    tags: ['Dijkstra', '最短路径', '贪心'],
  },
  {
    id: 'q-ds-search-01',
    subject: 'ds',
    chapter: '查找',
    type: 'single',
    stem: '对长度为 $11$ 的有序表进行折半查找，查找失败时最多需要比较多少次关键字？',
    options: ['$3$', '$4$', '$5$', '$6$'],
    answer: [1],
    analysis: `## 解析
- 折半查找的判定树是一棵平衡的二叉排序树，查找失败时的比较次数等于从根走到失败结点（外部结点）所经过的内部结点层数。
- 长度 $11$ 时判定树的前三层恰好容纳 $1+2+4 = 7$ 个结点，剩余 $4$ 个排在第 $4$ 层，故最多比较 $4$ 次。
- 一般地，$n$ 个元素的折半查找成功时最多比较 $\\lfloor \\log_2 n \\rfloor + 1$ 次，失败时最多比较 $\\lceil \\log_2(n+1) \\rceil$ 次，代入均为 $4$。
> 选项 A 的 $3$ 是按「三层判定树最多 $7$ 个元素」直接得出的，忽略了 $n=11$ 需要第四层；$5$、$6$ 都是把失败结点所在层数多算了一层。`,
    difficulty: 2,
    tags: ['折半查找', '比较次数', '判定树'],
  },
  {
    id: 'q-ds-search-02',
    subject: 'ds',
    chapter: '查找',
    type: 'single',
    stem: '在长度为 $n$ 的顺序表上进行顺序查找，若每个元素的查找概率相等，则查找失败时的平均比较次数为？',
    options: ['$n/2$', '$(n+1)/2$', '$n$', '$n+1$'],
    answer: [2],
    analysis: `## 解析
- 顺序查找失败意味着与表中全部 $n$ 个元素都比较过且都不等，还要额外比较一次越界判断的话另算。
- 按只统计与元素的关键字比较计算，失败时比较次数恒为 $n$。
- 对照来看，等概率下成功查找的平均比较次数才是 $(n+1)/2$。
> 把 $(n+1)/2$ 填到失败情形是最常见的混淆，它其实是成功查找的平均值；$n+1$ 则多算了哨兵或越界那一次。`,
    difficulty: 1,
    tags: ['顺序查找', '平均查找长度', '失败情形'],
  },
  {
    id: 'q-ds-search-03',
    subject: 'ds',
    chapter: '查找',
    type: 'single',
    stem: '一棵 $5$ 阶 $B$ 树，除根结点外的每个非叶结点至少含有的关键字个数为？',
    options: ['$1$', '$2$', '$3$', '$4$'],
    answer: [1],
    analysis: `## 解析
- $m$ 阶 $B$ 树除根外的非叶结点至少有 $\\lceil m/2 \\rceil$ 棵子树，从而关键字个数至少为 $\\lceil m/2 \\rceil - 1$。
- 代入 $m=5$ 得 $\\lceil 2.5 \\rceil - 1 = 3-1 = 2$。
- 关键字个数上限为 $m-1 = 4$，故非根非叶结点的关键字个数在 $2$ 到 $4$ 之间；根结点至少有 $1$ 个关键字。
> 写成 $3$ 是把「子树棵数下限」当成了「关键字个数下限」，漏掉了减 $1$；写成 $1$ 是错把根结点的宽松条件套到了所有结点上。`,
    difficulty: 2,
    tags: ['B树', '关键字个数', '阶'],
  },
  {
    id: 'q-ds-search-04',
    subject: 'ds',
    chapter: '查找',
    type: 'judge',
    stem: '在装填因子一定时，开放定址法处理冲突的散列表的查找效率只依赖于装填因子，而与表中已有的具体关键字无关。',
    options: ['正确', '错误'],
    answer: [0],
    analysis: `## 解析
- 开放定址法的探测序列只由散列函数与探测方式决定，平均查找长度是装填因子 $\\alpha$ 的函数。
- 装填因子 $\\alpha$ 定义为表中记录数与表长之比，$\\alpha$ 越大冲突越多，平均查找长度越长。
- 因此散列表的效率不随 $n$ 单独变化，这是它与折半查找（$O(\\log n)$）在分析口径上的重要差别。
> 需要辨析的是「无关」的范围：同一次查找的具体位置仍与关键字有关，但**平均**查找长度在 $\\alpha$ 固定时基本稳定，这正是散列分析的核心结论。`,
    difficulty: 2,
    tags: ['散列表', '装填因子', '开放定址'],
  },
  {
    id: 'q-ds-sort-01',
    subject: 'ds',
    chapter: '排序',
    type: 'single',
    stem: '对 $n$ 个元素进行快速排序，最坏情况下的时间复杂度为？',
    options: ['$O(n)$', '$O(n\\log n)$', '$O(\\log n)$', '$O(n^{2})$'],
    answer: [3],
    analysis: `## 解析
- 快排每趟以枢轴划分，若枢轴总是当前区间的最小或最大元素，划分就退化成一边空、一边 $n-1$ 个。
- 此时递归深度为 $n$，总比较次数约 $n+(n-1)+\\cdots+1$，即 $O(n^{2})$。原表基本有序且固定取首元素为枢轴时最容易触发。
- 平均情况与最好情况下划分较均衡，时间复杂度为 $O(n\\log n)$。
> 选项 B 是平均情况而非最坏情况，题目问「最坏」时不要条件反射地选它；选项 C 的 $O(\\log n)$ 连把数据读一遍都不够，明显偏低。`,
    difficulty: 1,
    tags: ['快速排序', '最坏时间复杂度', '枢轴'],
  },
  {
    id: 'q-ds-sort-02',
    subject: 'ds',
    chapter: '排序',
    type: 'single',
    stem: '下列排序算法中，哪一个是稳定的？',
    options: ['直接插入排序', '堆排序', '快速排序', '希尔排序'],
    answer: [0],
    analysis: `## 解析
- 稳定性指关键字相同的记录排序前后相对次序不变，直接插入排序只把较大元素后移，不会跨越相等元素，因此稳定。
- 希尔排序按增量跳跃分组，可能把相等元素分到不同组，不稳定。
- 快排的划分交换会跨越枢轴，堆排序的筛选会上浮下沉，两者都会打乱相等元素的相对次序。
> 记忆要点：不稳定的典型有「快、希、选、堆」，稳定的有「插、冒、归、基」；选项 B、C、D 恰好把不稳定阵营的代表都凑齐了，属于本考点最常见的干扰组合。`,
    difficulty: 1,
    tags: ['排序稳定性', '插入排序', '算法比较'],
  },
  {
    id: 'q-ds-sort-03',
    subject: 'ds',
    chapter: '排序',
    type: 'single',
    stem: '将序列 $\\{49, 38, 65, 97, 76, 13, 27\\}$ 建成大根堆，初始建堆完成后堆顶元素为？',
    options: ['$13$', '$97$', '$76$', '$65$'],
    answer: [1],
    analysis: `## 解析
- 大根堆要求每个结点都不小于其孩子，建堆后堆顶必为整个序列的最大值。
- 序列中最大元素是 $97$，因此建堆完成后堆顶一定是 $97$。
- 自底向上从最后一个非叶结点（下标 $\\lfloor n/2 \\rfloor = 3$）开始依次筛选即可完成建堆，时间 $O(n)$。
> $13$ 是最小元素，那是小根堆的堆顶；$76$ 与 $65$ 都是序列中的中间值，把它们当堆顶说明把「堆顶是极值」这一性质记成了「堆顶是某个较大值」。`,
    difficulty: 2,
    tags: ['堆排序', '建堆', '大根堆'],
  },
  {
    id: 'q-ds-sort-04',
    subject: 'ds',
    chapter: '排序',
    type: 'single',
    stem: '对序列 $\\{49, 38, 65, 97, 76, 13, 27\\}$ 按升序排序，一趟结束后序列变为 $\\{27, 38, 13, 49, 76, 97, 65\\}$，则采用的排序方法是？',
    options: ['冒泡排序', '直接插入排序', '快速排序', '简单选择排序'],
    answer: [2],
    analysis: `## 解析
- 观察结果：$49$ 落在第 $4$ 个位置，其左侧 $27, 38, 13$ 全部小于 $49$，右侧 $76, 97, 65$ 全部大于 $49$，这是快排一趟划分后枢轴归位的标志。
- 冒泡排序一趟只会把最大元素 $97$ 送到序列末尾，其余元素相对次序基本不动。
- 简单选择排序一趟只把最小元素 $13$ 换到最前面；直接插入排序一趟仅使前若干元素局部有序，不会出现整表按枢轴分成两半的局面。
> 关键特征是「枢轴归位、左小右大」；若看到的是尾部变成最大值，那才是冒泡或堆排序的痕迹，别只看元素个数就下结论。`,
    difficulty: 3,
    tags: ['排序方法识别', '快速排序', '一趟划分'],
  },
  {
    id: 'q-ds-sort-05',
    subject: 'ds',
    chapter: '排序',
    type: 'single',
    stem: '当待排序序列已经基本有序时，下列说法正确的是？',
    options: [
      '快速排序仍然一定比直接插入排序快',
      '直接插入排序此时接近最好情况，快速排序则可能退化为 $O(n^{2})$',
      '直接插入排序此时一定退化为 $O(n^{2})$',
      '两种算法此时的时间复杂度必定相同',
    ],
    answer: [1],
    analysis: `## 解析
- 直接插入排序对基本有序的序列移动次数很少，最好情况只需 $n-1$ 次比较，时间降为 $O(n)$。
- 快速排序若固定取首元素为枢轴，基本有序时划分极不均衡，递归深度接近 $n$，退化为 $O(n^{2})$。
- 这正是实际实现中采用「三者取中」或随机化枢轴的原因，用以避开有序输入这一最坏情形。
> 选项 A 用平均复杂度掩盖了最坏情况，是常见误区；选项 C 把退化的帽子扣到了插入排序头上，恰恰相反。`,
    difficulty: 2,
    tags: ['快速排序', '插入排序', '最好最坏情况'],
  },
];
