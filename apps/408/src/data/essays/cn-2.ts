import type { SeedEssay } from '../essay-types';

/**
 * 计算机网络 · 综合应用题（第二批，6 道）。
 * 覆盖：体系结构（时延与交换方式）、数据链路层（CRC 与最小帧长、GBN 与 SR）、
 * 网络层（RIP 距离向量路由表更新）、传输层（TCP 序号确认与接收窗口）、
 * 应用层（DNS 递归查询与 HTTP 连接方式）。
 */
export const CN_ESSAYS2: SeedEssay[] = [
  {
    id: 'e-cn2-arch-01',
    subject: 'cn',
    chapter: '体系结构',
    difficulty: 2,
    minutes: 12,
    tags: ['时延', '分组交换', '报文交换', '电路交换', '存储转发'],
    stem: `一条从主机 A 到主机 B 的路径由 3 段链路和 2 台中间路由器串接而成，每段链路的长度均为 200 km，数据传输速率均为 10 Mb/s，信号在链路上的传播速率均为 $2\\times10^8$ m/s。中间路由器采用存储转发方式工作，节点的处理时延与排队时延均忽略不计。

A 要发送的数据总长为 8000 bit。采用分组交换时，把这 8000 bit 等分成 4 个分组，每个分组长 2000 bit；采用报文交换时，把 8000 bit 作为一个完整报文发送。

1. 求一个 2000 bit 的分组在一段链路上的发送时延，以及一段链路的传播时延。
2. 采用分组交换，求从 A 开始发送第 1 个分组到 B 收完第 4 个分组的最后一个比特所需的时间。
3. 采用报文交换，把整个 8000 bit 报文作为一个单位逐段存储转发，求所需时间，并与第 2 问的结果比较，说明分组交换为什么更快。
4. 若改用电路交换，建立连接需要 50 ms，连接建立后数据在通路上连续传送（中间节点不再存储转发），求从开始建立连接到 B 收完全部数据所需的时间。`,
    points: [
      {
        label: '算出一个分组的发送时延与一段链路的传播时延',
        score: 2,
        answer: `一个 2000 bit 的分组在一段链路上的发送时延 $t_s=\\dfrac{2000}{10\\times10^6}=2\\times10^{-4}$ s $=0.2$ ms；一段链路的传播时延 $t_p=\\dfrac{200\\times10^3}{2\\times10^8}=1\\times10^{-3}$ s $=1$ ms。`,
      },
      {
        label: '算出分组交换所需的总时间',
        score: 3,
        answer: `最后一个分组要等前 3 个分组发完才能出发，等待 $(4-1)\\times0.2=0.6$ ms；它再过 3 段链路需 $3\\times0.2+3\\times1=3.6$ ms；总时间 $0.6+3.6=4.2$ ms。也可用公式 $(K+N-1)t_s+Kt_p$，$K=3$、$N=4$，得 $(3+4-1)\\times0.2+3\\times1=1.2+3=4.2$ ms。`,
      },
      {
        label: '算出报文交换所需时间并与分组交换比较',
        score: 2,
        answer: `整个报文的发送时延 $t_s'=\\dfrac{8000}{10\\times10^6}=0.8$ ms，报文必须整块到达路由器才能转发，无流水线效果，总时间 $3\\times0.8+3\\times1=2.4+3=5.4$ ms，比分组交换多 $5.4-4.2=1.2$ ms。原因是分组后不同分组可在不同链路上并行（流水线）传输，而报文交换必须整块逐段转发。`,
      },
      {
        label: '算出电路交换所需时间并指出其开销所在',
        score: 3,
        answer: `通路建立后数据连续传送，发送时延只算一次：$50+t_s'+3t_p=50+0.8+3=53.8$ ms。其中建立连接的 50 ms 占绝大部分，说明对短数据而言电路交换的建立开销过大。`,
      },
    ],
    solution: `## 参考解答

**第 1 问：发送时延与传播时延**

一个 2000 bit 分组在一段链路上的发送时延为

$t_s=\\dfrac{2000\\ \\text{bit}}{10\\times10^6\\ \\text{bit/s}}=2\\times10^{-4}\\ \\text{s}=0.2$ ms。

一段链路的传播时延为

$t_p=\\dfrac{200\\times10^3\\ \\text{m}}{2\\times10^8\\ \\text{m/s}}=1\\times10^{-3}\\ \\text{s}=1$ ms。

**第 2 问：分组交换所需时间**

路径上有 3 段链路、2 台路由器，共 4 个分组。最后一个分组不能提前出发，它必须等前 3 个分组全部发完才开始发送，因此它在 A 处的等待时间为

$(4-1)\\times t_s=3\\times0.2=0.6$ ms。

此后它还要依次经过 3 段链路，每段都要经历一次发送时延和一次传播时延：

$3t_s+3t_p=3\\times0.2+3\\times1=0.6+3=3.6$ ms。

所以从开始发送到全部收完共需

$0.6+3.6=4.2$ ms。

写成通用公式即 $(K+N-1)t_s+Kt_p$，其中 $K=3$ 是链路数、$N=4$ 是分组数，代入得 $(3+4-1)\\times0.2+3\\times1=1.2+3=4.2$ ms。

**第 3 问：报文交换**

整个报文长 8000 bit，在一段链路上的发送时延为

$t_s'=\\dfrac{8000\\ \\text{bit}}{10\\times10^6\\ \\text{bit/s}}=8\\times10^{-4}\\ \\text{s}=0.8$ ms。

报文必须完整到达中间路由器后才能开始向下一段链路转发，所以整块报文逐段推进，总时间为

$3t_s'+3t_p=3\\times0.8+3\\times1=2.4+3=5.4$ ms。

比分组交换多 $5.4-4.2=1.2$ ms。原因在于分组交换把长报文切成多个小分组后，第 1 个分组在路由器 1 上转发的同时，第 2 个分组已经在链路 1 上传输，多段链路与多个分组像流水线一样并行工作，把总时间从「各段串行累加」压缩为「最后一段的收尾时间」；报文交换只能整块逐段转发，无法并行。

**第 4 问：电路交换**

电路交换要先建立连接，之后数据在专用通路上连续传送，中间节点不做存储转发，因此发送时延只发生一次：

$50+t_s'+3t_p=50+0.8+3=53.8$ ms。

其中 50 ms 是建立连接的开销，占了总时间的绝大部分。可见对本例这种短数据，电路交换因为建立连接的代价过大而最慢；但对长时间、大数据的通信，电路交换没有逐段存储转发的时延，反而更有优势。`,
    pitfalls: `> 传播时延必须每段链路各算一次，3 段共 3 ms，只算一段会漏掉 2 ms；分组交换的总时间不能写成 $4\\times(0.2+1)=4.8$ ms，因为第 2 个及以后的分组只需额外增加 0.2 ms 的发送时延，正确结果是 4.2 ms；电路交换在通路建立后不再逐段存储转发，0.8 ms 的发送时延只能算一次，写成 $3\\times0.8$ 会多算 1.6 ms。`,
  },
  {
    id: 'e-cn2-dll-01',
    subject: 'cn',
    chapter: '数据链路层',
    difficulty: 2,
    minutes: 12,
    tags: ['CRC', '生成多项式', '最小帧长', '争用期', 'CSMA/CD'],
    stem: `某网络中需要发送的数据比特序列为 $M=1101011011$，收发双方约定的生成多项式为 $G(x)=x^4+x+1$，采用 CRC 进行差错检测。

该网络的另一段采用 CSMA/CD 协议，数据传输速率为 100 Mb/s，总线两端之间的最远距离为 2000 m，信号在总线上的传播速率为 $2\\times10^8$ m/s，沿途中继器引入的单程时延合计为 2 μs。

1. 写出生成多项式 $G(x)$ 对应的除数比特串，并求出余数（CRC 校验码）。
2. 写出实际发送出去的帧的比特序列，它共有多少位。
3. 求信号在该总线上的单程传播时延 $\\tau$（含中继器时延）和争用期。
4. 求该以太网允许的最短帧长为多少比特、合多少字节。
5. 说明最短帧长为什么不能小于第 4 问算出的值，若发送的帧过短会产生什么后果。`,
    points: [
      {
        label: '写出除数比特串并求出 CRC 余数',
        score: 3,
        answer: `$G(x)=x^4+x+1$ 最高次为 4，除数为 $10011$（5 位），校验码取 4 位。在数据后补 4 个 0 得被除数 $11010110110000$，与 $10011$ 做模 2 除法，商为 $1100001010$，余数为 $1110$，即 CRC 校验码为 $1110$。`,
      },
      {
        label: '写出实际发送的帧并给出位数',
        score: 1,
        answer: `把校验码接在数据之后，发送的帧为 $1101011011$ 后接 $1110$，即 $11010110111110$，共 $10+4=14$ 位。`,
      },
      {
        label: '算出含中继器时延的单程传播时延与争用期',
        score: 2,
        answer: `仅考虑总线传播的时延为 $\\dfrac{2000}{2\\times10^8}=1\\times10^{-5}$ s $=10$ μs，再加上中继器的单程时延 $2$ μs 得 $\\tau=12$ μs；争用期（冲突窗口）为 $2\\tau=24$ μs。`,
      },
      {
        label: '算出最短帧长',
        score: 2,
        answer: `最短帧长 $=2\\tau\\times$ 数据速率 $=24\\times10^{-6}\\times100\\times10^6=2400$ bit，合 $2400\\div8=300$ 字节。`,
      },
      {
        label: '说明最短帧长的由来及帧过短的后果',
        score: 2,
        answer: `CSMA/CD 采用边发边听的方式，最坏情况下冲突信号要经过 $2\\tau$ 才能传回发送方，因此发送方必须保证在 $2\\tau$ 内帧还没有发完，才能检测到冲突并停止发送。若帧太短，帧已发送完毕并认为发送成功，即使发生了冲突也无法察觉，会造成数据丢失且不会重传，所以规定最短帧长不小于 $2\\tau$ 与数据速率的乘积。`,
      },
    ],
    solution: `## 参考解答

**第 1 问：除数与 CRC 余数**

$G(x)=x^4+x+1$ 的最高次为 4，因此除数（生成多项式对应的比特串）是 $10011$，共 5 位，CRC 校验码取 4 位。

在数据 $M=1101011011$ 后面补 4 个 0，得到被除数

$11010110110000$。

用它对 $10011$ 做模 2 除法：每一步看当前最高位，为 1 就与 $10011$ 按位异或（异或没有进位和借位），为 0 则商 0 并把下一位移下来。整个除法做完得到

商 $=1100001010$，余数 $=1110$。

余数只有 4 位，不足除数位数，除法结束，所以 CRC 校验码为 $1110$。

可以验算：模 2 除法的关系式是「被除数 = 商 × 除数 ⊕ 余数」。把商与除数做模 2 乘法得

$1100001010\\times10011=11010110111110$，

它正好就是后面第 2 问要发送的帧；再与余数异或又能还原出被除数

$11010110111110\\oplus1110=11010110110000$，

说明余数 $1110$ 正确。

**第 2 问：实际发送的帧**

发送帧由「数据 + 校验码」拼接而成：

$1101011011\\ +\\ 1110\\ =\\ 11010110111110$，

共 $10+4=14$ 位。接收方用同样的 $10011$ 去除收到的 14 位序列，若余数为 0 则认为传输无差错。

**第 3 问：单程传播时延与争用期**

总线两端最远距离 2000 m，纯传播时延为

$\\dfrac{2000\\ \\text{m}}{2\\times10^8\\ \\text{m/s}}=1\\times10^{-5}\\ \\text{s}=10$ μs。

沿途中继器还会引入单程 $2$ μs 的时延，故单程传播时延为

$\\tau=10+2=12$ μs，

争用期（冲突窗口）为

$2\\tau=2\\times12=24$ μs。

**第 4 问：最短帧长**

以太网规定最短帧长等于发送方在争用期内能够发送的比特数：

$2\\tau\\times R=24\\times10^{-6}\\times100\\times10^6=2400$ bit，

合 $2400\\div8=300$ 字节。也就是说该网络中最短的帧也要有 300 字节（实际以太网规定最短帧长 64 字节，对应的就是它自己的争用期 51.2 μs 与 10 Mb/s）。

**第 5 问：最短帧长的由来**

CSMA/CD 的站点边发送边检测冲突。设 A 在 $t=0$ 开始发送，最坏的情形是 B 恰好在信号快要到达它时（$t=\\tau$ 之前）也检测到信道空闲并开始发送，于是冲突发生，冲突信号要再经过 $\\tau$ 才能传回 A。因此从 A 开始发送算起，最迟要经过

$\\tau+\\tau=2\\tau=24$ μs

A 才能确定自己的帧没有遇到冲突。

如果帧太短，A 在 $2\\tau$ 之前就把整帧发送完毕并关闭发送电路、认为发送成功，那么后到的冲突信号就无法被检测到，A 不会重传，这一帧数据就白白丢失了。反之，只要帧长不小于 $2\\tau\\times R=2400$ bit，A 在整个争用期内都处于发送状态，必定能发现冲突并及时停止、退避重发。`,
    pitfalls: `> CRC 除法要用模 2 除法（按位异或），不是普通二进制除法，$11010110110000\\div10011$ 的余数是 $1110$ 而不是 $1001$ 之类；补 0 的个数等于除数的位数减 1（本例补 4 个 0），补错个数会得到完全不同的余数；最短帧长必须用争用期 $2\\tau$ 而不是 $\\tau$，且 $\\tau$ 要计入中继器时延，本例是 $2\\times12=24$ μs，算成 $2\\times10=20$ μs 会得到 2000 bit 的错误答案。`,
  },
  {
    id: 'e-cn2-dll-02',
    subject: 'cn',
    chapter: '数据链路层',
    difficulty: 2,
    minutes: 13,
    tags: ['GBN', '选择重传', '发送窗口', '信道利用率', '序号位数'],
    stem: `主机 A 与主机 B 之间有一条数据链路，数据传输速率 $R=4$ Mb/s，单向传播时延 $t_p=5$ ms，数据帧长度固定为 $L=4000$ bit，确认帧很短、其发送时延可忽略，节点的处理时延也忽略不计。数据链路层可以选用后退 N 帧（GBN）协议或选择重传（SR）协议，帧的序号字段为 $n$ 位，序号取 $0\\sim2^n-1$。

1. 求一个数据帧的发送时延 $T_f$ 和链路的往返时延 RTT。
2. 若采用 GBN，$n=4$ 时发送窗口最大可以取多少？现取发送窗口 $W_T=8$，求该链路的最高信道利用率。
3. 若采用 SR，$n=4$ 时发送窗口和接收窗口最大各为多少？
4. 第 3 问取 $W_T=8$ 时，链路的最高信道利用率是多少？
5. 若要求发送窗口 $W_T=20$，GBN 和 SR 分别至少需要多少位序号？`,
    points: [
      {
        label: '算出数据帧发送时延与往返时延',
        score: 2,
        answer: `$T_f=\\dfrac{4000}{4\\times10^6}=1\\times10^{-3}$ s $=1$ ms；$RTT=2t_p=2\\times5=10$ ms。`,
      },
      {
        label: '给出 GBN 的窗口上限并算出 W=8 时的信道利用率',
        score: 3,
        answer: `GBN 要求 $W_T\\le2^n-1$，$n=4$ 时 $W_T\\le2^4-1=15$，故发送窗口最大取 15。取 $W_T=8$ 时，$WT_f=8\\times1=8$ ms $<T_f+RTT=1+10=11$ ms，信道不会一直空闲，利用率 $U=\\dfrac{WT_f}{T_f+RTT}=\\dfrac{8}{11}\\approx72.7\\%$。`,
      },
      {
        label: '给出 SR 的发送窗口与接收窗口上限',
        score: 2,
        answer: `选择重传协议要求发送窗口与接收窗口都不超过序号空间的一半，即 $W_T\\le2^{n-1}$、$W_R\\le2^{n-1}$。$n=4$ 时二者最大都取 $2^3=8$。若取 $W_T=W_R=8$，接收方可把乱序到达的帧全部缓存，不会出现新旧帧混淆。`,
      },
      {
        label: '算出 SR 在 W=8 时的信道利用率',
        score: 2,
        answer: `信道利用率只取决于发送窗口连续发送数据所占的时间，与接收窗口大小无关，故仍为 $U=\\dfrac{8\\times1}{1+10}=\\dfrac{8}{11}\\approx72.7\\%$，与 GBN 在同样窗口下相同。`,
      },
      {
        label: '算出 W=20 时两种协议所需的最少序号位数',
        score: 2,
        answer: `GBN 需 $2^n-1\\ge20$，即 $2^n\\ge21$，$n=4$ 时只有 15 不够，$n=5$ 时为 31，故至少 5 位；SR 需 $2^{n-1}\\ge20$，即 $2^n\\ge40$，$n=5$ 时只有 32 不够，$n=6$ 时为 64，故至少 6 位。`,
      },
    ],
    solution: `## 参考解答

**第 1 问：发送时延与往返时延**

一个数据帧的发送时延为

$T_f=\\dfrac{4000\\ \\text{bit}}{4\\times10^6\\ \\text{bit/s}}=1\\times10^{-3}\\ \\text{s}=1$ ms。

链路的往返时延为

$RTT=2t_p=2\\times5=10$ ms。

**第 2 问：GBN 的窗口上限与信道利用率**

后退 N 帧协议的接收窗口恒为 1，序号空间必须能区分「正在发送的新帧」和「可能重传的旧帧」，因此要求

$W_T\\le2^n-1$。

$n=4$ 时 $W_T\\le2^4-1=15$，所以发送窗口最大可以取 15。

取 $W_T=8$ 时，发送方连续发送 8 帧耗时 $WT_f=8\\times1=8$ ms，而一个「发送并收到确认」的周期为 $T_f+RTT=1+10=11$ ms。由于 $8<11$，发送方发完 8 帧后还要等 3 ms 才有新确认可用，信道并非满负荷，其最高利用率为

$U=\\dfrac{WT_f}{T_f+RTT}=\\dfrac{8\\times1}{1+10}=\\dfrac{8}{11}\\approx72.7\\%$。

**第 3 问：SR 的窗口上限**

选择重传协议允许接收方缓存乱序到达的帧，为避免接收窗口内的新帧与发送方重传的旧帧序号重叠，必须保证

$W_T+W_R\\le2^n$，工程上通常取 $W_T\\le2^{n-1}$、$W_R\\le2^{n-1}$。

$n=4$ 时 $2^{4-1}=8$，所以发送窗口和接收窗口最大都取 8。

**第 4 问：SR 在 W=8 时的信道利用率**

信道利用率衡量的是发送方占用信道的时间比例，只由发送窗口决定：窗口越大，连续发送时间越长，等待确认的空闲越少。$W_T=8$ 时仍有 $WT_f=8$ ms $<11$ ms，所以

$U=\\dfrac{8\\times1}{1+10}=\\dfrac{8}{11}\\approx72.7\\%$。

与 GBN 在同样窗口下的结果相同。若把窗口开到 $W_T\\ge11$，利用率才可能达到 $100\\%$。

**第 5 问：W=20 时所需的最少序号位数**

对 GBN：

$2^n-1\\ge20\\Rightarrow2^n\\ge21$。

$n=4$ 时 $2^4-1=15<20$，不够；$n=5$ 时 $2^5-1=31\\ge20$，够用，所以至少需要 **5 位**序号。

对 SR：

$2^{n-1}\\ge20\\Rightarrow2^n\\ge40$。

$n=5$ 时 $2^5=32<40$，不够；$n=6$ 时 $2^6=64\\ge40$，够用，所以至少需要 **6 位**序号。

可见在窗口相同的情况下，选择重传协议需要更多的序号位。`,
    pitfalls: `> GBN 的窗口上限是 $2^n-1$，SR 的是 $2^{n-1}$，两者不能混用：$n=4$、$W_T=20$ 时 GBN 要 5 位而 SR 要 6 位；信道利用率的分母是 $T_f+RTT$ 而不是 $T_f+t_p$，$RTT$ 一定要算两倍单程传播时延；窗口只有 8、而填满周期需要 11 帧，所以利用率是 $8/11$ 而不是 100%，只有 $WT_f\\ge T_f+RTT$ 时利用率才能取 1。`,
  },
  {
    id: 'e-cn2-net-01',
    subject: 'cn',
    chapter: '网络层',
    difficulty: 2,
    minutes: 11,
    tags: ['RIP', '距离向量', '路由表更新', '不可达', '下一跳'],
    stem: `某互联网中的路由器 R1、R2、R3、R4 都运行 RIP 协议，R1 与 R2 是相邻路由器，两者之间链路的代价为 1。R1 当前的路由表如下：

到网络 Net1 的距离为 3，下一跳为 R2；
到网络 Net2 的距离为 4，下一跳为 R3；
到网络 Net3 的距离为 5，下一跳为 R2；
到网络 Net4 的距离为 2，下一跳为 R4。

现在 R1 收到 R2 发来的路由通告，其中包含：Net1 距离 1，Net2 距离 1，Net3 距离 16，Net4 距离 3，Net5 距离 2。RIP 规定距离 16 表示不可达，网络的最大可用距离为 15。

1. 写出 R1 依据邻居通告更新自己路由表的判断规则（距离如何换算、下一跳如何填写、什么情况下更新）。
2. 分别写出 R1 对 Net1 和 Net2 的更新结果。
3. 写出 R1 对 Net3 的处理结果，并说明原因。
4. 写出 R1 对 Net4 的处理结果，并说明原因。
5. 写出更新后 R1 的完整路由表，并说明距离 16 和距离 15 各自代表什么。`,
    points: [
      {
        label: '写出 RIP 路由更新的判断规则',
        score: 2,
        answer: `R1 把 R2 通告中每个网络的距离加 1（因为 R1 到 R2 的链路代价为 1），下一跳一律记为 R2，得到候选路由；再与本地原表项比较：候选距离更小则替换，候选距离更大则不更新。若原表项的下一跳本来就是 R2，则无论距离变大还是变为不可达，都必须用新信息替换旧表项。`,
      },
      {
        label: '写出 Net1 与 Net2 的更新结果',
        score: 2,
        answer: `Net1：通告距离 1 加 1 得 2，小于原来的 3，更新为「距离 2，下一跳 R2」。Net2：通告距离 1 加 1 得 2，小于原来的 4，更新为「距离 2，下一跳 R2」（原下一跳为 R3，被替换）。`,
      },
      {
        label: '写出 Net3 的处理结果并说明原因',
        score: 3,
        answer: `R2 通告 Net3 距离 16，加 1 后仍为不可达。R1 原有 Net3 表项的下一跳恰好就是 R2，说明这条路由完全依赖 R2，R2 已经不通，R1 必须删除 Net3 表项（距离记为 16，即不可达）。此时不能保留原来「距离 5，下一跳 R2」的旧表项，否则会形成路由环路。`,
      },
      {
        label: '写出 Net4 的处理结果并说明原因',
        score: 2,
        answer: `R2 通告 Net4 距离 3，加 1 得 4，大于 R1 原有的距离 2，且原表项的下一跳是 R4 而不是 R2，所以不更新，保留「距离 2，下一跳 R4」这条更优的路由。RIP 的原则是只接受更短的路由。`,
      },
      {
        label: '写出完整路由表并说明 16 与 15 的含义',
        score: 2,
        answer: `更新后 R1 的路由表为：Net1 距离 2 下一跳 R2；Net2 距离 2 下一跳 R2；Net4 距离 2 下一跳 R4；Net5 距离 3 下一跳 R2（新增表项）；Net3 不可达，表项被删除。距离 16 表示不可达，是一条路由失效的标志；15 是 RIP 允许的最大有效距离，即一条路径最多经过 15 跳，超过 15 跳的目的网络被视为不可达，这样可以限制坏消息的传播时间、避免计数到无穷。`,
      },
    ],
    solution: `## 参考解答

**第 1 问：更新规则**

RIP 是距离向量协议，相邻路由器之间周期性地交换各自的路由表。R1 收到 R2 的通告后按下面三步处理每一个目的网络：

1. 把通告中的距离加 1，因为从 R1 出发要先经过 R2 这一跳，R1 到 R2 的链路代价为 1；
2. 下一跳统一记为 R2；
3. 与本地路由表比较：本地没有该网络就新增；本地有且候选距离更小就替换；候选距离更大就保持不变。特别地，如果原表项的下一跳正是 R2，则说明这条路由的信息来源就是 R2，收到的新信息无论好坏都必须采纳，包括通告距离为 16（不可达）时要删除表项。

**第 2 问：Net1 与 Net2**

- Net1：通告距离 1，加 1 得 2，原距离为 3，$2<3$，更新为「距离 2，下一跳 R2」；
- Net2：通告距离 1，加 1 得 2，原距离为 4，$2<4$，更新为「距离 2，下一跳 R2」，原来的下一跳 R3 被替换。

**第 3 问：Net3**

R2 通告 Net3 的距离为 16，表示 R2 认为 Net3 不可达。加 1 后距离仍不小于 16，仍是不可达。R1 原有的 Net3 表项是「距离 5，下一跳 R2」，它的下一跳正是发通告的 R2，说明 R1 获得这条路由的唯一途径就是 R2。既然 R2 报告不可达，R1 必须把 Net3 的表项删除（可记为距离 16）。

如果 R1 反而保留「距离 5，下一跳 R2」，一旦有分组发往 Net3，它会被送到 R2，而 R2 又认为经由 R1 可以到达（在更早的状态下），分组就会在 R1 与 R2 之间来回转发形成路由环路。这正是 RIP「坏消息传得慢」的根源。

**第 4 问：Net4**

R2 通告 Net4 的距离为 3，加 1 得 4，而 R1 原有的距离只有 2，$4>2$。同时原表项的下一跳是 R4，与 R2 无关，不存在「必须采纳」的问题。因此 R1 保持原表项不变，仍然是「距离 2，下一跳 R4」。RIP 只接受更短的路由，变长的候选路由一律忽略。

**第 5 问：更新后的路由表与距离 16、15 的含义**

综合以上各点，R1 更新后的路由表为：

- Net1：距离 2，下一跳 R2（由 3 变 2）；
- Net2：距离 2，下一跳 R2（由 4 变 2，下一跳由 R3 改为 R2）；
- Net4：距离 2，下一跳 R4（保持不变）；
- Net5：距离 3，下一跳 R2（原表没有，新增）；
- Net3：不可达，表项被删除。

其中距离 16 是 RIP 专门定义的「不可达」标志：当一条路由的距离达到 16 时即认为该网络不可达，路由器把这一信息传播出去，邻居收到后把距离加 1 仍视为不可达，从而逐步删除失效路由。距离 15 是 RIP 允许的最大有效距离，表示一条路径最多经过 15 跳，跳数超过 15 的目的网络被视为不可达。这一上限把「计数到无穷」的过程限制在有限步之内，使网络能够较快地收敛。`,
    pitfalls: `> 收到邻居通告后必须先把距离加 1 再比较，直接拿通告里的 1 去和本地的 3 比较会得出 Net1 距离为 1 的错误结果；Net3 的关键在于原表项下一跳就是 R2，R2 报告不可达时必须删除该表项，保留旧表项会造成路由环路；Net4 的候选距离 4 大于原有的 2，不能因为收到通告就盲目改为经 R2，距离变大的一律不更新；通告中出现的 Net5 是本地没有的网络，要新增表项而不是忽略。`,
  },
  {
    id: 'e-cn2-tcp-01',
    subject: 'cn',
    chapter: '传输层',
    difficulty: 2,
    minutes: 13,
    tags: ['TCP', '序号', '确认号', '接收窗口', '零窗口'],
    stem: `主机 A 与主机 B 之间已经建立了一条 TCP 连接。A 当前的待发序号（下一个要发送的字节序号）为 1001；B 的接收缓存总容量为 4000 字节，此时缓存为空，B 通告的接收窗口为 4000 字节。A 的拥塞窗口足够大，因此 A 的发送窗口完全由接收窗口决定。A 共有 6000 字节数据要发送，并总是按最大长度报文段发送，每个报文段携带 1000 字节数据。

B 每收到一个报文段就立即发回确认，确认号填写期望收到的下一个字节的序号，通告窗口填写自己当前的剩余可用缓存大小。在 B 通告窗口为 0 期间，B 的应用进程从缓存中取走了 2000 字节数据。

1. 说明 A 的发送窗口由什么决定，并写出 A 在收到第一个确认之前可以连续发送的报文段的序号（用字节编号范围表示）。
2. 写出 B 对前 4 个报文段的确认号和通告窗口。
3. 说明第 2 问中最后一个确认到达后 A 为什么必须停止发送，以及 A 用什么机制打破这个局面。
4. B 的应用进程取走数据后，B 会发出什么报文？A 收到后还能发送哪几个报文段？全部数据发送完毕后 B 的确认号是多少？
5. 说明通告窗口的含义，以及为什么 TCP 不允许接收方中途把窗口向回收缩。`,
    points: [
      {
        label: '说明发送窗口的决定因素并写出可连续发送的 4 个报文段序号',
        score: 2,
        answer: `A 的发送窗口取拥塞窗口与接收窗口中的较小者，即 $W=\\min(cwnd,rwnd)$，因 $cwnd$ 足够大，故 $W=4000$ 字节，说明 A 在收到确认前最多可连续发送 4000 字节，即 4 个报文段：$seq=1001$（字节 1001 至 2000）、$seq=2001$（2001 至 3000）、$seq=3001$（3001 至 4000）、$seq=4001$（4001 至 5000）。`,
      },
      {
        label: '写出 B 对前 4 个报文段的确认号与通告窗口',
        score: 3,
        answer: `收到第 1 段后：$ack=2001$，$win=3000$；收到第 2 段后：$ack=3001$，$win=2000$；收到第 3 段后：$ack=4001$，$win=1000$；收到第 4 段后：$ack=5001$，$win=0$（零窗口通告）。确认号始终是已连续收到的下一个字节序号，窗口每次减少 1000。`,
      },
      {
        label: '说明零窗口时必须停发及持续计时器的作用',
        score: 2,
        answer: `$win=0$ 时 A 的发送窗口为 0，即使拥塞窗口很大也不能再发送数据，连接进入零窗口等待。为防止 B 的窗口更新报文丢失造成双方互相等待的死锁，A 启动持续计时器（坚持计时器），超时后发送零窗口探测报文，B 必须回送当前窗口值；若仍为 0 则重新计时继续探测，直到窗口打开为止。`,
      },
      {
        label: '写出窗口更新报文与后续报文段及最终确认号',
        score: 2,
        answer: `B 的应用进程取走 2000 字节后，剩余可用缓存为 $4000-2000=2000$ 字节，B 发送窗口更新报文：$ack=5001$，$win=2000$。A 收到后还能发送 2 个报文段：$seq=5001$（字节 5001 至 6000）和 $seq=6001$（字节 6001 至 7000）。6 个报文段共 6000 字节，全部发完后 B 的确认号为 $ack=7001$。`,
      },
      {
        label: '说明通告窗口的含义与窗口不能收缩的原因',
        score: 2,
        answer: `B 通告的窗口是以确认号为起点的可用接收缓存大小，A 发送窗口的右边界为 $ack+win$，随着确认到达不断向前滑动。TCP 不允许窗口向右收缩，因为 A 可能已经依据较早的窗口通告把数据发出，若新的右边界变小，这些数据就落在窗口之外，B 只能丢弃，A 必须重传，反而浪费带宽、加重拥塞。`,
      },
    ],
    solution: `## 参考解答

**第 1 问：发送窗口与可连续发送的报文段**

TCP 发送方的发送窗口取拥塞窗口与接收窗口中的较小者：

$W=\\min(cwnd,rwnd)$。

题目中拥塞窗口足够大，所以发送窗口由接收窗口决定，$W=4000$ 字节。也就是说，A 在收到任何确认之前最多能把 4000 字节的数据连续发送出去，正好是 4 个报文段：

- 第 1 段：$seq=1001$，字节编号 $1001\\sim2000$；
- 第 2 段：$seq=2001$，字节编号 $2001\\sim3000$；
- 第 3 段：$seq=3001$，字节编号 $3001\\sim4000$；
- 第 4 段：$seq=4001$，字节编号 $4001\\sim5000$。

**第 2 问：B 的确认号与通告窗口**

B 每收到 1000 字节，接收缓存中未被应用进程读走的数据就增加 1000 字节，剩余可用缓存减少 1000 字节；确认号是已经连续正确收到的下一个字节序号：

- 收到第 1 段后：$ack=2001$，$win=4000-1000=3000$；
- 收到第 2 段后：$ack=3001$，$win=4000-2000=2000$；
- 收到第 3 段后：$ack=4001$，$win=4000-3000=1000$；
- 收到第 4 段后：$ack=5001$，$win=4000-4000=0$。

第 4 个确认中的 $win=0$ 称为零窗口通告，表示 B 的接收缓存已被占满。

**第 3 问：零窗口的处理**

A 收到 $win=0$ 的确认后，发送窗口变成 0，此时 A 一个字节都不能再发，连接进入零窗口等待状态。但是 B 的窗口打开时发出的窗口更新报文只有确认号没有数据，属于不可靠的普通报文段，一旦丢失，A 就永远不知道窗口已经打开，B 也在等 A 发数据，双方就会互相等待形成死锁。

为了避免死锁，A 在收到零窗口通告后启动持续计时器（坚持计时器）：超时后 A 发送一个零窗口探测报文段，B 收到探测报文后必须回送自己当前的窗口值。如果窗口仍为 0，A 重新启动计时器，如此反复，直到窗口变成非 0，A 才恢复正常发送。

**第 4 问：窗口更新后的继续发送**

B 的应用进程从缓存中取走 2000 字节后，缓存中还剩 2000 字节未被读取，可用缓存为

$4000-2000=2000$ 字节。

B 于是向 A 发送窗口更新报文：$ack=5001$，$win=2000$。A 收到后发送窗口重新变为 2000 字节，可以继续发送两段数据：

- 第 5 段：$seq=5001$，字节编号 $5001\\sim6000$；
- 第 6 段：$seq=6001$，字节编号 $6001\\sim7000$。

6 个报文段共 $6\\times1000=6000$ 字节，正好是 A 要发送的全部数据。B 收到第 6 段后回送的确认号为

$ack=7001$，

表示已经连续收到 $1001\\sim7000$ 的全部字节，期望下一次收到第 7001 号字节。

**第 5 问：通告窗口的含义与不能收缩的原因**

B 通告的窗口值是从确认号开始还能接收多少字节，A 可用的发送窗口右边界为 $ack+win$。随着确认号不断增大，这个右边界不断向前滑动，A 才能继续发送新数据。

TCP 规定窗口只能向前滑动而不能向右收缩。原因是 A 可能已经按较早的窗口通告把数据发送出去，如果 B 随后把右边界往回收，这些已经在途的数据就落在新窗口之外，B 只能把它们丢弃，A 又必须重传，既浪费带宽又加重网络负担。因此接收方在应用进程没有及时读取数据时，应当通过减小窗口通告值来限制发送方，而不能收回已经承诺过的窗口。`,
    pitfalls: `> 确认号是「期望收到的下一个字节序号」，收到 1001 至 2000 后应写 $ack=2001$，写成 2000 是常见错误；窗口值是剩余可用缓存，每收 1000 字节就减 1000，算成累计已收字节数会得出错误的窗口序列；$win=0$ 时 A 必须停发，靠持续计时器和零窗口探测解决，而不是靠重传计时器；第 4 问中 B 取走 2000 字节后窗口是 2000 而不是 4000（缓存总容量），最终确认号是 7001 而不是 7000。`,
  },
  {
    id: 'e-cn2-app-01',
    subject: 'cn',
    chapter: '应用层',
    difficulty: 3,
    minutes: 15,
    tags: ['DNS', '递归查询', 'HTTP', '持久连接', '流水线', '并行连接'],
    stem: `主机 H 用浏览器访问 http://www.xyz.com/index.html，该主页面中内嵌 8 个小对象，这 8 个对象与主页面都存放在同一台服务器 www.xyz.com 上。已知：

- H 与本地域名服务器之间的往返时间 $RTT_D=8$ ms；
- 本地域名服务器中没有缓存，采用递归查询：它只需向根域名服务器发出一次查询，根域名服务器再向 .com 顶级域名服务器发出递归查询，.com 服务器再向 xyz.com 的权威域名服务器发出递归查询。已知「本地域名服务器与根之间」「根与 .com 服务器之间」「.com 服务器与 xyz.com 权威服务器之间」的往返时间分别为 25 ms、35 ms 和 25 ms；
- H 与服务器 www.xyz.com 之间的往返时间 $RTT=20$ ms；
- 浏览器最多同时建立 4 条并行的 TCP 连接；
- 忽略报文段的发送时延、各服务器的处理时延以及 DNS 报文的传输时延。

1. 求从 H 发出域名解析请求到取得 www.xyz.com 的 IP 地址所需的时间；若紧接着 H 再解析同一个名字，需要多长时间？
2. 若使用 HTTP/1.0 的非持久连接并充分利用 4 条并行连接，求从域名解析完成到主页面与 8 个对象全部下载完毕所需的时间，以及整个访问过程的总时间。
3. 若改用 HTTP/1.1 的持久连接但不使用流水线，并且只使用 1 条连接，求下载全部内容所需的时间和整个访问过程的总时间。
4. 若使用持久连接并采用流水线方式，求下载全部内容所需的时间和整个访问过程的总时间。
5. 比较第 2、3、4 问的结果，说明并行连接和流水线各自起什么作用。`,
    points: [
      {
        label: '算出递归查询的域名解析时间与缓存命中的时间',
        score: 3,
        answer: `本地域名服务器采用递归查询，H 只发出一次请求，三级查询的往返时间串行累加：$25+35+25=85$ ms；结果再送回 H 还要一次 $RTT_D=8$ ms，共 $85+8=93$ ms。若紧接着再解析同一个名字，本地域名服务器的高速缓存中已有该名字与 IP 的映射，只需 H 与本地域名服务器的一次往返，即 8 ms。`,
      },
      {
        label: '算出非持久连接加 4 条并行连接所需的时间',
        score: 3,
        answer: `非持久连接下每个对象都要单独建立 TCP 连接，需要 2 个 RTT（建连接 1 个，请求并接收 1 个）。主页面必须先下载才能发现内嵌对象，独占 $2$ 个 RTT；随后 8 个对象在 4 条并行连接上分 $8\\div4=2$ 批下载，每批 $2$ 个 RTT，共 $2\\times2=4$ 个 RTT。合计 $2+4=6$ 个 RTT，即 $6\\times20=120$ ms，整个访问过程共 $93+120=213$ ms。`,
      },
      {
        label: '算出持久连接非流水线方式所需的时间',
        score: 2,
        answer: `持久连接只建立一次 TCP 连接：主页面用 2 个 RTT，此后 8 个对象在同一条连接上逐个请求，每个 1 个 RTT，共 8 个 RTT，合计 $2+8=10$ 个 RTT，即 $10\\times20=200$ ms，整个访问过程共 $93+200=293$ ms。`,
      },
      {
        label: '算出持久连接加流水线方式所需的时间',
        score: 2,
        answer: `主页面仍是 2 个 RTT；随后 8 个对象的请求可以在一个 RTT 内连续发出，服务器依次把对象送回，只需 1 个 RTT。合计 $2+1=3$ 个 RTT，即 $3\\times20=60$ ms，整个访问过程共 $93+60=153$ ms。`,
      },
      {
        label: '比较三种方式并说明并行连接与流水线的作用',
        score: 2,
        answer: `下载全部内容所需时间：持久连接加流水线 60 ms，优于非持久连接加 4 条并行连接 120 ms，优于单条持久连接非流水线 200 ms。流水线把多个请求在一个往返时间内连续发出，省去了每个对象等待响应的一个 RTT，效果最好；并行连接用多建立连接的开销换取多个对象的并行传输，也能明显缩短时间；而单条持久连接不使用流水线时，每个对象仍要白白等待一个往返，反而最慢。`,
      },
    ],
    solution: `## 参考解答

**第 1 问：域名解析时间**

本地域名服务器采用递归查询，H 只向它发出一次请求，后面三级查询由各级服务器代为完成，且这些查询是串行的：

$25+35+25=85$ ms。

其中 25 ms 是本地域名服务器与根之间的往返，35 ms 是根与 .com 顶级域名服务器之间的往返，25 ms 是 .com 服务器与 xyz.com 权威域名服务器之间的往返。解析结果送回 H 还需要 H 与本地域名服务器之间的一次往返 $RTT_D=8$ ms，所以

$85+8=93$ ms。

如果紧接着再解析同一个名字，本地域名服务器的高速缓存中已经保存了该名字与 IP 地址的映射，不必再询问任何上级服务器，只需要 H 与本地域名服务器之间的一次往返：

$8$ ms。

**第 2 问：非持久连接并使用 4 条并行连接**

页面共有 $1+8=9$ 个对象。非持久连接下每个对象都要单独建立一次 TCP 连接，需要 2 个 RTT：第 1 个 RTT 完成三次握手（第三次握手的 ACK 可以与 HTTP 请求一起发出），第 2 个 RTT 完成请求与响应。

主页面必须先下载下来，浏览器才知道它引用了哪 8 个对象，所以主页面单独占用

$2$ 个 RTT。

此后 8 个对象在 4 条并行连接上分 $8\\div4=2$ 批下载，每批 4 个对象各占 2 个 RTT：

$2\\times2=4$ 个 RTT。

合计

$2+4=6$ 个 RTT，即 $6\\times20=120$ ms。

整个访问过程还要加上域名解析的 93 ms：

$93+120=213$ ms。

**第 3 问：持久连接、非流水线**

持久连接整个过程只建立一次 TCP 连接：

- 主页面：建连接 1 个 RTT 加请求响应 1 个 RTT，共 2 个 RTT；
- 其余 8 个对象在同一条连接上一个接一个地请求与接收，每个 1 个 RTT，共 8 个 RTT。

合计

$2+8=10$ 个 RTT，即 $10\\times20=200$ ms。

整个访问过程共 $93+200=293$ ms。这里可以看到，只开 1 条持久连接而不使用流水线时，每个对象都要空等一个往返，反而比开 4 条并行连接的非持久方式更慢。

**第 4 问：持久连接、流水线**

主页面仍然需要 2 个 RTT。之后 8 个对象的请求可以在同一个 RTT 内连续发出，服务器收到请求后依次把 8 个对象送回，因此这 8 个对象只占 1 个 RTT：

$2+1=3$ 个 RTT，即 $3\\times20=60$ ms。

整个访问过程共 $93+60=153$ ms，是三种方式中最快的。

**第 5 问：三种方式的比较**

按「域名解析完成后下载全部内容」的时间排序：

- 持久连接加流水线：$3$ 个 RTT $=60$ ms，最快；
- 非持久连接加 4 条并行连接：$6$ 个 RTT $=120$ ms；
- 单条持久连接非流水线：$10$ 个 RTT $=200$ ms，最慢。

流水线的作用是让多个请求在一个往返时间内连续发出，服务器连续响应，从而把 8 个对象的时间从 8 个 RTT 压缩到 1 个 RTT，节省最多。并行连接的作用是用额外的连接建立开销换取多个对象同时传输，也能把 9 个对象的时间从 18 个 RTT 压缩到 6 个 RTT。若既不使用流水线也不使用并行连接，每个对象都要串行地等待一个往返，性能最差。

此外，三种方式都要先花 93 ms 做域名解析，这部分时间与连接方式无关，占整个访问时间的比例在流水线方式下最大。`,
    pitfalls: `> 递归查询要把三级往返时间全部累加，还要加上 H 与本地域名服务器的 8 ms，只算 85 ms 会漏掉最后一段；非持久连接下每个对象是 2 个 RTT，并且 4 条并行连接下 8 个对象只需 2 批共 4 个 RTT，写成 8 个或 16 个 RTT 都是错的；主页面必须先下载才能发现内嵌对象，不能把 9 个对象当成一批并行下载；持久连接非流水线是 $2+8=10$ 个 RTT，流水线是 $2+1=3$ 个 RTT，两种持久方式不要混淆。`,
  },
];
