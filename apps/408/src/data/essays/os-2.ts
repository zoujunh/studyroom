import type { SeedEssay } from '../essay-types';

/**
 * 操作系统综合应用题第二批种子（6 道）。
 * chapter 只取：进程管理 / 进程同步 / 死锁 / 内存管理 / 文件系统 / IO 管理
 * 与 os.ts 不重复：本批为读者-写者 PV、前驱图同步、EAT 与地址变换、
 * 多级页表大小、inode 混合索引定位、单双缓冲与 SPOOLing。
 * 书写约定：stem / solution / pitfalls 均为 Markdown + KaTeX，模板串中 LaTeX 反斜杠一律双写；
 * PV 代码用行内代码逐行写在 `-` 列表里，不使用代码围栏。
 */
export const OS_ESSAYS2: SeedEssay[] = [
  {
    id: 'e-os2-rw-01',
    subject: 'os',
    chapter: '进程同步',
    difficulty: 2,
    minutes: 14,
    tags: ['PV操作', '读者写者', '写者优先'],
    stem: `某系统中有一份被多个进程共享的数据文件，进程分为读者与写者两类，约定：

- 允许多个读者同时读该文件；
- 任何时刻只允许一个写者写文件；
- 一个进程在读文件时不允许任何写者写，反之亦然；
- 读者、写者都可能在任意时刻反复到来，数量不确定。

请回答：
1. 定义所需的信号量（含保护计数变量的互斥信号量）并给出各自初值，说明每个量的作用。
2. 用 P、V 操作写出「读者优先」方案下读者进程与写者进程的完整代码。
3. 「读者优先」方案下写者会出现什么问题？请说明原因，并给出一种「写者优先」的修改方案（写出修改后的关键代码）。
4. 说明你的最终方案为什么不会产生死锁。`,
    points: [
      {
        label: '信号量定义与初值完全正确',
        score: 2,
        answer: '设 $mutex=1$ 用于保护读者计数变量；$rw=1$ 用于读—写互斥与写—写互斥；$w=1$ 作为写者优先的排队门（读者优先方案中可不使用）；共享变量 $count=0$ 记录当前正在读的读者数目，它是**普通整型变量**、不是信号量。',
      },
      {
        label: '写出读者进程的 PV 代码',
        score: 2,
        answer: '读者优先方案：`P(mutex)` → `count++` → `if count == 1 then P(rw)` → `V(mutex)` → 读文件 → `P(mutex)` → `count--` → `if count == 0 then V(rw)` → `V(mutex)`。关键点：第一个读者替全体读者申请 $rw$，最后一个读者负责释放 $rw$。',
      },
      {
        label: '写出写者进程的 PV 代码',
        score: 2,
        answer: '写者：`P(rw)` → 写文件 → `V(rw)`。写者之间靠 $rw$ 实现写—写互斥，写者与读者之间靠 $rw$ 实现读—写互斥。',
      },
      {
        label: '指出写者饥饿并给出写者优先的改进',
        score: 2,
        answer: '读者优先下只要读者源源不断到来，$count$ 就始终大于 0，$rw$ 一直被读者群体把持，写者可能无限期等待（写者饥饿）。改进：读者与写者进入前都先执行 `P(w)`，读者在完成计数修改后立即 `V(w)`，即读者代码改为 `P(w)` → `P(mutex)` → `count++` → `if count == 1 then P(rw)` → `V(mutex)` → `V(w)` → 读 → `P(mutex)` → `count--` → `if count == 0 then V(rw)` → `V(mutex)`；写者代码改为 `P(w)` → `P(rw)` → 写 → `V(rw)` → `V(w)`，写者持有 $w$ 直到写完，从而挡住后续读者，实现写者优先。',
      },
      {
        label: '说明不会死锁的理由',
        score: 2,
        answer: '读者与写者申请信号量的顺序一致（都是先 $w$ 后 $rw$），不会形成环路等待；$mutex$ 只用于保护 $count$，临界区极短，写者从不申请 $mutex$，故不存在「持有一个再等另一个」的环；$w$、$rw$ 的 P/V 成对出现且方向与初值匹配，任何持有者都会在有限步内释放。',
      },
    ],
    solution: `## 第 1 问：信号量定义与初值
- $mutex=1$：读者计数变量 $count$ 的互斥访问，保证「判断 $count$ 是否为 1」与「修改 $count$」原子完成。
- $rw=1$：读写互斥、写写互斥。第一个读者申请它、最后一个读者释放它；写者每次写操作都要独占它。
- $w=1$：写者优先的排队门，写者到达后先把门关上，后续读者只能在门外排队。
- $count=0$：正在读文件的读者数目，普通共享整型变量（**不是信号量**），初值 0。

## 第 2 问：读者优先的 PV 代码

读者进程：

- \`P(mutex)\`
- \`count++\`
- \`if count == 1 then P(rw)\`（第一个读者独占文件，挡住写者）
- \`V(mutex)\`
- 读文件
- \`P(mutex)\`
- \`count--\`
- \`if count == 0 then V(rw)\`（最后一个读者离场，允许写者进入）
- \`V(mutex)\`

写者进程：

- \`P(rw)\`
- 写文件
- \`V(rw)\`

以 $count=0$、$rw=1$ 为例走一遍：读者 $R_1$ 到来后 \`count\` 变为 1，执行 \`P(rw)\` 把 $rw$ 降为 0；读者 $R_2$ 到来时 \`count\` 变为 2，因 $count\\neq 1$ 不再申请 $rw$，故 $R_1,R_2$ 可并行读；此时写者 \`P(rw)\` 被阻塞。$R_1$ 读完后 \`count\` 变为 1，不释放 $rw$；只有 $R_2$ 也离开、\`count\` 回到 0 时才 \`V(rw)\`，写者被唤醒。

## 第 3 问：写者饥饿与写者优先改进
**问题**：读者优先方案中，只要不断有新读者到达，\`count\` 就一直大于 0，$rw$ 永远不会被释放，等待中的写者被无限推迟，即**写者饥饿**。

**改进思路**：为读者设置一道门 $w$。写者到达时先 \`P(w)\`，由于写者不释放 $w$ 直到写完，新来的读者会在 \`P(w)\` 上排队，从而让已经在等的写者优先获得服务。

改进后的读者：

- \`P(w)\`
- \`P(mutex)\`
- \`count++\`
- \`if count == 1 then P(rw)\`
- \`V(mutex)\`
- \`V(w)\`（计数已改完，读者不必再占着门）
- 读文件
- \`P(mutex)\`
- \`count--\`
- \`if count == 0 then V(rw)\`
- \`V(mutex)\`

改进后的写者：

- \`P(w)\`
- \`P(rw)\`
- 写文件
- \`V(rw)\`
- \`V(w)\`

注意 $w$ 只被读者短暂持有（仅覆盖 $count$ 的修改），读文件的长时间过程并不占 $w$，因此多个读者仍能并行读；但一旦有写者在等待，后续读者就被挡在门外，写者可以尽早进入。

## 第 4 问：不会死锁的理由
1. **申请顺序一致**：所有进程都按「先 $w$、后 $rw$」的顺序申请，不存在相反顺序的循环等待，破坏了死锁的环路条件。
2. **不嵌套等待**：写者持有 $rw$ 时已不再需要其他信号量（它已持有 $w$）；读者持有 $mutex$ 去等 $rw$ 时，持有 $rw$ 的写者不需要 $mutex$，故不会互相等待。
3. **P/V 方向与初值匹配**：$w,rw$ 初值为 1，且每个 \`P\` 都有配对的 \`V\`，任何一个进程获得信号量后都会在有限步内释放。
4. 时间上有限：写者写完即 \`V(rw)\`、\`V(w)\`，读者读完最后一份即 \`V(rw)\`，排队者总能被唤醒。`,
    pitfalls: `> 忘记 $count$ 是共享变量：必须用 $mutex$ 保护「判断 $count==1$」与「$count{+}{+}$」，否则两个读者可能同时判出 $count==1$ 且只有一个成功申请 $rw$，或最后一个读者漏判 $count==0$ 而使 $rw$ 永不被释放。
> 初值写错：$mutex$ 与 $rw$ 的初值都必须是 1（可用的锁），若把 $rw$ 写成 0 则任何进程都进不去；若把 $mutex$ 写成 0 则第一个读者就阻塞。
> 改进方案不完整：只给写者加 \`P(w)\` 而读者不执行 \`P(w)\` 是没有效果的，读者必须先排队、写者才能优先；反之若读者持有 $w$ 贯穿整个读过程，就退化成「写者与读者串行」，失去读者并行的意义。
> 死锁分析不能只写「不会死锁」四个字：要指出**申请顺序一致**（先 $w$ 后 $rw$）这一关键点按步给分。`,
  },
  {
    id: 'e-os2-pred-02',
    subject: 'os',
    chapter: '进程同步',
    difficulty: 2,
    minutes: 12,
    tags: ['PV操作', '前驱图', '信号量配平'],
    stem: `某计算任务被划分为 6 个程序段 $S_1\\sim S_6$，它们之间的前驱关系为：

- $S_1\\to S_2$，$S_1\\to S_3$
- $S_2\\to S_4$
- $S_3\\to S_4$，$S_3\\to S_5$
- $S_4\\to S_6$，$S_5\\to S_6$

约束是：每个程序段开始执行前，它的**所有**前驱程序段都必须已经执行完毕；没有前驱关系的程序段之间可以并发执行。程序段本身是并发进程，各自循环执行固定的计算。

请回答：
1. 为每条前驱关系定义一个信号量，写出信号量名称、对应的前驱关系与初值，并说明初值取该值的含义。
2. 用 P、V 操作写出 $S_1$ 与 $S_2$ 的同步代码（用「执行本段」表示该段计算）。
3. 用 P、V 操作写出 $S_3$ 与 $S_4$ 的同步代码。
4. 写出 $S_5$ 与 $S_6$ 的同步代码。
5. 逐个验证信号量的 P、V 次数是否配平，并说明该前驱图能否被正确实现、终态各信号量取值是多少。`,
    points: [
      {
        label: '信号量定义、对应关系与初值',
        score: 2,
        answer: '按 7 条有向边设 7 个信号量，初值全为 0（表示「前驱尚未完成」）：$a:S_1\\to S_2$、$b:S_1\\to S_3$、$c:S_2\\to S_4$、$d:S_3\\to S_4$、$e:S_3\\to S_5$、$f:S_4\\to S_6$、$g:S_5\\to S_6$。初值为 0 意味着后继进程一开始必然在对应的 \`P\` 上等待，直到前驱用 \`V\` 把它唤醒。',
      },
      {
        label: '$S_1$ 与 $S_2$ 的 PV 代码',
        score: 2,
        answer: '$S_1$：执行本段 → `V(a)` → `V(b)`（$S_1$ 有两个后继，故 \`V\` 两次）；$S_2$：`P(a)` → 执行本段 → `V(c)`。',
      },
      {
        label: '$S_3$ 与 $S_4$ 的 PV 代码',
        score: 2,
        answer: '$S_3$：`P(b)` → 执行本段 → `V(d)` → `V(e)`；$S_4$：`P(c)` → `P(d)` → 执行本段 → `V(f)`（$S_4$ 有两个前驱，必须先 \`P\` 两次、两个条件全部满足才能执行）。',
      },
      {
        label: '$S_5$ 与 $S_6$ 的 PV 代码',
        score: 2,
        answer: '$S_5$：`P(e)` → 执行本段 → `V(g)`；$S_6$：`P(f)` → `P(g)` → 执行本段。',
      },
      {
        label: '信号量配平验证与正确性结论',
        score: 2,
        answer: '$a,b,c,d,e,f,g$ 每个都恰好被 \`V\` 一次、被 \`P\` 一次，7 个信号量的 P/V 全部配平，总 P 次数 = 总 V 次数 = 7。有向图无环、依赖单向流动，任何执行顺序都不会死锁；每个信号量初值 0、等量的 V 与 P 相抵，全部程序段执行完后各信号量终态均为 0。',
      },
    ],
    solution: `## 第 1 问：信号量定义
前驱图共有 7 条有向边，每条边用一个信号量表示「这条边上的数据/许可是否已经传递」：

- $a$：$S_1\\to S_2$，初值 0
- $b$：$S_1\\to S_3$，初值 0
- $c$：$S_2\\to S_4$，初值 0
- $d$：$S_3\\to S_4$，初值 0
- $e$：$S_3\\to S_5$，初值 0
- $f$：$S_4\\to S_6$，初值 0
- $g$：$S_5\\to S_6$，初值 0

初值只能取 0：后继必须先于前驱执行到 \`P\` 处被阻塞，前驱完成任务后再 \`V\` 唤醒它。若初值取 1，后继会误以为前驱已完成而提前执行。

## 第 2 问：$S_1$、$S_2$ 的代码
- $S_1$：执行本段 → \`V(a)\` → \`V(b)\`
- $S_2$：\`P(a)\` → 执行本段 → \`V(c)\`

$S_1$ 是唯一入度为 0 的结点之一，它可以立即开始；完成后分别通知 $S_2$、$S_3$。

## 第 3 问：$S_3$、$S_4$ 的代码
- $S_3$：\`P(b)\` → 执行本段 → \`V(d)\` → \`V(e)\`
- $S_4$：\`P(c)\` → \`P(d)\` → 执行本段 → \`V(f)\`

$S_4$ 的前驱是 $S_2$ 与 $S_3$，两者都完成后才能开始，因此用两个 \`P\` 分别等待 $c$、$d$；先执行哪个 \`P\` 都可以，但两个都必须通过。

## 第 4 问：$S_5$、$S_6$ 的代码
- $S_5$：\`P(e)\` → 执行本段 → \`V(g)\`
- $S_6$：\`P(f)\` → \`P(g)\` → 执行本段

$S_6$ 是唯一的汇点，必须等 $S_4$、$S_5$ 都完成才执行。

## 第 5 问：配平验证与正确性

逐边统计（每条边对应一次 V 与一次 P）：

- $a$：$S_1$ 中 \`V(a)\` 一次，$S_2$ 中 \`P(a)\` 一次 —— 配平
- $b$：$S_1$ 中 \`V(b)\` 一次，$S_3$ 中 \`P(b)\` 一次 —— 配平
- $c$：$S_2$ 中 \`V(c)\` 一次，$S_4$ 中 \`P(c)\` 一次 —— 配平
- $d$：$S_3$ 中 \`V(d)\` 一次，$S_4$ 中 \`P(d)\` 一次 —— 配平
- $e$：$S_3$ 中 \`V(e)\` 一次，$S_5$ 中 \`P(e)\` 一次 —— 配平
- $f$：$S_4$ 中 \`V(f)\` 一次，$S_6$ 中 \`P(f)\` 一次 —— 配平
- $g$：$S_5$ 中 \`V(g)\` 一次，$S_6$ 中 \`P(g)\` 一次 —— 配平

每条边「先 V 后 P」，且图中没有回路，因此每个 \`P\` 最终都会被对应的 \`V\` 唤醒，程序段总能按拓扑序推进，不会死锁。若按 $S_1\\to S_2\\to S_3\\to S_4\\to S_5\\to S_6$ 顺序执行，可验证每个信号量在相应 \`P\` 之前都已经过 \`V\`；执行完毕后 7 个信号量全部回到初值 0。

（若某程序段的前驱个数为 $k$，该段开头就要有 $k$ 个 \`P\`；若其后继个数为 $m$，该段结尾就要有 $m$ 个 \`V\`。本例中 $S_3$ 的 $m=2$，$S_4$、$S_6$ 的 $k=2$，与此规则一致。）`,
    pitfalls: `> 最典型错误：把 $S_4$ 写成只有一个 \`P\`，或把 $S_3$ 写成只有一个 \`V\`——**后继个数决定 V 的个数、前驱个数决定 P 的个数**，必须与有向图的出入度一一对应。
> 信号量初值写成 1 或与边数混淆：每条边一个初值为 0 的信号量；不要用「一个信号量管多条边」的偷懒写法，否则无法表达 $S_6$ 要等两个前驱的约束。
> 配平检查只说「次数相同」而不列出每个信号量的 P/V 各一次，按步给分时容易丢分；另外要注意终态各信号量应回到 0，这是判断设计正确与否的直接依据。`,
  },
  {
    id: 'e-os2-eat-03',
    subject: 'os',
    chapter: '内存管理',
    difficulty: 2,
    minutes: 12,
    tags: ['地址变换', '有效访问时间', '快表'],
    stem: `某分页存储管理系统中，页面大小为 4 KB，逻辑地址为 32 位，页表项占 4 B，页表常驻内存，并设置了一个快表（TLB）。已知：

- 访问一次内存的时间为 100 ns；
- 查一次快表的时间为 10 ns（**无论命中与否**都要花这 10 ns）；
- 快表命中时可直接得到物理块号，命中的页面比例（命中率）记为 $h$；
- 快表未命中时，需再访问内存中的页表取得物理块号，然后访问数据。

某进程页表的有关内容为：页号 0 对应物理块 5，页号 1 对应物理块 8，页号 2 对应物理块 6，页号 3 对应物理块 9，页号 4 对应物理块 3。

请回答：
1. 写出有效访问时间 $\\text{EAT}$ 的计算式，并计算 $h=90\\%$ 时的 $\\text{EAT}$。
2. 计算 $h=98\\%$ 时的 $\\text{EAT}$，与第 1 问比较并说明命中率对 $\\text{EAT}$ 的影响。
3. 该进程访问的逻辑地址为 $\\text{0x2ABC}$，求页号与页内偏移量。
4. 求该逻辑地址对应的物理地址，用十六进制与十进制两种形式表示。
5. 若系统不设快表，访问一个数据需要几次访存？此时的 $\\text{EAT}$ 是多少？`,
    points: [
      {
        label: 'EAT 计算式与 h=90% 的结果',
        score: 2,
        answer: '$\\text{EAT}=h\\times(10+100)+(1-h)\\times(10+100+100)$；$h=0.9$ 时 $\\text{EAT}=0.9\\times110+0.1\\times210=99+21=120$ ns。',
      },
      {
        label: 'h=98% 的结果与比较',
        score: 2,
        answer: '$h=0.98$ 时 $\\text{EAT}=0.98\\times110+0.02\\times210=107.8+4.2=112$ ns，比 $h=90\\%$ 时快 8 ns。命中率越高，$\\text{EAT}$ 越接近下限 110 ns（一次查快表加一次访存），说明提高快表命中率对系统性能非常关键。',
      },
      {
        label: '逻辑地址的页号与页内偏移',
        score: 2,
        answer: '页面大小 $4096=2^{12}$，页内偏移占低 12 位，页号占高 20 位。$\\text{0x2ABC}=10940$，页号 $=\\lfloor 10940/4096\\rfloor=2$，页内偏移 $=10940-2\\times4096=2748=\\text{0xABC}$。',
      },
      {
        label: '物理地址的十六进制与十进制结果',
        score: 2,
        answer: '页号 2 对应物理块 6，故物理地址 $=6\\times4096+2748=24576+2748=27324=\\text{0x6ABC}$。',
      },
      {
        label: '无快表时的访存次数与 EAT',
        score: 2,
        answer: '不使用快表时，每次访问数据须先访问内存中的页表（1 次访存）再访问数据本身（1 次访存），共 **2 次**访存，$\\text{EAT}=2\\times100=200$ ns。快表把平均访存开销由 200 ns 降到 120 ns，代价是增加一次 10 ns 的快表查找。',
      },
    ],
    solution: `## 第 1 问：$\\text{EAT}$ 计算式与 $h=90\\%$
快表命中时的时间开销为「查快表 + 访问内存取数据」；未命中时还要多访问一次内存读页表：

$$\\text{EAT}=h\\times(t_{\\text{TLB}}+t_{\\text{mem}})+(1-h)\\times(t_{\\text{TLB}}+2t_{\\text{mem}})$$

代入 $t_{\\text{TLB}}=10$ ns、$t_{\\text{mem}}=100$ ns、$h=0.9$：

$$\\text{EAT}=0.9\\times(10+100)+0.1\\times(10+100+100)=0.9\\times110+0.1\\times210=99+21=120\\text{ ns}$$

## 第 2 问：$h=98\\%$ 的 $\\text{EAT}$
$$\\text{EAT}=0.98\\times110+0.02\\times210=107.8+4.2=112\\text{ ns}$$

比较：$h$ 由 $90\\%$ 提高到 $98\\%$，$\\text{EAT}$ 由 120 ns 降到 112 ns，改善了

$$120-112=8\\text{ ns}$$

当 $h\\to1$ 时 $\\text{EAT}\\to110$ ns（下限）；当 $h\\to0$ 时 $\\text{EAT}\\to210$ ns（上限）。可见 $\\text{EAT}$ 对命中率相当敏感：命中率每提高 1 个百分点，约减少 1 ns（因为命中与否的时间差为 $210-110=100$ ns）。

## 第 3 问：页号与页内偏移
页面大小 $4\\text{ KB}=4096=2^{12}$ B，故页内偏移占 12 位，页号占 $32-12=20$ 位。

$$\\text{0x2ABC}=2\\times16^3+10\\times16^2+11\\times16+12=8192+2560+176+12=10940$$

$$\\text{页号}=\\left\\lfloor\\frac{10940}{4096}\\right\\rfloor=2,\\qquad \\text{页内偏移}=10940-2\\times4096=10940-8192=2748=\\text{0xABC}$$

也可以直接按十六进制拆位：$\\text{0x2ABC}$ 的低三位十六进制（12 位二进制）$\\text{ABC}=2748$ 是页内偏移，高位 $\\text{0x2}=2$ 是页号，两者一致。

## 第 4 问：物理地址
查页表得页号 2 对应物理块 6：

$$\\text{物理地址}=\\text{物理块号}\\times\\text{页面大小}+\\text{页内偏移}=6\\times4096+2748=24576+2748=27324$$

即 $\\text{0x6ABC}$（高位 $\\text{0x6}$ 为块号 6，低三位 $\\text{ABC}$ 为页内偏移，与页号、偏移拼接的结果一致）。

## 第 5 问：不使用快表的情形
不使用快表时，每次访问数据必须先在内存中查页表得到物理块号（第 1 次访存），再按物理地址访问数据（第 2 次访存），共 **2 次访存**：

$$\\text{EAT}=2\\times100=200\\text{ ns}$$

引入快表后平均只需约 120 ns，减少了 $200-120=80$ ns，约 $40\\%$。这就是现代系统普遍设置 TLB 的原因：用一次代价极低的相联查找，换掉一次昂贵的内存页表访问。`,
    pitfalls: `> 公式漏项：命中时也要算查快表的 10 ns（题目已声明「无论命中与否都要花这 10 ns」），写成 $0.9\\times100+0.1\\times200=110$ ns 就漏掉了快表时间。
> 未命中时容易只算一次访存：未命中要「先访存读页表、再访存取数据」，是 $t_{\\text{TLB}}+2t_{\\text{mem}}=210$ ns，不是 $t_{\\text{TLB}}+t_{\\text{mem}}$。
> 十六进制与十进制混算：$\\text{0x2ABC}$ 的页号是 $\\text{0x2}=2$、页内偏移是 $\\text{0xABC}=2748$，若把 $\\text{0x2ABC}$ 直接除以 4096 而不先化成十进制或按 12 位拆位，极易算错页内偏移。
> 物理地址公式忘乘块号：正确的拼接是「物理块号 $\\times$ 页面大小 $+$ 页内偏移」，只写页内偏移或写成「页号 $\\times$ 页面大小」都会丢分。`,
  },
  {
    id: 'e-os2-mlpt-04',
    subject: 'os',
    chapter: '内存管理',
    difficulty: 3,
    minutes: 13,
    tags: ['多级页表', '页表大小', '访存次数'],
    stem: `某系统采用分页存储管理，逻辑地址为 32 位，页面大小为 4 KB，页表项占 4 B。为节省页表所占空间，系统采用**两级页表**，把页号平均拆成两部分。

某进程的地址空间只使用了以下两段，其余部分均未使用：

- 从地址 0 开始连续 $8\\text{ MB}$，即地址 $0\\sim 8\\text{ MB}-1$；
- 地址 $\\text{0x40000000}$ 处的一个页面。

系统约定：快表未命中、且所需页表项不在缓存中。

请回答：
1. 若采用单级页表，页表共有多少个页表项？页表本身占多少字节，折合多少个页面？
2. 页号共多少位？两级页表应如何划分页号？每一级页表最多有多少个页表项、占多少字节？
3. 该进程的两级页表实际一共需要多少个页表（含最上层页目录）？总大小是多少字节？
4. 与单级页表相比节省了多少空间（倍数）？说明多级页表为什么能节省空间。
5. 在快表未命中的情况下，两级页表下访问一个数据共需访问几次内存？`,
    points: [
      {
        label: '单级页表的页表项数与大小',
        score: 2,
        answer: '页面大小 $4096=2^{12}$ B，页内偏移占 12 位，页号占 $32-12=20$ 位，故单级页表共有 $2^{20}=1048576$ 个页表项，占 $1048576\\times4\\text{ B}=4194304\\text{ B}=4\\text{ MB}$，折合 $4\\text{ MB}/4\\text{ KB}=1024$ 个页面。',
      },
      {
        label: '页号划分与各级页表大小',
        score: 2,
        answer: '页号 20 位平均拆成 10 位与 10 位（高位 10 位为一级索引、低位 10 位为二级索引）。每级页表最多 $2^{10}=1024$ 个页表项，占 $1024\\times4\\text{ B}=4096\\text{ B}=4\\text{ KB}$，恰好等于一个页面。',
      },
      {
        label: '该进程实际页表数量与总大小',
        score: 3,
        answer: '地址 $0\\sim8\\text{ MB}-1$ 覆盖页号 $0\\sim2047$：页号 $0\\sim1023$ 用第 0 个二级页表，页号 $1024\\sim2047$ 用第 1 个二级页表，共 2 个；地址 $\\text{0x40000000}=2^{30}$ 的页号为 $2^{30}/2^{12}=2^{18}=262144$，其一级索引 $=\\lfloor262144/1024\\rfloor=256$、二级索引 $=262144\\bmod 1024=0$，需要第 3 个二级页表。故页表总数为「1 个页目录 + 3 个二级页表 $=4$ 个」，总大小 $4\\times4096\\text{ B}=16384\\text{ B}=16\\text{ KB}$。',
      },
      {
        label: '节省倍数与节省空间的原因',
        score: 1,
        answer: '单级 4 MB、两级实际 16 KB，$4\\text{ MB}/16\\text{ KB}=256$，节省约 256 倍。原因是只有被使用到的二级页表才需要建立，未被使用的地址区间对应的二级页表根本不存在，页表空间与进程**实际使用**的地址范围成正比，而不是与整个 $2^{20}$ 页的虚拟地址空间成正比。',
      },
      {
        label: '两级页表下的访存次数',
        score: 2,
        answer: '快表未命中时，需要先访问内存读**页目录**得到二级页表所在块号（1 次），再访问内存读**二级页表**得到物理块号（1 次），最后访问数据（1 次），共 **3 次**访存；若无多级页表而用单级页表，则只需 2 次访存。因此多级页表是用时间换空间。',
      },
    ],
    solution: `## 第 1 问：单级页表的大小
$$\\text{页内偏移位数}=\\log_2 4096=12,\\qquad \\text{页号位数}=32-12=20$$

$$\\text{页表项数}=2^{20}=1048576,\\qquad \\text{页表大小}=1048576\\times4\\text{ B}=4194304\\text{ B}=4\\text{ MB}$$

折合页面数 $=\\dfrac{4\\text{ MB}}{4\\text{ KB}}=1024$ 个页面。也就是说，每个进程仅页表就要占用 1024 个页面，对小进程极其浪费。

## 第 2 问：页号划分与各级页表大小
将 20 位页号平均拆成两段，即一级索引 10 位、二级索引 10 位。

$$\\text{每级页表项数}=2^{10}=1024,\\qquad \\text{每级页表大小}=1024\\times4\\text{ B}=4096\\text{ B}=4\\text{ KB}$$

每个页表恰好占一个页面，这是拆分位数的常用原则：使页表大小等于页面大小，便于管理与换入换出。

地址的划分形式为：一级索引（10 位）$\\mid$ 二级索引（10 位）$\\mid$ 页内偏移（12 位）。

## 第 3 问：该进程实际需要的页表
**第 1 段**：$8\\text{ MB}=2^{23}$ B，对应页数 $=2^{23}/2^{12}=2^{11}=2048$，即页号 $0\\sim2047$。

- 页号 $0\\sim1023$：一级索引 $\\lfloor \\text{页号}/1024\\rfloor=0$，用第 0 个二级页表
- 页号 $1024\\sim2047$：一级索引 $=1$，用第 1 个二级页表

即 2 个二级页表。

**第 2 段**：$\\text{0x40000000}=2^{30}=1073741824$，

$$\\text{页号}=\\frac{2^{30}}{2^{12}}=2^{18}=262144$$

$$\\text{一级索引}=\\left\\lfloor\\frac{262144}{1024}\\right\\rfloor=256,\\qquad \\text{二级索引}=262144\\bmod 1024=0$$

即它落在页目录的第 256 项，需要第 3 个二级页表。

**汇总**：页目录 1 个 + 二级页表 3 个 = 4 个页表，

$$\\text{总大小}=4\\times4\\text{ KB}=16\\text{ KB}=16384\\text{ B}$$

注意页目录本身是**满的**（1024 项全在），但其中只有 3 项有效，其余 1021 项为空（可置无效标志，不占二级页表）。

## 第 4 问：节省倍数与原因
$$\\frac{4\\text{ MB}}{16\\text{ KB}}=\\frac{4096\\text{ KB}}{16\\text{ KB}}=256$$

即节省约 256 倍（由 4194304 B 降到 16384 B）。原因在于二级页表是**按需建立**的：虚拟地址空间中大片未使用的区域（本例中大部分地址）在一级页表中标记为无效，不需要任何二级页表，于是页表空间正比于进程实际使用的地址范围；而单级页表必须为整个页号空间连续分配 $2^{20}$ 个页表项，与实际使用量无关。

## 第 5 问：访存次数
快表未命中时，两级页表下取一个数据的路径是：

1. 访问内存读**页目录**，取得二级页表所在物理块号
2. 访问内存读**二级页表**，取得数据所在物理块号
3. 访问内存取**数据**

共 **3 次**访存（单级页表只需 2 次）。若算上快表查找，时间开销还要再加一次 TLB 访问。这体现了多级页表的本质权衡：**以增加访存次数为代价，换取页表空间的显著节省**；实际系统依靠快表把大部分访问压回 1 次有效访存。`,
    pitfalls: `> 页号位数与拆位算错：页面 4 KB 对应页内偏移 12 位，32 位地址的页号是 20 位而非 16 位；拆分时要与「页表项占 4 B、一页放 1024 项」互相印证，10+10 恰好使每级页表等于一页。
> 页目录本身也要计入：只数出 3 个二级页表而漏掉最上层页目录，会把答案写成 12 KB；正确是 $1+3=4$ 个页表、16 KB。
> $\\text{0x40000000}$ 的定位：$2^{30}/2^{12}=2^{18}$，一级索引 256、二级索引 0；不要忘记先化成页号再分别取商和余数，直接拿十六进制数值去取商极易出错。
> 访存次数不要写成 2 次：一级页表（页目录）+ 二级页表 + 数据 = 3 次，多一级页表就多一次访存，这是多级页表的固有代价。`,
  },
  {
    id: 'e-os2-inode-05',
    subject: 'os',
    chapter: '文件系统',
    difficulty: 3,
    minutes: 14,
    tags: ['混合索引', '最大文件长度', '逻辑块号定位'],
    stem: `某文件系统的索引结点（inode）采用混合索引结构：10 个直接地址项、1 个一级间接地址项、1 个二级间接地址项。磁盘块大小为 4 KB，每个磁盘地址项占 4 B。约定目录检索与 inode 读入均已在内存中完成，不再计入磁盘访问次数；逻辑块号从 0 开始编号。

请回答：
1. 一个索引块最多能存放多少个磁盘地址项？只用 10 个直接地址项时文件最大为多少字节？
2. 分别写出直接索引、一级间接索引、二级间接索引所能覆盖的逻辑块号区间，以及各区间的字节容量。
3. 该结构支持的最大文件长度是多少字节？约合多少 GB？
4. 某文件的逻辑块号为 1000，应采用哪一级索引定位？读出该块数据需要访问几个磁盘块？写出中间结果。
5. 若逻辑块号为 5200，结论又如何？`,
    points: [
      {
        label: '索引块容量与直接索引范围',
        score: 2,
        answer: '每个索引块可存放 $4096/4=1024$ 个磁盘地址项。10 个直接地址项直接指向 10 个数据块，文件最大 $10\\times4096=40960\\text{ B}=40\\text{ KB}$。',
      },
      {
        label: '三级索引的块号区间与字节容量',
        score: 3,
        answer: '直接索引：块号 $0\\sim9$，10 块 $=40\\text{ KB}$；一级间接：块号 $10\\sim1033$，$1024$ 块 $=1024\\times4096=4194304\\text{ B}=4\\text{ MB}$；二级间接：块号 $1034\\sim1049609$，$1024^2=1048576$ 块 $=1048576\\times4096=4294967296\\text{ B}=4\\text{ GB}$。',
      },
      {
        label: '最大文件长度',
        score: 2,
        answer: '$L_{\\max}=40960+4194304+4294967296=4299202560\\text{ B}\\approx4.004\\text{ GB}$。',
      },
      {
        label: '逻辑块号 1000 的定位与访盘次数',
        score: 2,
        answer: '块号 1000 满足 $10\\le 1000\\le 1033$，属**一级间接索引**；在一级间接块中的偏移为 $1000-10=990$（即第 991 个地址项）。访问路径为：读一级间接块 → 读数据块，共 **2 次**磁盘访问（inode 已在内存，不计）。',
      },
      {
        label: '逻辑块号 5200 的定位与访盘次数',
        score: 3,
        answer: '块号 5200 超过 1033，属**二级间接索引**；相对二级间接区起点的偏移为 $5200-1034=4166$；支配它的一级索引项为 $\\lfloor4166/1024\\rfloor=4$，在二级索引块内的偏移为 $4166-4\\times1024=70$。访问路径为：读一级间接块 → 读第 5 个二级索引块 → 读数据块，共 **3 次**磁盘访问；若 inode 也不在内存，则再加 1 次，共 4 次。',
      },
    ],
    solution: `## 第 1 问：索引块容量与直接索引
每个磁盘地址项 4 B，索引块 4 KB：

$$\\text{每个索引块的地址项数}=\\frac{4096}{4}=1024$$

10 个直接地址项各指向一个数据块，故仅用直接索引时

$$L_{\\text{直接}}=10\\times4096\\text{ B}=40960\\text{ B}=40\\text{ KB}$$

## 第 2 问：各级索引的覆盖范围
- **直接索引**：直接使用 inode 中的 10 个地址项，覆盖块号 $0\\sim9$，共 10 块，$10\\times4096=40960\\text{ B}=40\\text{ KB}$。
- **一级间接**：inode 的一级间接地址项指向一个索引块，块内 1024 个地址项各指向一个数据块，覆盖块号 $10\\sim10+1024-1=1033$，共 1024 块，$1024\\times4096=4194304\\text{ B}=4\\text{ MB}$。
- **二级间接**：一级索引块中每个地址项指向一个二级索引块，每个二级索引块再指向 1024 个数据块，故共 $1024\\times1024=1048576$ 块，覆盖块号 $1034\\sim1034+1048576-1=1049609$，容量 $1048576\\times4096=4294967296\\text{ B}=4\\text{ GB}$。

## 第 3 问：最大文件长度
$$L_{\\max}=40\\text{ KB}+4\\text{ MB}+4\\text{ GB}=40960+4194304+4294967296=4299202560\\text{ B}$$

换算：$4299202560/2^{30}\\approx4.0039\\text{ GB}$，即约 $4.004\\text{ GB}$，也等于 $4198440\\text{ KB}$。

## 第 4 问：逻辑块号 1000
先判断区间：$0\\sim9$ 为直接区，$10\\sim1033$ 为一级间接区，$1034\\sim1049609$ 为二级间接区。

$1000$ 落在 $10\\sim1033$ 内，故为**一级间接索引**。偏移：

$$1000-10=990$$

即在一级索引块中的第 990 号（从 0 起）地址项指向该数据块。磁盘访问路径为：inode 已在内存（0 次）→ 读一级间接索引块（1 次）→ 读目标数据块（1 次），共 **2 次**。

## 第 5 问：逻辑块号 5200
$5200>1033$，故为**二级间接索引**。相对二级间接区起点的偏移：

$$5200-1034=4166$$

一级间接地址项指向的索引块中，第

$$\\left\\lfloor\\frac{4166}{1024}\\right\\rfloor=4$$

个地址项（即第 5 个）指向所需的二级索引块；该块内的偏移为

$$4166-4\\times1024=4166-4096=70$$

即第 71 个地址项指向目标数据块。磁盘访问路径为：读一级间接索引块（1 次）→ 读该二级索引块（1 次）→ 读数据块（1 次），共 **3 次**；若 inode 尚未读入内存，还要先读 inode 1 次，共 4 次。

（也可换一种算法验证：二级间接区内第 4166 个块，每 1024 块为一个二级索引块，$4166=4\\times1024+70$，与上面一致。）`,
    pitfalls: `> 区间编号算错：二级间接区的起点是 $1034$（$10+1024$ 个块已被直接区与一级间接区占用），求偏移时必须先减去 1034，再除以 1024 取商和余；直接把 5200 除以 1024 会得到完全错误的一级索引项。
> 最大文件长度漏项：只算直接区与一级间接区（$40\\text{ KB}+4\\text{ MB}$）或把二级间接区写成 2 GB；$1024^2$ 块乘 4 KB 才是 4 GB。
> 访盘次数与 inode 的约定相关：题目已约定 inode 在内存，若不看清条件，把 inode、一级索引块、二级索引块、数据块全算上会多记一次。
> 位号与序号混用：偏移 990 指「第 990 号地址项」（从 0 起），等价于「第 991 个地址项」，答题时说明从 0 开始编号以免歧义。`,
  },
  {
    id: 'e-os2-buf-06',
    subject: 'os',
    chapter: 'IO 管理',
    difficulty: 2,
    minutes: 12,
    tags: ['单缓冲', '双缓冲', 'SPOOLing'],
    stem: `某系统从磁盘读入数据块并交给 CPU 处理。已知：

- 把一块数据从磁盘读入缓冲区的时间 $T=100\\ \\mu\\text{s}$；
- 把一块数据从缓冲区传送到用户区的时间 $M=50\\ \\mu\\text{s}$；
- CPU 处理一块数据的时间 $C=50\\ \\mu\\text{s}$。

约定：缓冲区操作与 CPU 处理按「读入 → 传送 → 处理」的顺序进行；本题按流水线进入稳态后每块的平均时间计算总时间，忽略首块的启动偏差。

请回答：
1. 采用单缓冲时，处理一块数据的平均时间是多少？写出推导过程。
2. 采用双缓冲时，处理一块数据的平均时间是多少？并说明与单缓冲的差别。
3. 处理 100 块数据，单缓冲与双缓冲各需要多少时间？两者相差多少？
4. 该系统的一台打印机属于独占设备。若用 SPOOLing 技术管理它，系统需要哪些组成部分？说明它把独占设备改造为共享设备的原理。
5. 设每个打印页数据为 4 KB，主机把数据写入磁盘的速度为 2 MB/s，打印机的打印速度为 20 页/分钟。用户进程要输出 100 页，它需要等待多长时间？打印机真正打印完这 100 页又需要多长时间？由此说明 SPOOLing 的作用。`,
    points: [
      {
        label: '单缓冲每块平均时间与推导',
        score: 2,
        answer: '单缓冲下缓冲区同一时刻只能有一个作业使用：$T$ 与 $M$ 不能并行，$M$ 与 $C$ 也不能并行，但上一块进入 CPU 处理（$C$）后磁盘可以为下一块读入（$T$）而并行。故一块的平均时间 $=\\max(T,C)+M=\\max(100,50)+50=150\\ \\mu\\text{s}$。',
      },
      {
        label: '双缓冲每块平均时间与差别',
        score: 2,
        answer: '双缓冲下两个缓冲区可交替使用，磁盘读入一个缓冲区与「传送 + 处理」另一个缓冲区可完全并行，故一块的时间 $=\\max(T,M+C)=\\max(100,50+50)=\\max(100,100)=100\\ \\mu\\text{s}$，比单缓冲每块省 $50\\ \\mu\\text{s}$（缩短三分之一）。',
      },
      {
        label: '处理 100 块的总时间与差值',
        score: 2,
        answer: '单缓冲 $100\\times150=15000\\ \\mu\\text{s}=15\\text{ ms}$；双缓冲 $100\\times100=10000\\ \\mu\\text{s}=10\\text{ ms}$；两者相差 $15000-10000=5000\\ \\mu\\text{s}=5\\text{ ms}$，双缓冲节省约 $33\\%$。',
      },
      {
        label: 'SPOOLing 的组成与原理',
        score: 2,
        answer: '组成包括：磁盘上的**输入井与输出井**（用于暂存输入数据与待打印数据）、**输入进程与输出进程**（SPOOLing 进程，负责设备与井之间的数据搬移）、**井管理程序**（负责井的分配与回收），以及内存缓冲区。原理是：用户进程的输出不再直接送往打印机，而是先写入磁盘输出井并立即返回，由输出进程在打印机空闲时依次把井中数据真正打印出去；这样用共享设备（磁盘）模拟了独占设备，使打印机在逻辑上可被多个进程同时「占用」，即**虚拟设备技术**。',
      },
      {
        label: 'SPOOLing 的等待时间与实际打印时间',
        score: 2,
        answer: '$100$ 页共 $100\\times4\\text{ KB}=400\\text{ KB}$，写入磁盘需 $400\\text{ KB}\\div2\\text{ MB/s}=400/2048\\text{ s}\\approx0.195\\text{ s}$，即用户进程约 $0.2\\text{ s}$ 就「输出完毕」并继续执行；打印机每页需 $60/20=3\\text{ s}$，100 页共 $100\\times3=300\\text{ s}=5\\text{ min}$，全部由输出进程在后台完成。可见用户进程的等待时间由磁盘速度决定（约 0.2 s）而不再受 5 分钟的慢速打印机拖累，独占设备被改造成了可共享的虚拟设备。',
      },
    ],
    solution: `## 第 1 问：单缓冲
单缓冲只有一块缓冲区，数据必须先读完（$T$）才能被传送走（$M$），因此 $T$ 与 $M$ 串行；$M$ 把数据送入用户区后 CPU 才开始处理（$C$），$C$ 与下一块的 $T$ 可以重叠，但不能与 $M$ 重叠。于是每块时间为

$$\\text{单缓冲每块时间}=\\max(T,C)+M=\\max(100,50)+50=100+50=150\\ \\mu\\text{s}$$

时间线（单位 $\\mu\\text{s}$）：$T_1[0,100]$，$M_1[100,150]$，$C_1[150,200]$；$T_2[150,250]$（缓冲区 150 空出即开始读入），$M_2[250,300]$，$C_2[300,350]$……可见从第 2 块起每块稳定占用 $150\\ \\mu\\text{s}$，$T$ 与 $C$ 重叠。

## 第 2 问：双缓冲
双缓冲设置两块缓冲区，磁盘把数据读入缓冲区 1 的同时，CPU 可以处理已由缓冲区 2 送出的数据，即 $T$ 与 $M+C$ 完全并行：

$$\\text{双缓冲每块时间}=\\max(T,M+C)=\\max(100,50+50)=\\max(100,100)=100\\ \\mu\\text{s}$$

时间线：$T_1[0,100]$，$M_1[100,150]$，$C_1[150,200]$；同时 $T_2[100,200]$ 与 $C_1$ 并行，$M_2[200,250]$，$C_2[250,300]$，$T_3[200,300]$……每块稳定占用 $100\\ \\mu\\text{s}$。

差别：单缓冲的瓶颈是 $\\max(T,C)+M=150$，双缓冲把 $M$ 也放进了并行段，瓶颈降为 $\\max(T,M+C)=100$，每块少 $50\\ \\mu\\text{s}$。

## 第 3 问：处理 100 块的总时间
- 单缓冲：$100\\times150=15000\\ \\mu\\text{s}=15\\text{ ms}$
- 双缓冲：$100\\times100=10000\\ \\mu\\text{s}=10\\text{ ms}$

$$\\text{节省}=\\frac{15000-10000}{15000}=\\frac{5000}{15000}\\approx33.3\\%$$

（若严格计入首块启动时间：单缓冲首块为 $T+M+C=100+50+50=200\\ \\mu\\text{s}$，总时间 $=200+99\\times150=15050\\ \\mu\\text{s}$；双缓冲首块在 $t=200$ 完成、此后每块 $100\\ \\mu\\text{s}$，总时间 $=200+99\\times100=10100\\ \\mu\\text{s}$。两者仍相差近 $5\\text{ ms}$，本题按约定的稳态算法取 15000 与 10000。）

## 第 4 问：SPOOLing 的组成与原理
组成部分：

- **输入井与输出井**：磁盘上开辟的存储区域。输入井暂存由输入设备预先读入的数据，输出井暂存用户进程待输出的数据。
- **输入进程与输出进程**：即 SPOOLing 进程，前者把输入设备的数据送入输入井，后者把输出井的数据送往输出设备。
- **井管理程序**：负责井空间的分配、回收与队列管理。
- 内存缓冲区：配合设备与井之间的数据传送。

原理：把独占设备（打印机）的「使用权」与「实际动作」分离。用户进程输出时只是把数据写入输出井（共享设备磁盘），随即认为自己已用完设备而继续运行；真正的打印由输出进程在打印机空闲时按井中队列依次完成。于是多个进程可「同时」使用同一台打印机，独占设备被改造成逻辑上的共享设备，即**虚拟设备技术**。

## 第 5 问：等待时间与实际打印时间
100 页数据总量：

$$100\\times4\\text{ KB}=400\\text{ KB}$$

主机写入磁盘（写入输出井）所需时间：

$$\\frac{400\\text{ KB}}{2\\text{ MB/s}}=\\frac{400}{2048}\\text{ s}\\approx0.195\\text{ s}$$

即用户进程大约 $0.2\\text{ s}$ 后就可以继续执行，无需等待打印完成。

打印机每页耗时：

$$\\frac{60\\text{ s}}{20}=3\\text{ s/页},\\qquad 100\\text{ 页}=100\\times3=300\\text{ s}=5\\text{ min}$$

结论：用户进程的等待时间约 $0.2\\text{ s}$，而实际打印需要 $5\\text{ min}$，两者相差约 1500 倍以上。SPOOLing 把慢速独占设备的时间开销从用户进程中剥离出去，交由后台的输出进程承担，既提高了设备利用率，也让多个进程能共享同一台打印机。`,
    pitfalls: `> 单缓冲不要写成 $T+M+C=200\\ \\mu\\text{s}$：$T$ 与 $C$ 是可并行的，正确公式是 $\\max(T,C)+M=150\\ \\mu\\text{s}$；写成简单相加是本题最常见的错误。
> 双缓冲公式记错：应为 $\\max(T,M+C)=\\max(100,100)=100\\ \\mu\\text{s}$，写成 $\\max(T,C)=100$ 虽然数值巧合相同，但推理不正确（当 $C$ 很大时结果就会不同，例如 $C=200$ 时正确答案是 $\\max(100,250)=250$）。
> 单位换算：$\\mu\\text{s}$ 与 ms（$1\\text{ ms}=1000\\ \\mu\\text{s}$）、KB 与 MB（本题 $2\\text{ MB/s}=2048\\text{ KB/s}$）都要统一；把 $400\\text{ KB}\\div2\\text{ MB/s}$ 算成 $200\\text{ s}$ 是典型的单位错误。
> SPOOLing 的作用要说清「用户进程只把数据写入输出井即返回」，若答成「用户进程直接等打印机打完」，就完全没有体现虚拟设备与并行的思想，按步给分时这一问会全失。`,
  },
];
