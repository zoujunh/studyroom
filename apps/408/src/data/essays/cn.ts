import type { SeedEssay } from '../essay-types';

/**
 * 计算机网络 · 综合应用题（大题）。
 * 覆盖：网络层（子网划分/CIDR 最长前缀匹配、IP 分片）、传输层（TCP 拥塞控制、连接管理与序号推演）、
 * 数据链路层（停等/滑动窗口信道利用率）、应用层（DNS 与 HTTP 时延、邮件协议）。
 */
export const CN_ESSAYS: SeedEssay[] = [
  {
    id: 'e-cn-net-01',
    subject: 'cn',
    chapter: '网络层',
    difficulty: 2,
    minutes: 12,
    tags: ['子网划分', 'CIDR', '最长前缀匹配'],
    stem: `某单位申请到一个地址块 $200.10.20.0/24$，现要把它等长划分为 $4$ 个子网，分配给 4 个部门独立使用。设所有子网的子网号位数相同，且每个子网内主机号全 0 和全 1 的地址都不分配给主机使用。

1. 求出子网的子网掩码，以及每个子网中可分配给主机的地址数量。
2. 写出 4 个子网各自的网络地址、广播地址和可用主机地址范围。
3. 若某台主机被配置为 $200.10.20.130$，判断它属于哪个子网。
4. 该单位的边界路由器上配置了下列转发表项：$200.10.20.0/26$ 发往接口 1，$200.10.20.128/25$ 发往接口 2，$200.10.20.0/24$ 发往接口 3，$0.0.0.0/0$ 发往接口 4。请分别给出目的地址为 $200.10.20.30$、$200.10.20.70$、$200.10.20.150$、$200.10.21.9$ 的 IP 分组应当从哪个接口转发，并说明依据。`,
    points: [
      {
        label: '求出子网掩码与每个子网的可用主机地址数',
        score: 2,
        answer: `划分 $4$ 个等长子网需要向主机号借 $\\log_2 4=2$ 位作为子网号，原来 8 位主机号剩下 $8-2=6$ 位。子网掩码前 $24+2=26$ 位为 1，即 $255.255.255.192$（可写作 $/26$）。每个子网共有 $2^6=64$ 个地址，去掉网络地址和广播地址后可用主机地址数为 $2^6-2=62$ 个。`,
      },
      {
        label: '写出 4 个子网的地址范围（网络地址、广播地址、可用范围）',
        score: 3,
        answer: `子网号取 $00,01,10,11$，第 4 字节以 $2^6=64$ 为步长递增：子网 1 网络地址 $200.10.20.0$、广播地址 $200.10.20.63$、可用 $200.10.20.1\\sim200.10.20.62$；子网 2 网络地址 $200.10.20.64$、广播地址 $200.10.20.127$、可用 $200.10.20.65\\sim200.10.20.126$；子网 3 网络地址 $200.10.20.128$、广播地址 $200.10.20.191$、可用 $200.10.20.129\\sim200.10.20.190$；子网 4 网络地址 $200.10.20.192$、广播地址 $200.10.20.255$、可用 $200.10.20.193\\sim200.10.20.254$。`,
      },
      {
        label: '判断主机 200.10.20.130 所属子网',
        score: 2,
        answer: `第 4 字节 $130$ 落在 $128\\sim191$ 区间，因此该主机属于子网 3，即 $200.10.20.128/26$。`,
      },
      {
        label: '按最长前缀匹配给出 4 个目的地址的转发接口',
        score: 3,
        answer: `$200.10.20.30$ 落在 $0\\sim63$，匹配 $/26$ 与 $/24$，取最长前缀 $/26$，走接口 1；$200.10.20.70$ 落在 $64\\sim127$，既不匹配只覆盖 $0\\sim63$ 的 $/26$，也不匹配只覆盖 $128\\sim255$ 的 $200.10.20.128/25$，只能匹配 $/24$，走接口 3；$200.10.20.150$ 落在 $128\\sim255$，匹配 $/25$ 与 $/24$，取 $/25$，走接口 2；$200.10.21.9$ 的第 3 字节为 $21$，不匹配 $200.10.20.0/24$，只能走默认路由 $0.0.0.0/0$，从接口 4 转发。`,
      },
    ],
    solution: `## 参考解答

**第 1 问：子网掩码与可用主机地址数**

要把 $/24$ 等长划分为 $4$ 个子网，需要的子网号位数为 $\\log_2 4=2$ 位，即向 8 位主机号借 2 位，主机号剩余 $8-2=6$ 位。

- 子网掩码：前 $24+2=26$ 位为 1，后 6 位为 0，即 $255.255.255.192$，也可写成 $/26$；
- 每个子网共有 $2^6=64$ 个地址，去掉主机号全 0 的网络地址和主机号全 1 的广播地址，可用主机地址数为 $2^6-2=62$ 个。

**第 2 问：4 个子网的地址范围**

子网号依次为 $00,01,10,11$，第 4 字节以 $2^6=64$ 为步长递增：

- 子网 1：网络地址 $200.10.20.0$，广播地址 $200.10.20.63$，可用范围 $200.10.20.1\\sim200.10.20.62$；
- 子网 2：网络地址 $200.10.20.64$，广播地址 $200.10.20.127$，可用范围 $200.10.20.65\\sim200.10.20.126$；
- 子网 3：网络地址 $200.10.20.128$，广播地址 $200.10.20.191$，可用范围 $200.10.20.129\\sim200.10.20.190$；
- 子网 4：网络地址 $200.10.20.192$，广播地址 $200.10.20.255$，可用范围 $200.10.20.193\\sim200.10.20.254$。

4 个子网的网络地址恰好把 $200.10.20.0/24$ 完整覆盖，没有重叠也没有空隙。

**第 3 问：主机 200.10.20.130 的归属**

把 $130$ 与子网掩码 $255.255.255.192$ 逐位与运算：$130=1000\\ 0010$，与 $1100\\ 0000$ 相与得 $1000\\ 0000=128$，所以网络地址为 $200.10.20.128$。也可直接看区间：$130$ 落在 $128\\sim191$ 内，属于子网 3，即 $200.10.20.128/26$。

**第 4 问：最长前缀匹配**

路由器按「前缀最长者优先」转发，与表项的书写顺序无关：

- $200.10.20.30$：第 4 字节 $30$ 落在 $0\\sim63$，同时匹配 $200.10.20.0/26$ 和 $200.10.20.0/24$，取最长的 $/26$，从接口 1 转发；
- $200.10.20.70$：$70$ 落在 $64\\sim127$。它不在 $/26$ 覆盖的 $0\\sim63$ 内，也不在 $200.10.20.128/25$ 覆盖的 $128\\sim255$ 内，只能匹配 $200.10.20.0/24$，从接口 3 转发；
- $200.10.20.150$：$150$ 落在 $128\\sim255$，同时匹配 $/25$ 与 $200.10.20.0/24$，取最长的 $/25$，从接口 2 转发；
- $200.10.21.9$：第 3 字节为 $21\\ne20$，不匹配 $200.10.20.0/24$，其余表项也都不能匹配，只能走默认路由 $0.0.0.0/0$，从接口 4 转发。`,
    pitfalls: `> 可用主机地址数必须减去网络地址和广播地址，答案是 $2^6-2=62$，写成 64 是最常见的丢分点；最长前缀匹配要先比前缀长度再比表项顺序，$200.10.20.70$ 落在 $64\\sim127$，只能匹配 $200.10.20.0/24$ 走接口 3，很容易误判成接口 1。`,
  },
  {
    id: 'e-cn-tcp-01',
    subject: 'cn',
    chapter: '传输层',
    difficulty: 2,
    minutes: 15,
    tags: ['TCP', '拥塞控制', '慢开始', '拥塞避免'],
    stem: `主机 A 与主机 B 之间建立了一条 TCP 连接，A 始终有大量数据要发送，B 的接收窗口足够大，因此 A 的发送窗口完全由拥塞窗口 $cwnd$ 决定。设每个报文段长 $1$ KB（记为 $1$ MSS），A 的初始拥塞窗口 $cwnd=1$ MSS，初始慢开始门限 $ssthresh=4$ MSS。A 采用慢开始与拥塞避免算法，并作如下约定：

- 慢开始阶段每经过一个传输轮次（一个 RTT），$cwnd$ 加倍；
- 当 $cwnd$ 达到 $ssthresh$ 后改用拥塞避免算法，每经过一个传输轮次，$cwnd$ 只加 $1$ MSS；
- 若发生超时，则新的 $ssthresh$ 取超时时刻 $cwnd$ 的一半，$cwnd$ 重置为 $1$ MSS，并重新执行慢开始。

设第 7 个传输轮次发送的报文段全部丢失，A 直到计时器超时才得知。

1. 写出第 1 至第 7 个传输轮次开始时 $cwnd$ 的值（以 MSS 为单位），并指出 A 从第几轮开始进入拥塞避免阶段。
2. 写出超时后 $ssthresh$ 的新值，以及第 8 至第 12 个传输轮次开始时 $cwnd$ 的值。
3. 求前 12 个传输轮次中 A 一共发送了多少个报文段（折合多少 KB 数据）。`,
    points: [
      {
        label: '写出第 1~3 轮 cwnd，并指出转入拥塞避免的轮次',
        score: 2,
        answer: `慢开始阶段每轮加倍：第 1 轮 $cwnd=1$，第 2 轮 $cwnd=2$，第 3 轮 $cwnd=4$。第 3 轮开始时 $cwnd$ 已达到 $ssthresh=4$，因此从第 4 轮起改用拥塞避免算法。`,
      },
      {
        label: '写出拥塞避免阶段第 4~7 轮的 cwnd 序列',
        score: 3,
        answer: `拥塞避免阶段每轮只加 $1$ MSS：第 4 轮 $cwnd=5$，第 5 轮 $cwnd=6$，第 6 轮 $cwnd=7$，第 7 轮 $cwnd=8$。第 7 轮发送的 8 个报文段全部丢失，发生超时时 $cwnd=8$ MSS。`,
      },
      {
        label: '算出超时后 ssthresh 的新值',
        score: 2,
        answer: `新的慢开始门限取超时时刻 $cwnd$ 的一半：$ssthresh=8\\div2=4$ MSS；同时 $cwnd$ 重置为 $1$ MSS，重新执行慢开始。`,
      },
      {
        label: '写出第 8~12 轮的 cwnd 序列',
        score: 3,
        answer: `第 8 轮 $cwnd=1$，第 9 轮 $cwnd=2$，第 10 轮 $cwnd=4$（重新达到 $ssthresh=4$，从第 11 轮起再次进入拥塞避免），第 11 轮 $cwnd=5$，第 12 轮 $cwnd=6$。`,
      },
      {
        label: '算出前 12 轮累计发送的报文段数',
        score: 2,
        answer: `某一轮发送的报文段数等于该轮开始时的 $cwnd$。第 1~7 轮共 $1+2+4+5+6+7+8=33$ 个；第 8~12 轮共 $1+2+4+5+6=18$ 个；合计 $33+18=51$ 个报文段，即 $51$ KB 数据。`,
      },
    ],
    solution: `## 参考解答

**第 1 问：慢开始阶段与转入拥塞避免的时刻**

初始 $cwnd=1$ MSS，$ssthresh=4$ MSS。只要 $cwnd<ssthresh$ 就用慢开始，每轮加倍：

- 第 1 轮开始：$cwnd=1$；
- 第 2 轮开始：$cwnd=2$；
- 第 3 轮开始：$cwnd=4$，已经达到 $ssthresh$。

所以从**第 4 轮**起改用拥塞避免算法。

**第 2 问：拥塞避免阶段（第 4~7 轮）**

拥塞避免阶段每轮只增加 $1$ MSS：

- 第 4 轮：$cwnd=5$
- 第 5 轮：$cwnd=6$
- 第 6 轮：$cwnd=7$
- 第 7 轮：$cwnd=8$

第 7 轮按 $cwnd=8$ 连续发送 8 个报文段，全部丢失，A 计时器超时，此时 $cwnd=8$ MSS。

**第 3 问：超时后的参数调整**

$ssthresh=8\\div2=4$ MSS，$cwnd$ 重置为 $1$ MSS，并重新执行慢开始。

**第 4 问：第 8~12 轮的 cwnd**

- 第 8 轮：$cwnd=1$（慢开始）
- 第 9 轮：$cwnd=2$
- 第 10 轮：$cwnd=4$，重新达到 $ssthresh=4$，从第 11 轮起进入拥塞避免
- 第 11 轮：$cwnd=5$
- 第 12 轮：$cwnd=6$

**第 5 问：前 12 轮累计发送的报文段数**

每一轮发送的报文段数恰好等于该轮开始时的 $cwnd$（单位 MSS）：

- 第 1~7 轮：$1+2+4+5+6+7+8=33$ 个；
- 第 8~12 轮：$1+2+4+5+6=18$ 个；
- 合计 $33+18=51$ 个报文段，每段 $1$ KB，即 $51$ KB 数据。`,
    pitfalls: `> 转入拥塞避免的判据是 cwnd 达到 ssthresh：第 3 轮 cwnd 已经是 4，所以第 4 轮才是拥塞避免的第一轮，容易整段错位；超时后要用超时时刻的 cwnd=8 减半得到 ssthresh=4，不能拿第 6 轮的 7 去算；统计发送段数时不要漏掉慢开始阶段的 1 和 2。`,
  },
  {
    id: 'e-cn-ip-01',
    subject: 'cn',
    chapter: '网络层',
    difficulty: 3,
    minutes: 15,
    tags: ['IP分片', 'MTU', '片偏移', 'MF'],
    stem: `主机 A 向主机 B 发送一个 IP 数据报，其总长度为 $3040$ 字节，其中固定首部 $20$ 字节，数据部分 $3020$ 字节。该数据报在到达 B 之前依次经过两段链路：第一段链路的最大传送单元 $MTU=1420$ 字节，第二段链路的最大传送单元 $MTU=820$ 字节。

1. 在第一段链路上，每个分片最多能封装多少字节的数据？这个数据报需要分成几片？
2. 写出第一段链路分片后各分片的总长度、MF 标志（1 或 0）以及片偏移字段的值。
3. 第一段链路产生的分片中，哪些还需要在第二段链路上继续分片？在第二段链路上每个分片最多能封装多少字节的数据？
4. 写出到达主机 B 时全部分片的总长度、MF 标志和片偏移字段的值，并说明片偏移是否为 8 的整数倍。
5. 与不分片相比，分片过程一共额外增加了多少字节的首部开销？IP 数据报的重组在什么地方进行？`,
    points: [
      {
        label: '算出第一段链路每个分片的最大数据长度与分片数',
        score: 2,
        answer: `每个分片仍要携带 $20$ 字节固定首部，故每片最多封装 $1420-20=1400$ 字节数据。除最后一片外，分片的数据长度必须是 $8$ 的整数倍，$1400\\div8=175$，可以取满。$3020=1400+1400+220$，所以需要分成 $3$ 片。`,
      },
      {
        label: '写出第一段链路各分片的总长度、MF 与片偏移',
        score: 3,
        answer: `片 1：数据 $1400$ 字节，总长度 $1420$ 字节，$MF=1$，片偏移 $0\\div8=0$；片 2：数据 $1400$ 字节，总长度 $1420$ 字节，$MF=1$，片偏移 $1400\\div8=175$；片 3：数据 $220$ 字节，总长度 $240$ 字节，$MF=0$，片偏移 $2800\\div8=350$。`,
      },
      {
        label: '判断哪些分片需继续分片并算出第二段链路的最大数据长度',
        score: 2,
        answer: `第二段链路 $MTU=820$ 字节，每片最多封装 $820-20=800$ 字节数据（$800\\div8=100$，合法）。片 1、片 2 的数据部分各 $1400$ 字节，超过 $800$ 字节，必须继续分片；片 3 只有 $220$ 字节，不需要再分片。`,
      },
      {
        label: '写出到达主机 B 的全部分片的总长度、MF 与片偏移',
        score: 3,
        answer: `共 $5$ 个分片。由片 1 分出：分片 A 总长 $820$、$MF=1$、偏移 $0$，分片 B 总长 $620$、$MF=1$、偏移 $800\\div8=100$；由片 2 分出：分片 C 总长 $820$、$MF=1$、偏移 $1400\\div8=175$，分片 D 总长 $620$、$MF=1$、偏移 $2200\\div8=275$；片 3 不再分片，作为分片 E 总长 $240$、$MF=0$、偏移 $2800\\div8=350$。五个片偏移 $0,100,175,275,350$ 都是 $8$ 的整数倍，数据长度合计 $800+600+800+600+220=3020$ 字节。`,
      },
      {
        label: '算出额外首部开销并说明重组位置',
        score: 2,
        answer: `原来只有 $1$ 个 IP 首部共 $20$ 字节，分片后共 $5$ 个首部共 $5\\times20=100$ 字节，额外增加 $100-20=80$ 字节（即 4 个新增分片各 $20$ 字节）。IP 分片的重组只在最终目的主机 B 上进行，中间路由器只做分片与转发，不做重组。`,
      },
    ],
    solution: `## 参考解答

**第 1 问：第一段链路的每片数据长度与分片数**

分片后每一片都要带上自己的 $20$ 字节固定首部，所以每片能封装的最大数据长度为

$1420-20=1400$ 字节。

IP 规定除最后一片外，每片的数据长度必须是 $8$ 字节的整数倍，而 $1400\\div8=175$ 恰好是整数，可以取满 $1400$ 字节。

$3020=1400+1400+220$，因此需要分成 **3 片**。

**第 2 问：第一段链路的分片结果**

片偏移字段以 $8$ 字节为单位，记录该片数据在原数据报数据部分中的起始位置：

- 片 1：数据 $1400$ 字节，总长度 $1400+20=1420$ 字节，$MF=1$，片偏移 $0\\div8=0$；
- 片 2：数据 $1400$ 字节，总长度 $1420$ 字节，$MF=1$，片偏移 $1400\\div8=175$；
- 片 3：数据 $220$ 字节，总长度 $240$ 字节，$MF=0$，片偏移 $2800\\div8=350$。

$MF=1$ 表示后面还有分片，$MF=0$ 表示这是最后一片。

**第 3 问：第二段链路上的再分片**

第二段链路 $MTU=820$ 字节，每片最多封装 $820-20=800$ 字节数据，$800\\div8=100$ 也是 $8$ 的整数倍，合法。

- 片 1 的数据 $1400>800$，必须继续分片；
- 片 2 的数据 $1400>800$，也必须继续分片；
- 片 3 的数据 $220<800$，直接通过，不再分片。

**第 4 问：到达主机 B 的全部分片**

再分片时片偏移仍以**原数据报**的数据起点为基准计算：

- 片 1 分成两片：分片 A 数据 $800$ 字节、总长 $820$、$MF=1$、偏移 $0\\div8=0$；分片 B 数据 $600$ 字节、总长 $620$、$MF=1$、偏移 $800\\div8=100$；
- 片 2 分成两片：分片 C 数据 $800$ 字节、总长 $820$、$MF=1$、偏移 $1400\\div8=175$；分片 D 数据 $600$ 字节、总长 $620$、$MF=1$、偏移 $2200\\div8=275$；
- 片 3 保持原样：分片 E 数据 $220$ 字节、总长 $240$、$MF=0$、偏移 $2800\\div8=350$。

最终共有 $5$ 个分片，数据长度依次为 $800,600,800,600,220$，合计 $3020$ 字节；片偏移 $0,100,175,275,350$ 全部是 $8$ 的整数倍，相邻分片的数据区间首尾相接，正好覆盖原数据报的全部数据。

**第 5 问：额外开销与重组位置**

不分片时只有 $1$ 个首部 $20$ 字节；分片后共有 $5$ 个首部，合计 $5\\times20=100$ 字节，额外开销为

$100-20=80$ 字节。

这部分开销来自新增的 4 个分片各自的 $20$ 字节首部。IP 分片的重组只在最终目的主机 B 进行，中间路由器只负责按下一段链路的 MTU 分片并转发，不进行重组。`,
    pitfalls: `> 第二段链路再分片时，MF 仍按「原数据报后面是否还有数据」判断：由片 1、片 2 分出的前三个分片 MF 都必须写 1，只有最后一个分片 MF=0；片偏移始终相对原数据报的数据起点，分片 C 的偏移是 $1400\\div8=175$ 而不是 0，分片 D 是 $2200\\div8=275$ 而不是 100。`,
  },
  {
    id: 'e-cn-dll-01',
    subject: 'cn',
    chapter: '数据链路层',
    difficulty: 2,
    minutes: 10,
    tags: ['信道利用率', '停等协议', '滑动窗口', 'GBN'],
    stem: `主机甲与主机乙之间是一条长度为 $2000$ km 的点对点链路，信号在链路上的传播速率为 $2\\times10^8$ m/s，链路的数据传输速率为 $1$ Mb/s。甲向乙发送长度为 $1000$ bit 的数据帧，乙收到后立即回送一个很短的确认帧，确认帧的发送时延和处理时延均忽略不计。

1. 求该链路的单程传播时延和一个数据帧的发送时延。
2. 采用停等协议时，该链路的信道利用率是多少（保留一位小数）？
3. 若改用滑动窗口协议，为使信道利用率达到 $100\\%$，发送窗口至少应为多少个数据帧？
4. 若采用后退 N 帧协议（GBN），帧的序号字段只有 $3$ 位（序号为 $0\\sim7$），能否满足第 3 问算出的窗口要求？为使 GBN 正常工作，序号字段至少需要多少位？`,
    points: [
      {
        label: '算出单程传播时延与数据帧发送时延',
        score: 2,
        answer: `单程传播时延 $t_p=2000\\times10^3\\div(2\\times10^8)=0.01$ s $=10$ ms；一个数据帧的发送时延 $T_f=1000\\div(1\\times10^6)=0.001$ s $=1$ ms。`,
      },
      {
        label: '算出停等协议的信道利用率',
        score: 3,
        answer: `忽略确认帧的发送时延，一次「发数据帧并收到确认」的周期为 $T_f+RTT$，其中 $RTT=2t_p=20$ ms。信道利用率 $U=\\dfrac{T_f}{T_f+RTT}=\\dfrac{1}{1+20}=\\dfrac{1}{21}\\approx4.8\\%$。`,
      },
      {
        label: '算出使利用率达到 100% 的最小发送窗口',
        score: 3,
        answer: `窗口为 $W$ 时一个周期内可连续发送 $W$ 个数据帧，占用信道 $W T_f$，要填满整个周期须 $W T_f\\ge T_f+RTT$，故 $W\\ge\\dfrac{T_f+RTT}{T_f}=\\dfrac{1+20}{1}=21$。窗口只能取整数并向上取整，所以发送窗口至少为 $21$ 个数据帧。`,
      },
      {
        label: '判断 3 位序号是否够用并算出 GBN 所需的序号位数',
        score: 2,
        answer: `后退 N 帧协议要求发送窗口 $W\\le2^n-1$。$n=3$ 时 $W\\le2^3-1=7<21$，远不能满足要求，序号回绕会使新旧帧无法区分。由 $2^n-1\\ge21$ 得 $2^n\\ge22$，故 $n\\ge5$；$n=4$ 时上限只有 $15$，$n=5$ 时上限为 $2^5-1=31\\ge21$，所以序号字段至少需要 $5$ 位。`,
      },
    ],
    solution: `## 参考解答

**第 1 问：两种基本时延**

单程传播时延：

$t_p=\\dfrac{2000\\times10^3\\ \\text{m}}{2\\times10^8\\ \\text{m/s}}=0.01\\ \\text{s}=10$ ms。

一个数据帧的发送时延：

$T_f=\\dfrac{1000\\ \\text{bit}}{1\\times10^6\\ \\text{bit/s}}=0.001\\ \\text{s}=1$ ms。

**第 2 问：停等协议的信道利用率**

确认帧的发送时延忽略不计，一次「发送数据帧—收到确认」的周期为 $T_f+RTT$，而往返时间

$RTT=2t_p=2\\times10=20$ ms。

信道利用率为一个周期中真正用于发送数据帧的时间比例：

$U=\\dfrac{T_f}{T_f+RTT}=\\dfrac{1}{1+20}=\\dfrac{1}{21}\\approx4.8\\%$。

可见在长时延链路上停等协议效率极低，绝大部分时间信道处于空闲等待状态。

**第 3 问：使利用率达到 100% 的最小窗口**

发送窗口为 $W$ 时，一个周期内甲可以连续发送 $W$ 个数据帧而不必等确认，占用信道的时间为 $W T_f$。只要

$W T_f\\ge T_f+RTT$

信道就不会出现空闲，利用率可达 $100\\%$。于是

$W\\ge\\dfrac{T_f+RTT}{T_f}=\\dfrac{1+20}{1}=21$。

窗口必须取整数并向上取整，所以发送窗口至少为 **21 个数据帧**。

**第 4 问：GBN 所需的序号位数**

后退 N 帧协议的发送窗口受序号空间限制，必须满足 $W\\le2^n-1$：

- 当 $n=3$ 时 $W\\le2^3-1=7$，而需要 $21$，不满足要求，序号很快回绕，接收方无法区分新帧与重传的旧帧；
- 由 $2^n-1\\ge21$ 得 $2^n\\ge22$，$n=4$ 时 $2^4-1=15<21$，仍不够；$n=5$ 时 $2^5-1=31\\ge21$，满足要求。

所以序号字段至少需要 **5 位**。`,
    pitfalls: `> 信道利用率的分母是 $T_f+RTT$ 而不是 $T_f+t_p$，往返时延一定要算两倍单程传播时延（本例 $RTT=20$ ms），否则利用率会算成 $1/11$；求最小窗口时 $21$ 要向上取整，且 GBN 的窗口上限是 $2^n-1$，不要与选择重传协议的 $2^{n-1}$ 混淆。`,
  },
  {
    id: 'e-cn-tcp-02',
    subject: 'cn',
    chapter: '传输层',
    difficulty: 2,
    minutes: 12,
    tags: ['TCP', '三次握手', '四次挥手', 'MSL'],
    stem: `主机 A 主动与服务器 B 建立一条 TCP 连接，连接建立后 A 向 B 发送 $400$ 字节数据，随后 B 向 A 发送 $200$ 字节数据。数据传送结束后，由 A 主动发起连接释放。设 A 选择的初始序号 $seq_A=1000$，B 选择的初始序号 $seq_B=2000$；按 TCP 规则，SYN 和 FIN 各占用 $1$ 个序号，不携带数据的纯确认报文段不占用序号；确认号填写为期望收到的下一个字节的序号。又设报文段最长存活时间 $MSL=30$ s。

1. 写出三次握手过程中三个报文段的序号与确认号，并指出 SYN 对序号的影响。
2. 写出数据传送阶段四个报文段（A 发数据、B 确认、B 发数据、A 确认）的序号与确认号。
3. 写出四次挥手过程中四个报文段的序号与确认号。
4. 说明 A 在发送完最后一个报文段后还需要等待多长时间才能进入 CLOSED 状态，并说明等待这段时间的原因。`,
    points: [
      {
        label: '写出三次握手三个报文段的序号与确认号',
        score: 3,
        answer: `第一段 A→B：$SYN=1$，$seq=1000$，不携带数据，SYN 占用 $1000$ 这一个序号，故 A 下一个待发序号为 $1001$。第二段 B→A：$SYN=1,ACK=1$，$seq=2000$，$ack=1001$，SYN 占用 $2000$，B 下一个待发序号为 $2001$。第三段 A→B：$ACK=1$，$seq=1001$，$ack=2001$，不携带数据、不占序号，A 下一个待发序号仍为 $1001$。`,
      },
      {
        label: '写出数据传送阶段四个报文段的序号与确认号',
        score: 2,
        answer: `A→B 数据：$seq=1001$，携带 $400$ 字节，字节编号 $1001\\sim1400$，A 下一个待发序号为 $1401$；B→A 确认：$seq=2001$，$ack=1401$；B→A 数据：$seq=2001$，携带 $200$ 字节，字节编号 $2001\\sim2200$，B 下一个待发序号为 $2201$；A→B 确认：$seq=1401$，$ack=2201$。`,
      },
      {
        label: '写出四次挥手四个报文段的序号与确认号',
        score: 3,
        answer: `A→B：$FIN=1,ACK=1$，$seq=1401$，$ack=2201$，FIN 占用 $1401$，A 下一个待发序号变为 $1402$；B→A：$ACK=1$，$seq=2201$，$ack=1402$（纯确认不占序号）；B→A：$FIN=1,ACK=1$，$seq=2201$，$ack=1402$，FIN 占用 $2201$，B 下一个待发序号变为 $2202$；A→B：$ACK=1$，$seq=1402$，$ack=2202$。`,
      },
      {
        label: '说明序号占用规则（含 B 的 FIN 与 ACK 序号相同的原因）',
        score: 2,
        answer: `SYN 和 FIN 虽然不携带数据，但各要占用 $1$ 个序号，因为它们需要被确认；不携带数据的纯确认报文段（如三次握手的第三段、四次挥手第二、四段）不占用序号。因此 B 发送的纯 ACK 不消耗序号，紧接其后的 FIN 报文段序号仍为 $2201$，两者序号相同是正确的。`,
      },
      {
        label: '算出 2MSL 的等待时长并说明原因',
        score: 2,
        answer: `A 发送完最后一个 ACK 后要等待 $2MSL=2\\times30=60$ s 才进入 CLOSED 状态。原因有两点：一是若该 ACK 丢失，B 会超时重传 FIN，A 在 $2MSL$ 内仍能收到并重发 ACK，保证 B 正常关闭；二是让本次连接中滞留在网络中的旧报文段全部消亡，避免它们干扰之后建立的、使用相同端口的新连接。`,
      },
    ],
    solution: `## 参考解答

**第 1 问：三次握手**

- 第一段 A→B：$SYN=1$，$seq=1000$。SYN 报文段不携带数据，但 SYN 本身占用 $1$ 个序号，所以 A 的下一个待发序号为 $1001$。
- 第二段 B→A：$SYN=1,ACK=1$，$seq=2000$，$ack=1001$（期望收到 A 的第 $1001$ 号字节）。SYN 占用 $2000$，B 的下一个待发序号为 $2001$。
- 第三段 A→B：$ACK=1$，$seq=1001$，$ack=2001$。该段不携带数据也不占序号，A 的下一个待发序号仍为 $1001$。

**第 2 问：数据传送阶段**

- A→B 发送数据：$seq=1001$，长度 $400$ 字节，字节编号为 $1001\\sim1400$，所以 A 的下一个待发序号为 $1401$；
- B→A 确认：$seq=2001$，$ack=1401$；
- B→A 发送数据：$seq=2001$，长度 $200$ 字节，字节编号为 $2001\\sim2200$，所以 B 的下一个待发序号为 $2201$；
- A→B 确认：$seq=1401$，$ack=2201$。

**第 3 问：四次挥手（A 主动关闭）**

- 第一段 A→B：$FIN=1,ACK=1$，$seq=1401$，$ack=2201$。FIN 占用 $1401$ 这一个序号，A 的下一个待发序号变为 $1402$；
- 第二段 B→A：$ACK=1$，$seq=2201$，$ack=1402$。这是不携带数据的纯确认，不占序号，B 的下一个待发序号仍为 $2201$；
- 第三段 B→A：$FIN=1,ACK=1$，$seq=2201$，$ack=1402$。B 的数据已发完，FIN 占用 $2201$，B 的下一个待发序号变为 $2202$；
- 第四段 A→B：$ACK=1$，$seq=1402$，$ack=2202$。

**第 4 问：2MSL 等待**

A 在发出最后一个 ACK 后进入 TIME_WAIT 状态，需等待

$2MSL=2\\times30=60$ s

才能进入 CLOSED 状态。这样安排有两个目的：

1. 若 A 发出的最后一个 ACK 丢失，B 会因超时重传 FIN。A 在这 $2MSL$ 内仍保留连接状态，可以收到重传的 FIN 并再次确认，保证 B 能够正常释放连接；
2. 经过 $2MSL$，本次连接产生的所有报文段都已在网络中消失，可以防止滞留下来的旧报文段被之后使用相同端口号的新连接误收。`,
    pitfalls: `> SYN 和 FIN 各占用 1 个序号，纯 ACK 不占序号，两者混用会导致后面所有 seq 全部错位；B 的纯确认与紧随其后的 FIN 序号相同（都是 2201）是正确结果，不要误以为写错；2MSL 是 $60$ s 而不是 $30$ s，且两个理由（保证最后 ACK 到达、清除旧报文段）都要写全。`,
  },
  {
    id: 'e-cn-app-01',
    subject: 'cn',
    chapter: '应用层',
    difficulty: 2,
    minutes: 12,
    tags: ['DNS', 'HTTP', 'RTT', 'SMTP'],
    stem: `主机 H 用浏览器访问网址 http://www.example.com/index.html，该网页中内嵌 $5$ 个对象（图片），这些对象与主页面存放在同一台 Web 服务器上。已知：

- H 与本地域名服务器之间的往返时间 $RTT_D=4$ ms；
- 本地域名服务器中没有任何缓存，它采用迭代查询，依次询问根域名服务器、顶级域名服务器（.com）和 example.com 的权威域名服务器，这三段查询的往返时间分别为 $20$ ms、$30$ ms、$40$ ms；
- H 与 Web 服务器之间的往返时间 $RTT=10$ ms；
- 忽略报文段的发送时延、DNS 报文的传输时延以及各服务器的处理时延。

1. 从 H 发出域名解析请求到取得 Web 服务器的 IP 地址，共需要多长时间？
2. 若浏览器使用 HTTP/1.0 的非持久连接，从域名解析完成到主页面和 $5$ 个对象全部下载完毕，共需要多少个 RTT、多少时间？整个访问过程（含域名解析）共耗时多少？
3. 若改用 HTTP/1.1 的持久连接但不使用流水线，需要多少个 RTT？整个访问过程共耗时多少？
4. 若使用持久连接并采用流水线方式，需要多少个 RTT？整个访问过程共耗时多少？
5. 若稍后 H 的用户要用邮件客户端给远方的朋友发一封信，请说明发送邮件和接收邮件分别使用什么协议，以及邮件在两台邮件服务器之间如何传送。`,
    points: [
      {
        label: '算出域名解析所需的总时间',
        score: 3,
        answer: `本地域名服务器采用迭代查询，依次访问根、顶级和权威域名服务器：$20+30+40=90$ ms；再加上 H 与本地域名服务器之间的一次询问与应答 $RTT_D=4$ ms，域名解析共需 $90+4=94$ ms。`,
      },
      {
        label: '算出非持久连接所需的 RTT 数与总时间',
        score: 3,
        answer: `非持久连接下每个对象都要新建一次 TCP 连接：建立连接需要 $1$ 个 RTT（第三次握手的 ACK 可与 HTTP 请求合并发出），再花 $1$ 个 RTT 请求并接收对象，所以每个对象需要 $2$ 个 RTT。主页面加 $5$ 个对象共 $6$ 个对象，需要 $6\\times2=12$ 个 RTT，即 $12\\times10=120$ ms。加上域名解析的 $94$ ms，整个访问过程共 $94+120=214$ ms。`,
      },
      {
        label: '算出持久连接非流水线方式所需的 RTT 数与总时间',
        score: 2,
        answer: `持久连接只建立一次 TCP 连接：主页面需要 $1$ 个 RTT 建连接加 $1$ 个 RTT 请求响应，共 $2$ 个 RTT；此后 $5$ 个对象在同一连接上串行请求，每个只需 $1$ 个 RTT，共 $5$ 个 RTT。合计 $2+5=7$ 个 RTT，即 $70$ ms。整个访问过程共 $94+70=164$ ms。`,
      },
      {
        label: '算出持久连接流水线方式所需的 RTT 数与总时间',
        score: 2,
        answer: `流水线方式下主页面仍需 $2$ 个 RTT（建连接 $1$ 个、请求响应 $1$ 个）；随后 $5$ 个对象的请求可以一次性连续发出，服务器连续响应，只需 $1$ 个 RTT。合计 $2+1=3$ 个 RTT，即 $30$ ms。整个访问过程共 $94+30=124$ ms。`,
      },
      {
        label: '说明邮件发送与接收所用的协议及服务器间的传送方式',
        score: 2,
        answer: `发送邮件时，用户代理把邮件用 SMTP 推送到发送方邮件服务器，发送方邮件服务器再用 SMTP 把邮件推送到接收方邮件服务器，因此发送方向使用的是推送式协议 SMTP。接收邮件时，用户代理用 POP3 或 IMAP 从接收方邮件服务器上把邮件拉取到本地，因此接收方向使用的是拉取式协议 POP3（或 IMAP）。`,
      },
    ],
    solution: `## 参考解答

**第 1 问：域名解析时间**

本地域名服务器采用迭代查询，自己要依次联系三台服务器：

$20+30+40=90$ ms。

解析结果返回给 H 还需要 H 与本地域名服务器之间的一次往返：

$90+RTT_D=90+4=94$ ms。

所以从发出解析请求到取得 IP 地址共需 **94 ms**。

**第 2 问：非持久连接（HTTP/1.0）**

非持久连接下每个对象都要单独建立 TCP 连接，因此每个对象花费 $2$ 个 RTT：$1$ 个 RTT 完成 TCP 三次握手（第三次握手的 ACK 可以与 HTTP 请求一起发出），再 $1$ 个 RTT 完成请求与响应。

主页面 $1$ 个加内嵌对象 $5$ 个，共 $6$ 个对象：

$6\\times2=12$ 个 RTT，即 $12\\times10=120$ ms。

整个访问过程共 $94+120=214$ ms。

**第 3 问：持久连接、非流水线（HTTP/1.1）**

持久连接只需建立一次 TCP 连接：

- 主页面：建连接 $1$ 个 RTT，请求并接收 $1$ 个 RTT，共 $2$ 个 RTT；
- 其余 $5$ 个对象在同一连接上逐个请求，每个 $1$ 个 RTT，共 $5$ 个 RTT。

合计 $2+5=7$ 个 RTT，即 $70$ ms。整个访问过程共 $94+70=164$ ms。

**第 4 问：持久连接、流水线**

主页面仍是 $2$ 个 RTT。所有对象都在同一台服务器上，$5$ 个请求可以连续发出而不必等待前一个响应，服务器依次响应，因此这 $5$ 个对象只需 $1$ 个 RTT。

合计 $2+1=3$ 个 RTT，即 $30$ ms。整个访问过程共 $94+30=124$ ms。

可以看出：非持久连接 $214$ ms 大于持久非流水线 $164$ ms 大于持久流水线 $124$ ms，减少连接建立次数和采用流水线都能明显缩短时延。

**第 5 问：电子邮件所用协议**

- 发送邮件：用户代理用 **SMTP** 把邮件推送到发送方邮件服务器；发送方邮件服务器再用 **SMTP** 把邮件推送到接收方的邮件服务器并存入收件人邮箱。这两个环节都是「推送」方式。
- 接收邮件：收件人的用户代理用 **POP3** 或 **IMAP** 从接收方邮件服务器上读取邮件，属于「拉取」方式。

因此 SMTP 负责发送方向（用户代理到服务器、服务器到服务器），POP3 或 IMAP 负责接收方向（服务器到用户代理）。`,
    pitfalls: `> 非持久连接每个对象要算 $2$ 个 RTT（建连接 $1$ 个加请求响应 $1$ 个），漏掉建连接的 RTT 会把 $12$ 个 RTT 错算成 $6$ 个；三类方式的 RTT 数分别是 $12$、$7$、$3$，流水线方式下 $5$ 个对象只占 $1$ 个 RTT，最后别忘了把 $94$ ms 的域名解析时间加进去。`,
  },
];
