/**
 * 408 知识点刷卡背诵 —— 计算机组成原理种子卡
 *
 * 40 张卡，覆盖 7 个章节：
 * 数据的表示与运算 7 / 存储系统 8 / 指令系统 6 / CPU 与指令周期 6 /
 * 指令流水线 5 / 总线 3 / IO 与中断 5。
 * back 语法限定：## 小标题、- 无序列表、**粗体**、`行内代码`、$行内公式$、$$块公式$$、> 易错。
 */

import type { SeedCard } from '../seed-cards';

export const CO_BANK: SeedCard[] = [
  // ==================== 数据的表示与运算（7 张） ====================
  {
    id: 'co-fixed-point-add-overflow',
    subject: 'co',
    chapter: '数据的表示与运算',
    tags: ['补码', '溢出', '计算'],
    title: '补码加减运算怎么算？溢出如何判定？',
    back: `## 结论
- 补码把减法统一成加法：$[A-B]_{\\text{补}}=[A]_{\\text{补}}+[-B]_{\\text{补}}$，符号位一起参与运算，最高位向外的进位**直接舍去**。
- 只有**同号相加**或**异号相减**才可能溢出，异号相加结果绝不会溢出。
- 一位符号位判据：$V=C_{s}\\oplus C_{1}$，即符号位进位与最高数值位进位**异或**；双符号位时 $01$ 为正溢、$10$ 为负溢。
> 易错：有进位≠溢出，丢掉最高进位恰恰是补码运算的正常现象，要看两个进位是否一致。`,
    importance: 5,
    hint: '同号相加才溢出，进位异或判溢出',
  },
  {
    id: 'co-code-conversion',
    subject: 'co',
    chapter: '数据的表示与运算',
    tags: ['原码', '补码', '移码'],
    title: '由真值如何快速写出原码、反码、补码与移码？',
    back: `## 结论
- **原码**：符号位 $0$ 表示正、$1$ 表示负，数值位放绝对值。
- **反码**：正数与原码相同；负数符号位不变、数值位**逐位取反**。
- **补码**：正数与原码相同；负数可在反码末位加 $1$，也可用速算法"从右往左第一个 $1$ 及其右侧不变，其余取反"。
- **移码**：把补码的符号位取反即可，$[x]_{\\text{移}}=[x]_{\\text{补}}\\oplus 2^{n-1}$。
> 易错：$-128$ 的 $8$ 位补码是 $10000000$，它**没有**对应的原码和反码，故补码比原码多表示一个数。`,
    importance: 4,
    hint: '负数取反加一得补码，符号位取反得移码',
  },
  {
    id: 'co-ieee754-single-double',
    subject: 'co',
    chapter: '数据的表示与运算',
    tags: ['浮点数', 'IEEE754'],
    title: 'IEEE 754 单精度与双精度的字段划分和真值公式有何异同？',
    back: `## 结论
- 单精度 $32$ 位 = $1$ 位符号 $+8$ 位阶码 $+23$ 位尾数，阶码偏移量 $127$。
- 双精度 $64$ 位 = $1$ 位符号 $+11$ 位阶码 $+52$ 位尾数，偏移量 $1023$。
- 规格化数的真值公式完全相同：$x=(-1)^{S}\\times 1.M\\times 2^{E-bias}$，尾数都**隐含最高位 $1$**，故精度比存储位数多一位。
> 易错：偏移量取 $2^{k-1}-1$ 而不是 $2^{k-1}$；**阶码位数决定表示范围，尾数位数决定精度**。`,
    importance: 5,
    hint: '阶码管范围，尾数管精度',
  },
  {
    id: 'co-float-normalize',
    subject: 'co',
    chapter: '数据的表示与运算',
    tags: ['浮点数', '规格化'],
    title: '浮点数为什么要规格化？左规与右规分别在什么时候用？',
    back: `## 结论
- **规格化**要求尾数最高数值位是有效数字，使同一数的表示**唯一**，并让尾数有效位数被充分利用。
- IEEE 754 用**隐含最高位 $1$** 实现规格化，所以尾数实际精度比存储的位数多一位。
- 尾数运算后若形如 $00.0\\cdots$ 或 $11.1\\cdots$，需**左规**：尾数左移、阶码相应减小。
- 若尾数绝对值超过 $1$（形式溢出），需**右规**：尾数右移一位、阶码加 $1$。
> 易错：右规最多只做一次；阶码溢出才是真溢出，尾数溢出只是形式问题。`,
    importance: 4,
    hint: '左规阶码减，右规阶码加',
  },
  {
    id: 'co-float-add-steps',
    subject: 'co',
    chapter: '数据的表示与运算',
    tags: ['浮点数', '加减运算'],
    title: '浮点数加减运算分哪几步？每一步的要点是什么？',
    back: `## 结论
- 五步：**对阶 → 尾数加减 → 规格化 → 舍入 → 判溢出**。
- **对阶**遵循"小阶向大阶看齐"，把阶码小的尾数右移、阶码补到与大阶相同，移出的低位被舍弃。
- 舍入常用**就近舍入（0 舍 1 入）**或恒置 $1$ 法；规格化后再舍入可能又需规格化，要循环判断。
- 最后检查阶码是否上溢（真溢出）或下溢（按机器零处理）。
> 易错：对阶绝不能把大阶的尾数左移；先对阶后加减的次序不可颠倒，否则尾数无法直接对齐。`,
    importance: 5,
    hint: '对阶小向大，末尾判溢出',
  },
  {
    id: 'co-bcd-check-code',
    subject: 'co',
    chapter: '数据的表示与运算',
    tags: ['BCD', '校验码', '海明码'],
    title: 'BCD 码与奇偶校验、海明校验分别有什么能力？',
    back: `## 结论
- **BCD 码**用 $4$ 位二进制表示一位十进制数：$8421$ 码只取前 $10$ 个编码，$1010\\sim1111$ 为非法码；余三码等于 $8421$ 码加 $3$。
- **奇偶校验**只增加 $1$ 位校验位，码距为 $2$，能**检出奇数个错**但无法定位，检不出偶数个错。
- **海明码**把校验位放在 $2^{i}$ 位上，校验位个数满足 $2^{k}\\ge n+k+1$，码距为 $3$，可**检错并纠正一位错**。
> 易错：码距越大检错纠错能力越强；只检错不纠错需码距 $2$，纠一位错需码距 $3$。`,
    importance: 4,
    hint: '奇偶只检错，海明能纠一位',
  },
  {
    id: 'co-endian-alignment',
    subject: 'co',
    chapter: '数据的表示与运算',
    tags: ['大小端', '边界对齐'],
    title: '大端与小端存放有什么区别？为什么要边界对齐？',
    back: `## 结论
- **大端**：数据的高位字节存放在低地址，符合人的书写顺序；**小端**：低位字节存放在低地址。
- 同一数据按两种端序存放时**字节顺序相反，但每个字节内部的位序不变**。
- **边界对齐**要求多字节数据的起始地址是其长度的整数倍，这样一次访存就能取完；否则可能跨两个存储字，需要多次访存。
> 易错：结构体为满足对齐会插入填充字节，故其总长度大于各成员长度之和，是典型的"以空间换时间"。`,
    importance: 4,
    hint: '大端高位在低地址，对齐为省访存',
  },

  // ==================== 存储系统（8 张） ====================
  {
    id: 'co-memory-hierarchy-locality',
    subject: 'co',
    chapter: '存储系统',
    tags: ['存储层次', '局部性'],
    title: '存储层次为什么能兼顾速度与容量？局部性原理指什么？',
    back: `## 结论
- 层次自顶向下：**寄存器 → Cache → 主存 → 辅存**，越往上越快、越小、越贵。
- 依据是**局部性原理**：**时间局部性**指刚被访问的数据很可能马上又被访问（如循环变量）；**空间局部性**指相邻地址的数据很可能接着被访问（如数组、顺序执行的指令）。
- 上层是下层内容的**副本**，命中时由上层直接服务，缺失才向下层取，于是整体速度接近上层、容量接近下层。
> 易错：Cache 的调度对程序员**透明**，虚存的页调度由操作系统参与，两者不能混为一谈。`,
    importance: 4,
    hint: '时间空间局部性，上层下层做副本',
  },
  {
    id: 'co-sram-dram-rom',
    subject: 'co',
    chapter: '存储系统',
    tags: ['SRAM', 'DRAM', '刷新'],
    title: 'SRAM、DRAM 与 ROM 各有什么特性？DRAM 为什么要刷新？',
    back: `## 结论
- **SRAM**用触发器存储，速度快、功耗大、集成度低、**无需刷新**，常用作 Cache。
- **DRAM**用电容存储，电荷会泄漏，必须**定期刷新**（通常每 $2$ 毫秒一遍），刷新以**行**为单位；速度较慢但集成度高、价格低，用作主存。
- **ROM**掉电不丢失，包括 PROM、EPROM、EEPROM 与 Flash，常用来固化 BIOS 等程序。
> 易错：刷新操作由存储器控制器完成、按行进行，会占用访存时间但不改变数据；DRAM 是**破坏性读出**，读后要重写。`,
    importance: 5,
    hint: 'SRAM 不刷新，DRAM 按行刷新',
  },
  {
    id: 'co-memory-chip-address-range',
    subject: 'co',
    chapter: '存储系统',
    tags: ['主存扩展', '地址分配', '计算'],
    title: '由芯片规格如何计算所需片数与各片的地址范围？',
    back: `## 结论
- 片数 $=\\dfrac{\\text{主存总容量}}{\\text{单片容量}}$；位扩展看数据线位数差，字扩展看地址单元数差。
- 字扩展时**低位地址**接各芯片的地址引脚，**高位地址**经译码器产生片选信号。
- 例：用 $16\\mathrm{K}\\times 8$ 位芯片组成 $64\\mathrm{K}\\times 8$ 位主存，需 $4$ 片，用 $A_{15}A_{14}$ 译码。
- 片选编码为 $00$ 的那片，地址范围是 $0000\\mathrm{H}\\sim 3\\mathrm{FFF}\\mathrm{H}$。
> 易错：$16\\mathrm{K}$ 的芯片只需 $14$ 根地址线，多出的高位地址绝不能接进芯片地址引脚，它属于片选地址。`,
    importance: 5,
    hint: '低地址进芯片，高地址做片选',
  },
  {
    id: 'co-cache-hit-ratio',
    subject: 'co',
    chapter: '存储系统',
    tags: ['Cache', '计算'],
    title: 'Cache 命中率与平均访问时间怎么算？',
    back: `## 结论
- 设访问 Cache 次数为 $N_c$、访问主存次数为 $N_m$，则命中率 $H=\\dfrac{N_c}{N_c+N_m}$，缺失率 $M=1-H$。
- 若 Cache 与主存**同时启动**，平均访问时间 $T_a=H\\cdot T_c+(1-H)\\cdot T_m$。
- 若必须**先查 Cache、未命中再访存**，则 $T_a=T_c+M\\cdot T_m$，此时还要算上查 Cache 的那一拍。
- 访问效率 $e=\\dfrac{T_c}{T_a}$，$H$ 越高越接近 $1$。
> 易错：$H$、$M$ 都要用**小数**代入，别把百分数直接乘；两个公式对应不同的访存过程，先看题目描述再套。`,
    importance: 5,
    hint: 'H 用小数，缺失率别忘 1-H',
  },
  {
    id: 'co-cache-address-fields',
    subject: 'co',
    chapter: '存储系统',
    tags: ['Cache', '地址划分', '计算'],
    title: 'Cache 的地址字段如何划分？标记、组号、块内地址各几位？',
    back: `## 结论
- 主存地址从低位到高位依次是**块内地址、行号（组号）、标记**。
- 块内地址位数 $b=\\log_{2}(\\text{块大小})$，是三种映射方式都必须有的字段。
- **直接映射**：行号位数 $=\\log_{2}(\\text{行数})$，标记位数 = 地址总位数 $-$ 行号位数 $-$ $b$。
- **组相联**：组号位数 $=\\log_{2}(\\text{组数})$，组内相联度只增加比较器数量，**不占地址位**；全相联没有行号与组号字段。
> 易错：相联度翻倍会使组数减半、组号位少一位，于是**标记位反而变长**，别把相联度当成地址字段。`,
    importance: 5,
    hint: '标记加组号加块内，相联度不占位',
  },
  {
    id: 'co-cache-write-replace',
    subject: 'co',
    chapter: '存储系统',
    tags: ['Cache', '写策略', '替换算法'],
    title: 'Cache 的替换算法与写策略各有哪些？写直达和写回怎么选？',
    back: `## 结论
- **替换算法**：随机、FIFO、LRU、LFU，其中 LRU 最符合时间局部性、命中率较高，但需要额外硬件记录访问历史。
- **写命中**：**写直达**同时修改 Cache 与主存，一致性好但访存频繁，常配写缓冲；**写回**只改 Cache 并置**脏位**，被替换时才写回主存，省访存但一致性差。
- **写不命中**：**写分配**先把主存块调入再写，常与写回搭配；**非写分配**直接写主存，常与写直达搭配。
> 易错：替换算法只在**组相联和全相联**中才需要；写直达法的 Cache 与主存内容始终一致，写回法则可能不一致。`,
    importance: 5,
    hint: '写直达配写不分配，写回配写分配',
  },
  {
    id: 'co-tlb-page-table-cache',
    subject: 'co',
    chapter: '存储系统',
    tags: ['虚拟存储', 'TLB', '页表'],
    title: 'TLB、页表与 Cache 在地址变换中如何配合？',
    back: `## 结论
- 虚地址分为**虚页号 + 页内偏移**，页内偏移在整个变换过程中**保持不变**。
- 变换顺序：用虚页号查 **TLB**，命中直接得到物理页号；未命中则查主存中的**页表**（可能是多级页表），并把该页表项**回填 TLB**。
- 页表项有效位为 $0$ 表示该页不在主存，触发**缺页**，需从辅存调入后再访问。
- 得到物理地址后，再用它去访问 **Cache 与主存**取数据。
> 易错：TLB 中存放的是**页表项的副本**；缺页时 TLB 必然缺失，但 TLB 缺失不一定是缺页。`,
    importance: 5,
    hint: '偏移不变，先快表后页表',
  },
  {
    id: 'co-interleaving-bandwidth',
    subject: 'co',
    chapter: '存储系统',
    tags: ['多模块', '交叉编址', '带宽'],
    title: '多模块交叉编址存储器如何提高访存带宽？',
    back: `## 结论
- **顺序编址（高位交叉）**：同一模块内地址连续，连续访问都落在同一模块，只能扩大容量，**不能提高带宽**。
- **交叉编址（低位交叉）**：地址低位选择模块、高位选择模块内地址，连续地址轮流落到不同模块，可实现**流水线式的连续访存**。
- 设模块数为 $m$、存取周期为 $T$、总线传送周期为 $r$，要不间断供数需满足 $m\\ge T/r$。
- $m$ 体低位交叉的带宽极限可达单模块的 $m$ 倍。
> 易错：只有**低位交叉**才能提速；交叉存储器要求各模块分时错开启动，不是同时传送。`,
    importance: 4,
    hint: '低位交叉才提速，模块数大于 T 比 r',
  },

  // ==================== 指令系统（6 张） ====================
  {
    id: 'co-instruction-format-opcode',
    subject: 'co',
    chapter: '指令系统',
    tags: ['指令格式', '操作码'],
    title: '指令格式包含哪些字段？操作码扩展的基本思路是什么？',
    back: `## 结论
- 一条指令由**操作码 OP + 地址码 A + 寻址方式字段**组成，地址码可有 $0\\sim 4$ 个。
- 操作码位数 $n$ 决定最多 $2^{n}$ 条指令：**定长操作码**译码简单但浪费编码，**扩展操作码**让地址码少的指令使用更长的操作码。
- 扩展的铁律：**短码不能是长码的前缀**，否则译码歧义；各类指令条数之和不能超过可用编码总数。
> 易错：地址码个数不同会导致指令字长不同；扩展操作码是在**有限字长**内兼顾指令条数与寻址范围的折中。`,
    importance: 4,
    hint: '短码不做长码前缀',
  },
  {
    id: 'co-addressing-effective-address',
    subject: 'co',
    chapter: '指令系统',
    tags: ['寻址方式', '有效地址', '计算'],
    title: '各种寻址方式的有效地址如何计算？各需访存几次？',
    back: `## 结论
- **立即寻址**：形式地址就是操作数，取指后即可用，**不再访存**。
- **直接寻址**：$EA=A$，取操作数访存 $1$ 次；**间接寻址**：$EA=(A)$，每级间址访存 $1$ 次，一次间址共访存 $2$ 次。
- **寄存器寻址**：操作数在寄存器中，不访存；**寄存器间接寻址**：$EA=(R)$，访存 $1$ 次。
- **相对寻址** $EA=(PC)+A$，**基址寻址** $EA=(BR)+A$（基址由系统给定、面向操作系统），**变址寻址** $EA=(IX)+A$（变址值用户可改、面向数组）。
> 易错：相对寻址中的 $PC$ 已是**下一条指令**的地址；比较访存次数时要把取指与取操作数分开计算。`,
    importance: 5,
    hint: '立即不访存，间接多一次',
  },
  {
    id: 'co-risc-why-pipeline-friendly',
    subject: 'co',
    chapter: '指令系统',
    tags: ['RISC', 'CISC', '流水线'],
    title: '为什么 RISC 比 CISC 更适合做指令流水线？',
    back: `## 结论
- RISC 指令**长度固定、格式规整**，取指的同时就能完成译码和读寄存器，各段负载均衡。
- RISC 采用 \`Load/Store\` 结构，**只有访存指令访问主存**，运算都在寄存器间进行，访存段不易成为瓶颈。
- RISC 大多数指令**一个时钟周期**完成，控制器多用**硬布线**，便于按段切分流水寄存器。
- CISC 指令长度可变、功能复杂，各条指令执行时间差异大，段间严重不平衡，常需多拍甚至微程序完成。
> 易错：CISC 并非不能流水，只是要付出更复杂的取指对齐与译码代价。`,
    importance: 4,
    hint: '定长单周期，访存只 load store',
  },
  {
    id: 'co-instruction-word-length',
    subject: 'co',
    chapter: '指令系统',
    tags: ['指令字长', '机器字长'],
    title: '指令字长、机器字长与存储字长是什么关系？',
    back: `## 结论
- **机器字长**是 CPU 一次能处理的二进制位数，等于通用寄存器与 ALU 的宽度，直接决定运算精度。
- **指令字长**是一条指令的位数；**存储字长**是一个存储单元能存放的位数。
- 三者可以互不相等，但常令指令字长等于存储字长的整数倍，便于按存储字取指：半字长指令取一次，双字长指令要取两次。
> 易错：机器字长决定数据总线宽度与寄存器位数；指令字长**可变**时取指必须先找到指令边界，这正是 CISC 流水的难点。`,
    importance: 4,
    hint: '机器字长看寄存器，指令字长看指令',
  },
  {
    id: 'co-variable-length-fetch-align',
    subject: 'co',
    chapter: '指令系统',
    tags: ['变长指令', '对齐', '取指'],
    title: '变长指令如何确定指令边界？为什么要求指令对齐？',
    back: `## 结论
- 变长指令必须先取出足够字节，再由**操作码**判定整条指令的长度，因此取指可能跨存储字、分多次访存。
- **指令对齐**指指令起始地址是字长或 $2$ 的幂次的整数倍，不足处用填充字节补齐，例如定长 $4$ 字节的指令按 $4$ 字节边界存放。
- 对齐后一条指令不会跨越两个存储字，一次访存即可取完，流水线的取指段才稳定。
> 易错：定长指令天然对齐、无需填充；变长指令未对齐时，取指要多等一次访存，效果等同于插入气泡。`,
    importance: 3,
    hint: '先取字节再判长度，对齐才不跨字',
  },
  {
    id: 'co-opcode-extension-calc',
    subject: 'co',
    chapter: '指令系统',
    tags: ['扩展操作码', '计算'],
    title: '扩展操作码的编码题如何计算各类指令的条数上限？',
    back: `## 结论
- 设指令字长 $16$ 位、每个地址码 $4$ 位，则三地址指令的操作码只有 $4$ 位，最多 $2^{4}=16$ 条。
- 若三地址指令只用了 $m$ 个编码，余下的 $2^{4}-m$ 个编码就作为**扩展前缀**，每条前缀再接 $4$ 位地址码，可构成 $(2^{4}-m)\\times 2^{4}$ 条二地址指令。
- 逐层类推：下一层条数上限 = 上一层**剩余编码数** $\\times 2^{\\text{本层新增编码位数}}$。
> 易错：扩展要牺牲编码做标志位，实际可用条数比理论最大值少；算完必须验证各类指令条数总和不超过编码总数。`,
    importance: 4,
    hint: '剩余编码乘 2 的位数次方',
  },

  // ==================== CPU 与指令周期（6 张） ====================
  {
    id: 'co-timing-control-modes',
    subject: 'co',
    chapter: 'CPU 与指令周期',
    tags: ['时序', '控制方式'],
    title: 'CPU 的时序控制方式有哪几种？同步与异步控制怎么区分？',
    back: `## 结论
- **同步控制**：所有机器周期都按统一的时钟节拍推进，每个微操作在固定节拍完成；电路简单，但节拍长度要按最慢部件确定，快部件被拖慢。
- **异步控制**：没有统一节拍，靠**应答（握手）信号**决定下一步何时开始，各操作时间可长可短，效率高但控制电路复杂。
- **联合控制**：多数操作同步，少数耗时长的操作（乘除、访存）采用异步，是实际机器常用的折中方案。
> 易错：同步方式的代价是"以慢就快"、时间浪费；异步方式时序设计复杂，对信号传输延迟要求更严。`,
    importance: 3,
    hint: '同步统一节拍，异步靠应答',
  },
  {
    id: 'co-fetch-indirect-cycle',
    subject: 'co',
    chapter: 'CPU 与指令周期',
    tags: ['取指周期', '间址周期', '微操作'],
    title: '取指周期与间址周期各完成哪些微操作？',
    back: `## 结论
- **取指周期**：$PC$ 送 $MAR$；发读命令，$M(MAR)$ 送 $MDR$；$MDR$ 送 $IR$；$PC$ 加 $1$ 指向下一条指令。
- **间址周期**：把 $IR$ 中的形式地址字段送 $MAR$；发读命令，$M(MAR)$ 送 $MDR$；再把 $MDR$ 的内容送回 $IR$ 的地址字段，得到**有效地址**。
- 取指周期**每条指令都必须有**；间址周期只在指令采用间接寻址时才出现，多次间址就重复多次。
> 易错：取指是"取出指令本身"，间址是"用形式地址去取操作数的地址"；有间址时访存次数比直接寻址多一次。`,
    importance: 4,
    hint: '取指每条都有，间址按需重复',
  },
  {
    id: 'co-datapath-microoperation',
    subject: 'co',
    chapter: 'CPU 与指令周期',
    tags: ['数据通路', '微操作'],
    title: '数据通路与微操作是什么关系？',
    back: `## 结论
- **数据通路**是数据在各部件之间传送所经过的路径，由 ALU、寄存器、内部总线与多路选择器构成。
- **微操作**是在数据通路上一步完成的**基本传送或运算**，如 $PC\\to MAR$、$MDR\\to IR$、$R_1+R_2\\to R_3$。
- 一条机器指令可分解为若干微操作序列，控制信号负责选通通路与打开寄存器写入端，所以微操作是控制器设计的**最小单位**。
> 易错：目的端由控制信号决定，同一时刻**不允许两条通路写同一个寄存器**，否则产生数据冲突。`,
    importance: 4,
    hint: '微操作是通路上的最小一步',
  },
  {
    id: 'co-microprogram-vs-hardwired',
    subject: 'co',
    chapter: 'CPU 与指令周期',
    tags: ['微程序', '硬布线', '控制器'],
    title: '微程序控制与硬布线控制各有什么优缺点？',
    back: `## 结论
- **硬布线控制**用组合逻辑门电路直接产生控制信号，**速度最快**，但设计复杂、修改困难，适合指令规整的 RISC。
- **微程序控制**把每条机器指令编成一段**微程序**存放在控制存储器 CM 中，设计规整、便于扩充与修改，但要逐条读取微指令，**速度较慢**，适合 CISC。
- 微指令由操作控制字段与顺序控制字段组成：一条机器指令对应一段微程序，一条微指令对应若干微操作。
> 易错：微程序存放在**控制存储器**中而不是主存；微指令操作的对象是数据通路，与用户程序的指令不是一回事。`,
    importance: 4,
    hint: '硬布线快而难改，微程序慢而易扩',
  },
  {
    id: 'co-controller-registers',
    subject: 'co',
    chapter: 'CPU 与指令周期',
    tags: ['控制器', '寄存器'],
    title: '控制器的组成与 PC、IR、MAR、MDR 各自的作用是什么？',
    back: `## 结论
- 控制器由**程序计数器 PC、指令寄存器 IR、时序电路与微操作信号发生器**组成，负责按节拍发出控制信号。
- **PC**存放下一条指令地址，位数等于主存地址位数，取指后自动加上指令长度。
- **IR**存放当前正在执行的指令，位数等于指令字长。
- **MAR**存放要访问的主存单元地址，**MDR**存放与主存交换的数据，其位数由**存储字长**决定。
> 易错：取指的数据流是 $PC\\to MAR\\to \\text{主存}\\to MDR\\to IR$；MDR 宽度不等于机器字长，别按 CPU 位数去填。`,
    importance: 4,
    hint: 'PC 存地址，IR 存指令，MDR 看存储字长',
  },
  {
    id: 'co-exception-interrupt-response',
    subject: 'co',
    chapter: 'CPU 与指令周期',
    tags: ['异常', '中断响应', '中断隐指令'],
    title: '异常与中断在什么时刻被响应？响应时硬件做了哪几件事？',
    back: `## 结论
- 外部**中断**通常在一条指令**执行结束之后**才被检测并响应；**异常**由指令执行**过程中**的内部条件触发，如缺页、除零、非法操作码。
- 响应时硬件自动执行**中断隐指令**（它不是程序中的指令，用户无法编程调用），完成三件事：**关中断、保存断点与程序状态字、按中断向量送 PC** 转入服务程序。
- 通用寄存器等现场的保护由**中断服务程序**用指令完成。
> 易错：保存的断点是**下一条**待执行指令的地址；关中断是为了让"保存现场"过程不被打断。`,
    importance: 5,
    hint: '隐指令关中断、存断点、取向量',
  },

  // ==================== 指令流水线（5 张） ====================
  {
    id: 'co-pipeline-throughput-efficiency',
    subject: 'co',
    chapter: '指令流水线',
    tags: ['流水线', '计算'],
    title: '流水线的吞吐率、加速比与效率怎么算？',
    back: `## 结论
- 设 $k$ 段流水线、每段一个时钟周期 $\\Delta t$、共执行 $n$ 条指令，总时间 $T=(k+n-1)\\Delta t$。
- **吞吐率** $TP=\\dfrac{n}{(k+n-1)\\Delta t}$，理想极限是 $\\dfrac{1}{\\Delta t}$。
- **加速比** $S=\\dfrac{nk}{k+n-1}$，极限为段数 $k$；**效率** $E=\\dfrac{S}{k}=\\dfrac{n}{k+n-1}$，极限为 $1$。
- 时钟周期 $\\Delta t$ 取**最慢一段的逻辑延迟**加流水寄存器延迟。
> 易错：装满流水线需要 $k-1$ 个周期；段数越多极限加速比越高，但寄存器开销与段间不平衡会吃掉收益。`,
    importance: 5,
    hint: '总时间 k 加 n 减一，加速比极限是 k',
  },
  {
    id: 'co-pipeline-forwarding-load-use',
    subject: 'co',
    chapter: '指令流水线',
    tags: ['数据冒险', '转发', '阻塞'],
    title: '转发能解决哪些数据冒险？load-use 为什么必须阻塞？',
    back: `## 结论
- **转发（旁路）**把运算结果从流水寄存器直接送到后面指令所需的入口，使相邻的 RAW 冒险不必等写回寄存器，省下等待时间。
- **load-use 冒险**中前一条是取数指令，数据要到 **MEM 段末**才拿到，而后续指令在 **EX 段初**就要使用，转发也来不及，只能**阻塞一拍**再转发。
- 相邻两条 ALU 运算指令之间的冲突可用转发解决，也可利用寄存器堆"前半拍写、后半拍读"化解。
> 易错：转发只解决数据冒险，对结构冒险与控制冒险无效；阻塞会插入气泡，使实际 CPI 大于 $1$。`,
    importance: 5,
    hint: '转发解决 RAW，load-use 必阻塞一拍',
  },
  {
    id: 'co-superscalar-superpipeline',
    subject: 'co',
    chapter: '指令流水线',
    tags: ['超标量', '动态调度'],
    title: '超标量、超流水与动态流水线有什么区别？',
    back: `## 结论
- **超标量**：每个时钟周期**并发发射多条**指令，依靠多套功能部件与多发射端口提高并行度。
- **超流水**：把流水段继续细分以提高主频，每拍仍只发射一条指令，用"更细的流水"换取速度。
- **动态流水线**：由**硬件在运行时**调度指令（乱序执行、记分牌、Tomasulo），允许越过被阻塞的指令继续发射无关指令。
- 超标量可静态调度（编译器排）也可动态调度（硬件排）。
> 易错：超标量靠**硬件**发现并行性，VLIW 靠**编译器**在指令中显式编码并行性，两者不是一回事。`,
    importance: 3,
    hint: '超标量多发射，超流水细分段',
  },
  {
    id: 'co-pipeline-register-role',
    subject: 'co',
    chapter: '指令流水线',
    tags: ['流水寄存器', '时序'],
    title: '流水线寄存器起什么作用？为什么不能省略？',
    back: `## 结论
- 流水寄存器把相邻各段**隔离**开：同一时钟沿统一锁存上一段的输出，本拍供下一段使用。
- 它使各段能够**并行工作**，是流水线成立的前提；若没有它，后段的组合逻辑会直接影响前段输出，产生竞争与错误结果。
- 寄存器本身有建立与传输延迟，故时钟周期 = 最慢段逻辑延迟 + 寄存器延迟。
> 易错：段数越多寄存器总开销越大，加速比不会无限提升；寄存器的位宽必须能容纳该段产生的全部信息，包括数据、控制信号与目的寄存器号。`,
    importance: 3,
    hint: '隔离各段，同沿锁存',
  },
  {
    id: 'co-pipeline-cpi-stall',
    subject: 'co',
    chapter: '指令流水线',
    tags: ['CPI', '停顿', '计算'],
    title: '流水线冲突如何折算成对 CPI 的影响？',
    back: `## 结论
- 理想流水线 $CPI=1$，每发生一次停顿就多花若干时钟周期，故 $CPI=1+\\text{平均每条指令的停顿周期数}$。
- 停顿周期数 = 各类冒险的**发生频率** $\\times$ 每次造成的**停顿拍数**，再按类求和。
- 例：$20\\%$ 的指令引发 $2$ 拍阻塞，则 $CPI=1+0.2\\times 2=1.4$。
- 转发、分支预测、增加功能部件等手段，本质都是把这些附加停顿压回 $0$。
> 易错：算加速比与执行时间必须代入**实际 CPI**，不能默认 $CPI=1$；结构冒险的停顿取决于部件冲突频率。`,
    importance: 4,
    hint: '实际 CPI 等于 1 加平均停顿',
  },

  // ==================== 总线（3 张） ====================
  {
    id: 'co-bus-bandwidth-calc',
    subject: 'co',
    chapter: '总线',
    tags: ['总线带宽', '计算'],
    title: '总线带宽如何计算？与总线宽度、频率是什么关系？',
    back: `## 结论
- **总线带宽**（数据传输率）$=$ 总线宽度（字节）$\\times$ 总线工作频率。
- 例：$32$ 位总线、工作频率 $66\\mathrm{MHz}$，带宽 $=4\\mathrm{B}\\times 66\\times 10^{6}=264\\mathrm{MB/s}$。
- 若一个总线周期需要用 $k$ 个时钟才传送一次数据，实际带宽还要再除以 $k$。
- 地址线与数据线复用、插入等待周期都会降低有效带宽。
> 易错：$32$ 位是 **$4$ 字节**而不是 $32$ 字节；总线宽度特指**数据线**根数，与地址线根数无关。`,
    importance: 4,
    hint: '字节数乘频率，32 位是 4 字节',
  },
  {
    id: 'co-bus-arbitration-timing',
    subject: 'co',
    chapter: '总线',
    tags: ['总线仲裁', '总线定时'],
    title: '总线仲裁与总线定时各有哪几种方式？',
    back: `## 结论
- **仲裁**解决多个主设备争用总线的问题：**集中式**有链式查询、计数器定时查询、独立请求三种，另有分布式仲裁。链式查询线少但对**靠近控制器的设备有利**，独立请求响应最快但连线最多。
- **定时**规定总线操作的时间配合：**同步定时**由统一时钟约定，速度快、适合速度相近的设备；**异步定时**靠**握手（应答）**信号，适应性强但传输较慢。
- 异步定时按互锁程度又分不互锁、半互锁、全互锁，全互锁最可靠。
> 易错：仲裁决定"谁用总线"，定时决定"何时用、用多久"，两者是不同层面的问题。`,
    importance: 4,
    hint: '仲裁选主设备，定时定时间配合',
  },
  {
    id: 'co-bus-classification-perf',
    subject: 'co',
    chapter: '总线',
    tags: ['总线分类', '性能指标'],
    title: '总线如何分类？主要性能指标有哪些？',
    back: `## 结论
- 按**功能**分三类：**数据总线**（双向，宽度决定一次能传送多少位）、**地址总线**（单向，由 CPU 输出，根数决定可寻址空间大小）、**控制总线**（各信号方向不一）。
- 按**层次位置**分：片内总线、系统总线、通信总线；按传输方式还可分串行与并行。
- 主要性能指标：**总线宽度、总线频率、带宽（数据传输率）、信号线数、是否总线复用**。
> 易错：地址总线是**单向**的、数据总线是**双向**的；总线复用能减少引脚数，但会牺牲有效带宽。`,
    importance: 3,
    hint: '数据双向，地址单向',
  },

  // ==================== IO 与中断（5 张） ====================
  {
    id: 'co-io-three-modes',
    subject: 'co',
    chapter: 'IO 与中断',
    tags: ['IO 方式', '对比'],
    title: '程序查询、中断与 DMA 三种 I/O 方式如何对比？',
    back: `## 结论
- **程序查询**：CPU 反复读取状态寄存器等待设备就绪，CPU 与外设完全**串行**工作，效率最低。
- **中断方式**：外设就绪后主动发出中断请求，CPU 暂停现行程序，通过服务程序传送**一个字节或一个字**，传送仍由 CPU 参与。
- **DMA 方式**：由 DMA 控制器直接在主存与外设之间成批传送，**不经过 CPU**，只在传送前后打扰 CPU 一次，适合磁盘等高速块设备。
> 易错：中断方式仍要 CPU 执行服务程序搬数据，只有 DMA 才把传送工作从 CPU 手里拿走；DMA 请求的优先级通常**高于**中断请求。`,
    importance: 5,
    hint: '查询串行，中断传一个，DMA 成批',
  },
  {
    id: 'co-interrupt-handling-flow',
    subject: 'co',
    chapter: 'IO 与中断',
    tags: ['中断处理', '现场保护'],
    title: '中断服务程序的完整处理流程是怎样的？',
    back: `## 结论
- 典型流程：**关中断 → 保护断点与现场 → 判优并转入服务程序 → 执行中断服务 → 恢复现场 → 开中断 → 中断返回**。
- 保护现场是把相关寄存器内容**压栈**，恢复现场则是**出栈**还原，两者的次序相反、内容一一对应。
- 中断返回由**中断返回指令**完成，它把保存的断点地址送回 PC，使原程序接着执行。
> 易错：先关中断是为了保证"保存现场"过程本身不被打断；单重中断在整个服务期间不开中断，多重中断则在进入服务程序后尽早开中断。`,
    importance: 4,
    hint: '关中断保现场，恢复后开中断返回',
  },
  {
    id: 'co-dma-transfer-cycle-stealing',
    subject: 'co',
    chapter: 'IO 与中断',
    tags: ['DMA', '周期挪用'],
    title: 'DMA 的三种传送方式是什么？周期挪用指什么？',
    back: `## 结论
- DMA 传送前，CPU 需向控制器设置**主存起始地址、传送长度、传送方向**并启动。
- **停止 CPU 访存**：DMA 独占总线成批传送，控制简单，但 CPU 长时间无法访存。
- **周期挪用（周期窃取）**：DMA 趁 CPU 不使用总线时**借用一个存储周期**传送一个字，CPU 只被延迟一个周期，主存利用率最高。
- **交替访存**：把存储周期分给 CPU 与 DMA 轮流使用，无需申请总线，但要求主存速度足够高。
> 易错：DMA 传送结束后由控制器发**中断**通知 CPU 做善后，整个传送过程既不执行程序，也不需要 CPU 干预。`,
    importance: 5,
    hint: '周期挪用借一个存储周期',
  },
  {
    id: 'co-io-interface-port',
    subject: 'co',
    chapter: 'IO 与中断',
    tags: ['IO 接口', '端口编址'],
    title: 'I/O 接口有哪些功能？端口有哪两种编址方式？',
    back: `## 结论
- I/O 接口的主要功能：**地址译码与设备选择、数据缓冲、速度匹配、格式转换（串并转换）、传送控制与状态报告**。
- **端口**是接口中可被 CPU 直接访问的寄存器，分为**数据端口、状态端口、控制端口**。
- **统一编址（存储器映射）**：I/O 与主存共用地址空间，用普通访存指令访问，不需专用指令，但会占用一部分主存地址空间。
- **独立编址**：I/O 有独立地址空间和专门的 I/O 指令，不占主存空间，但需要额外的控制信号线。
> 易错：状态端口与数据端口可能共用同一地址，靠读写信号区分；统一编址时不能使用 I/O 指令。`,
    importance: 4,
    hint: '统一占主存空间，独立需专用指令',
  },
  {
    id: 'co-interrupt-mask-multiple',
    subject: 'co',
    chapter: 'IO 与中断',
    tags: ['多重中断', '屏蔽字'],
    title: '中断屏蔽字如何控制多重中断的处理次序？',
    back: `## 结论
- **多重中断**指在中断服务过程中**提前开中断**，允许更高优先级的中断打断当前服务程序，形成中断嵌套。
- **中断屏蔽字**的每一位对应一个中断源：$1$ 表示**屏蔽（禁止）**，$0$ 表示允许响应。
- 编写规则：某中断源的服务程序中，把**优先级不高于自己**的各位都置 $1$。
- 于是处理次序由屏蔽字决定，可与硬件优先级不同，从而灵活调整。
> 易错：屏蔽字中**自己也必须屏蔽**，否则同级中断会反复打断自己；屏蔽字只能改变**处理**次序，改变不了硬件判优决定的**响应**次序。`,
    importance: 4,
    hint: '同级及低级都屏蔽，自己也要屏蔽',
  },
];
