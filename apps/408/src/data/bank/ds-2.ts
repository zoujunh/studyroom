/**
 * 数据结构科目种子卡（第二批，24 张）
 *
 * 只覆盖 408 考纲中「线性表 / 栈与队列 / 数组与矩阵 / 串」四个章节，
 * 与 ds.ts、seed-cards.ts 中已有卡片主题不重复。
 * back 字段语法限定：## 小标题、- 无序列表、**粗体**、行内代码、$行内公式$、> 提示。
 * 模板字符串内不写插值符号，LaTeX 反斜杠一律双写。
 */

import type { SeedCard } from '../seed-cards';

export const DS_BANK2: SeedCard[] = [
  // ==================== 线性表（8 张） ====================
  {
    id: 'ds2-seqlist-address-vs-value-search',
    subject: 'ds',
    chapter: '线性表',
    tags: ['顺序表', '查找', '复杂度'],
    title: '顺序表的按位查找与按值查找有什么不同？',
    back: `## 结论
- **按位查找**按序号取元素，可由起始地址直接算出位置，时间 $O(1)$，这是顺序表独有的随机访问能力。
- **按值查找**要从头逐个比较，时间 $O(n)$，等概率查找成功时平均比较 $(n+1)/2$ 次。
- 链表两种查找都得顺指针走，按位查找平均也要 $n/2$ 次比较，同样不是 $O(1)$。
> 易错：$(n+1)/2$ 是查找成功时的平均值，查找失败还要比完 $n$ 个元素，别把两者混为一谈。`,
    importance: 4,
    hint: '按位 O(1)，按值平均 (n+1)/2 次',
  },
  {
    id: 'ds2-linkedlist-headnode-diff',
    subject: 'ds',
    chapter: '线性表',
    tags: ['单链表', '头结点', '辨析'],
    title: '单链表带头结点与不带头结点有什么差别？',
    back: `## 结论
- **头结点**不存数据（也可存表长），它的存在让空表与非空表、首元位置与中间位置的插删写法**完全统一**。
- 不带头结点时，插入或删除第一个结点必须单独修改头指针，还要特判空表，代码分支多、容易写错。
- 头指针是链表唯一的入口，**必须存在**；头结点只是辅助手段，可以没有。
> 易错：带头结点时头指针指向的是头结点而不是首元结点，判空条件也随之变成头结点的指针域是否为空。`,
    importance: 4,
    hint: '头结点统一写法，头指针必须有',
  },
  {
    id: 'ds2-linkedlist-reverse-inplace',
    subject: 'ds',
    chapter: '线性表',
    tags: ['单链表', '逆置', '指针操作'],
    title: '怎样就地逆置一个单链表？',
    back: `## 结论
- 用三个指针：$pre$ 记住已逆置部分的头，$cur$ 指向当前结点，$next$ 暂存它的后继。
- 每步先存后继，再把 $cur$ 的指针改为指向 $pre$，然后三个指针整体后移，最后让头结点指向新的首元结点。
- 时间 $O(n)$、辅助空间 $O(1)$；本质上就是拿原有结点又做了一次**头插法**重建。
> 易错：必须先保存后继再改指针，否则断链丢结点；逆置后原来的首元结点变成尾结点，其指针域要置空。`,
    importance: 4,
    hint: '三指针翻转，先存后继再改向',
  },
  {
    id: 'ds2-linkedlist-merge-sorted',
    subject: 'ds',
    chapter: '线性表',
    tags: ['单链表', '合并', '指针操作'],
    title: '如何合并两个有序单链表？',
    back: `## 结论
- 用**尾插法**：比较两个表的首元结点，把较小者摘下接到结果表尾并让该表指针后移，如此反复。
- 只要有一个表为空，就把另一个非空表的整段直接挂到结果尾部，不必再逐个比较。
- 时间 $O(m+n)$、辅助空间 $O(1)$，而且**不额外申请结点**，只是把原有结点重新链接。
> 易错：题目要求就地合并时不能新建结点；两元素相等时先取哪个表由题目规定，这会决定结果的稳定性。`,
    importance: 3,
    hint: '尾插法摘小结点，余段整挂',
  },
  {
    id: 'ds2-circular-list-tail-judge',
    subject: 'ds',
    chapter: '线性表',
    tags: ['循环链表', '尾指针'],
    title: '循环链表中怎样判断尾结点？只设尾指针有什么好处？',
    back: `## 结论
- 带头结点的**循环单链表**中，$p$ 是尾结点等价于 $p\\to next=head$；不带头结点时则等价于它指向首元结点。
- 只设尾指针 $rear$ 时，$rear\\to next$ 就是头结点，找表头、表头插入与表尾插入都能 $O(1)$ 完成。
- 循环双链表判尾同理看后继，判空则要求前驱与后继都指回头结点。
> 易错：循环链表中没有空指针，遍历以"回到起点"为结束条件，不能写判 NULL 的循环。`,
    importance: 3,
    hint: '尾结点后继回头，尾指针省事',
  },
  {
    id: 'ds2-doublelist-delete-node',
    subject: 'ds',
    chapter: '线性表',
    tags: ['双向链表', '删除', '复杂度'],
    title: '已知双链表中结点 p，怎样 $O(1)$ 删除 p 自身？',
    back: `## 结论
- 改两条指针：$p\\to prior\\to next=p\\to next$ 与 $p\\to next\\to prior=p\\to prior$，最后释放 $p$。
- 双链表知道结点自身即可找到前驱，删除为 $O(1)$；单链表同场景要找前驱，为 $O(n)$。
- 单链表的"搬运后继数据再删后继"只适用于 $p$ 不是尾结点时。
> 易错：删尾结点时 $p\\to next$ 为空，要跳过改后继前驱那一步，否则空指针出错。`,
    importance: 4,
    hint: '双链改两条指针，单链要 O(n)',
  },
  {
    id: 'ds2-storage-density-compare',
    subject: 'ds',
    chapter: '线性表',
    tags: ['存储密度', '顺序表', '链表'],
    title: '顺序表与链表的存储密度分别是多少？',
    back: `## 结论
- 存储密度 = 结点中**数据本身**占的空间除以结点**总共**占的空间，取值满足 $0<$ 存储密度 $\\le 1$。
- 顺序表连续存放，除数据外没有额外的结构开销，存储密度为 $1$。
- 单链表每个结点还要多一个指针域，若数据域占 $4$ 字节、指针域占 $4$ 字节，存储密度只有 $0.5$。
> 易错：存储密度高只说明空间利用率高，并不等于查找更快；链表插删方便与密度偏低是两件独立的事。`,
    importance: 3,
    hint: '顺序表密度 1，单链表约一半',
  },
  {
    id: 'ds2-seqlist-index-boundary',
    subject: 'ds',
    chapter: '线性表',
    tags: ['顺序表', '位序', '边界'],
    title: '顺序表的位序与下标怎样换算？插入删除的位置范围是多少？',
    back: `## 结论
- 教材用**位序**（从 $1$ 起）描述元素，程序用**下标**（从 $0$ 起），第 $i$ 个元素存放在下标 $i-1$ 处。
- 长为 $n$ 的顺序表中，合法插入位置为 $1\\sim n+1$，合法删除位置为 $1\\sim n$。
- 按位查找时下标范围是 $0\\sim n-1$，越界即非法。
> 易错：插入位置可以取 $n+1$（相当于尾插），删除最多到 $n$；混淆位序与下标判越界最常见。`,
    importance: 3,
    hint: '插入到 n+1，删除只到 n',
  },

  // ==================== 栈与队列（7 张） ====================
  {
    id: 'ds2-stack-top-convention',
    subject: 'ds',
    chapter: '栈与队列',
    tags: ['顺序栈', '栈顶指针', '判空判满'],
    title: '顺序栈的栈顶指针有哪两种约定？判空判满怎么写？',
    back: `## 结论
- 约定一：$top$ 指向**栈顶元素**，初始 $top=-1$，判空 $top=-1$，判满 $top=n-1$；入栈先加后存，出栈先取后减。
- 约定二：$top$ 指向**栈顶上面的空位**，初始 $top=0$，判空 $top=0$，判满 $top=n$；次序与约定一相反。
- 两种约定的元素个数分别为 $top+1$ 与 $top$。
> 易错：判空与判满不能用同一个条件；换约定时入栈出栈的次序要一起翻转。`,
    importance: 4,
    hint: '指向栈顶 -1 起，指向空位 0 起',
  },
  {
    id: 'ds2-stack-linked-impl',
    subject: 'ds',
    chapter: '栈与队列',
    tags: ['链栈', '存储结构'],
    title: '链栈是怎样实现的？它需要判满吗？',
    back: `## 结论
- 链栈用单链表实现，通常让**表头当栈顶**：入栈就是头插，出栈就是删首元结点，两者都是 $O(1)$。
- 链栈可以带头结点也可以不带头结点，带头结点能让空栈与非空栈的写法统一；栈底结点的指针域为空。
- 链栈的结点是运行时动态申请的，只有内存耗尽才谈得上"满"，日常**不需要判满**。
> 易错：出栈前必须判栈空；入栈与出栈都只在表头进行，千万不要遍历到链表尾去找栈底。`,
    importance: 3,
    hint: '表头当栈顶，链栈不必判满',
  },
  {
    id: 'ds2-catalan-counting',
    subject: 'ds',
    chapter: '栈与队列',
    tags: ['卡特兰数', '计数', '出栈序列'],
    title: '卡特兰数在 408 里有哪些计数应用？',
    back: `## 结论
- $n$ 个元素依次入栈再出栈，合法出栈序列的种数为卡特兰数 $\\dfrac{1}{n+1}C_{2n}^{n}$。
- 同一数列还用于：$n$ 个结点的不同二叉树形态数、$n$ 对括号的合法匹配方案数、栈的合法入出操作序列数。
- 前几项是 $1,2,5,14,42$，考试常只用到 $n=3$ 与 $n=4$，记住这两项即可。
> 易错：卡特兰数只负责**计数**，判断某一个具体序列是否合法，仍要按栈模拟从头验证一遍。`,
    importance: 5,
    hint: '出栈序列数等于 n 阶卡特兰数',
  },
  {
    id: 'ds2-bracket-matching',
    subject: 'ds',
    chapter: '栈与队列',
    tags: ['栈的应用', '括号匹配'],
    title: '括号匹配为什么用栈？算法怎样写？',
    back: `## 结论
- 括号的嵌套满足**后进先出**：最后出现的左括号必须最先被它的右括号配上，正好对应栈的特点。
- 算法：遇左括号就入栈；遇右括号先看栈是否为空，再与栈顶左括号比较，类型相同则弹出，不同则匹配失败。
- 主串扫描完后栈必须为空，否则说明有左括号没被闭合。时间 $O(n)$，最坏辅助空间 $O(n)$。
> 易错：只有一种圆括号时用计数器就够了，多种括号混排则必须用栈，因为还要区分括号的类型。`,
    importance: 4,
    hint: '左括号入栈，右括号配栈顶',
  },
  {
    id: 'ds2-infix-two-stack-eval',
    subject: 'ds',
    chapter: '栈与队列',
    tags: ['栈的应用', '表达式求值'],
    title: '不转后缀，直接用栈求中缀表达式的值可以吗？',
    back: `## 结论
- 可以，用**两个栈**：一个存操作数，一个存运算符，从左到右扫描中缀表达式。
- 遇到运算符时，只要运算符栈顶的优先级不低于它，就弹出栈顶运算符并取两个操作数计算，结果压回操作数栈；遇右括号则一直弹到左括号为止。
- 扫描结束后弹空运算符栈，操作数栈顶即结果。时间 $O(n)$，与先转后缀再求值等价。
> 易错：优先级**相同**时也要先弹出栈顶，因为同级运算符按从左到右结合；括号只起定界作用，不参与优先级比较。`,
    importance: 3,
    hint: '双栈法，栈顶优先不低于就弹',
  },
  {
    id: 'ds2-circular-queue-empty-full',
    subject: 'ds',
    chapter: '栈与队列',
    tags: ['循环队列', '判空判满', '辨析'],
    title: '循环队列不牺牲存储单元时怎样区分队空与队满？',
    back: `## 结论
- 队头与队尾指针重合时单看指针无法区分空满，牺牲一个单元只是其中一法。
- 办法二**增设长度变量**：入队加一、出队减一，判空即长度 $0$、判满即长度 $n$，元素个数直接读该变量。
- 办法三**增设标志位**：指针重合时，标志 $0$ 表示空、$1$ 表示满，标志在入队与出队成功后改写。
> 易错：这三种办法都能用满全部单元，而牺牲单元法的判满条件是 $(rear+1)\\bmod n=front$，别混用。`,
    importance: 4,
    hint: '牺牲单元、记长度、设标志三选一',
  },
  {
    id: 'ds2-call-stack-recursion',
    subject: 'ds',
    chapter: '栈与队列',
    tags: ['栈的应用', '递归', '函数调用'],
    title: '函数调用与递归为什么用栈而不用队列？',
    back: `## 结论
- 调用的嵌套关系是**后调用先返回**，与栈的后进先出一致；若用队列，返回顺序会完全错乱。
- 每进入一层函数，系统就把返回地址、实参、局部变量与被保存的寄存器压入**函数调用栈**，返回时弹出以恢复现场。
- 递归深度决定调用栈的最大深度，递归太深会**栈溢出**；各层的局部变量互不影响。
> 易错：递归的工作栈由系统自动管理，空间开销与递归深度成正比，不能想当然认为它只用 $O(1)$ 辅助空间。`,
    importance: 3,
    hint: '后调用先返回，深度即栈深',
  },

  // ==================== 数组与矩阵（5 张） ====================
  {
    id: 'ds2-array-row-col-major',
    subject: 'ds',
    chapter: '数组与矩阵',
    tags: ['多维数组', '地址计算'],
    title: '二维数组按行优先与按列优先存储时地址怎么算？',
    back: `## 结论
- 设数组有 $m$ 行 $n$ 列、元素占 $L$ 字节，下标从 $0$ 起。
- **行优先**：$LOC(a[i][j])=LOC(a[0][0])+(i\\times n+j)L$。
- **列优先**：$LOC(a[i][j])=LOC(a[0][0])+(j\\times m+i)L$。
> 易错：行优先乘**列数** $n$，列优先乘**行数** $m$；C 按行优先，Fortran 按列优先。`,
    importance: 5,
    hint: '行优先乘列数，列优先乘行数',
  },
  {
    id: 'ds2-symmetric-matrix-index',
    subject: 'ds',
    chapter: '数组与矩阵',
    tags: ['对称矩阵', '压缩存储', '下标换算'],
    title: '对称矩阵压缩存储时下标怎样换算？',
    back: `## 结论
- $n$ 阶对称矩阵满足 $a[i][j]=a[j][i]$，只需存下三角共 $n(n+1)/2$ 个元素。
- 按行优先、下标从 $0$ 起时，$i\\ge j$ 的元素存放在 $k=\\dfrac{i(i+1)}{2}+j$ 处。
- 若 $i<j$，先交换 $i$ 与 $j$ 再代入，即取对称位置。
> 易错：若下标从 $1$ 起，公式改为 $k=\\dfrac{i(i-1)}{2}+j$；先确认起点与是否含对角线。`,
    importance: 5,
    hint: '存下三角，i<j 就交换再算',
  },
  {
    id: 'ds2-triangular-matrix-index',
    subject: 'ds',
    chapter: '数组与矩阵',
    tags: ['三角矩阵', '压缩存储', '下标换算'],
    title: '上三角与下三角矩阵压缩后各需要多少存储单元？',
    back: `## 结论
- 下三角矩阵的上三角全为同一常数，除下三角的 $n(n+1)/2$ 个元素外再留 $1$ 个单元存常数。
- 按行优先、下标从 $0$ 起时，$i\\ge j$ 的元素在 $k=\\dfrac{i(i+1)}{2}+j$ 处，$i<j$ 一律填常数。
- 上三角矩阵对称处理，只是改为按行存上三角，偏移量累加该行之前的元素个数。
> 易错：三角矩阵比对称矩阵多一个常数单元，对称矩阵只要 $n(n+1)/2$ 个，没有这一格。`,
    importance: 4,
    hint: '三角矩阵多一格放常数',
  },
  {
    id: 'ds2-tridiagonal-matrix-index',
    subject: 'ds',
    chapter: '数组与矩阵',
    tags: ['三对角矩阵', '压缩存储', '计算'],
    title: '三对角矩阵压缩存储的下标公式是什么？',
    back: `## 结论
- 三对角矩阵只有 $\\lvert i-j\\rvert\\le 1$ 处可能非零，共 $3n-2$ 个元素。
- 按行优先、下标从 $0$ 起时 $k=2i+j$，如 $a[1][1]$ 在 $k=3$。
- 反向换算 $i=\\lfloor (k+1)/3\\rfloor$、$j=k-2i$，可由位置反推行列。
> 易错：第一行只 $2$ 个元素，第 $i$ 行前偏移是 $3i-1$ 而非 $3i$；其余位置按 $0$ 处理。`,
    importance: 4,
    hint: 'k=2i+j，共 3n-2 个元素',
  },
  {
    id: 'ds2-sparse-triple-crosslist',
    subject: 'ds',
    chapter: '数组与矩阵',
    tags: ['稀疏矩阵', '三元组', '十字链表'],
    title: '稀疏矩阵的三元组表示与十字链表各有什么特点？',
    back: `## 结论
- **三元组表**用（行，列，值）记录每个非零元并按行序排列，另外还要存总行数、总列数与非零元个数。
- 三元组表失去了随机存取特性，取某个位置的元素得顺序查找，转置与相加也都需要遍历整表。
- **十字链表**给每一行、每一列各设一条链，非零元结点同时挂在所在行链与列链上，适合非零元频繁增删的场合。
> 易错：压缩存储省的是空间，换来的是访问变慢；三元组表的空间是 $O(t)$，其中 $t$ 为非零元个数。`,
    importance: 3,
    hint: '三元组省空间，十字链表好插删',
  },

  // ==================== 串（4 张） ====================
  {
    id: 'ds2-string-empty-substring',
    subject: 'ds',
    chapter: '串',
    tags: ['串', '基本概念', '计数'],
    title: '空串与空格串有什么区别？长度为 n 的串有多少个子串？',
    back: `## 结论
- **空串**长度为 $0$、不含任何字符；**空格串**由一个或多个空格组成，长度等于空格个数，两者不是一回事。
- 长度为 $n$ 的串，非空子串共有 $n(n+1)/2$ 个，再加上空串，子串总数为 $n(n+1)/2+1$。
- 空串是任意串的子串，也是它的前缀与后缀；子串要求字符**连续**，子序列不要求。
> 易错：按位置计数时，内容相同的子串会被重复统计；若题目要求互不相同的子串，需去重，该公式不再适用。`,
    importance: 3,
    hint: '空串长为零，子串 n(n+1)/2+1',
  },
  {
    id: 'ds2-string-storage-forms',
    subject: 'ds',
    chapter: '串',
    tags: ['串', '存储结构', '辨析'],
    title: '串的定长顺序存储与堆分配存储有什么区别？',
    back: `## 结论
- **定长顺序存储**用固定长度的数组存串，超出容量的部分会被**截断**，串长不能超过数组上限。
- **堆分配存储**在程序运行时动态申请空间，串长不受预设上限约束，但仍是存放在一组连续单元中。
- 两者都能随机访问单个字符；此外还有**块链存储**，每个结点放若干字符，存储密度较低但插删灵活。
> 易错：串长与数组容量是两个概念，定长存储中串长不能超过容量；块链存储的最后一个结点往往放不满。`,
    importance: 3,
    hint: '定长会截断，堆分配可动态扩',
  },
  {
    id: 'ds2-next-nextval-example',
    subject: 'ds',
    chapter: '串',
    tags: ['KMP', 'next 数组', '计算'],
    title: '模式串 ababaa 的 next 与 nextval 分别是多少？',
    back: `## 结论
- 约定 $next[1]=0$，其余按前 $j-1$ 个字符的最长相等前后缀加 $1$ 求得：$next=0,1,1,2,3,4$。
- 再用"同则继承、异则照抄 next"递推，得 $nextval=0,1,0,1,0,4$。
- 如 $j=3$ 处是 a，而 $next[3]=1$ 处也是 a，回退必再失配，故 $nextval[3]=0$。
> 易错：next 只与模式串自身有关；nextval 要由前向后递推。`,
    importance: 5,
    hint: 'next 011234，nextval 010104',
  },
  {
    id: 'ds2-naive-match-worst-case',
    subject: 'ds',
    chapter: '串',
    tags: ['朴素匹配', '最坏情况', '复杂度'],
    title: '朴素模式匹配的最坏情形是怎样构造的？比较次数怎么数？',
    back: `## 结论
- 最坏情形：主串与模式串前面大量字符都相同，只在最后一位失配，例如主串全为 0、模式串是若干 0 后接一个 1。
- 此时每趟都要比较 $m$ 次才失败，主串指针回退到本趟起点下一位重来。
- 主串长 $n$、模式串长 $m$ 时，总比较次数约为 $(n-m+1)\\times m$，即最坏时间 $O(nm)$。
> 易错：失配后主串指针要回退到 $i-j+2$（模式串从 $1$ 起编号），正是这种回退让朴素匹配变慢。`,
    importance: 4,
    hint: '差一位才失配，回退造成 O(nm)',
  },
];
