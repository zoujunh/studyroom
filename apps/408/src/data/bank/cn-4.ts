import type { SeedCard } from '../seed-cards';

export const CN_BANK4: SeedCard[] = [
  // ==================== 传输层（15 张） ====================
  {
    id: 'cn4-transport-layer-position',
    subject: 'cn',
    chapter: '传输层',
    tags: ['传输层功能', '端到端', '网络层对比'],
    title: '传输层在网络体系结构中处于什么地位，提供哪些功能？',
    back: `## 结论
- 传输层之上是应用层、之下是网络层，是**面向通信部分的最高层**，也是**面向用户功能的最底层**。
- 网络层把分组送到目的**主机**，传输层再交付给主机中的具体**进程**，故称**进程到进程**（端到端）。
- 主要功能：为进程提供逻辑通信、**复用与分用**、差错检测、可靠或高效的传输。
> 易错：网络层的端点标识是 IP 地址，传输层的端点标识是**端口号**；"端到端"指进程与进程，网络层虽能端到端寻址但每跳仍要处理分组。`,
    importance: 3,
    hint: '网络层找主机，传输层找进程',
  },
  {
    id: 'cn4-port-number-classes',
    subject: 'cn',
    chapter: '传输层',
    tags: ['端口号', '熟知端口', '必背'],
    title: '端口号如何分类，常见熟知端口号有哪些？',
    back: `## 结论
- 端口号是 $16$ 位无符号数，范围 $0\\sim 65535$，标识主机中的**进程**。
- **熟知端口** $0\\sim 1023$：IANA 统一分配，固定给常用服务；**登记端口** $1024\\sim 49151$；**短暂端口** $49152\\sim 65535$ 供客户临时申请。
- 常见熟知端口：FTP 控制 $21$、数据 $20$；SSH $22$；SMTP $25$；DNS $53$；DHCP 服务器 $67$、客户 $68$；HTTP $80$；POP3 $110$；IMAP $143$；HTTPS $443$。
> 易错：服务器用熟知端口**监听**，客户端用短暂端口发起连接，故服务器端口一般小于客户端端口。`,
    importance: 4,
    hint: '熟知小于 1024，短暂大于 49151',
  },
  {
    id: 'cn4-socket-and-multiplexing',
    subject: 'cn',
    chapter: '传输层',
    tags: ['套接字', '复用', '分用'],
    title: '什么是套接字，复用与分用如何实现？',
    back: `## 结论
- **套接字** $=$（IP 地址，端口号），是传输层通信的端点；一条 TCP 连接由一对套接字唯一确定，即四元组（源 IP、源端口、目的 IP、目的端口）。
- **复用**：发送方多个进程的数据靠各自端口号封装进网络层数据报，共享同一条通道。
- **分用**：接收方按首部中的**目的端口号**把数据交给对应进程。
- **无连接分用**（UDP）只看目的端口号；**面向连接分用**（TCP）还要看源 IP、源端口。
> 易错：同一主机内端口必须唯一，但 TCP 与 UDP 的端口空间彼此独立，$53$ 可同时被 DNS 的 UDP 与 TCP 使用。`,
    importance: 4,
    hint: '套接字是 IP 加端口，分用靠目的端口',
  },
  {
    id: 'cn4-udp-features-realtime',
    subject: 'cn',
    chapter: '传输层',
    tags: ['UDP', '特点', '实时应用'],
    title: 'UDP 有哪些特点，为什么适合实时应用？',
    back: `## 结论
- **无连接**：发送前不建连接，减小开销与延迟。
- **尽最大努力交付**：不保证可靠，不确认、不重传、不排序。
- **面向报文**：保留报文边界，不合并不拆分，一次收一个完整报文。
- **无拥塞控制**：拥塞时发送速率不受约束，速率恒定。
- **首部开销小**：只有 $8$ B，而 TCP 最少 $20$ B。
## 为什么适合实时应用
- 语音视频宁可**丢几个分组**，也不能因等重传而**长时间卡顿**。
- 无拥塞控制使速率稳定，避免 TCP"拥塞就猛降速"造成的抖动。
> 易错：面向报文意味着应用层要自己处理边界；若需要可靠，只能在**应用层**自行加确认与重传。`,
    importance: 4,
    hint: '无连接无拥塞，报文有边界',
  },
  {
    id: 'cn4-udp-checksum-pseudo',
    subject: 'cn',
    chapter: '传输层',
    tags: ['UDP', '校验和', '伪首部'],
    title: 'UDP 检验和的计算范围与伪首部的作用是什么？',
    back: `## 结论
- 检验和覆盖 $12$ B **伪首部**、$8$ B UDP 首部和全部数据，按 $16$ 位做**二进制反码求和**，结果取反码写入检验和字段。
- 伪首部：源 IP、目的 IP 各 $4$ B，全 $0$ 与协议号 $17$ 各 $1$ B，UDP 长度 $2$ B。
- 伪首部**只参与计算，不随报文传输**。
- 数据为奇数字节时末尾补一个全 $0$ 字节凑偶，但**该字节不传输**。
## 伪首部的作用
- 把 IP 地址与协议号纳入校验，可检出**分组投递到错误主机**或承载协议弄错。
> 易错：检验和是**可选的**，字段全 $0$ 表示未计算；若结果恰为 $0$，须写成全 $1$（$\\text{0xFFFF}$）以区别于"未校验"。`,
    importance: 4,
    hint: '伪首部只算不发，奇数字节补零',
  },
  {
    id: 'cn4-tcp-seq-ack-semantics',
    subject: 'cn',
    chapter: '传输层',
    tags: ['TCP', '序号', '确认号', '计算'],
    title: 'TCP 序号与确认号的语义是什么，一个报文段消耗多少序号？',
    back: `## 结论
- TCP 把应用层数据看成**无结构的字节流**，对每一个**字节**编号，序号填的是本报文段**数据第一个字节**的编号。
- **确认号**表示"期望收到的下一个字节编号"，等价于"该编号之前的字节全收到了"。
- 初始序号 ISN 随机选取；因 SYN 占 $1$ 个序号，$\\text{SYN}=x$ 的确认号是 $x+1$。
## 序号消耗规则
- **SYN 与 FIN 各占 $1$ 个序号**（即使不带数据）。
- 只带 ACK 的纯确认报文**不消耗序号**。
- 长度 $L$ 的数据报文占用区间 $[seq,\\ seq+L-1]$，下一个序号为 $seq+L$。
> 易错：收到 $\\text{seq}=200$、长度 $100$ 的报文，确认号是 **$300$** 不是 $301$；只有 SYN 或 FIN 才多占一位。`,
    importance: 5,
    hint: 'SYN 和 FIN 各占一个号，确认号是下一个期望字节',
  },
  {
    id: 'cn4-tcp-header-flags-offset',
    subject: 'cn',
    chapter: '传输层',
    tags: ['TCP', '标志位', '数据偏移'],
    title: 'TCP 首部的数据偏移与六个标志位分别控制什么？',
    back: `## 结论
- **数据偏移**占 $4$ 位，以 $4$ B 为单位，最小值 $5$ 即首部 $20$ B，最大 $15$ 即 $60$ B，故选项最长 $40$ B。
- **URG** 紧急指针有效；**ACK** 确认号有效，连接建立后**所有报文都置 $1$**。
- **PSH** 尽快交付应用层；**RST** 复位连接。
- **SYN** 同步序号请求建连；**FIN** 数据发完请求释放。
- 窗口 $16$ 位作流量控制；检验和 $16$ 位覆盖首部与数据；紧急指针指向紧急数据**最后一个字节**。
> 易错：ACK 置位只说明确认号**有效**，不等于"我在确认你刚发的那个报文"；$20$ B 只是最小首部，选项使其可变长。`,
    importance: 4,
    hint: '偏移乘四得首部长，六个标志位各管一事',
  },
  {
    id: 'cn4-tcp-handshake-why-not-two',
    subject: 'cn',
    chapter: '传输层',
    tags: ['TCP', '三次握手', '计算', '序号推演'],
    title: 'TCP 三次握手中各报文的序号与确认号如何推演？',
    back: `## 结论
- 客户端发 $\\text{SYN}=x$ 不带数据，SYN 占 $1$ 个序号，进入 SYN-SENT。
- 服务器回 $\\text{SYN}=y,\\ \\text{ACK}=x+1$，SYN 同样占 $1$ 个序号，进入 SYN-RCVD。
- 客户端再发 $\\text{ACK}=y+1$，不带数据、不占序号，双方进入 ESTABLISHED。
## 携带数据时一起算
- 若第三个报文捎带 $D$ 字节，其序号为 $x+1$，服务器下一个确认号是 $x+1+D$。
- 若服务器第二个报文捎带 $D$ 字节，其序号为 $y+1$，客户端确认号为 $y+1+D$。
> 易错：$\\text{ACK}=x+1$ 确认的是 **SYN 本身**而非数据；服务器未收到客户端对 $y$ 的确认前，连接只是**半开**。`,
    importance: 5,
    hint: 'SYN 占一号，确认号加一',
  },
  {
    id: 'cn4-tcp-fin-teardown-halfclose',
    subject: 'cn',
    chapter: '传输层',
    tags: ['TCP', '四次挥手', '半关闭', '序号'],
    title: 'TCP 四次挥手的报文交互与半关闭状态怎么理解？',
    back: `## 结论
- 设双方待发序号分别为 $u$、$v$：主动方发 $\\text{FIN}=u,\\ \\text{ACK}=v$，FIN 占 $1$ 个序号，进入 FIN-WAIT-1。
- 被动方回 $\\text{ACK}=u+1$，进入**半关闭**：主动方不能再发数据，被动方**仍可继续发**，状态为 CLOSE-WAIT。
- 被动方数据发完后发 $\\text{FIN}=w,\\ \\text{ACK}=u+1$，进入 LAST-ACK；主动方回 $\\text{ACK}=w+1$ 并等 $2$MSL 后关闭。
> 易错：FIN 只表示"我不再发数据"，不消耗数据序号但占一个序号，故确认号要加 $1$；被动方无数据可发时 ACK 与 FIN 可合并，退化为三次挥手。`,
    importance: 5,
    hint: 'FIN 各占一号，半关闭时对方仍能发数据',
  },
  {
    id: 'cn4-tcp-timewait-2msl',
    subject: 'cn',
    chapter: '传输层',
    tags: ['TCP', 'TIME-WAIT', '2MSL'],
    title: 'TIME-WAIT 为什么要等 2MSL，MSL 指什么？',
    back: `## 结论
- **MSL** 是最长报文段寿命；$2$MSL 就是一个报文一去一回的上限。
- 作用一：**保证最后一个 ACK 能到达**。该 ACK 若丢失，被动方会超时重传 FIN，主动方在 $2$MSL 内仍能收到并重发 ACK，否则被动方会卡在 LAST-ACK。
- 作用二：**让本连接的旧报文段全部消亡**，避免迟滞报文出现在复用同一四元组的新连接中。
- 只有**主动关闭方**进入 TIME-WAIT；服务器大量短连接由服务端主动关闭时会堆积 TIME-WAIT。
> 易错：$2$MSL 不是"重传两次"，也不是固定 $2$ 分钟；实现常取 $30$ s 或 $2$ min，按两个作用作答即可。`,
    importance: 5,
    hint: '保最后 ACK 送达，让旧报文消亡',
  },
  {
    id: 'cn4-tcp-fsm-states',
    subject: 'cn',
    chapter: '传输层',
    tags: ['TCP', '状态转换', '状态机'],
    title: 'TCP 的 11 种连接状态与典型转换路径是什么？',
    back: `## 结论
- 建立：CLOSED $\\to$ SYN-SENT（客户）或 LISTEN $\\to$ SYN-RCVD（服务器）$\\to$ ESTABLISHED。
- 释放（主动方）：ESTABLISHED $\\to$ FIN-WAIT-1 $\\to$ FIN-WAIT-2 $\\to$ TIME-WAIT $\\to$ CLOSED。
- 释放（被动方）：ESTABLISHED $\\to$ CLOSE-WAIT $\\to$ LAST-ACK $\\to$ CLOSED。
- 双方几乎同时发 FIN 时会出现 **CLOSING**；**CLOSED** 是假想状态，**LISTEN** 只在服务器端。
> 易错：FIN-WAIT-1 与 FIN-WAIT-2 的差别在于**自己的 FIN 是否已被确认**；只有 ESTABLISHED 能双向传数据，TIME-WAIT 只见于主动关闭方。`,
    importance: 3,
    hint: '建立三条路，释放两条路，另加 CLOSING',
  },
  {
    id: 'cn4-tcp-syn-flood-cookie',
    subject: 'cn',
    chapter: '传输层',
    tags: ['TCP', 'SYN 洪泛', 'SYN Cookie', '安全'],
    title: 'SYN 洪泛攻击的原理与 SYN Cookie 的防御思路是什么？',
    back: `## 结论
- 服务器收到 SYN 后要分配传输控制块、进入 SYN-RCVD 并启动计时器，本身即**资源消耗**。
- 攻击者伪造大量源 IP 发 SYN 却不完成第三次握手，半开队列被塞满，无法响应正常请求。
- **SYN Cookie**：收到 SYN 时**不分配资源**，把源 IP、源端口、目的 IP、目的端口加秘密口令做哈希，结果当作初始序号 $y$ 写进 SYN+ACK。
- 合法 ACK 回来时重算一遍即可验证 $y$ 并直接分配资源，无需保存中间状态。
> 易错：SYN Cookie 把"保存状态"换成"验证计算"，代价是**无法在 SYN+ACK 中携带选项**；其他缓解手段有限制半开连接数、缩短 SYN 重传超时等。`,
    importance: 3,
    hint: '不存状态，用哈希当序号',
  },
  {
    id: 'cn4-tcp-fast-retransmit-sack',
    subject: 'cn',
    chapter: '传输层',
    tags: ['TCP', '快重传', '重复确认', 'SACK'],
    title: 'TCP 收到三个重复确认后为什么要立即重传？',
    back: `## 结论
- 接收方收到**失序**报文段时立即重复发送上一个确认号，提示"我还在等这个字节"。
- 发送方连续收到**三个重复确认**（同一确认号共收到 $4$ 次）即判定丢失，**不必等超时**立刻重传，即**快重传**。
- 单个重复确认可能只是报文**乱序**到达；连续三个说明后续报文都已到达而缺口迟迟不补。
- **SACK** 用选项字段报告已收到的**不连续数据块**，只需重传真正缺失的部分。
> 易错：快重传后配合**快恢复**（门限与窗口减半后继续拥塞避免）；而**超时**必须回到慢开始、窗口重置为 $1$，不可混用。`,
    importance: 5,
    hint: '三个重复确认即快重传，别等超时',
  },
  {
    id: 'cn4-tcp-rto-karn',
    subject: 'cn',
    chapter: '传输层',
    tags: ['超时重传', 'Karn', 'RTO', '计算'],
    title: 'RTO 的初始值、退避策略与 Karn 算法如何配合？',
    back: `## 结论
- $RTO$ 在 $RTT_S+4RTT_D$ 上留余量：过小导致不必要的重传，过大则反应迟钝。
- 未测得样本前取经验初值（RFC 建议 $1$ s 左右）；每次重传后**指数退避**，RTO 加倍。
- **Karn 算法**：重传过的报文段**不用于计算新的 RTT 样本**，因为确认分不清对应原报文还是重传报文。
- 改进版配合**时间戳选项**区分确认对应哪次发送，既避免污染又能继续采样。
> 易错：收到有效样本后 RTO 要重新按公式计算并"复位"，不能一直沿用加倍值；Karn 算法只约束**采样**，不禁止重传。`,
    importance: 4,
    hint: '重传就加倍，重传过的报文不采样',
  },
  {
    id: 'cn4-tcp-flow-vs-congestion',
    subject: 'cn',
    chapter: '传输层',
    tags: ['流量控制', '拥塞控制', '辨析'],
    title: '流量控制与拥塞控制为什么要分开讨论？',
    back: `## 结论
- **流量控制**解决**收发双方速度不匹配**：接收方用窗口字段告知自己还能缓存多少字节，是点对点的。
- **拥塞控制**解决**整个网络资源不足**：路由器队列溢出、带宽不够，是全局性的。
- 二者共同约束发送窗口：$\\text{发送窗口}=\\min(rwnd,\\ cwnd)$。
- $rwnd$ 很大但网络已拥塞时，只做流控会让发送方猛发压垮网络；反过来只做拥塞控制会大量丢包重传。
> 易错：流量控制靠**接收方通告**，拥塞控制靠**发送方自行探测**（丢包、时延、重复确认），且对每条连接独立生效。`,
    importance: 4,
    hint: '窗口取两者最小，一个管端点一个管网络',
  },

  // ==================== 应用层（9 张） ====================
  {
    id: 'cn4-app-cs-vs-p2p',
    subject: 'cn',
    chapter: '应用层',
    tags: ['C/S', 'P2P', '辨析'],
    title: '客户/服务器模型与 P2P 模型各有什么特点？',
    back: `## 结论
- **客户/服务器（C/S）**：客户主动请求、服务器被动响应；服务器需长期运行、固定地址与熟知端口。
- **P2P**：对等方**直接通信**，既可作客户也可作服务器，没有永远在线的中心节点。
- C/S 便于集中控制与计费，但服务器是瓶颈与单点故障来源。
- P2P 可扩展性好、成本低、健壮，但难管理、安全性差、带宽占用大。
- 要可靠交互用 TCP：HTTP、SMTP、FTP、POP3、IMAP；要快、可容忍丢失用 UDP：DNS 查询、DHCP、实时音视频。
> 易错：P2P 不等于"不用服务器"，很多系统仍有索引服务器；也并非都用 UDP，实际多为 TCP 与 UDP 混合。`,
    importance: 3,
    hint: 'C/S 有中心，P2P 无中心可扩展',
  },
  {
    id: 'cn4-dns-hierarchy-servers',
    subject: 'cn',
    chapter: '应用层',
    tags: ['DNS', '域名服务器', '域名空间'],
    title: 'DNS 的域名空间与四类域名服务器如何组织？',
    back: `## 结论
- 域名空间是**层次树状**结构：根、顶级域、二级域……**越靠右级别越高**。
- 顶级域分三类：国家顶级域（$\\text{cn}$）、通用顶级域（$\\text{com}$）、基础结构域（$\\text{arpa}$）。
## 四类域名服务器
- **根**：知道所有顶级域服务器地址，全球 $13$ 组，用**任播**就近响应。
- **顶级域**：负责某个顶级域下的所有二级域名。
- **权限（权威）**：负责一个**区**，保存该区全部映射，是最终答案来源。
- **本地**：不属层次结构，是主机默认配置的服务器，并缓存结果。
> 易错：区与域不是一回事，一个域可划为多个区；域名服务器按**区**而非按域设置。`,
    importance: 4,
    hint: '根顶级权限加本地，本地不属层次树',
  },
  {
    id: 'cn4-dns-resolution-rtt-count',
    subject: 'cn',
    chapter: '应用层',
    tags: ['DNS', '查询次数', '计算', '高速缓存'],
    title: 'DNS 域名解析要经历多少次交互，缓存能省多少？',
    back: `## 结论
- 客户端向本地域名服务器：**$1$ 次**（递归）。
- 本地服务器依次问根、顶级域、权限域名服务器：无缓存需 **$3$ 次**（迭代），每次只拿到下一跳地址。
- 最后本地服务器把结果回送客户端，共 **$4$ 次请求回答往返**。
## 缓存的作用
- 每台域名服务器都保存近期结果并带 **TTL**，命中即直接返回；主机本地也有缓存（含 hosts 文件）。
- 若中途某级已有缓存，则该级之后的查询全部省去，故 $1+3=4$ 只是无缓存时的上限。
> 易错：DNS 默认用 **UDP $53$**，一次查询是一个 UDP 往返；报文超过 $512$ B 或做区域传送时改用 TCP。`,
    importance: 5,
    hint: '客户一次递归，本地三次迭代',
  },
  {
    id: 'cn4-mail-send-receive-path',
    subject: 'cn',
    chapter: '应用层',
    tags: ['电子邮件', 'SMTP', 'POP3', 'IMAP', '流程'],
    title: '一封电子邮件的完整投递与读取过程是怎样的？',
    back: `## 结论
- 发送方用户代理把邮件**推送**给发送方邮件服务器，用 **SMTP**（TCP，$25$）。
- 发送方服务器再作为 SMTP 客户把邮件**推送**到接收方服务器；送不到就暂存重试。
- 收件人用用户代理**拉取**邮件：POP3（$110$）或 IMAP（$143$）。
## POP3 与 IMAP 的选择
- POP3 下载到本地并默认删除服务器副本，适合单设备离线阅读。
- IMAP 邮件留在服务器上，支持文件夹、标记与多设备同步。
> 易错：**推送（发送）一律用 SMTP**，**拉取（读取）用 POP3 或 IMAP**，不能互换；MIME 不取代 SMTP，只是在其上传送非 ASCII 内容。`,
    importance: 5,
    hint: '发信用 SMTP 推，读信用 POP3 或 IMAP 拉',
  },
  {
    id: 'cn4-url-structure',
    subject: 'cn',
    chapter: '应用层',
    tags: ['URL', 'HTTP', '格式'],
    title: 'URL 由哪几部分组成，各部分的含义是什么？',
    back: `## 结论
- 一般形式：$\\langle \\text{协议}\\rangle ://\\langle \\text{主机}:\\text{端口}\\rangle /\\langle \\text{路径}\\rangle ?\\langle \\text{查询}\\rangle \\#\\langle \\text{片段}\\rangle$。
- **协议**决定应用层协议与默认端口；**主机**是域名或 IP；**端口**省略时用协议默认端口（http $80$、https $443$）。
- **查询字符串**以 $?$ 开头，用 $\\&$ 连接多个"名 $=$ 值"对，GET 参数就在这里。
- **片段标识**以 $\\#$ 开头，只供浏览器内部定位，**不发送给服务器**。
> 易错：中文与保留字符要按 UTF-8 做百分号编码（空格为 $\\%20$）；端口省略时用的是**协议默认端口**而非短暂端口。`,
    importance: 2,
    hint: '协议主机端口路径查询片段',
  },
  {
    id: 'cn4-http-request-response-format',
    subject: 'cn',
    chapter: '应用层',
    tags: ['HTTP', '报文格式', '请求行'],
    title: 'HTTP 请求报文与响应报文的格式有什么区别？',
    back: `## 结论
- 两类报文都由**开始行 + 首部行 + 空行 + 实体主体**构成，空行（CRLF）标志首部结束。
- 请求报文是**请求行**：方法 + URL + 版本，如 \`GET /index.html HTTP/1.1\`。
- 响应报文是**状态行**：版本 + 状态码 + 短语，如 \`HTTP/1.1 200 OK\`。
- 常见首部：Host（HTTP/1.1 必需）、User-Agent、Content-Type、Content-Length、ETag。
- 方法：**GET** 幂等、参数在 URL 中；**POST** 不幂等、数据在主体中；**HEAD** 只要首部、探测用。
- **PUT** 上传替换、**DELETE** 删除、**OPTIONS** 查支持的方法。
> 易错：$1.1$ 强制要求 Host 以支持虚拟主机；HEAD 的响应**不能带主体**，但状态码与首部应与 GET 一致。`,
    importance: 4,
    hint: '请求行带方法，状态行带状态码',
  },
  {
    id: 'cn4-http-status-code-classes',
    subject: 'cn',
    chapter: '应用层',
    tags: ['HTTP', '状态码', '辨析'],
    title: 'HTTP 状态码五个大类各自代表什么，常见码如何辨析？',
    back: `## 结论
- $1\\times\\times$ **信息性**；$2\\times\\times$ **成功**；$3\\times\\times$ **重定向**；$4\\times\\times$ **客户端错误**；$5\\times\\times$ **服务器错误**。
## 常见码
- $200$ OK；$301$ 永久重定向；$302$ 临时重定向；$304$ 配合条件请求、**不带实体**。
- $400$ 请求语法错误；$403$ 服务器拒绝；$404$ 资源不存在；$500$ 服务器内部错误。
> 易错：$304$ 属重定向类却用于缓存校验，收到后**使用本地副本**；$403$ 是"知道你要什么但不给"，$404$ 是"不知道你要什么"。`,
    importance: 4,
    hint: '三永久换址，四客户有错，五服务器出错',
  },
  {
    id: 'cn4-http-rtt-time-calculation',
    subject: 'cn',
    chapter: '应用层',
    tags: ['HTTP', 'RTT', '计算', '持久连接'],
    title: '网页对象传输需要多少个 RTT，如何按连接方式计算？',
    back: `## 结论
- 设基础页面含 $N$ 个内嵌对象，忽略传输时延，只统计 RTT：
- **非持久**：每个对象都新建 TCP 连接，各需 $1$ RTT 建连 $+1$ RTT 请求响应，共 $2(N+1)$ RTT。
- **持久、非流水线**：建连 $2$ RTT，之后每个对象各 $1$ RTT，共 $2+N$ RTT。
- **持久、流水线**：建连 $2$ RTT 后所有请求一次发出，理想情况 $1$ RTT 收齐，共 $3$ RTT。
## 常见变形
- 用**并行连接**（同时开 $k$ 条）时，非持久方式总时间约按 $\\lceil N/k\\rceil$ 轮算。
- 题目给了 DNS 解析时间或传输时延，要把 $T_{DNS}$、$L/R$ 额外加上。
> 易错：$2$ RTT 是"建连 $1$ 次 $+$ 请求响应 $1$ 次"，别漏握手；流水线省的是**等待往返**的轮数，服务端处理与带宽时延仍在。`,
    importance: 5,
    hint: '非持久每对象二 RTT，持久一 RTT，流水线一轮一 RTT',
  },
  {
    id: 'cn4-tls-handshake-cert',
    subject: 'cn',
    chapter: '应用层',
    tags: ['HTTPS', 'TLS', '证书', '安全'],
    title: 'TLS 握手如何协商密钥，证书在其中起什么作用？',
    back: `## 结论
- **ClientHello**：客户端发支持的 TLS 版本、密码套件与客户端随机数。
- **ServerHello**：服务器选定版本与套件，回送服务器随机数与**数字证书**（含公钥，由 CA 私钥签名）。
- **验证证书**：客户端用内置 **CA 公钥**验证签名与证书链，确认服务器未被冒充。
- **密钥协商**：客户端生成预主密钥并用服务器公钥加密送出，双方用两个随机数加它推出同一**会话密钥**。
- **Finished**：用会话密钥加密发送校验值，验证握手未被篡改。
- 分工：**非对称加密**（或 DH）只用于协商密钥；**对称加密**用于后续全部数据，快得多。
> 易错：证书由 CA 私钥签名、用 CA 公钥验证；"用服务器公钥解密"是错的，它只用于**加密**预主密钥。`,
    importance: 4,
    hint: '证书验明身份，握手协商对称密钥',
  },
];
