/**
 * 408 知识点刷卡背诵 —— 计算机组成原理·存储系统专题
 *
 * 24 张卡，覆盖 2 个章节：存储系统 15 / Cache 9。
 * back 语法限定：## 小标题、- 无序列表、**粗体**、$行内公式$、$$块公式$$、> 易错。
 */

import type { SeedCard } from '../seed-cards';

export const CO_BANK3: SeedCard[] = [
  // ==================== 存储系统（15 张） ====================
  {
    id: 'co3-memory-classify',
    subject: 'co',
    chapter: '存储系统',
    tags: ['存储器分类', '存取方式'],
    title: '存储器按存取方式、介质、易失性与作用分别可以分成哪几类？',
    back: `## 结论
- 按**存取方式**分：**随机存取**（RAM，访问时间与地址无关）、**顺序存取**（磁带）、**直接存取**（磁盘）、**相联存取**（按内容查找）。
- 按**介质**分半导体、磁表面与光存储器；按**易失性**分易失（DRAM）与非易失（ROM、Flash、磁盘）。
- 按**作用**分寄存器、Cache、主存、辅存，越靠上越快越贵越小。
> 易错：随机存取强调时间与地址无关，不是"能任意改写"；相联存取靠内容比较，硬件代价最高。`,
    importance: 3,
    hint: '随机顺序直接相联，四种存取方式',
  },
  {
    id: 'co3-memory-hierarchy-transfer-unit',
    subject: 'co',
    chapter: '存储系统',
    tags: ['存储层次', '传送单位'],
    title: '存储层次中各层之间传送数据的单位是什么？各层指标如何变化？',
    back: `## 结论
- 层次自上而下为**寄存器、Cache、主存、辅存**：速度递减、容量递增、单位成本递减。
- **CPU 与 Cache** 之间按**字**传送；**Cache 与主存**之间以**块（行）**为单位整块调入调出。
- **主存与辅存**之间以**页**或段为单位传送，由操作系统与硬件共同管理。
- 整块传送把一次缺失的代价摊到整块数据上，靠的正是空间局部性。
> 易错：Cache 的调入与替换由**硬件**完成、对程序员透明；虚存的页调度需要操作系统参与。`,
    importance: 3,
    hint: '字对上 Cache，块对主存，页对辅存',
  },
  {
    id: 'co3-memory-perf-metrics',
    subject: 'co',
    chapter: '存储系统',
    tags: ['性能指标', '存取周期'],
    title: '存取时间、存取周期与存储器带宽有什么区别？',
    back: `## 结论
- **存取时间** $T_a$ 是从发出读写命令到数据可用的时间；**存取周期** $T_m$ 是连续两次独立访存所需的最小间隔。
- 因为 DRAM 读出后要**重写**、总线与存储阵列需要恢复，所以总有 $T_m>T_a$。
- **带宽** $=$ 一次传送的数据位数 $\\div$ 存取周期，表示单位时间能传送多少数据。
- 其余常用指标：容量、单位成本、功耗与可靠性。
> 易错：算带宽必须用存取周期而不是存取时间；两者相差一个恢复时间，混用会把带宽算大。`,
    importance: 3,
    hint: '周期等于时间加恢复，带宽除周期',
  },
  {
    id: 'co3-locality-analysis',
    subject: 'co',
    chapter: '存储系统',
    tags: ['局部性', '数组访问'],
    title: '怎样判断一段程序的时间局部性与空间局部性？',
    back: `## 结论
- **时间局部性**看同一数据或指令是否被**反复访问**，典型是循环体内的变量与循环指令。
- **空间局部性**看访问地址是否**集中连续**，典型是数组顺序遍历、指令顺序执行。
- 二维数组按**行**访问时相邻元素地址连续，空间局部性好；按**列**访问时每步跨越一整行，局部性差。
- 顺序执行的指令本身空间局部性很强，所以取指也能被指令 Cache 很好地服务。
> 易错：判据是**地址访问序列**而不是语句多少；递归调用栈与紧凑结构体同样有良好局部性。`,
    importance: 4,
    hint: '看地址序列，行访问优于列访问',
  },
  {
    id: 'co3-rom-family',
    subject: 'co',
    chapter: '存储系统',
    tags: ['ROM', 'Flash'],
    title: 'PROM、EPROM、EEPROM 与 Flash 各有什么特点？',
    back: `## 结论
- **掩膜 ROM** 在制造时写入，用户不能修改；**PROM** 可由用户**一次性**写入，写完即固化。
- **EPROM** 用紫外线擦除后可重写，但擦除是**整片**进行，还需专用设备。
- **EEPROM** 可**电擦除**、能按字节改写、支持在线编程，代价是集成度低、价格高。
- **Flash** 由 EEPROM 发展而来，擦写以**块**为单位、速度快容量大，广泛用作 U 盘与固态硬盘。
> 易错：它们都**掉电不丢失**，属非易失性存储器；Flash 改写一个字节常要先搬移整个块。`,
    importance: 3,
    hint: '一次写入、紫外擦、电擦、按块擦',
  },
  {
    id: 'co3-dram-destructive-read',
    subject: 'co',
    chapter: '存储系统',
    tags: ['DRAM', '破坏性读出'],
    title: '什么是破坏性读出？为什么 DRAM 读出后必须重写？',
    back: `## 结论
- DRAM 用**电容**上的电荷表示信息，读出时电荷被送到位线上并被消耗，原信息随即被破坏，故称**破坏性读出**。
- 因此每次读操作后都要**重写（再生）**，把数据重新写回电容，这使读写周期长于存取时间。
- SRAM 用**双稳态触发器**存储，读出的是电平而不改变状态，属**非破坏性读出**，无需重写。
- 重写由存储器内部电路自动完成，与周期性刷新是两件事。
> 易错：刷新是**周期性**的、按行补齐电荷；重写是**每次读出后**的恢复动作，不能混为一谈。`,
    importance: 4,
    hint: '电容读出即破坏，读后必重写',
  },
  {
    id: 'co3-dram-address-multiplex-pins',
    subject: 'co',
    chapter: '存储系统',
    tags: ['DRAM', '地址复用', '计算'],
    title: 'DRAM 为什么把地址分两次传送？芯片引脚数如何计算？',
    back: `## 结论
- DRAM 容量大、地址位数多，若一次送入会使**地址引脚**过多，故把地址拆成**行地址与列地址**分两次传送，由 $\\overline{RAS}$ 与 $\\overline{CAS}$ 分别选通。
- 于是地址引脚约为 $\\dfrac{1}{2}\\log_{2}(\\text{单元数})$ 根，是原来的一半。
- 例：$16\\mathrm{M}\\times 8$ 位 DRAM 需 $24$ 位地址，地址引脚只需 $12$ 根，数据引脚 $8$ 根。
> 易错：地址复用是用**时间换引脚数**，会使访存变慢；计算时先求单元数对应的地址位数再折半。`,
    importance: 4,
    hint: '行列分两次送，地址引脚折半',
  },
  {
    id: 'co3-dram-refresh-modes',
    subject: 'co',
    chapter: '存储系统',
    tags: ['DRAM', '刷新方式'],
    title: '集中、分散与异步刷新各有什么特点？刷新开销怎么算？',
    back: `## 结论
- 设芯片有 $R$ 行、刷新间隔为 $t$。**集中刷新**连续刷完所有行，期间无法访存，形成**死时间**，比例为 $R\\cdot t_{\\text{刷}}/t$。
- **分散刷新**在每个存取周期后顺带刷新一行，没有死区，但**存取周期变长**、系统变慢。
- **异步刷新**把 $t$ 均分给 $R$ 行，每过 $t/R$ 刷新一行，兼取两者之长，最常用。
- 刷新以**行**为单位，只需行地址，由存储器控制器自动完成，不占用 CPU。
> 易错：三种方式刷新总次数相同，差别只在时间如何分配；刷新不是指令，也不需要列地址。`,
    importance: 4,
    hint: '集中有死区，分散周期长，异步折中',
  },
  {
    id: 'co3-decoding-line-partial',
    subject: 'co',
    chapter: '存储系统',
    tags: ['地址译码', '片选'],
    title: '线选法与译码片选法有什么区别？部分译码为什么会地址重叠？',
    back: `## 结论
- **线选法**用一根高位地址线直接接一片的片选端，不用译码器，但地址**不连续**、空间浪费，只适合芯片很少的场合。
- **全译码**把全部高位地址送入译码器，每个存储单元的地址**唯一**，不存在重叠。
- **部分译码**只用部分高位地址译码，未参与译码的地址位取任意值都选中同一片，于是**一个单元对应多个地址**，即地址重叠。
- 片选信号通常**低电平有效**；片选无效时芯片数据线呈**高阻**，不会干扰总线。
> 易错：地址重叠会被误认为容量变大，还可能导致误改数据；线选法可能出现两片同时被选中。`,
    importance: 4,
    hint: '全译码地址唯一，部分译码会重叠',
  },
  {
    id: 'co3-bit-word-expansion-count',
    subject: 'co',
    chapter: '存储系统',
    tags: ['存储器扩展', '芯片数', '计算'],
    title: '字位同时扩展时，所需芯片数与各信号线如何连接？',
    back: `## 结论
- 位扩展片数 $=$ 目标字长 $\\div$ 单片字长；字扩展组数 $=$ 目标单元数 $\\div$ 单片单元数；总片数 $=$ 两者之积。
- 位扩展时各片**地址线与片选线全并联**，数据线分别接数据总线的不同位。
- 字扩展时各片**数据线并联**，由高位地址经译码器分别提供片选，保证同一时刻只选中一组。
- 例：$1\\mathrm{K}\\times 4$ 位芯片构成 $2\\mathrm{K}\\times 8$ 位主存需 $4$ 片。
> 易错：芯片地址引脚只接**低位**地址线，多出的高位地址一律用于片选；先位扩展凑字长，再字扩展凑容量。`,
    importance: 5,
    hint: '位置扩并数据，字扩展译片选',
  },
  {
    id: 'co3-dual-port-ram',
    subject: 'co',
    chapter: '存储系统',
    tags: ['双端口 RAM', '访存冲突'],
    title: '双端口 RAM 如何提高并行访存能力？冲突时怎么处理？',
    back: `## 结论
- **双端口 RAM** 具有两套独立的地址、数据与控制端口，允许两个部件（CPU 与 I/O、两个 CPU）同时访问同一存储器。
- 两端口访问**不同单元**时可以完全并行，各自独立读写。
- 访问**同一单元**时发生冲突，由仲裁逻辑决定谁先访问，另一方通过忙信号**插入等待**。
- 它提高的是**并行访问能力**，而多模块交叉存储器靠多个模块错开启动来提高**带宽**。
> 易错：双端口 RAM 也不能同时读写同一单元；冲突是让一方等待，而不是两次访问结果都出错。`,
    importance: 2,
    hint: '两套端口可并行，同单元须仲裁',
  },
  {
    id: 'co3-interleaving-n-word-time',
    subject: 'co',
    chapter: '存储系统',
    tags: ['低位交叉', '带宽', '计算'],
    title: '低位交叉存储器连续读取 $n$ 个字需要多少时间？带宽是多少？',
    back: `## 结论
- 设模块数为 $m$、存取周期为 $T$、总线传送周期为 $r$，各模块错开 $r$ 启动。
- 连续读取 $n$ 个字的时间 $t=T+(n-1)r$：第一个字要等一个完整存取周期，之后每 $r$ 就能送出一个字。
- 要不间断供数必须满足 $m\\ge T/r$；此时带宽可达单模块的 $m$ 倍，$m$ 越大越接近 $1/r$ 的极限。
- 高位交叉（顺序编址）中连续地址落在同一模块，只能扩展容量，**不能提高带宽**。
> 易错：$T$ 是存取周期、$r$ 是总线传送周期，两者不能混；$n$ 个字用的是 $(n-1)r$ 而不是 $nr$。`,
    importance: 5,
    hint: '首字等 T，其余每 r 一个字',
  },
  {
    id: 'co3-virt-addr-page-table-size',
    subject: 'co',
    chapter: '存储系统',
    tags: ['虚拟存储', '页表大小', '计算'],
    title: '虚拟地址如何划分字段？页表项位数与页表大小怎么算？',
    back: `## 结论
- 虚拟地址 $=$ **虚页号 $+$ 页内偏移**，偏移位数 $=\\log_{2}(\\text{页面大小})$；物理地址 $=$ **物理页号 $+$ 同一偏移**。
- 页表项位数 $=$ 物理页号位数 $+$ 有效位、修改位等标志位，常凑成整字节。
- 页表大小 $=$ 虚页数 $\\times$ 页表项长度，虚页数 $=2^{\\text{虚页号位数}}$。
- 页表本身按页存放时占 $\\dfrac{\\text{页表大小}}{\\text{页面大小}}$ 页，页数太多就要用**多级页表**。
> 易错：页内偏移在变换前后**不变**；多级页表省的是连续空间，代价是访存次数增加。`,
    importance: 5,
    hint: '虚页号加偏移，虚页数乘表项长',
  },
  {
    id: 'co3-tlb-access-count-page-fault',
    subject: 'co',
    chapter: '存储系统',
    tags: ['TLB', '缺页', '访存次数'],
    title: 'TLB 命中与缺失时各需访存几次？缺页时系统做了什么？',
    back: `## 结论
- 虚页号先查 **TLB**：命中即得物理页号，再用它访问 Cache 或主存取数，比缺失少一次查页表的访存。
- TLB 缺失时要到主存中查**页表**取页表项，再**回填 TLB**，故访存次数多一次。
- 页表项有效位为 $0$ 说明该页不在主存，触发**缺页**：操作系统从辅存调入页面、更新页表与 TLB，再重新执行这条指令。
- 最坏情况依次包含查 TLB、查页表、访存取数多次主存访问，缺页还要加一次磁盘 I/O。
> 易错：TLB 缺失**不等于**缺页；缺页时必然 TLB 缺失，但 TLB 缺失时页很可能就在主存里。`,
    importance: 5,
    hint: '命中少一次查表，缺页要调页',
  },
  {
    id: 'co3-paging-segmentation-segpage',
    subject: 'co',
    chapter: '存储系统',
    tags: ['页式', '段式', '段页式'],
    title: '页式、段式与段页式虚拟存储各有什么优缺点？',
    back: `## 结论
- **页式**：地址空间等分成固定大小的页，划分由硬件完成、对用户透明；页内无碎片，最后一页有**内部碎片**。
- **段式**：按逻辑单位（代码段、数据段）划分，长度可变、便于共享与保护，但会产生**外部碎片**。
- **段页式**：先按逻辑分段，段内再分页，兼具逻辑性与内存利用率，地址变换需先查**段表**再查**页表**，开销最大。
- 页式只需一次查表即可定位，段页式至少要查两次表。
> 易错：页的划分对用户不可见，段是用户可见的逻辑单位；共享与保护看**段**，内存分配看**页**。`,
    importance: 4,
    hint: '页有内部碎片，段有外部碎片',
  },

  // ==================== Cache（9 张） ====================
  {
    id: 'co3-cache-why-fast',
    subject: 'co',
    chapter: 'Cache',
    tags: ['Cache', '局部性'],
    title: 'Cache 为什么能提高访存速度？它对程序员透明吗？',
    back: `## 结论
- 依据**局部性原理**，把主存中近期会被访问的**块**整块复制到速度更快、容量更小的 Cache 中，让多数访问在上层完成。
- 块传送使一次缺失换来后续多次命中，用空间局部性摊薄调入开销。
- 命中率高时平均访问时间接近 Cache 而远小于主存，实现**速度接近 Cache、容量仍是主存**。
- 地址映射、替换与写回全部由**硬件**完成，对程序员与应用**透明**，程序无需改一行代码即可加速。
> 易错：Cache 的内容只是主存的**副本**，不是独立地址空间；命中率受容量、块大小与程序局部性共同影响。`,
    importance: 4,
    hint: '局部性加整块调入，硬件透明',
  },
  {
    id: 'co3-cache-miss-penalty-amat',
    subject: 'co',
    chapter: 'Cache',
    tags: ['Cache', '缺失代价', '计算'],
    title: '缺失代价是什么？平均访存时间 AMAT 怎么算？',
    back: `## 结论
- $AMAT=$ 命中时间 $+$ 缺失率 $\\times$ 缺失代价，缺失代价是从下层取回整块并装入 Cache 的时间。
- 同时访问时 $T_a=H\\cdot T_c+(1-H)\\cdot T_m$；先查 Cache 未命中再访存时 $T_a=T_c+(1-H)\\cdot T_m$。
- 例：$T_c=1$、$T_m=20$ 周期、$H=0.95$ 时，$T_a=0.95+0.05\\times 20=1.95$ 周期。
- 提高命中率、降低缺失代价（多级 Cache、写缓冲）都能缩短 $AMAT$。
> 易错：两种访存顺序的公式不能混用；先查 Cache 时 $T_c$ 已含在过程中，不要再重复相加。`,
    importance: 5,
    hint: '命中时间加缺失率乘代价',
  },
  {
    id: 'co3-cache-capacity-field-calc',
    subject: 'co',
    chapter: 'Cache',
    tags: ['Cache', '地址划分', '计算'],
    title: '由容量、块大小与路数如何求行数、组数和各地址字段的位数？',
    back: `## 结论
- 行数 $=\\dfrac{\\text{Cache 容量}}{\\text{块大小}}$；组数 $=\\dfrac{\\text{行数}}{\\text{路数}}$。
- 块内地址位数 $b=\\log_{2}(\\text{块大小})$，组号位数 $=\\log_{2}(\\text{组数})$。
- 标记位数 $=$ 主存地址位数 $-$ 组号位数 $-$ $b$；全相联没有组号字段，标记最长。
- 直接映射行号 $=$ 块号 $\\bmod$ 行数，组相联组号 $=$ 块号 $\\bmod$ 组数，组内可放任意一路。
> 易错：相联度加倍则组数减半、组号少一位，**标记反而多一位**。`,
    importance: 5,
    hint: '行数除块，组数除路，组号取对数',
  },
  {
    id: 'co3-cache-set-assoc-hardware',
    subject: 'co',
    chapter: 'Cache',
    tags: ['组相联', '标记比较'],
    title: '组相联 Cache 如何判断命中？为什么比较器数量随路数增加？',
    back: `## 结论
- 组相联把主存块按模映射到某一**组**，组内各路的标记必须**同时比较**，某路标记相等且有效位为 $1$ 即为命中。
- 因此每组需要**路数**个标记比较器，再用一个多路选择器按命中结果选出数据。
- **全相联**相当于只有一组、路数等于行数，要与所有行的标记比较，硬件成本最高，只用于行数很少的场合。
- **直接映射**每组只有一路，只需一个比较器，硬件最省，但冲突缺失最多。
> 易错：提高相联度降低冲突缺失，代价是比较器与选择器成倍增长，本质是**用硬件换命中率**。`,
    importance: 3,
    hint: '组内标记同时比，路数即比较器数',
  },
  {
    id: 'co3-cache-replace-lru-hw',
    subject: 'co',
    chapter: 'Cache',
    tags: ['LRU', '替换算法'],
    title: 'LRU 替换算法如何用硬件实现？它比 FIFO、随机好在哪？',
    back: `## 结论
- **LRU** 淘汰最久未被访问的块，符合时间局部性，命中率通常高于 FIFO 与随机算法。
- 实现思路一：每行设一个**计数器**，命中行清零、其余加一，替换时选计数值最大的一路。
- 实现思路二：用**栈**记录各路的访问顺序，命中或装入时把该路移到栈顶，栈底就是最久未使用者。
- 比较只需在**组内**（全相联则在全部行之间）进行，故路数越多硬件越复杂。
> 易错：LRU 要记录访问历史，开销大于 FIFO；LFU 按访问**次数**淘汰，依据与 LRU 不同。`,
    importance: 3,
    hint: '计数器清零或栈顶移动，栈底淘汰',
  },
  {
    id: 'co3-cache-write-buffer',
    subject: 'co',
    chapter: 'Cache',
    tags: ['写策略', '写缓冲'],
    title: '写缓冲起什么作用？写直达与写回为什么都需要它？',
    back: `## 结论
- **写缓冲**是位于 Cache 与主存之间的小容量先进先出队列：CPU 把数据放入缓冲即可继续执行，由缓冲在后台写入主存。
- **写直达**每次写命中都写主存，访存频繁，配写缓冲后能把等待主存的时间隐藏起来。
- **写回**只在被替换且**脏位**为 $1$ 时才写回主存，平时不占总线，但替换瞬间可能需要等待。
- 写不命中时，**写分配**先调入整块再写，**非写分配**直接写主存；常见搭配是写回配写分配、写直达配非写分配。
> 易错：缓冲填满时 CPU 仍会被迫等待；写回法的 Cache 与主存可能**不一致**，需一致性协议维护。`,
    importance: 4,
    hint: '写缓冲隐藏写主存时间',
  },
  {
    id: 'co3-cache-split-unified',
    subject: 'co',
    chapter: 'Cache',
    tags: ['分离 Cache', '统一 Cache'],
    title: '分离 Cache 与统一 Cache 各有什么利弊？为什么 L1 要分指令与数据 Cache？',
    back: `## 结论
- **统一 Cache** 把指令与数据放在一起，容量利用率高、总缺失率较低，但取指与取数会争用同一个 Cache。
- **分离 Cache** 把 L1 分成**指令 Cache 与数据 Cache**，两者可同时访问，取指与访存不再争用端口，便于流水线并行。
- 代价是同样容量被拆成两块，各自命中率略低，且需要两套标记与替换逻辑。
- 典型方案是 **L1 分离、L2 统一**：L1 保证带宽，L2 保证容量与命中率。
> 易错：分离 Cache 的收益是**减少结构冒险**，不是提高命中率；统一 L2 能同时容纳指令与数据，利用率更高。`,
    importance: 3,
    hint: 'L1 分离减冲突，L2 统一省空间',
  },
  {
    id: 'co3-multilevel-cache-avg-time',
    subject: 'co',
    chapter: 'Cache',
    tags: ['多级 Cache', '平均访问时间', '计算'],
    title: '多级 Cache 中 L1、L2、L3 各承担什么角色？平均访问时间如何逐层计算？',
    back: `## 结论
- 典型分工：**L1** 紧邻 CPU、容量小速度最快；**L2** 居中，常由各核独享；**L3** 容量最大，由所有核共享。
- L1 缺失时到 L2 去取，避免每次都访主存，从而显著**降低平均缺失代价**。
- 设 L1 命中时间 $t_1$、缺失率 $M_1$，L2 为 $t_2$ 与 $M_2$，主存为 $t_m$，则 $T_a=t_1+M_1\\big(t_2+M_2\\cdot t_m\\big)$。
> 易错：L2 的缺失率是**局部缺失率**，指 L1 缺失的访问中又在 L2 缺失的比例，别与全局缺失率混用。`,
    importance: 5,
    hint: 'L1 快 L3 大，逐层嵌套算时间',
  },
  {
    id: 'co3-cache-block-size-tradeoff',
    subject: 'co',
    chapter: 'Cache',
    tags: ['块大小', '命中率'],
    title: '块大小如何影响命中率与缺失代价？块、行数与容量是什么关系？',
    back: `## 结论
- 容量一定时块越大则**行数越少**：行数 $=\\dfrac{\\text{容量}}{\\text{块大小}}$。
- 块增大能更好地利用**空间局部性**，减少首次访问造成的强制缺失；但行数随之减少，**冲突缺失**增加。
- 块越大不仅命中时间略升，**缺失代价**也明显上升，因为每次都要从主存搬来更长的一块。
- 所以块大小存在**最优值**，通常取几个字到一个存储字组，并与主存的传送单位相匹配。
> 易错：不能只按局部性一味加大块；行数太少还会放大替换算法的影响，冲突缺失可能反超收益。`,
    importance: 3,
    hint: '块大省强制缺失，行少增冲突',
  },
];
