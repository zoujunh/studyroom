import type { SeedCard } from '../seed-cards';

/**
 * 操作系统·文件系统与 IO 管理补充种子卡（24 张：文件系统 14、IO 管理 10）
 * chapter 只取：文件系统 / IO 管理
 * back 语法限定：## 小标题、- 列表、**粗体**、`行内代码`、$行内公式$、$$块公式$$、> 易错提醒
 */
export const OS_BANK4: SeedCard[] = [
  // ==================== 文件系统（14 张） ====================
  {
    id: 'os4-file-concept-ops',
    subject: 'os',
    chapter: '文件系统',
    tags: ['文件', '基本操作'],
    title: '文件有哪些属性，create、delete、read、write 分别做什么？',
    back: `## 结论
- 文件是具有符号名的相关数据集合，属性含**名字、标识符、类型、位置、大小、保护信息、时间戳**。
- **create**：在目录中新建一个目录项，登记文件名与属性。
- **delete**：找到目录项，回收它占用的**磁盘块**并删除目录项。
- **read/write**：按逻辑位置把数据在**内存与磁盘**之间传送。
- **open/close** 只与索引结点和打开文件表打交道，**读写才是真正访问数据**。
> 易错：删除文件会同时释放空间，而删除**目录项（硬链接名）**只是断掉一个名字。`,
    importance: 4,
    hint: '建删管目录项，读写搬数据，开关管表',
  },
  {
    id: 'os4-open-close-tables',
    subject: 'os',
    chapter: '文件系统',
    tags: ['打开文件表', '系统调用'],
    title: '打开文件与关闭文件分别做了什么，打开文件表怎么组织？',
    back: `## 结论
- 每当进程读写文件时需先**打开文件**，把文件信息装入内存，而不是每次都从磁盘检索目录。
- **打开**：按路径名逐级查目录找到 FCB，把它复制到内存并建立表项；**进程级**打开文件表按**文件描述符（下标）**索引，其中存读写位置与打开方式。
- **系统级**打开文件表记录文件名、inode 与**打开计数**，多进程打开同一文件时共享，计数归零才可删除表项。
- **关闭**：删除该进程的表项，系统级表项计数减一，并断开与内存索引结点的连接。
> 易错：open 并不把**文件数据**读入内存，只把**目录信息**读入内存，这是概念题的常设陷阱。`,
    importance: 5,
    hint: '进程级记读写位置，系统级记打开计数',
  },
  {
    id: 'os4-logical-vs-physical',
    subject: 'os',
    chapter: '文件系统',
    tags: ['逻辑结构', '物理结构'],
    title: '文件的逻辑结构与物理结构有什么区别？',
    back: `## 结论
- **逻辑结构**是用户看到的记录组织方式，分无结构（流式）文件与有结构（记录式）文件：顺序文件、索引文件、索引顺序文件。
- **物理结构**是操作系统看到的盘块组织方式，指连续分配、链接分配与索引分配。
- 同一逻辑结构的文件可以映射为不同物理结构；逻辑结构由用户决定，物理结构由系统决定。
- **无结构文件**把数据看成字节流，在磁盘上仍要按块分配。
> 易错：目录项中记录的**物理位置**属于物理结构；"记录定长还是变长"属于逻辑结构，二者不可混为一谈。`,
    importance: 5,
    hint: '逻辑看记录，物理看盘块',
  },
  {
    id: 'os4-inode-disk-vs-memory',
    subject: 'os',
    chapter: '文件系统',
    tags: ['索引结点', 'inode'],
    title: '磁盘索引结点与内存索引结点有什么区别，inode 起什么作用？',
    back: `## 结论
- **磁盘索引结点**存放文件的静态信息：模式、链接计数、大小、时间戳、物理地址项表。
- **内存索引结点**是文件打开后在内存建立的副本，除上述内容外还增设**引用计数、是否已修改、进程打开的索引结点链指针**等动态信息。
- inode 中**不存文件名**；目录项只留"文件名 $+$ 索引结点号"，于是目录项变短，检索更快。
- 靠 inode 号而非名字定位文件，才能用**硬链接**让多个名字指向同一文件。
> 易错：索引结点的**引用计数为零**后文件才真正被删除；磁盘索引结点常排在磁盘的固定区域，编号与位置可换算。`,
    importance: 4,
    hint: '磁盘存静态，内存存动态，inode 无文件名',
  },
  {
    id: 'os4-dir-structures',
    subject: 'os',
    chapter: '文件系统',
    tags: ['目录结构', '辨析'],
    title: '单级、两级、树形与无环图目录结构各有什么优缺点？',
    back: `## 结论
- **单级目录**：全系统一张目录表，实现简单，但不允许重名、查找慢、不便共享。
- **两级目录**：分出主目录与用户目录，解决了**不同用户重名**问题并便于隔离，但用户不能自行分类。
- **树形目录**：可分层分类、便于权限控制与路径定位，缺点是路径变长、不便共享，且查找要走多级。
- **无环图目录**：在树基础上允许**共享**（软链接、硬链接），但删除文件要判断是否还有引用，且需防止形成环路。
> 易错：树形目录不用链接，所以**不存在共享**；只有无环图目录才允许一个文件出现在多个目录下。`,
    importance: 4,
    hint: '重名靠两级，分类靠树形，共享靠无环图',
  },
  {
    id: 'os4-dir-implementation',
    subject: 'os',
    chapter: '文件系统',
    tags: ['目录实现', '检索'],
    title: '目录的线性表实现与哈希表实现各有什么特点？',
    back: `## 结论
- **线性表**：目录项排成一张线性表，查找时**逐个比较文件名**，创建简单但目录项多时检索效率低。
- 改进办法是把文件名单独建索引，或按名排序后**二分查找**，减少比较次数。
- **哈希表**：按文件名算出散列值直接定位目录项，平均检索为**常数时间** $O(1)$，但需要处理**冲突**。
- 目录项**不等长**时插入删除要移动数据，通常让目录项定长，或把文件名正文放到独立的堆中。
> 易错：哈希查找必须解决冲突（如开放定址、链地址法）；删除目录项后该槽位要妥善回收。`,
    importance: 3,
    hint: '线性表逐项比，哈希一次定位但有冲突',
  },
  {
    id: 'os4-access-control-list',
    subject: 'os',
    chapter: '文件系统',
    tags: ['文件保护', '访问控制'],
    title: '访问控制表如何实现文件保护，还有哪些保护手段？',
    back: `## 结论
- **访问控制表（ACL）**为每个文件规定**哪些用户可执行哪些操作**（读、写、执行、删除等），访问前核对用户身份。
- 精简做法是**将用户分组**，同一组用户分配一组权限，避免为每个用户单独登记。
- 常见的保护手段还有**用户口令**与**加密**，前者易泄露、后者密钥管理复杂；**访问矩阵**是权限关系的抽象模型。
- 保护还需对**目录**设置权限，否则用户可绕过目录破坏文件结构。
> 易错：访问控制表从**文件**出发列出可用用户（列长可变，可能很大）；能力表从**用户**出发列出可操作对象，两者互为补充。`,
    importance: 3,
    hint: 'ACL 由文件列用户，能力表由用户列文件',
  },
  {
    id: 'os4-fs-layers',
    subject: 'os',
    chapter: '文件系统',
    tags: ['层次结构', '文件系统'],
    title: '文件系统分成哪些层次，各层职责如何划分？',
    back: `## 结论
- 自上而下依次为：**用户接口 → 文件目录系统 → 存取控制模块 → 逻辑文件系统与文件信息缓冲区 → 物理文件系统 → 设备管理 → 辅助分配**。
- **用户接口**面向用户提供"按名存取"与创建、删除、打开、读写等操作。
- **文件目录系统**按路径名找到 FCB 及其索引结点；**存取控制**核对用户权限。
- **逻辑文件系统**把记录位置换算成逻辑块号并管理缓冲区；**物理文件系统**把逻辑块号换算成物理地址。
- **设备管理**启动驱动程序；**辅助分配**管理空闲磁盘块。
> 易错：问到"谁把逻辑块号变成物理块号"要答**物理文件系统**，这是本考点最常设的辨析陷阱。`,
    importance: 4,
    hint: '接口、目录、存取控制、逻辑、物理、设备、辅助分配',
  },
  {
    id: 'os4-alloc-methods-compare',
    subject: 'os',
    chapter: '文件系统',
    tags: ['物理结构', '分配方式'],
    title: '连续分配、链接分配与索引分配各自适用于什么场景？',
    back: `## 结论
- **连续分配**：顺序读写最快、支持随机访问，但不易增长、有外部碎片，适合写入后很少改动的大文件。
- **隐式链接**：每块末尾存下一块指针，便于增长、无碎片，但**只能顺序访问**，且指针占空间。
- **显式链接**：所有指针集中成**文件分配表**并常驻内存，从而支持随机访问，代价是表本身占用内存。
- **索引分配**：集中存块号，既能随机访问又便于增长，代价是索引块开销，适合随机访问频繁的大文件。
> 易错：链接分配的指针存放在**数据盘块**中，并不占索引块；文件分配表的项数由**磁盘总块数**决定，与文件个数无关。`,
    importance: 5,
    hint: '连续快而难长，链接易长而慢，索引两全但费块',
  },
  {
    id: 'os4-implicit-vs-explicit-chain',
    subject: 'os',
    chapter: '文件系统',
    tags: ['链接分配', 'FAT'],
    title: '隐式链接与显式链接（FAT）在访问方式上有什么差别？',
    back: `## 结论
- **隐式链接**把指针存在每个盘块的**末尾**，首块指针存在 FCB 里，只能从头顺着指针走，**不支持随机访问**，查第 $i$ 块需读前 $i$ 个盘块。
- **显式链接**把全部指针放进**文件分配表（FAT）**，表项以块号为下标，内容为下一块号，文件首块号存在 FCB 中。
- FAT 常驻内存后，查块号**不必再访问磁盘**，于是显式链接支持**随机访问**，也便于回收与追加。
- 代价是 FAT 体积随磁盘容量增大；为提高可靠性，FAT 常需**多副本**保存。
> 易错：显式链接的随机访问优势来自 FAT 常驻内存，若 FAT 不在内存，查表仍要访盘，优势不复存在。`,
    importance: 4,
    hint: '隐式指针在盘块只能顺序，显式指针在 FAT 可随机',
  },
  {
    id: 'os4-hybrid-index-maxsize',
    subject: 'os',
    chapter: '文件系统',
    tags: ['最大文件长度', '计算'],
    title: '混合索引下的最大文件长度如何计算？',
    back: `## 结论
- 设盘块大小与地址项大小之商为 $n$，有 $10$ 个直接项、一级与二级间接各一个。
- 一级间接覆盖 $n$ 个数据块，二级间接覆盖 $n^2$ 个，三级间接覆盖 $n^3$ 个。
- 最大长度 $=$ 直接项数 $\\times$ 盘块大小 $+n\\times$ 盘块大小 $+n^{2}\\times$ 盘块大小 $+\\cdots$
- 例：$4$ KB 盘块、$4$ B 地址项时 $n=1024$，两个直接项时最大为 $2\\times 4\\text{KB}+4\\text{MB}+4\\text{GB}$。
> 易错：切勿把 $n$ 与盘块大小混用；算间接项覆盖块数时**每一级都要连乘一次 $n$**，最后还要把直接项与各级间接项**相加**。`,
    importance: 5,
    hint: '一级乘 n，二级乘 n²，最后相加再乘块大小',
  },
  {
    id: 'os4-bitmap-space-calc',
    subject: 'os',
    chapter: '文件系统',
    tags: ['位示图', '计算'],
    title: '位示图需要多少字节，字号位号与块号怎样换算？',
    back: `## 结论
- 位示图用**一位表示一个磁盘块**的空闲状态，所需字节数 $=\\text{磁盘块总数}\\div 8$。
- 设字长为 $L$ 位、块号从 $0$ 开始，则字号 $=\\lfloor\\text{块号}\\div L\\rfloor$，位号 $=\\text{块号}\\bmod L$。
- 反向换算：块号 $=$ 字号 $\\times L+$ 位号。
- 例：$N=1000$、$L=32$ 时需 $1000\\div 8=125$ 字节共 $32$ 字；块号 $100$ 在第 $3$ 字、第 $4$ 位。
> 易错：题目若说明**从 $1$ 开始编号**，换算要写成字号 $=\\lfloor(\\text{块号}-1)\\div L\\rfloor$、位号 $=(\\text{块号}-1)\\bmod L$，差一位就整题错。`,
    importance: 5,
    hint: '字节数除八，字号除字长取商，位号取余',
  },
  {
    id: 'os4-disk-sched-look',
    subject: 'os',
    chapter: 'IO 管理',
    tags: ['LOOK', '磁盘调度'],
    title: 'SCAN 与 LOOK、C-SCAN 与 C-LOOK 的差别在哪里？',
    back: `## 结论
- **SCAN** 要求磁头一直移动到**磁盘端点**才折返，即使端点之外已无请求；**LOOK** 只到**最边缘的请求处**即折返。
- **C-SCAN** 沿一个方向服务到端点，然后**快速返回到另一端**重新单向扫描；**C-LOOK** 只返回到**最内侧的请求**再扫描。
- LOOK 与 C-LOOK 省掉了对**无请求磁道**的空跑，因此总寻道距离一般小于对应的 SCAN 与 C-SCAN。
- 计算平均寻道长度时，用**磁头移动的磁道总数除以请求个数**。
> 易错：C-SCAN、C-LOOK 使各磁道等待时间更**均匀**且**不会饥饿**；SSTF 与 FCFS 都可能让远端请求长期得不到服务。`,
    importance: 5,
    hint: 'SCAN 跑到端点，LOOK 只到最远请求',
  },

  // ==================== IO 管理（10 张） ====================
  {
    id: 'os4-io-device-classify',
    subject: 'os',
    chapter: 'IO 管理',
    tags: ['IO 设备', '分类'],
    title: 'I/O 设备按哪些方式分类，块设备与字符设备有何不同？',
    back: `## 结论
- 按**信息交换单位**分为块设备与字符设备：**块设备**以数据块为单位传输，可寻址、可随机读写；**字符设备**以字节流为单位传输，通常不可寻址。
- 按**共享属性**分为独占设备（一次只准一个进程使用）、共享设备（可并发使用）与虚拟设备（用软件把独占设备改造成可共享）。
- 按**传输速率**还可分为低速、中速与高速设备。
- 块设备多用缓冲与 DMA，字符设备多以中断方式驱动，二者的驱动程序差别很大。
> 易错：磁盘属于块设备，键盘属于字符设备；**字符设备不能随机访问**只表示它不按块寻址，不代表不能接受中断。`,
    importance: 3,
    hint: '按单位分块与字符，按共享分独占共享虚拟',
  },
  {
    id: 'os4-io-control-methods',
    subject: 'os',
    chapter: 'IO 管理',
    tags: ['IO 控制方式', '对比'],
    title: '程序查询、中断驱动、DMA 与通道四种 I/O 控制方式有何不同？',
    back: `## 结论
- **程序查询**：CPU 反复读设备状态寄存器直到就绪，再亲自搬运数据，CPU 干预最频繁、效率最低。
- **中断驱动**：设备就绪后向 CPU 发中断，CPU 在中断服务程序中搬运；**每传一个字节就中断一次**，干预频率仍高。
- **DMA**：由 DMA 控制器直接在内存与设备间传送**整块**数据，CPU 只在传送前后各干预一次，适合块设备。
- **通道**：CPU 只发一条启动命令，由通道执行通道程序控制一组设备，**CPU 干预频率最低**。
> 易错：按 CPU 干预频率排序为**程序查询 > 中断驱动 > DMA > 通道**；DMA 以数据块为单位，中断方式通常以字节为单位。`,
    importance: 5,
    hint: '查询最费 CPU，通道最省 CPU',
  },
  {
    id: 'os4-device-alloc-datatables',
    subject: 'os',
    chapter: 'IO 管理',
    tags: ['设备分配', '数据结构'],
    title: '设备分配用到哪几张表，静态分配与动态分配有何区别？',
    back: `## 结论
- **DCT（设备控制表）**：每台设备一张，记录设备状态、队列指针与控制器指针。
- **COCT（控制器控制表）**：每个控制器一张；**CHCT（通道控制表）**：每个通道一张；**SDT（系统设备表）**：全系统一张，登记各设备及驱动程序入口。
- 分配时由系统设备表找到设备及其控制器、通道，三者都空闲才可分配，并把进程排入相应等待队列。
- **静态分配**在作业运行前一次分好，不会死锁但利用率低；**动态分配**运行时按需申请，效率高但须做**安全性检查**。
> 易错：分配失败时进程被放入该设备的**等待队列**（阻塞），并**不占用**已分到的控制器与通道，否则会形成环路导致死锁。`,
    importance: 4,
    hint: 'DCT 管设备，COCT 管控制器，CHCT 管通道，SDT 总览',
  },
  {
    id: 'os4-spooling-detail',
    subject: 'os',
    chapter: 'IO 管理',
    tags: ['SPOOLing', '虚拟设备'],
    title: 'SPOOLing 系统由哪几部分组成，怎样实现虚拟设备？',
    back: `## 结论
- 组成：磁盘上的**输入井与输出井**、内存中的输入输出**缓冲区**、**输入进程与输出进程**、**井管理程序**。
- 以打印为例：进程需打印时把数据写入**输出井**后即可返回，由输出进程在打印机空闲且队列轮到它时真正打印。
- 于是进程"看到"的是一台随时可用的共享打印机，这就是**虚拟设备**：把**独占设备**改造成逻辑上的共享设备。
- SPOOLing 需要**大容量磁盘和多道程序设计**支持，能提高设备利用率与吞吐量。
> 易错：SPOOLing 只是**逻辑上**把设备虚拟成共享，物理上仍是独占；"井"在**磁盘**上而缓冲区在**内存**中，位置不要答反。`,
    importance: 4,
    hint: '井加进程加井管理，独占设备虚拟成共享',
  },
  {
    id: 'os4-buffer-purposes',
    subject: 'os',
    chapter: 'IO 管理',
    tags: ['缓冲区', '循环缓冲'],
    title: '缓冲技术有哪几种，缓冲区起什么作用？',
    back: `## 结论
- 按缓冲区的数量与组织分为**单缓冲、双缓冲、循环缓冲与缓冲池**四类。
- **循环缓冲**用多个等长缓冲区首尾相接，配合输入、输出指针与计算指针，适合**输入输出速度相差不大**的连续数据流。
- **缓冲池**把多个缓冲区集中管理，是所有进程共享的公共缓冲区，通常设空闲、输入、输出三条队列。
- 缓冲的核心作用是**缓和 CPU 与 I/O 设备的速度差异**，减少对 CPU 的中断频率，并解决数据**粒度不匹配**的问题。
> 易错：缓冲并不提高设备的物理传输速率，它靠**并行**换取吞吐量；缓冲在**内存**中，而"井"在磁盘上，两者不可混淆。`,
    importance: 4,
    hint: '单双循环加缓冲池，缓和速度差与粒度差',
  },
  {
    id: 'os4-device-independence-vs-driver',
    subject: 'os',
    chapter: 'IO 管理',
    tags: ['设备独立性', '逻辑设备名'],
    title: '设备独立性软件与设备驱动程序各负责什么？',
    back: `## 结论
- **设备独立性软件**又称与设备无关的系统软件，负责**逻辑设备名到物理设备名的映射**、统一接口、缓冲管理、设备分配与差错处理。
- **设备驱动程序**直接与硬件打交道，把上层给出的抽象命令翻译成对**设备寄存器**的具体操作，不同型号设备的驱动程序不同。
- 因此设备驱动程序**与设备相关**、位于下层，而设备独立性软件**与设备无关**、在其上层。
- 用逻辑设备名申请设备后，换一台同类物理设备**不必修改用户程序**，这正是设置这一层的目的。
> 易错：设备独立性软件要用到**逻辑设备表（LUT）**完成映射；驱动程序里才出现端口地址与寄存器操作，二者层次不可颠倒。`,
    importance: 4,
    hint: '独立性管映射与分配，驱动程序翻译成寄存器操作',
  },
  {
    id: 'os4-io-interrupt-handling',
    subject: 'os',
    chapter: 'IO 管理',
    tags: ['中断处理', 'IO 故障'],
    title: 'I/O 中断处理程序的工作流程是怎样的？',
    back: `## 结论
- 设备完成一次传送后发出**中断请求**，CPU 响应后保存现场并转入中断处理程序。
- 处理程序先**读取设备状态**判断本次操作是否出错或结束，再按状态作出相应处理。
- 若数据已就绪就把它从设备（或缓冲区）取走，**唤醒等待该设备的进程**，最后恢复现场并返回被中断的指令。
- I/O 故障处理的一般步骤为：**保护现场 → 分析故障原因 → 处理故障 → 恢复正常操作**。
> 易错：磁盘 I/O 完成后才发中断，**真正大量数据的搬运靠 DMA 或通道完成**，中断处理程序只负责收尾与唤醒，不要把它说成搬运数据的主体。`,
    importance: 4,
    hint: '读状态、取数据、唤醒进程、恢复现场',
  },
  {
    id: 'os4-buffer-pool',
    subject: 'os',
    chapter: 'IO 管理',
    tags: ['缓冲池', '队列'],
    title: '缓冲池由哪些队列组成，如何工作？',
    back: `## 结论
- 缓冲池是把多个缓冲区集中起来、供所有进程**共享**的公共缓冲区域，比专用缓冲高效。
- 缓冲区按状态挂成三条队列：**空闲队列**、**输入队列**、**输出队列**，每个缓冲区设标识、设备号等数据项。
- 需要输入时从空闲队列取一个缓冲区去装数据，装满后挂入**输入队列**；输出时先装入缓冲区再挂入**输出队列**。
- 取走数据的缓冲区可**重新挂回空闲队列**循环使用，于是缓冲区数量固定而设备与进程都能持续推进。
> 易错：用缓冲池后缓冲区的**利用率**与系统并发度都会提高，但**并没有消除**设备与 CPU 的速度差，只是把它隐藏起来。`,
    importance: 4,
    hint: '空闲、输入、输出三条队列循环使用',
  },
  {
    id: 'os4-disk-address-capacity',
    subject: 'os',
    chapter: '文件系统',
    tags: ['磁盘', '容量计算'],
    title: '磁盘容量怎么算？磁盘地址由哪几部分组成？',
    back: `## 结论
- 磁盘容量 = **盘面数 × 每面磁道数 × 每道扇区数 × 每扇区字节数**。
- 磁盘地址 = **柱面号（磁道号）+ 盘面号（磁头号）+ 扇区号**，按柱面优先编址以**减少寻道**。
- 柱面是所有盘面上**半径相同**的磁道集合：读写时先寻道定位柱面，再选盘面，最后等扇区转过来（旋转延迟）。
> 易错：一个盘片有两个盘面，通常一个盘面对应一个磁头，别把「盘面数」与「盘片数」搞混。`,
    importance: 4,
    hint: '容量 = 面 × 道 × 扇 × 字节',
  },
  {
    id: 'os4-format-and-boot-block',
    subject: 'os',
    chapter: '文件系统',
    tags: ['格式化', '引导'],
    title: '低级格式化与高级格式化有什么区别？引导块做什么？',
    back: `## 结论
- **低级格式化（物理格式化）**：划分扇区、写入扇区头尾与校验信息，一般出厂前完成，会破坏盘上原有数据。
- **高级格式化（逻辑格式化）**：在分区上建立文件系统的**根目录与空闲空间管理结构**，由用户执行。
- **引导块**：存放自举程序，开机时由 ROM 中的自举程序读入并执行，进而把操作系统装入内存。
> 易错：整个磁盘的**主引导记录 MBR** 与某个分区的**引导块**是两回事，别混为一谈。`,
    importance: 3,
    hint: '低级划扇区、高级建文件系统',
  },
  {
    id: 'os4-free-space-group-link',
    subject: 'os',
    chapter: '文件系统',
    tags: ['空闲空间', '成组链接'],
    title: '成组链接法是怎么管理空闲块的？',
    back: `## 结论
- 把空闲块**分组**（例如每组 100 块），每组第一块记录**下一组的块号与块数**，形成链；超级块中保存第一组的信息。
- 分配时从当前组取块；当一组**只剩最后一块**时，先把这一块的内容（下一组信息）读入内存，再继续分配，形成链式跳转。
- 相比空闲表法（只能记录连续块）与空闲链表法（指针散落在各块中），成组链接**一次访盘就能取回一组块号**，既省空间又减少 I/O 次数。
> 易错：内存里维护的是**当前那一组**的块号表，不是把全部空闲块都读进内存。`,
    importance: 3,
    hint: '每组 100 块，组首块存下一组信息',
  },
];
