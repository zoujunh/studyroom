/**
 * 数据结构科目种子卡（40 张）
 *
 * back 字段语法限定：## 小标题、- 无序列表、**粗体**、行内代码、$行内公式$、$$块公式$$、> 提示。
 * 模板字符串内不写插值符号，LaTeX 反斜杠一律双写。
 */

import type { SeedCard } from '../seed-cards';

export const DS_BANK: SeedCard[] = [
  // ==================== 线性表（8 张） ====================
  {
    id: 'ds-linearlist-seq-move',
    subject: 'ds',
    chapter: '线性表',
    tags: ['顺序表', '复杂度'],
    title: '顺序表插入与删除平均各要移动多少个元素？',
    back: `## 结论
- 长为 $n$ 的顺序表，在第 $i$ 个位置插入需移动 $n-i+1$ 个元素，等概率下平均移动 $n/2$ 个。
- 删除第 $i$ 个元素需移动 $n-i$ 个元素，等概率下平均移动 $(n-1)/2$ 个。
- 两者时间复杂度都是 $O(n)$，但插入平均比删除多移动约半个元素。
> 易错：位置以 1 起算还是以下标 0 起算，公式相差 1；只在表尾插入或删除时都退化为 $O(1)$。`,
    importance: 4,
    hint: '插平均 n/2，删平均 (n-1)/2',
  },
  {
    id: 'ds-linearlist-linked-insert-delete',
    subject: 'ds',
    chapter: '线性表',
    tags: ['单链表', '指针操作'],
    title: '单链表的插入与删除为什么都要先找前驱？',
    back: `## 结论
- 单链表只能顺着指针向后走，插入或删除某个结点前必须先拿到它的**前驱**，否则无法改链。
- 已知前驱指针时插入只需改两条指针，时间 $O(1)$；若只知道结点自身，就得从头找前驱，为 $O(n)$。
- 删除 $p$ 的后继很方便，删除 $p$ 自身则要把它后继的数据搬到 $p$ 里再摘掉后继结点。
> 易错：靠"搬数据"删除结点只适用于 $p$ 不是尾结点的情况；带头结点能让首元位置的插删写法与中间位置统一。`,
    importance: 4,
    hint: '单链找前驱，插删先定位',
  },
  {
    id: 'ds-linearlist-head-tail-build',
    subject: 'ds',
    chapter: '线性表',
    tags: ['单链表', '建表'],
    title: '头插法与尾插法建立单链表有什么不同？',
    back: `## 结论
- **头插法**：新结点总是插到头结点之后，单次 $O(1)$，但链表中结点的顺序与输入顺序**相反**。
- **尾插法**：用一个尾指针始终指向表尾，新结点接在尾部，结点顺序与输入顺序**一致**。
- 尾插法若不设尾指针，每次都要从头找表尾，建表总时间会退化成 $O(n^{2})$。
> 易错：头插法因此常被用来就地逆置链表；改指针前要先保存后继地址，否则断链丢结点。`,
    importance: 3,
    hint: '头插逆序，尾插保序',
  },
  {
    id: 'ds-linearlist-double-pointer-ops',
    subject: 'ds',
    chapter: '线性表',
    tags: ['双向链表', '指针操作'],
    title: '双向链表中插入和删除结点各要修改几条指针？',
    back: `## 结论
- 在结点 $p$ 之后插入新结点 $s$，共需修改 **4 条**指针：$s$ 的前驱与后继、$p$ 的后继、原后继的前驱。
- 删除 $p$ 的后继 $q$，需修改 **2 条**指针：$p$ 的后继、$q$ 后继的前驱。
- 一般**先把新结点的两条链接好，再断开旧链**，否则会丢失后继地址。
> 易错：双链表多花一个指针域，却换来 $O(1)$ 找前驱；删除表尾结点时要判它的后继为空。`,
    importance: 4,
    hint: '双链插入改四条，删除改两条',
  },
  {
    id: 'ds-linearlist-circular-empty',
    subject: 'ds',
    chapter: '线性表',
    tags: ['循环链表', '判空'],
    title: '循环单链表与循环双链表的判空条件是什么？',
    back: `## 结论
- 带头结点的**循环单链表**为空时 $head\\to next=head$，即头结点的后继指向自己。
- 带头结点的**循环双链表**为空时还要 $head\\to prior=head$，前驱后继都指回头结点。
- 只设尾指针 $rear$ 时 $rear\\to next$ 就是头结点，故首尾操作都能 $O(1)$ 完成。
> 易错：循环链表中不存在空指针，判空不能写"某指针是否为空"；遍历以回到起点作为结束。`,
    importance: 4,
    hint: '循环表无空指针，回到头即止',
  },
  {
    id: 'ds-linearlist-static-cursor',
    subject: 'ds',
    chapter: '线性表',
    tags: ['静态链表', '存储结构'],
    title: '静态链表用什么充当指针？表尾如何表示？',
    back: `## 结论
- 静态链表用**数组下标**充当指针：每个结点含数据域与游标域，游标存放后继结点的下标。
- 通常下标为 $0$ 的结点充当**头结点**，游标取 $-1$（或 $0$）表示链表结束。
- 插入删除只需改游标，不必移动元素，兼具顺序存储"不用指针"与链式存储"插删方便"的优点。
> 易错：静态链表适合不支持指针的语言或需要长期驻留的数据；它的"指针"是下标而不是内存地址。`,
    importance: 3,
    hint: '数组下标当指针，-1 表结束',
  },
  {
    id: 'ds-linearlist-seq-vs-linked',
    subject: 'ds',
    chapter: '线性表',
    tags: ['顺序表', '链表', '辨析'],
    title: '顺序表和链表分别适合什么场景？',
    back: `## 结论
- **顺序表**支持 $O(1)$ 随机访问、存储密度高、访存局部性好，适合**查多改少、规模可预估**的场景。
- **链表**插入删除只需改指针、容量可动态增长，适合**频繁插删、长度变化大**的场景。
- 若以按下标随机读取为主就选顺序表；若总在中间位置增删就选链表。
> 易错：链表的存储密度小于 $1$，每个结点都多花一个指针域；顺序表插删的代价是移动元素，链表则是找位置。`,
    importance: 4,
    hint: '查多改少顺序表，频繁插删用链表',
  },
  {
    id: 'ds-linearlist-fast-slow-pointer',
    subject: 'ds',
    chapter: '线性表',
    tags: ['单链表', '快慢指针'],
    title: '如何找链表的中间结点、判断链表中是否有环？',
    back: `## 结论
- **快慢指针**：慢指针每次走 1 步，快指针每次走 2 步，快指针到达表尾时慢指针恰好在**中间结点**。
- 若两指针最终**相遇**，则链表**有环**；快指针先走到空则无环，时间 $O(n)$、空间 $O(1)$。
- 找环的入口：相遇后把一个指针放回表头，两指针改为每次各走 1 步，再次相遇处就是环的入口。
> 易错：求倒数第 $k$ 个结点也用双指针，让快指针先走 $k$ 步再同步后移。`,
    importance: 4,
    hint: '差速找中点，相遇即有环',
  },

  // ==================== 栈与队列（6 张） ====================
  {
    id: 'ds-stack-pop-sequence',
    subject: 'ds',
    chapter: '栈与队列',
    tags: ['栈', '出栈序列'],
    title: '怎样判断一个出栈序列是否合法？',
    back: `## 结论
- 手算办法：按入栈序列依次压栈，同时对照出栈序列，需要哪个元素就弹哪个，**能全部弹出则合法**。
- 快速判据：出栈序列中某个元素之后，比它**更早入栈**的那些元素必须按**逆序**出现，否则非法。
- $n$ 个不同元素依次入栈，合法的出栈序列共有卡特兰数 $\\dfrac{1}{n+1}C_{2n}^{n}$ 种。
> 易错：入栈与出栈可以交替进行，并不是全部入栈后再统一出栈；栈空时无法再执行出栈。`,
    importance: 5,
    hint: '模拟栈走一趟，能弹完即合法',
  },
  {
    id: 'ds-stack-shared-top',
    subject: 'ds',
    chapter: '栈与队列',
    tags: ['共享栈', '判满'],
    title: '两个栈共享一段空间时如何判断栈满？',
    back: `## 结论
- 共享栈把两栈的栈底设在数组两端，栈顶指针**相向增长**。
- 设两栈顶为 $top_0$ 与 $top_1$，初始 $top_0=-1$、$top_1=n$，**判满**条件是 $top_0+1=top_1$。
- 判空条件：$0$ 号栈为 $top_0=-1$，$1$ 号栈为 $top_1=n$。
> 易错：判满是 $top_0+1=top_1$ 而非 $top_0=top_1$，因为栈顶指针指向各自栈顶元素。`,
    importance: 3,
    hint: '两栈顶相向，顶加一等顶即满',
  },
  {
    id: 'ds-queue-circular-formula',
    subject: 'ds',
    chapter: '栈与队列',
    tags: ['循环队列', '计算'],
    title: '循环队列的判空、判满与元素个数公式是什么？',
    back: `## 结论
- 牺牲一个单元区分空满：**队空** $front=rear$，**队满** $(rear+1)\\bmod n=front$。
- 元素个数为 $(rear-front+n)\\bmod n$，其中 $n$ 是数组容量。
- 若改用"队头 + 元素个数"或设标志位，就能用满 $n$ 个单元。
> 易错：$rear$ 指向队尾元素的**下一个**空位；入队改 $rear$、出队改 $front$，移动后都要对 $n$ 取模。`,
    importance: 5,
    hint: '空看 front=rear，满留一个空位',
  },
  {
    id: 'ds-deque-restricted',
    subject: 'ds',
    chapter: '栈与队列',
    tags: ['双端队列', '辨析'],
    title: '输入受限与输出受限的双端队列有什么不同？',
    back: `## 结论
- **双端队列**两端都能入队和出队；只允许一端插入的叫**输入受限**，只允许一端删除的叫**输出受限**。
- 判断某个序列能否得到，逐项试探：把下一个待入元素放到允许的一端，或从允许的一端弹出目标元素。
- 栈与队列都是双端队列的**特例**：同一端入出即栈，一端入另一端出即队列。
> 易错：两种受限方式并不等价，它们能产生的序列集合不同；判断哪端可以入、哪端可以出要先看清题目限制。`,
    importance: 3,
    hint: '受限方向决定能产生的序列',
  },
  {
    id: 'ds-stack-expression-recursion',
    subject: 'ds',
    chapter: '栈与队列',
    tags: ['栈', '表达式求值', '递归'],
    title: '栈在表达式求值与递归中各起什么作用？',
    back: `## 结论
- **中缀转后缀**：操作数直接输出，运算符与栈顶比较优先级，低就先弹出；左括号入栈，右括号弹到左括号为止。
- **后缀求值**：遇操作数压栈，遇运算符弹出两个操作数运算后再压回，最后栈顶即结果。
- **递归**：系统用栈保存每层的返回地址、参数与局部变量，递归太深就会栈溢出。
> 易错：后缀表达式**不需要括号**、也没有优先级歧义；转换时栈中优先级较高的运算符要先出栈。`,
    importance: 4,
    hint: '中缀转后缀，递归靠栈存现场',
  },
  {
    id: 'ds-queue-level-traversal',
    subject: 'ds',
    chapter: '栈与队列',
    tags: ['队列', '层次遍历'],
    title: '队列在树的层次遍历中怎样使用？',
    back: `## 结论
- 层次遍历：根结点先入队，然后反复出队一个结点并访问它，同时把它的孩子依次入队，直到队空。
- 图的广度优先搜索思路相同，只是必须配一个访问标记数组，防止顶点被重复入队。
- 队列保证按"离起点层数"由近及远访问，这正是 BFS 能求无权图最短路径的原因。
> 易错：若要按层分行输出，需要额外记录当前层的结点个数，或借助两个队列、层尾标记来分层。`,
    importance: 4,
    hint: '出队一个，孩子全入队',
  },

  // ==================== 串（3 张） ====================
  {
    id: 'ds-string-next-array',
    subject: 'ds',
    chapter: '串',
    tags: ['KMP', 'next 数组'],
    title: 'KMP 算法的 next 数组手工怎么求？',
    back: `## 结论
- next 只与**模式串自身**有关：$next[j]$ 等于前 $j-1$ 个字符中**最长相等前后缀**的长度加 1。
- 教材约定 $next[1]=0$：首字符就失配时主串后移一位、模式串从 1 号位重新开始。
- 手算时从短到长考察前后缀，取最长相等长度加 1；后缀不能取整个前缀。
> 易错：next 下标从 1 起还是 0 起，不同教材相差 1；失配后模式串右移 $j-next[j]$ 位。`,
    importance: 5,
    hint: 'next 是最长相等前后缀长度加一',
  },
  {
    id: 'ds-string-nextval',
    subject: 'ds',
    chapter: '串',
    tags: ['KMP', 'nextval'],
    title: 'nextval 数组解决了什么问题？如何递推？',
    back: `## 结论
- 若按 next 回退到的字符与当前字符**相同**，回退后必然再次失配，这些比较纯属浪费。
- 规则：当 $p_j\\neq p_{next[j]}$ 时 $nextval[j]=next[j]$；否则 $nextval[j]=nextval[next[j]]$。
- 例：模式串 aaaab 用 nextval 后无效回退明显减少。
> 易错：nextval 由 next **从左往右**递推得到，必须先算出前面的值。`,
    importance: 4,
    hint: '同则继承，异则照抄 next',
  },
  {
    id: 'ds-string-match-complexity',
    subject: 'ds',
    chapter: '串',
    tags: ['朴素匹配', 'KMP', '复杂度'],
    title: '朴素模式匹配与 KMP 的复杂度差在哪里？',
    back: `## 结论
- **朴素匹配**：失配后主串指针要回退到本次比较起点的下一位，最坏比较次数为 $O(nm)$。
- **KMP**：借助 next 数组，主串指针**永不回退**，比较次数 $O(n+m)$，代价是多花 $O(m)$ 存放 next。
- 根源是 KMP 把"已匹配部分"的信息提前算好，直接得出模式串右移多少位。
> 易错：$O(n+m)$ 中含求 next 的 $O(m)$；主串指针不回退，模式串指针仍会移动。`,
    importance: 4,
    hint: '朴素回退 O(nm)，KMP 不回退 O(n+m)',
  },

  // ==================== 树与二叉树（9 张） ====================
  {
    id: 'ds-tree-n0-n2',
    subject: 'ds',
    chapter: '树与二叉树',
    tags: ['二叉树性质', '计算'],
    title: '二叉树中 $n_0=n_2+1$ 是怎么推出来的？',
    back: `## 结论
- 设度为 $0$、$1$、$2$ 的结点数分别为 $n_0$、$n_1$、$n_2$，则总结点数 $n=n_0+n_1+n_2$。
- 由边数关系 $n-1=n_1+2n_2$ 与上式联立得 $n_0=n_2+1$。
- 该式对**任意**二叉树成立；完全二叉树中 $n_1$ 只能取 $0$ 或 $1$。
> 易错：叶子数总比双分支结点数多 1，常与完全二叉树的结点推算联考。`,
    importance: 5,
    hint: '叶子比双分支结点多一个',
  },
  {
    id: 'ds-tree-complete-numbering',
    subject: 'ds',
    chapter: '树与二叉树',
    tags: ['完全二叉树', '编号', '计算'],
    title: '完全二叉树顺序存储的编号关系与高度公式是什么？',
    back: `## 结论
- 从 1 编号时，结点 $i$ 的左孩子是 $2i$、右孩子是 $2i+1$、双亲是 $\\lfloor i/2\\rfloor$。
- 含 $n$ 个结点的完全二叉树**高度**为 $\\lfloor\\log_2 n\\rfloor+1$，即 $\\lceil\\log_2(n+1)\\rceil$。
> 易错：数组从 0 起存时孩子变为 $2i+1$ 与 $2i+2$，不能混用。`,
    importance: 4,
    hint: '左 2i 右 2i+1，高度取整加一',
  },
  {
    id: 'ds-tree-level-to-preorder',
    subject: 'ds',
    chapter: '树与二叉树',
    tags: ['遍历', '完全二叉树'],
    title: '已知完全二叉树的层序序列，能直接写出先序与中序序列吗？',
    back: `## 结论
- 能。完全二叉树的层序序列就是它的**顺序存储**，编号为 $i$ 的结点，孩子是编号 $2i$ 与 $2i+1$ 的结点。
- **先序**按"根、左、右"递归：先输出根结点，再递归处理左右孩子。
- **中序**按"左、根、右"递归：先递归左孩子，再输出根结点，最后递归右孩子。
> 易错：孩子为 $2i$ 与 $2i+1$ 以"从 1 编号"为前提，数组下标从 0 起时要先换成编号再套公式。`,
    importance: 4,
    hint: '顺序存层序，递归写先中',
  },
  {
    id: 'ds-tree-pre-post-unique',
    subject: 'ds',
    chapter: '树与二叉树',
    tags: ['遍历', '定树', '计算'],
    title: '前序序列与后序序列什么时候才能唯一确定二叉树？',
    back: `## 结论
- 要让前序与后序序列完全相同，只有**空树或单结点树**才做得到。
- 由前序加后序**一般不能唯一确定**二叉树；能唯一的充要条件是树中**不存在只有一个孩子的结点**。
- 常考计算：已知前序与后序问可能的树形棵数，每遇到一个度为 $1$ 的结点就乘 $2$。
> 易错：度为 $1$ 的结点无法从前序后序判断孩子挂在左边还是右边，这正是多解的唯一来源。`,
    importance: 4,
    hint: '单孩子结点可左可右，遇一个乘二',
  },
  {
    id: 'ds-tree-threaded-successor',
    subject: 'ds',
    chapter: '树与二叉树',
    tags: ['线索二叉树', '前驱后继'],
    title: '中序线索二叉树中怎样找某结点的前驱与后继？',
    back: `## 结论
- 若结点 $p$ 的**右标志为线索**，后继就是它的右指针；否则后继是**右子树中最左下的结点**。
- 若结点 $p$ 的**左标志为线索**，前驱就是它的左指针；否则前驱是**左子树中最右下的结点**。
- 有了线索，中序遍历可以不用栈、不用递归，在 $O(1)$ 辅助空间内完成。
> 易错：一定先看标志位，再决定走线索还是走子树；后序线索找后继往往还需知道双亲，故不如中序线索好用。`,
    importance: 4,
    hint: '先看标志位，线索直走否则下钻',
  },
  {
    id: 'ds-tree-forest-convert',
    subject: 'ds',
    chapter: '树与二叉树',
    tags: ['树与森林', '转换'],
    title: '树与森林怎样转换成二叉树？遍历如何对应？',
    back: `## 结论
- 口诀是"**左孩子右兄弟**"：二叉树中左指针指向第一个孩子，右指针指向下一个兄弟。
- 森林转换时先把各棵树的根看成兄弟，再统一套用左孩子右兄弟规则连成一棵二叉树。
- 遍历对应：树的**先根**遍历等于对应二叉树的**先序**，树的**后根**遍历等于对应二叉树的**中序**。
> 易错：森林的"中序"遍历对应的是各棵树的后根遍历，别与层次遍历混淆；转换后二叉树的根结点没有右子树。`,
    importance: 4,
    hint: '左孩子右兄弟，先根配先序',
  },
  {
    id: 'ds-tree-union-find',
    subject: 'ds',
    chapter: '树与二叉树',
    tags: ['并查集', '树的应用'],
    title: '并查集如何实现？凭什么接近 $O(1)$？',
    back: `## 结论
- 并查集用**双亲表示法**的数组存储，每个集合是一棵树，树根的下标就是这个集合的代表元。
- 查找操作沿双亲指针一路上溯到根；合并操作把一棵树的根挂到另一棵树的根上。
- 两种优化：**按秩（或按规模）合并**与**路径压缩**，配合后单次操作近似 $O(\\alpha(n))$，可视为常数。
> 易错：判断两个元素是否属于同一集合，必须比较它们的**根**是否相同；路径压缩改变树形但不改变集合划分。`,
    importance: 4,
    hint: '上溯到根，按秩合并加路径压缩',
  },
  {
    id: 'ds-tree-bst-delete',
    subject: 'ds',
    chapter: '树与二叉树',
    tags: ['二叉排序树', '删除'],
    title: '二叉排序树的查找与删除结点怎么操作？',
    back: `## 结论
- **查找**：从根出发，比当前结点小就往左、大就往右，平均 $O(\\log n)$，退化成单支树时最坏 $O(n)$。
- **删除叶子**直接删除；**只有一个孩子**时用这个孩子顶替它。
- **有两个孩子**时，用**中序前驱或中序后继**的值替换它，再把那个前驱（或后继）结点真正删掉。
> 易错：插入的新结点一定落在**叶子**位置；中序遍历二叉排序树必得递增有序序列，这是判断与验证的依据。`,
    importance: 5,
    hint: '双孩子换中序前驱，再删叶子',
  },
  {
    id: 'ds-tree-avl-min-nodes',
    subject: 'ds',
    chapter: '树与二叉树',
    tags: ['平衡二叉树', '计算'],
    title: '高度为 $h$ 的平衡二叉树最少有多少个结点？',
    back: `## 结论
- 要让高度最大而结点最少，根的两棵子树高度只能差 $1$，故 $N_h=N_{h-1}+N_{h-2}+1$，$N_0=0$、$N_1=1$。
- 依次得 $1,2,4,7,12,20,33$，即"结点最少时的高度"序列。
- 故含 $n$ 个结点的平衡二叉树高度为 $O(\\log n)$，查找才高效。
> 易错：红黑树不要求左右子树高度差不超过 $1$，它靠颜色约束把最长路径限制在最短路径两倍内，插删调整更少。`,
    importance: 4,
    hint: '最少结点按斐波那契式递推',
  },

  // ==================== 图（6 张） ====================
  {
    id: 'ds-graph-orthogonal-multilist',
    subject: 'ds',
    chapter: '图',
    tags: ['存储结构', '十字链表'],
    title: '十字链表与邻接多重表分别适合存储什么图？',
    back: `## 结论
- **十字链表**用于**有向图**：每个顶点有一条出弧链，每个弧结点同时挂在弧尾与弧头两条链上，出入度都好求。
- **邻接多重表**用于**无向图**：每条边只用一个边结点，同时挂在两个端点的链上，删除边、做访问标记都很方便。
- 普通邻接表存无向图时每条边要存两遍，删边还要改两个链表，这正是引入邻接多重表的原因。
> 易错：两者都不改变空间量级 $O(n+e)$，改变的是边的存储冗余度与操作便捷性。`,
    importance: 3,
    hint: '十字链表存有向，多重表存无向',
  },
  {
    id: 'ds-graph-dfs-bfs-spanning-tree',
    subject: 'ds',
    chapter: '图',
    tags: ['DFS', 'BFS', '生成树'],
    title: '连通图的 DFS 与 BFS 生成树各有什么特点？',
    back: `## 结论
- 从某顶点出发遍历连通图，走过的边与全部 $n$ 个顶点构成**生成树**，边数一定是 $n-1$。
- **BFS 生成树最矮**，其高度等于起点到各顶点的最短路径长度；DFS 生成树则往往又高又瘦。
- 非连通图只能得到**生成森林**，每调用一次遍历就得到一个连通分量。
> 易错：生成树形状**不唯一**，取决于起点与邻接表中边表的次序；邻接表存储时两种遍历都是 $O(n+e)$，邻接矩阵则是 $O(n^{2})$。`,
    importance: 4,
    hint: 'BFS 树最矮，DFS 树瘦高',
  },
  {
    id: 'ds-graph-prim-vs-kruskal',
    subject: 'ds',
    chapter: '图',
    tags: ['最小生成树', '对比'],
    title: 'Prim 与 Kruskal 算法该怎么选？最小生成树唯一吗？',
    back: `## 结论
- **Prim**：从一顶点出发，每步并入"离当前树最近"的顶点，时间 $O(n^{2})$ 只与顶点数有关，适合**稠密图**。
- **Kruskal**：把边按权值从小到大挑，用并查集判断是否成环，时间 $O(e\\log e)$，适合**稀疏图**。
- 最小生成树的**权值和唯一**，树形未必唯一；各边权值**互不相同**时最小生成树才唯一。
> 易错：最小生成树不一定含权值最小的边（它可能与其他边成环）。`,
    importance: 5,
    hint: '稠密 Prim，稀疏 Kruskal',
  },
  {
    id: 'ds-graph-reverse-topo',
    subject: 'ds',
    chapter: '图',
    tags: ['逆拓扑排序', 'AOV'],
    title: '逆拓扑排序怎样实现？它有什么用途？',
    back: `## 结论
- 逆拓扑排序每轮输出一个**出度为 $0$** 的顶点，并删除所有指向它的边（即它的入边）。
- 得到的结果就是拓扑序列的**逆序**；实现时用栈或队列存出度为 $0$ 的顶点。
- 主要用途是求 AOE 网的关键路径：正拓扑推进最早发生时间，逆拓扑回推最晚发生时间。
> 易错：出度为 $0$ 与入度为 $0$ 不能混；输出顶点数少于 $n$ 时同样说明图中有环，所以它也能用来判环。`,
    importance: 3,
    hint: '出度为零，逆着删边',
  },
  {
    id: 'ds-graph-critical-path-count',
    subject: 'ds',
    chapter: '图',
    tags: ['关键路径', 'AOE', '计算'],
    title: '关键路径只有一条吗？缩短关键活动一定能缩短工期吗？',
    back: `## 结论
- 求法：先按拓扑序正推各顶点的最早发生时间 $ve$，再按逆拓扑序回推最晚发生时间 $vl$。
- 活动余量 $d=vl(终点)-ve(起点)-权值$，余量为 $0$ 的活动是**关键活动**，全部由关键活动连成的路径才叫关键路径。
- 关键路径**可能有多条**，条数要按 $ve=vl$ 的顶点逐条数清楚。
> 易错：缩短某个关键活动的持续时间未必缩短总工期——它可能不再是关键活动，工期瓶颈转移到了另一条关键路径上。`,
    importance: 4,
    hint: '余量为零才关键，关键路可多条',
  },
  {
    id: 'ds-graph-components-scc',
    subject: 'ds',
    chapter: '图',
    tags: ['连通分量', '强连通分量'],
    title: '连通分量与强连通分量有什么区别？怎样计数？',
    back: `## 结论
- **无向图**中极大的连通子图叫**连通分量**；每个未访问顶点各发起一次遍历即可数出个数。
- **有向图**中任意两顶点互相可达的极大子图叫**强连通分量**；$n$ 个顶点要强连通至少要 $n$ 条边。
- $n$ 个顶点的无向图要保证连通最少需 $n-1$ 条边，最多可有 $\\dfrac{n(n-1)}{2}$ 条边。
> 易错："任意两点互相可达"与"存在一条路径"是两回事；强连通图必有回路。`,
    importance: 4,
    hint: '连通看分量，强连通必有回路',
  },

  // ==================== 查找（4 张） ====================
  {
    id: 'ds-search-binary-decision-tree',
    subject: 'ds',
    chapter: '查找',
    tags: ['折半查找', '判定树'],
    title: '折半查找的判定树有什么特点？最大比较次数怎么算？',
    back: `## 结论
- 折半查找的判定树是一棵**平衡二叉树**，左右子树高度差不超过 $1$，失败结点共有 $n+1$ 个。
- 查找成功时的最大比较次数等于判定树的**高度**，即 $\\lfloor\\log_2 n\\rfloor+1$。
- 判定树的形状只取决于元素个数 $n$，与元素的具体取值无关；每次比较都能排除掉一半区间。
> 易错：折半查找要求**顺序存储且有序**，链表无法随机定位中点故不能用；中点取整方式会影响具体比较次数。`,
    importance: 5,
    hint: '判定树高即最大比较次数',
  },
  {
    id: 'ds-search-block-index',
    subject: 'ds',
    chapter: '查找',
    tags: ['分块查找', '索引顺序表'],
    title: '分块查找的平均查找长度怎么算？块长取多少最好？',
    back: `## 结论
- 前提是"**块间有序、块内无序**"：索引表记录每块最大关键字与起始地址。
- 查找分两步：先在索引表中确定所在块（顺序或折半查找），再在块内顺序查找。
- 设均分 $b$ 块、每块 $s$ 个元素且 $n=bs$，索引也顺序查找时平均长度为 $\\dfrac{b+1}{2}+\\dfrac{s+1}{2}$，$s=\\sqrt{n}$ 时最小。
> 易错：性能介于顺序与折半查找之间；平均长度 = 索引部分 + 块内部分。`,
    importance: 3,
    hint: '块长取根号 n 时最优',
  },
  {
    id: 'ds-search-hash-collision',
    subject: 'ds',
    chapter: '查找',
    tags: ['散列表', '冲突处理'],
    title: '处理冲突的开放地址法与链地址法各有什么特点？',
    back: `## 结论
- **开放地址法**：线性探测易产生**堆积**，二次探测或再散列可缓解；删除元素必须做**删除标记**。
- **链地址法**：同义词拉成一条链表，删除方便、不会堆积，适合表长不定时使用。
- 关键是**装填因子** $\\alpha$，即表中元素数与表长之比，$\\alpha$ 越大平均查找长度越长。
> 易错：开放地址法直接置空会截断同义词的探测链，使已有元素查不到；平均查找长度与元素总数无直接关系。`,
    importance: 5,
    hint: '装填因子越大，冲突越多',
  },
  {
    id: 'ds-search-btree-insert-delete',
    subject: 'ds',
    chapter: '查找',
    tags: ['B 树', '插入删除'],
    title: 'B 树插入与删除时如何进行分裂、借位与合并？',
    back: `## 结论
- $m$ 阶 B 树每个结点最多有 $m-1$ 个关键字，除根外最少有 $\\lceil m/2\\rceil-1$ 个关键字。
- **插入**：找到最底层非叶结点插入；超过上限就**分裂**，把中间关键字上移到双亲。
- **删除**：低于下限就**下溢**，先向兄弟**借**（经双亲旋转），兄弟也紧张就**合并**，可能继续向上传播。
> 易错：插入只分裂不合并，删除只借位或合并；根结点的关键字数下限可放宽。`,
    importance: 4,
    hint: '插入分裂，删除借或合并',
  },

  // ==================== 排序（4 张） ====================
  {
    id: 'ds-sort-quick-worst',
    subject: 'ds',
    chapter: '排序',
    tags: ['快速排序', '最坏情况'],
    title: '快速排序什么情况最坏？如何优化？',
    back: `## 结论
- 当序列**基本有序**或每次划分都极不均匀时，递归深度达到 $O(n)$，时间退化为 $O(n^{2})$。
- 空间开销来自递归工作栈：平均 $O(\\log n)$，最坏 $O(n)$。
- 常用优化：随机选取枢轴、**三数取中**，小区间改用直接插入排序，划分后先递归较短的一侧。
> 易错：快排的"一趟"只保证枢轴元素**归位**，并不保证左右子区间内部有序；它是不稳定的原地排序。`,
    importance: 5,
    hint: '基本有序最坏，三数取中优化',
  },
  {
    id: 'ds-sort-heap-build',
    subject: 'ds',
    chapter: '排序',
    tags: ['堆排序', '建堆'],
    title: '堆是怎样建立与调整的？为什么建堆只要 $O(n)$？',
    back: `## 结论
- **向下调整**：把结点与较大的孩子比较，违反堆序就交换并继续下沉，单次代价 $O(\\log n)$。
- **建堆**：从最后一个非叶结点 $\\lfloor n/2\\rfloor$ 起，自后向前逐个向下调整。
- 建堆总代价是 $O(n)$，因为越靠下的结点下沉距离越短，求和后收敛；而逐个插入建堆要 $O(n\\log n)$。
> 易错：**插入**用向上调整；**删除堆顶**是把末尾元素搬到堆顶再向下调整。`,
    importance: 5,
    hint: '从 n/2 起向下调，建堆 O(n)',
  },
  {
    id: 'ds-sort-external-merge',
    subject: 'ds',
    chapter: '排序',
    tags: ['外部排序', '多路归并'],
    title: '外部排序的归并趟数由什么决定？怎样减少 I/O？',
    back: `## 结论
- 外部排序先生成**初始归并段**；$m$ 个段 $k$ 路归并的趟数为 $\\lceil\\log_k m\\rceil$。
- 减少 I/O：**增大归并路数 $k$**，或用**置换—选择排序**生成更长的归并段以减小 $m$。
- 多路归并用**败者树**选最小值，每次只需 $O(\\log k)$ 次比较。
> 易错：总 I/O 次数与归并趟数成正比；$k$ 过大会占用更多缓冲区，单块变小可能反而变慢。`,
    importance: 4,
    hint: '趟数 log_k m，败者树降比较',
  },
  {
    id: 'ds-sort-shell-gap',
    subject: 'ds',
    chapter: '排序',
    tags: ['希尔排序', '插入排序'],
    title: '希尔排序的增量序列有什么要求？它稳定吗？',
    back: `## 结论
- 希尔排序是**插入排序的改进**：取增量 $d$，相隔 $d$ 的元素分一组做插入排序，再缩小 $d$。
- 增量序列必须**最终为 1**，否则不能保证有序；常见取法有折半递减、Hibbard 序列。
- 时间依赖增量序列（最坏 $O(n^{2})$），空间 $O(1)$；相同元素可能被跨组交换，故**不稳定**。
> 易错：它只能用于**顺序存储**；一趟后只是"$d$ 有序"，并非全局有序。`,
    importance: 3,
    hint: '增量最终为 1，希尔不稳定',
  },
];
