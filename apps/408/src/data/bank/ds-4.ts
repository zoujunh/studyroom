/**
 * 数据结构科目种子卡 4（图 / 查找 / 排序，24 张）
 *
 * back 字段语法限定：## 小标题、- 无序列表、**粗体**、行内代码、$行内公式$、$$块公式$$、> 提示。
 * 模板字符串内不写插值符号，LaTeX 反斜杠一律双写。
 */

import type { SeedCard } from '../seed-cards';

export const DS_BANK4: SeedCard[] = [
  // ==================== 图（12 张） ====================
  {
    id: 'ds4-graph-handshake-degree',
    subject: 'ds',
    chapter: '图',
    tags: ['图', '性质'],
    title: '无向图所有顶点的度数之和等于什么？',
    back: `## 结论
- 无向图中每条边给两个端点各贡献 $1$ 度，故所有顶点的**度数之和等于 $2e$**。
- 有向图中每条弧既产生一个出度又产生一个入度，故**入度之和与出度之和都等于 $e$**。
- 因 $2e$ 必为偶数，**奇度顶点一定是偶数个**，可用来判断度数序列能否构成图。
> 易错：平均度数是 $2e/n$；自环对无向图贡献 $2$ 度，对有向图贡献 $1$ 出度加 $1$ 入度。`,
    importance: 4,
    hint: '无向度数和 2e，有向入等于出',
  },
  {
    id: 'ds4-graph-complete-edges',
    subject: 'ds',
    chapter: '图',
    tags: ['完全图', '计算'],
    title: '无向完全图与有向完全图各有多少条边？',
    back: `## 结论
- **无向完全图**中任意两顶点间恰有一条边，边数为 $\\dfrac{n(n-1)}{2}$。
- **有向完全图**中任意两顶点间有一对方向相反的弧，弧数为 $n(n-1)$，是无向时的 $2$ 倍。
- 稠密与稀疏常看 $e$ 与 $n^{2}$ 的数量级：邻接矩阵适合稠密图，邻接表适合稀疏图。
> 易错：$\\dfrac{n(n-1)}{2}$ 是**无向简单图**的边数上限，含平行边或自环的重图不受此限。`,
    importance: 4,
    hint: '无向一半，有向是其两倍',
  },
  {
    id: 'ds4-graph-path-circuit-concept',
    subject: 'ds',
    chapter: '图',
    tags: ['路径', '连通性'],
    title: '路径、简单路径、回路与简单回路怎么区分？',
    back: `## 结论
- **路径**是顶点与边交替出现的序列，路径长度按**边的条数**计算。
- **简单路径**要求顶点不重复出现；**回路**是首尾顶点相同的路径，**简单回路**除起点终点外顶点都不重复。
- 若无向图中任意两顶点都可达，称图**连通**；有向图中两个方向都可达，才称**强连通**。
> 易错：单个顶点本身也算长度为 $0$ 的路径；有向图中 $u$ 可达 $v$ 不代表 $v$ 也能到达 $u$。`,
    importance: 3,
    hint: '简单即顶点不重复，连通分有向无向',
  },
  {
    id: 'ds4-graph-adjlist-edge-nodes',
    subject: 'ds',
    chapter: '图',
    tags: ['邻接表', '存储结构'],
    title: '无向图的邻接表中有多少个边结点？空间是多少？',
    back: `## 结论
- 邻接表由 **$n$ 个顶点的顶点表**与**边表**组成，边结点存邻接点下标和指向下一条边的指针。
- 无向图中同一条边在两个端点处各挂一次，故边结点共 $2e$ 个；有向图只需存 $e$ 个。
- 因此邻接表空间为 $O(n+e)$，与边数成正比，**稀疏图**用它远比邻接矩阵省空间。
> 易错：无向图用邻接表求度只需数链上结点，但删一条边要改两个链表，故引入邻接多重表。`,
    importance: 4,
    hint: '无向边结点 2e，空间 n+e',
  },
  {
    id: 'ds4-graph-weighted-matrix-sentinel',
    subject: 'ds',
    chapter: '图',
    tags: ['邻接矩阵', '带权图'],
    title: '带权图的邻接矩阵中，无边与自身该存什么值？',
    back: `## 结论
- 带权图的邻接矩阵中 $A[i][j]$ 存边权：**顶点到自身**取 $0$，**两顶点间无边**取 $\\infty$。
- 无权图用 $1$ 表示有边、$0$ 表示无边，对角线同样取 $0$。
- 求最短路时 $\\infty$ 表示不可达，但要给它留出防止溢出的余量。
> 易错：若把无边也存成 $0$，算法会误认为两顶点间有权值为 $0$ 的边，故 $0$ 与 $\\infty$ 不可混用。`,
    importance: 2,
    hint: '自身零，无边无穷',
  },
  {
    id: 'ds4-graph-traverse-stack-queue',
    subject: 'ds',
    chapter: '图',
    tags: ['DFS', 'BFS', '复杂度'],
    title: '图的 DFS 与 BFS 分别借助什么结构？复杂度怎么算？',
    back: `## 结论
- **DFS** 沿一条路径走到底再回退，递归实现靠**系统栈**，非递归实现要自己设栈。
- **BFS** 从起点一圈圈向外扩展，靠**队列**实现，并要配访问标记数组防止重复入队。
- 复杂度：邻接矩阵存储 $O(n^{2})$，邻接表存储 $O(n+e)$，代价花在查邻接点上。
> 易错：访问标记应在**入队或入栈时就置位**，等出队再标记会让同一顶点被反复压入。`,
    importance: 5,
    hint: 'DFS 用栈，BFS 用队，矩阵 n²表 n+e',
  },
  {
    id: 'ds4-graph-bfs-shortest-path',
    subject: 'ds',
    chapter: '图',
    tags: ['BFS', '最短路径'],
    title: '为什么 BFS 能求无权图的单源最短路径？',
    back: `## 结论
- BFS 按离起点的**层数**由近及远访问，第一次遇到某顶点时经过的边数就是起点到它的最少边数。
- 实现：设距离数组 $d$，起点 $d=0$，从 $u$ 扩展到未访问的 $v$ 时令 $d[v]=d[u]+1$。
- 每个顶点只入队一次，时间为 $O(n+e)$，沿记录的前驱回溯还能还原具体路径。
> 易错：只对**无权图或各边等权图**成立；带权图要用 Dijkstra，BFS 得到的是边数最少而非权值和最小。`,
    importance: 3,
    hint: '按层扩展，首次到达即最短',
  },
  {
    id: 'ds4-graph-mst-vs-shortest-path',
    subject: 'ds',
    chapter: '图',
    tags: ['最小生成树', '辨析'],
    title: '最小生成树和最短路径树是一回事吗？',
    back: `## 结论
- **最小生成树**选 $n-1$ 条边连通全部顶点，使**边权总和**最小；**最短路径**只关心两点间权值最小。
- 最小生成树中两点间的路径**不一定**是最短路径，两者目标不同，不可互相代替。
- Dijkstra、Floyd 求最短路；Prim 与 Kruskal 求最小生成树，后两者**允许负权边**。
> 易错：Prim 适合稠密图、Kruskal 适合稀疏图；"总权值最小"是全局概念。`,
    importance: 4,
    hint: '一个求总和最小，一个求两点最短',
  },
  {
    id: 'ds4-graph-dijkstra-negative',
    subject: 'ds',
    chapter: '图',
    tags: ['Dijkstra', '负权'],
    title: 'Dijkstra 为什么不能处理负权边？',
    back: `## 结论
- Dijkstra 靠**贪心**：每轮把当前距离最小的未确定顶点"定死"，之后不再修改它的距离。
- 有负权边时，较长的路径可能因后面的负边变短，已定死的顶点仍需改写，贪心前提失效。
- 反例：$A\\to B$ 权 $2$、$A\\to C$ 权 $5$、$C\\to B$ 权 $-4$，$A\\to B$ 真长为 $1$，算法却已定 $B=2$。
> 易错：负权**回路**会让最短路不存在。`,
    importance: 4,
    hint: '贪心定死后无法反悔，负权会推翻',
  },
  {
    id: 'ds4-graph-floyd-matrix-meaning',
    subject: 'ds',
    chapter: '图',
    tags: ['Floyd', '动态规划'],
    title: 'Floyd 算法中 $A^{(k)}[i][j]$ 表示什么？',
    back: `## 结论
- $A^{(k)}[i][j]$ 表示从 $i$ 到 $j$、**中间顶点编号不超过 $k$** 的最短路长度。
- 递推：$A^{(k)}[i][j]=\\min(A^{(k-1)}[i][j],A^{(k-1)}[i][k]+A^{(k-1)}[k][j])$。
- $A^{(0)}$ 是邻接矩阵，迭代 $n$ 轮到 $A^{(n)}$，时间 $O(n^{3})$。
> 易错：中间点 $k$ 必须在**最外层循环**。`,
    importance: 4,
    hint: '只许前 k 个点中转，k 在最外层',
  },
  {
    id: 'ds4-graph-topo-sequence-check',
    subject: 'ds',
    chapter: '图',
    tags: ['拓扑排序', '判定'],
    title: '怎样判断一个序列是不是某有向图的拓扑序列？',
    back: `## 结论
- 按序列顺序**依次取出**每个顶点，检查它的所有前驱是否都已出现在更靠前的位置。
- 用邻接矩阵很方便：取到顶点 $v$ 时扫描第 $v$ **列**，若非零元所在行的顶点还没出现过，该序列就非法。
- 拓扑序列**通常不唯一**：某一步同时有多个入度为 $0$ 的顶点时，先取哪一个都能得到合法序列。
> 易错：若拓扑序列唯一，则每一步入度为 $0$ 的顶点都只有一个，图中必有一条贯穿全部顶点的有向路径。`,
    importance: 4,
    hint: '逐个取出，前驱必须先出现',
  },
  {
    id: 'ds4-graph-aoe-ve-vl-el',
    subject: 'ds',
    chapter: '图',
    tags: ['AOE', '关键路径'],
    title: 'AOE 网中事件与活动的最早、最迟时间怎么定义？',
    back: `## 结论
- **AOE 网**用边表示活动、顶点表示事件，边权为活动时间。
- 事件最早发生时间 $ve$ 等于**源点到该顶点的最长路径长度**，需等前驱活动结束。
- 最迟发生时间 $vl$ 是**不推迟总工期**的最晚时间：先令汇点 $vl=ve$ 再逆推。
- 活动最早开始 $e=ve$（弧尾事件），最迟开始 $l=vl-w$（弧头事件），余量 $l-e=0$ 即**关键活动**。
> 易错：汇点的 $ve$ 即总工期。`,
    importance: 5,
    hint: 've 取最长，vl 逆推，余量零关键',
  },

  // ==================== 查找（6 张） ====================
  {
    id: 'ds4-search-sequential-asl',
    subject: 'ds',
    chapter: '查找',
    tags: ['顺序查找', 'ASL'],
    title: '顺序查找的 ASL 是多少？哨兵有什么用？',
    back: `## 结论
- 表长 $n$、等概率时，成功的比较次数依次为 $1,2,\\dots,n$，故成功时 $ASL=\\dfrac{n+1}{2}$。
- 不成功要比较完整个表，此时 $ASL=n$；算上哨兵那次则为 $n+1$。
- **哨兵**放在 $0$ 号单元，循环只需判断元素是否等于目标，省去下标越界判断。
> 易错：哨兵减少的是**边界判断**，不改变 $ASL$ 量级；有序表失败可提前结束。`,
    importance: 4,
    hint: '成功 (n+1)/2，哨兵省边界判断',
  },
  {
    id: 'ds4-search-binary-asl',
    subject: 'ds',
    chapter: '查找',
    tags: ['折半查找', 'ASL', '判定树'],
    title: '折半查找成功与不成功的平均查找长度怎么算？',
    back: `## 结论
- 判定树是**平衡二叉树**，树高 $\\lfloor\\log_2 n\\rfloor+1=\\lceil\\log_2(n+1)\\rceil$ 即最大比较次数。
- 成功时 $ASL=\\dfrac{1}{n}\\sum_{i=1}^{n} l_i$，按判定树逐层点数，约为 $\\log_2(n+1)-1$。
> 易错：要求**顺序存储且有序**，链表不能随机定位中点；不成功走到 $n+1$ 个**外部失败结点**之一。`,
    importance: 4,
    hint: '逐层点数求 ASL，树高即最多比较',
  },
  {
    id: 'ds4-search-btree-height',
    subject: 'ds',
    chapter: '查找',
    tags: ['B 树', '高度', '计算'],
    title: '$m$ 阶 B 树的高度和关键字个数有什么关系？',
    back: `## 结论
- $m$ 阶 B 树每个结点最多 $m$ 棵子树，含 $N$ 个关键字时 $h\\ge \\log_m (N+1)$。
- 高度最大时结点最少：根 $2$ 棵子树，其余非叶结点 $\\lceil m/2\\rceil$ 棵。
- 由此得 $h\\le \\log_{\\lceil m/2\\rceil}\\dfrac{N+1}{2}+1$，$m$ 越大树越矮。
> 易错：高度只数**含关键字的结点层数**；查找最多 $h$ 次磁盘 I/O。`,
    importance: 4,
    hint: '最少子树 ⌈m/2⌉，越高越费关键字',
  },
  {
    id: 'ds4-search-hash-function',
    subject: 'ds',
    chapter: '查找',
    tags: ['散列表', '散列函数'],
    title: '常用散列函数怎么构造？除留余数法为什么要取素数？',
    back: `## 结论
- 常见构造法：**直接定址**、**除留余数**、**数字分析**、**平方取中**与**折叠法**。
- **除留余数法**取 $H(k)=k\\bmod p$，$p$ 常取不大于表长的**素数**，使地址分布更均匀。
- 好的散列函数应让关键字**均匀分布**且计算简单。
> 易错：散列把大关键字空间映射到小地址空间，冲突**不可能完全避免**，只能设法减少。`,
    importance: 3,
    hint: '除留余数取模，模数取素数',
  },
  {
    id: 'ds4-search-hash-linear-asl',
    subject: 'ds',
    chapter: '查找',
    tags: ['散列表', '线性探测', 'ASL'],
    title: '线性探测法下查找成功与不成功的 ASL 怎么算？',
    back: `## 结论
- 线性探测：地址被占就依次试 $H(k)+1$、$H(k)+2$，取模回表头直到遇空位。
- 成功的 $ASL$：把各元素"探测几次才找到"的次数求和再除以个数。
- 不成功的 $ASL$：对每个可能地址算"探测到第一个空位所需次数"，求和后除以地址个数。
- 线性探测会把不同地址的关键字挤成一片，形成**堆积**。
> 易错：$ASL$ 只与**装填因子**有关，与元素总数无关；删除只能做标记。`,
    importance: 5,
    hint: '成功数自己，失败数到空位',
  },
  {
    id: 'ds4-search-bplus-index',
    subject: 'ds',
    chapter: '查找',
    tags: ['B+ 树', '数据库索引'],
    title: 'B+ 树为什么适合做数据库索引？',
    back: `## 结论
- B+ 树把**数据全部放在叶子层**，非叶结点只存索引关键字，同样大小的结点能装下更多关键字，树因此更矮。
- 叶子结点按关键字顺序串成**有序链表**，范围查询定位到起点后顺序扫描即可，效率很高。
- 无论查找成功还是失败都必须走到叶子，每次查找的**磁盘 I/O 次数稳定**，便于估算性能。
> 易错：B 树在非叶结点命中就能返回，查找长度不稳定；B+ 树非叶结点的关键字只是分界标志，可能重复出现在叶子中。`,
    importance: 3,
    hint: '数据全在叶子，叶子成链表',
  },

  // ==================== 排序（6 张） ====================
  {
    id: 'ds4-sort-quick-partition',
    subject: 'ds',
    chapter: '排序',
    tags: ['快速排序', '划分'],
    title: '快速排序的一趟划分做了什么？枢轴最终落在哪？',
    back: `## 结论
- 一趟划分以首元素为**枢轴**，双指针从两端向中间扫描：右指针找比枢轴小的搬到左边空位，左指针找比枢轴大的搬到右边空位。
- 两指针相遇处就是枢轴的**最终位置**，一趟结束后枢轴左边都不大于它、右边都不小于它。
- 划分只在原数组内交换元素，不需要辅助数组，空间开销主要来自递归工作栈。
> 易错：一趟划分只保证**枢轴归位**，左右子区间内部仍可能无序；$n$ 个元素最多做 $n$ 趟划分就能全部有序。`,
    importance: 5,
    hint: '两端向中间挖坑填数，枢轴归位',
  },
  {
    id: 'ds4-sort-merge-passes',
    subject: 'ds',
    chapter: '排序',
    tags: ['归并排序', '趟数'],
    title: '归并排序的趟数与每趟比较次数由什么决定？',
    back: `## 结论
- 二路归并把相邻两个有序段合并，每趟段长翻倍，$n$ 个元素需 $\\lceil\\log_2 n\\rceil$ 趟。
- 每趟比较次数不超过 $n-1$，总时间 $O(n\\log n)$，最好、最坏与平均都是这个量级。
- 归并**稳定**，但需要 $O(n)$ 辅助数组，且顺序访问数据，适合外部排序。
> 易错：$k$ 路归并趟数是 $\\lceil\\log_k n\\rceil$，路数越多趟数越少，但选最小记录代价上升。`,
    importance: 4,
    hint: '二路归并 log₂n 趟，每趟至多 n-1 次',
  },
  {
    id: 'ds4-sort-radix',
    subject: 'ds',
    chapter: '排序',
    tags: ['基数排序', '复杂度'],
    title: '基数排序的时间与空间复杂度是多少？它稳定吗？',
    back: `## 结论
- 基数排序按**低位优先**逐位分配与收集：设关键字有 $d$ 位、每位取值 $r$ 种，共 $d$ 趟。
- 每趟分配与收集是 $O(n+r)$，总时间 $O(d(n+r))$，它**不比较关键字的大小**。
- 需要 $r$ 个队列与 $n$ 个元素的暂存空间，空间 $O(n+r)$；按原顺序收集，所以**稳定**。
> 易错：$d$ 或 $r$ 很大时效率反而不如 $O(n\\log n)$ 的比较类排序。`,
    importance: 3,
    hint: 'd 趟 n+r，不比较且稳定',
  },
  {
    id: 'ds4-sort-optimal-merge-tree',
    subject: 'ds',
    chapter: '排序',
    tags: ['外部排序', '最佳归并树'],
    title: '最佳归并树怎样构造？为什么要补虚段？',
    back: `## 结论
- 外部排序中把每个初始归并段看作叶子，**归并树**的带权路径长度等于归并读写的总记录数。
- 要让读写量最小，按**哈夫曼树**构造：每次取权值最小的 $k$ 个结点合并，建 $k$ 叉哈夫曼树。
- 若叶子数使某内部结点孩子不足 $k$ 个，要补**长度为 $0$ 的虚段**凑满，否则结果不是最佳。
> 易错：它优化的是读写**总记录数**，不能减少归并趟数，趟数仍由 $k$ 与初始段数决定。`,
    importance: 4,
    hint: 'k 叉哈夫曼，虚段补满 k 个孩子',
  },
  {
    id: 'ds4-sort-insertion-process',
    subject: 'ds',
    chapter: '排序',
    tags: ['直接插入排序', '复杂度'],
    title: '直接插入排序的过程与比较次数是怎样的？',
    back: `## 结论
- 从第 $2$ 个元素开始，把当前元素暂存，向前与已有序部分**从后往前**比较，较大者后移，最后插入空位。
- 原本有序时每趟只比较 $1$ 次且不移动，时间 $O(n)$；完全逆序时约 $n^{2}/2$ 次。
- 空间 $O(1)$，**稳定**；序列**基本有序**时接近线性，明显优于快速排序。
> 易错：改成**折半插入**只把比较次数降到 $O(n\\log n)$，元素移动次数仍是 $O(n^{2})$。`,
    importance: 3,
    hint: '从后往前找插入位，越有序越快',
  },
  {
    id: 'ds4-sort-bubble-vs-select',
    subject: 'ds',
    chapter: '排序',
    tags: ['冒泡排序', '简单选择排序'],
    title: '冒泡排序与简单选择排序在一趟后各能得到什么？',
    back: `## 结论
- **冒泡排序**比较相邻元素并把较大者后移，一趟结束最大元素到位；某趟无交换说明已经有序。
- **简单选择排序**每趟从未排序部分选出最小者，与第一个位置交换，该位置的元素就此确定。
- 两者时间都是 $O(n^{2})$、空间 $O(1)$；冒泡**稳定**，简单选择因交换跨越相同元素而**不稳定**。
> 易错：简单选择的比较次数固定为 $n(n-1)/2$，与初始序列**无关**，但移动次数最多 $n-1$ 次。`,
    importance: 3,
    hint: '冒泡出最大，选择出最小，比较固定',
  },
];
