# 修复计划：技术债务清理

## 问题列表

### 1. ThemeToggle 未接入
- **问题**：`ThemeToggle.tsx` 组件存在但没在任何页面渲染
- **修复**：在 StudyRoom 头部添加 ThemeToggle

### 2. App.css 模板残留
- **问题**：`App.css` 包含 Vite 模板代码（184行），未使用
- **修复**：清空 App.css 或删除文件

### 3. SoundMixer 和 sounds.ts 数据重复
- **问题**：两个文件各自定义了音效数据，不同步
- **修复**：统一使用 `sounds.ts` 的数据，SoundMixer 引用它

### 4. 白/粉/棕噪音空文件路径
- **问题**：SoundMixer 中这三种噪音的 `file` 字段为空字符串
- **修复**：生成对应的音频文件或移除这些选项

### 5. Modal 组件未使用
- **问题**：`Modal.tsx` 已创建但没在任何地方使用
- **保留**：后续功能会用到，暂不处理

---

## 执行顺序

1. 接入 ThemeToggle → StudyRoom 头部
2. 清理 App.css
3. 统一音效数据定义
4. 处理空音频文件

## 预期结果

- 主题切换可在学习室中使用
- App.css 不再有无用代码
- 音效数据统一管理
- 无空文件路径错误
